// Reemplazado por HeaderNav.tsx (que combina nav desktop + drawer mobile en un solo
// client component). Este archivo queda como deprecated stub para evitar imports rotos.

export { HeaderNav as MobileMenu } from "./HeaderNav";
