// Entry point. `registerRootComponent` is `AppRegistry.registerComponent` plus
// the Expo-specific setup that makes the same bundle run in a dev client and in
// a standalone build.
import { registerRootComponent } from "expo";

import App from "./App";

registerRootComponent(App);
