# Handoff — 2026-07-01 audit → fixes → QA

Single entry point for the QA cycle. Repo root is `C:\dev\dim` (run everything from `dim/`, NOT `C:\dev`).

## State of play
| # | Deliverable | Branch | Commits | Status |
|---|-------------|--------|---------|--------|
| D1 | XSS-001 + HDR-001 security fixes | `fix/jsonld-xss-and-security-headers` | `f2b72b20`, `6ef7e8af` | done, verified, **not pushed** |
| D2 | Test-gate fix (bucketize fallout) | `fix/test-stale-vimock-paths` | `ede859af`, `d2a35488` | done, `pnpm test` 258→2, **not pushed** |
| D3 | Audit + QA docs | (untracked in `docs/audits/`) | — | ready |

Base for both branches: `5f26cc89` on `chore/tech-debt-finish`. They fork independently — no ordering.

## Documents (all under `docs/audits/`)
- `ai-audit-2026-07-01.md` — audit part 1 (security/privacy).
- `ai-audit-2026-07-01-part2.md` — audit part 2 (gate/async/deps/regression), corrected.
- `qa-order-2026-07-01.md` — **read-only QA work-order for Cowork** (start here for automated QA).
- `pr-bodies-2026-07-01.md` — draft PR bodies for D1/D2/D3 (not opened).
- `audit-agent-permissions.json` (in `docs/`) — read-only permission gate for QA.

## Open items (tracked, not addressed by D1/D2)
- **DEP-001** — `postcss >=8.5.10` (prod path) + `vite/esbuild/tar` (dev-only) bumps.
- **TEST-002** — 2 local-Supabase integration tests (`pet-cache-rederivation`, `admin-analytics-perf`);
  need `pnpm db:start` + seed (human-gated).
- **CSP** (from HDR-001), **KEY-001**, **RNG-001** (part 1 Low).

## Human-gated (waiting on Ignacio)
Push, PR, DB migrations, dependency bumps, deploy. Everything above is local + reversible.

## For automated Cowork QA
Point Cowork at `docs/audits/qa-order-2026-07-01.md` with the read-only gate
(`docs/audit-agent-permissions.json`). It verifies D1 + D2 against base and re-runs the gate; its only
writable path is `docs/audits/**` and it writes `docs/audits/qa-report-2026-07-01.md`.
