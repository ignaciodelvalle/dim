# Superpowers — Specs & Plans

Index of design docs (`specs/`) and implementation plans (`plans/`) for MiMAR features.

- **`specs/`** — design docs. The *what* and *why* of each feature. Reviewed and locked before implementation.
- **`plans/`** — implementation plans for Claude Code. The *how*. Self-contained, file-level detail, executable.

A typical feature lifecycle: brainstorm in chat → spec in `specs/` → review/iterate → plan in `plans/` → Claude Code executes → mark done.

---

## What to attack next

Recommended order based on dependency chains and leverage. Each item is a Claude Code session that produces 1+ PRs.

Auditado 2026-05-19 contra `develop`: varios items que estaban acá ya están en código y se movieron a la sección "Implementado" abajo.

| Priority | Feature | Plan file | Why now |
|----------|---------|-----------|---------|
| ⭐ | **Metrics & operator-IA handoff** (Item 0 + 5 items) — projection primitives + nav regrouping + disease/disposal/enforcement métricas sobre el event log | `specs/2026-06-18-metrics-ia-handoff-design.md` (umbrella) + 6 item specs + `reference-intl-pet-systems-and-metrics.md` | Paquete read-only sobre el event log (sin schema, sin migrations). **Item 0 primero** (`lib/metrics/`: `ProjectionContext`, denominador único, **boundary k-anon que hoy NO existe en código**, period unificado, dedup) — refactor que además cierra un gap de privacidad vivo. Item 1 (nav `NavSection`) en paralelo; Item 2 (`/gob/mortalidad`, Ley 5470) **mayor leverage — datos ya existen**; Items 3-4 surveillance + compliance. SDD test-first por item, docs en el mismo PR. Empezar acá para métricas. |
| 1 | **Sistema de casos (expedientes)** — envoltorio unificado sobre el event log | `plans/2026-05-19-cases-system.md` | Dos specs sucesivas (attachment + lifecycles) + plan ejecutable. Toca schema + lib + 5 server-action workflows + RLS + UI base. ~2-3 semanas en 7 fases (A-G). Habilita welfare-officer queue unificado + adoption pipeline coherente + Mi Argentina alignment futuro. **Único item realmente grande pendiente.** |
| ~~2~~ | ~~Welfare moderation admin UI~~ | — | ✅ **Cerrado**: audit 2026-05-19 confirma que `/admin/moderacion` ya está montado (list + detail + `ModerationActions` con ambas server actions). Nav link en `app/admin/layout.tsx`. El README listaba el path equivocado (`/admin/maltrato/moderacion`). |
| 3 | **Bulk revoke UI** (follow-up Fase 13) | (mismo plan, Fase 13) | Server action `bulkRevokeAction` ya implementado y testeado. Falta la UI para las 4 queues de revoke (`/admin/usuarios`, `/gob/usuarios`, `/admin/organizaciones`, `/gob/organizaciones`) — necesita attachment uploader bulk. ~½ día. |
| ~~4~~ | ~~Validación canonical en `events.ts`~~ | — | ✅ **Cerrado**: audit 2026-05-19 confirma que `events.ts` no acepta province/locality de `formData` (todo viene de `pet.jurisdictionProvince`/`Locality` ya canónicos desde el registro). Las 5 actions con input de usuario (`pets.ts`, `upgrade.ts`, `service-offerings.ts`, `welfare.ts`, `intake.ts`) canonicalizan; `admin-institutional.ts` también vía `resolveCanonicalJurisdiction`. Sin gap real. |
| 5 | **Physical tag** (`/t/[serial]`) | (plan a escribir post-OK del spec v1.0) | Chapa identificadora física que cuelga del collar. Spec con preguntas abiertas en §15 (material, fabricante, auto-revoke on death). ~3 días cuando se decidan los §15. |
| 6 | **CABA barrios — execution** (script ya escrito, ejecutar + verificar) | `plans/2026-05-19-caba-barrios-import-execution.md` | El script `scripts/import-caba-barrios.ts` ya está listo (48 barrios Ley CABA 1.777). Ejecutar + verificar + tweak menor de ranking en `LocalityCombobox`. ~½ día. |
| 7 | **Fix: Vet portal routing default** (vet con `professional.provider` → /pro, no /mis-mascotas) | `plans/2026-05-19-fix-vet-portal-routing.md` | Coherencia con modelo de 4 roles. NO bloquea acceso por URL directa. Audit de chrome non-owner para sacar link auto a /mis-mascotas. Incluye placeholder mínimo del `/pro`. ~½-1 día. |
| 8 | **Fix: 404 de service dog** (link aparece para non-owners, página 404ea) | `plans/2026-05-19-fix-service-dog-404.md` | Asimetría: la lista de mis-mascotas no filtra por role, el profile muestra link a /asistencia siempre, la página filtra estricto `role='owner'`. Vecino-en-tránsito click → 404 cryptico. Fix: ocultar link a non-owners + mensaje friendly. ~½ día. |
| 9 | **Pet profile v2 + Achievements** (rediseño UX + gamification) | (plan a escribir post-OK del spec) | **v1.1** Saca timeline del profile (queda en /libreta y /historial). Profile = identidad + info viva derivada + 5 achievements POC chip-display (animal de servicio, fui adoptado, me perdí pero volví, tuve crías [TBD], trotamundos [TBD]). v1.1 agrega §4.7 PPP card + §4.8 Service Dog credential card. Sin schema migration; compute on-read. ~1 semana cuando se priorice. |
| 10 | **Cross-org transfer UX** — refugio→refugio handshake | (plan a escribir post-OK del spec) | Activa `custody_transfer_handshake` case_kind (estaba deferred). UI sender + queue receiver + accept/reject/cancel + cron expire 30d. Depende del sistema de casos. ~5 días. |
| 11 | **ENO direct vet report + owner alerts** (extiende symptom surveillance) | (plan a escribir post-OK del spec) | Vet con `vet.clinical_write` emite `clinical_info_logged.sub_kind='disease_diagnosis'` que bypassa matcher + emite `outbreak_signal` urgent. Owner-facing alerts para zoonoses peligrosas (override del D1 surveillance, lista cerrada en `lib/disease-public-alert-catalog.ts`). Catálogo legal por disease en `lib/disease-legal-anchors.ts`. ~5 días. |
| 12 | **Org abuse investigation** flagged higher | (plan a escribir post-OK del spec) | Org members de verified org emiten welfare_report con `reporter_organization_id` + severity auto-critical + notif urgent inmediata al govt/admin + priority sort en queue. Multi-source escalation. Capability `welfare.report` automatic a TODA la org. Depende del sistema de casos. ~4-5 días. |
| 13 | **Decomiso → refugio chain** (Ley 14.346 — UI lado autoridad) | (plan a escribir post-OK del spec) | Govt/admin ejecuta decomiso desde `/gob/decomisos/nuevo` con motivo + 2+ adjuntos OBLIGATORIOS + selección refugio destinatario. Activa `custody_episode` case_kind (deferred). Handoff govt→refugio con 7 días de expiry. Notif urgent al owner previo. ~7-8 días. Depende de sistema de casos + cross-org transfer. |
| 14 | **Pregnancy tracking** — `clinical_info_logged(sub_kind='pregnancy')` | (plan a escribir post-OK del spec) | Embarazo como sub_kind del umbrella clinical. Pair started/ended + denorm flag `pets.pregnancy_status`. Sección destacada en pet profile v2 mientras in_progress. Activa achievement A4 "Tuve crías". Capture rápida entries. Solo female + dog/cat/other. ~5 días. |
| 15 | **Performed_by autocomplete** — vets/clinics linkeados + texto libre | (plan a escribir post-OK del spec) | Schema dual: FK opcional + texto snapshot. 6 events afectados. Combobox `PerformedByCombobox` reusable. Display libreta distingue linked vs free-text. NO backfill retroactivo. Independiente del sistema de casos. ~5-6 días. |
| 16 | **Govt business rules POC** — config por jurisdicción | (plan a escribir post-OK del spec) | Tabla `govt_business_rules` con cascada locality > province > country > defaults. POC con 3 rule_types PPP (`ppp_breed_list`, `ppp_weight_threshold`, `ppp_attestation_required_registries`). Form específico por rule_type + dashboards admin (CRUD) + govt (read-only). Re-evaluation auto de pets afectados. Audit obligatorio. ~7-9 días. |
| 10 | **Bite from unowned animal** (víctima reporta mordedura sin pet registrada) | (plan a escribir post-OK del spec) | Extiende bite-rabies-observation con subject polimórfico. Tabla `temporary_pet_descriptions` + reconciliation post-signup por DNI matching + reasignación manual por govt. Cumplimiento legal (Decreto 4669, Ord. 41.831) sobre animales no-registrados. Solo víctimas autenticadas en v1. ~8 días cuando se priorice. **Depende del sistema de casos (priority #1).** |

---

## All specs & plans

### Specs (design docs)

| Spec | Status | Plan | Notas |
|------|--------|------|-------|
| `2026-06-18-metrics-ia-handoff-design.md` (umbrella) | 🟢 Ready for CC | (per-item, abajo) | Master del paquete metrics-IA: secuencia los 5 items, define el workflow SDD test-first, reglas globales de test/docs/privacy (k-anon, sin schema, sin event types). Empezar acá. Referencia cruzada: `reference-intl-pet-systems-and-metrics.md`. |
| `2026-06-18-projection-primitives-design.md` (Item 0) | ✅ Implementado (#626) | `plans/2026-06-18-projection-primitives.md` | **Foundation — hacer primero.** `lib/metrics/`: `ProjectionContext`, scope/denominador único (hoy `petsScopeClause` está duplicado en 2 archivos + re-derivado inline ~14×), **`suppressSmallCells` k=5 (la política k-anon de AGENTS.md NO está implementada hoy)**, period unificado, `React.cache` dedup. Migra fetchers existentes con value-pinning tests. Documenta el Pattern B en `hexagonal-lite.md`. Bloquea Items 2-4. |
| `2026-06-18-operator-nav-regrouping-design.md` (Item 1) | 🟢 Ready for CC | `plans/2026-06-18-operator-nav-regrouping.md` | Refactor puro: `GOB_NAV`/`ADMIN_NAV`/`buildOrgNav` → `NavSection[]` vía `OpRailNav sections=`. Sin rutas nuevas. Tests de invariante (ningún href perdido) + capability filtering org. |
| `2026-06-18-mortality-disposal-dashboard-design.md` (Item 2) | ✅ Implementado (#PR) | (plan post-OK) | **Mayor leverage.** Nuevo `/gob/mortalidad`: disposition mix, traceable-disposal rate, unknown-disposition %, reportable-death share (B1-B9). Ley CABA 5470. `disposition_method` ya se captura — pura proyección. |
| `2026-06-18-operator-nav-regrouping-design.md` (Item 1) | ✅ Implementado (#628, #629) | `plans/2026-06-18-operator-nav-regrouping.md` | Refactor puro: `GOB_NAV`/`ADMIN_NAV`/`buildOrgNav` → `NavSection[]` vía `OpRailNav sections=`. Sin rutas nuevas. Tests de invariante (ningún href perdido) + capability filtering org. |
| `2026-06-18-mortality-disposal-dashboard-design.md` (Item 2) | 🟢 Ready for CC | (plan post-OK) | **Mayor leverage.** Nuevo `/gob/mortalidad`: disposition mix, traceable-disposal rate, unknown-disposition %, reportable-death share (B1-B9). Ley CABA 5470. `disposition_method` ya se captura — pura proyección. |
| `2026-06-18-surveillance-metrics-hardening-design.md` (Item 3) | 🟢 Ready for CC | (plan post-OK) | Extiende `/gob/vigilancia`: ENO-notification SLA (A7), compliance observación 10d (A8/A9), AMR density (A12), reportable incidence + lab-confirmation (A6/A10). Introduce `lib/anonymity.ts`. |
| `2026-06-18-compliance-enforcement-metrics-design.md` (Item 4) | ✅ Implementado (#637) | `plans/2026-06-18-compliance-enforcement-metrics.md` | Microchip penetration (C1), ISO-validity (C2), chip-fraud (C5), dangerous-breed registry compliance (C7 graceful-0%), reunification rate (D4), decomisos (D5). Leyes 14.107/4078/14.346. C3 deferred (sin denominador externo). |
| `2026-06-18-surveillance-metrics-hardening-design.md` (Item 3) | ✅ (#636) | `plans/2026-06-18-surveillance-metrics-hardening.md` | Extiende `/gob/vigilancia`: ENO-notification SLA (A7), compliance observación 10d (A8/A9), AMR density (A12), reportable incidence + lab-confirmation (A6/A10). Reusa `lib/metrics/anonymity.ts` (Item 0), no crea `lib/anonymity.ts`. |
| `2026-06-18-compliance-enforcement-metrics-design.md` (Item 4) | 🟢 Ready for CC | (plan post-OK) | Microchip penetration (C1), ISO-validity (C2), chip-fraud (C5), dangerous-breed registry compliance (C7 graceful-0%), reunification rate (D4), decomisos (D5). Leyes 14.107/4078/14.346. |
| `2026-06-18-owner-health-status-nudges-design.md` (Item 5) | 🟢 Ready for CC | (plan post-OK) | `/inicio` nudges per-pet derivados (vacuna vencida, chip, recordatorios, scans). Owner-data only, sin surveillance. Independiente de Items 1-4. Coordina con Item 6 el split `/inicio` agregador vs perfil detalle. |
| `2026-06-18-pet-profile-v21-reorder-and-action-consolidation-design.md` (Item 6) | ✅ (#639) | `plans/2026-06-18-pet-profile-v21-reorder.md` | De la crítica de diseño 2026-06-18. Reordena el perfil de mascota (hero primero, `PetAlertStrip` priorizado, credenciales/logros dentro de Resumen) y colapsa los 3 hubs de acción: **`/anotar` canónico, `/eventos/nuevo` redirige a él**, una sola forma de anotar en el perfil. Consolida `PetReminders`. Tabs = modelo final del timeline. Lost cockpit conserva acceso al perfil. Sin cambios de datos. |
| `2026-06-18-unified-app-shell-design.md` (Item 7) | 🟢 Ready for CC (D1–D12 cerradas) | (plan post-OK) | De la crítica de navegación 2026-06-18. Un solo `AppShell` con variantes `citizen` (barra superior) / `operator` (riel lateral) que reemplaza los 3 chromes (`LnOwnerNav`/`AppHeader`/`OpShell`). Fuente única de nav, decisión auth-aware (`lib/shell-nav.ts`) que **arregla el varado del usuario logueado en surfaces públicas**, drawer mobile único, context switcher. Depende de Item 1 (consume sus `NavSection[]`). Strangler en 4 fases. Sin cambios de datos/rutas. |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 12**) | ✅ **Implementado** (`feat/wave2-item12-case-ux`) | — | Case (expediente) UX coherence. `CaseStatusBadge` (shared tones: open/escalated/closed/merged, cross-kind), `CaseDetailShell` (header: `OpCodeBadge` + `CaseStatusBadge` + parties PII-gated + normativa + jurisdicción + subject descriptor), `CaseQueue` (unified queue table: filter chips + shared status/kind/sort + optional `OpBulkBar`). Exported from `components/ui/dashboard`. `/casos/[publicCode]` migrado a `CaseDetailShell`. `/gob/disputas` migrado a `CaseQueue`. 30 unit tests (`__tests__/case-ux-components.test.tsx`). Edge cases: `unowned_animal` (graceful descriptor), multi-party dispute (lista mobile-safe), `isPublic=true` (PII gating). Deferred: `/gob/maltrato`, `/gob/decomisos`, `/admin/observaciones` — datos no son `CaseListItem`-shaped; follow-up cuando tengan `publicCode` linking. |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2, Items 8–15, 24) — **Item 10 ✅ Implementado** | 🟡 En progreso (Item 10 done) | `plans/2026-06-18-wave2-item10-operator-action.md` | Wave 2 UX hardening. **Item 10 — Operator action layer** shipped: `OpOmnibox` (búsqueda global scope-aware + PII-log en `app/actions/omnibox-search.ts`/`lib/omnibox-search.ts`) en el topbar operador + `OpBulkBar` genérico (`lib/bulk-select.ts`) cableado a `bulkRevokeAction` en las colas de revoke. Sin tablas/eventos/migrations. Items 8–9, 11–15, 24 pendientes. |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 11**) | ✅ **Implementado** (`feat/wave2-item11-a11y`) | `plans/2026-06-18-wave2-item11-a11y.md` | Accessibility hardening fuera de Track E (Ley 26.653). `OpStateBadge`: icon+texto por estado (no color solo). `OpKpi` delta: `aria-hidden` en flecha + sr-only dirección. 8 tablas densas (`/gob`, `/admin`, `/org`): `scope="col"` + `<caption>`. `BulkRevokeModal`: `aria-labelledby`. Axe e2e extendido: `/adoptar`, `/refugios`, `/casos`, `/denuncias/buscar`, páginas de token inválido (`/libreta/compartir`, `/r/invite`). Nuevo `e2e/a11y-operator-auth.spec.ts` para redirects de rutas operadoras. 18 unit tests nuevos (a11y-badge-kpi + a11y-table-scope). |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 9**) | ✅ **Implementado** (`feat/wave2-item9-forms-mobile`) | `plans/2026-06-18-wave2-item9-forms-mobile.md` | Event-forms consistency + mobile hardening. 17 forms auditados contra 4 design rules: 3 verb fixes (`clinico`, `medicacion-inicio`, `sintoma`), 1 SuccessScreen añadida (`mordedura` trámite), inputMode en campos numéricos/tel, font-size 16px (iOS zoom), min-h-44px (touch target), sticky CTA footer, `useFormErrorFocus` hook cross-form. Items 8, 10–15, 24: pendientes. |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 13**) | ✅ **Implementado** (`feat/wave2-item13-onboarding-qr`) | — | Onboarding aha — primera mascota → credencial QR. `createPetAction` redirige a `/mis-mascotas/nueva/[token]/credencial` (antes `?recienCreado=true` + `PostCreateModal`). Nueva página servidor genera QR SVG inline (240px, `qrcode`). `PetCreatedAha` client component: foco al heading en mount (a11y), Web Share API + fallback clipboard, 3 CTAs (Compartir / Ver perfil / Ver credencial pública), QR con `role=img` + `aria-label` con URL. `/mis-mascotas/nueva` envuelta en `LnWizardShell`. 9 unit tests. |
| `2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · Items 8–15) | ✅ Item 8 (#TBD) · 🟢 Items 9–15 Ready for CC | `plans/2026-06-18-wave2-item8-loading-skeletons.md` (Item 8) · (plan post-OK, por item) | Dev-handoff de las críticas forward-looking + owner. **Al FINAL del bloque autónomo, sin reordenar lo en curso.** **8: ✅ loading/skeletons (`loading.tsx`=0→16 archivos, 4 skeleton components, shimmer tokens, a11y).** 9: 17 event-forms. 10: omnibox + bulk. 11: a11y fuera de Track E. 12: expedientes. **13: ✅ onboarding QR.** **14: ✅ owner hub `/cuenta`+notice→action loop+libreta export (dep. 5/6) — PR feat/wave2-item14-owner-hub.** 15: corrección por enmienda — **implementa el principio locked #2**. 24: público — login intent-aware + UX de token expirado. |
| `2026-06-18-owner-health-status-nudges-design.md` (Item 5) | ✅ Implementado (#638) | `plans/2026-06-18-owner-health-status-nudges.md` | `/inicio` nudges per-pet derivados (vacuna vencida, chip, recordatorios, scans, esterilización) + rollup "Al día"/"N pendientes". Owner-data only, sin surveillance (umbrella §6). `lib/owner-nudges.ts` (derivación pura + reads owner-scoped, reusa `lib/projections/pet-microchip`) + `PetHealthStatusStrip` presentacional en `/inicio`. 12 tests integración TDD. Sin schema/event types/migrations. Coordina con Item 6 el split `/inicio` agregador vs perfil detalle (D10). |
| `2026-06-18-pet-profile-v21-reorder-and-action-consolidation-design.md` (Item 6) | 🟢 Ready for CC (§9 cerrado) | (plan post-OK) | De la crítica de diseño 2026-06-18. Reordena el perfil de mascota (hero primero, `PetAlertStrip` priorizado, credenciales/logros dentro de Resumen) y colapsa los 3 hubs de acción: **`/anotar` canónico, `/eventos/nuevo` redirige a él**, una sola forma de anotar en el perfil. Consolida `PetReminders`. Tabs = modelo final del timeline. Lost cockpit conserva acceso al perfil. Sin cambios de datos. |
| `2026-06-18-unified-app-shell-design.md` (Item 7) | ✅ Implementado (PRs #630–#634, strangler A–D) | (plan post-OK) | De la crítica de navegación 2026-06-18. Un solo `AppShell` con variantes `citizen` (barra superior) / `operator` (riel lateral) que reemplaza los 3 chromes (`LnOwnerNav`/`AppHeader`/`OpShell`). Fuente única de nav, decisión auth-aware (`lib/shell-nav.ts`) que **arregla el varado del usuario logueado en surfaces públicas**, drawer mobile único, context switcher. Depende de Item 1 (consume sus `NavSection[]`). Strangler en 4 fases. Sin cambios de datos/rutas. |
| `2026-05-15-timeline-type-filter-design.md` | ✅ Implementado | `plans/2026-05-15-timeline-type-filter.md` | `EventTimeline` tiene prop `chips` + `DEFAULT_FILTER_CHIPS` export |
| `2026-05-16-health-campaigns-and-scheduling-design.md` (v2.1) | ✅ Implementado | `plans/2026-05-16-health-campaigns-and-scheduling.md` | Provider polymorphic (org o vet independiente via sub-rutas `/pro/servicios`). Approval routing a govt scope-matching + admin fallback. Paths `/org/[orgToken]`, `/pro`, `/gob`, `/admin`. 10 fases. |
| `2026-05-17-admin-page-design.md` (v2.3) | ✅ Implementado (Fases 0-9) | — | Cuatro roles (`owner`, `vet`, `govt`, `admin`), dos `account_type`s (personal / institutional). Surfaces `/gob` y `/admin` activos. Migraciones 0010/0011/0015/0016. Fases 10+ continúan en `2026-05-18-admin-page-next-phases-design.md`. |
| `2026-05-18-admin-page-next-phases-design.md` (v3.0) | ✅ Implementado (Fases 10-14) | `plans/2026-05-18-admin-page-fases-10-14.md` | Fases 10-14 shipped en `6f6444a` (F11) + `465b1fc` (F12+13+14+10). Custody disputes (`/gob/disputas`), dashboards regionales govt (`/gob/vigilancia` + `/gob/perdidas`), métricas admin (`/admin/sistema`), bulk ops en cola, auto-expiry cron 60d (`cron_runs` table). Follow-up menor: UI de bulk revoke en las 4 queues de usuarios/orgs (server action ya implementado). |
| `2026-05-18-localities-catalog-design.md` (v1.0) | 🚫 **Superseded** por v2.0 — no implementar | — | Versión inicial chiquita (catálogo curado de 94 entradas hardcoded en TS). Reemplazada antes de implementar por v2.0 que importa el catálogo INDEC completo (~4500). Se mantiene en el repo como referencia histórica del razonamiento. |
| `2026-05-18-localities-catalog-indec-design.md` (v2.0) | ✅ Implementado (parcial — ver follow-ups) | `plans/2026-05-18-localities-catalog-indec.md` | **Reemplaza v1.0**. 4027 localidades INDEC importadas a `ar_localities` desde `https://infra.datos.gob.ar/georef/localidades_censales.csv`. Typeahead `<LocalityCombobox>` montado en `LocationFields` mode="jurisdiction" + CreateGovtForm + AssignLocalityForm. Validación canonical (`resolveCanonicalJurisdiction`) en las 2 actions govt críticas. Migrations 0019 + 0020. Script `normalize-existing-jurisdictions.ts` listo. **Follow-ups:** (a) validar 5 server actions restantes (vet upgrade, org creation, service-offerings, welfare, events) — siguen aceptando texto libre; (b) CABA barrios — INDEC trata CABA como 1 sola localidad, importar barrios de `data.buenosaires.gob.ar` con `source='caba_open_data'`. |
| `2026-05-17-symptom-disease-surveillance-design.md` | ✅ Implementado | `plans/2026-05-17-symptom-disease-surveillance.md` | Match fuzzy texto libre → enfermedades reportables → signal silencioso a autoridad. Owner no ve diagnósticos. |
| `2026-05-17-lost-and-found-complete-design.md` (v1.1) | ✅ Implementado | `plans/2026-05-17-lost-and-found-complete.md` | Microchip cross-check + return-to-owner + broadcast + disclosure prefs owner-controlled + enriched flow para pets sin chip. 7 fases. Paths via `/org/[orgToken]/mascotas/{petToken}`. |
| `2026-05-17-bidirectional-geocoding-design.md` | ✅ Implementado | `plans/2026-05-17-bidirectional-geocoding.md` | Sync bidireccional text ↔ map pin vía Nominatim/OSM proxy. `LocationFields` mode="point" integra text + sync, `MarkLostForm` migrado. UX-only, sin schema changes. |
| `2026-05-18-bite-rabies-observation-design.md` (v1.1) | ✅ Implementado | `plans/2026-05-18-bite-rabies-observation.md` | Bite reporting + observación 10 días, end-to-end. Migración 0021 + columna `pets.rabies_observation_status`. 2 event_types nuevos (`rabies_observation_started/ended`). Owner self-report `/mis-mascotas/[token]/eventos/nuevo/mordedura` + banner. Org-side reporting `/org/[orgToken]/mordedura/nuevo` (capability `bite.report`, vets + shelters). Surveillance escalation: rabia high-spec durante observación → `urgent` + nudge owner. Cron auto-close + script CLI. Death hook (auto-close con outcome=`dead` + escalada urgent). `/admin/observaciones` para cierre profesional con outcome configurable (negative / positive_rabies / dead / lost_to_followup). **Follow-up menor:** configurar cron schedule en vercel.json. Anclaje legal: Decreto 4669/1973 PBA, Ord. CABA 41.831/1987, Res. MS 1144/2018. |
| `2026-05-17-additional-species-design.md` | ✅ Implementado | — | **Bloque 1** (rabbit / guinea_pig / ferret): nested select en `PetForm` + `speciesLabel` extendida + fallback graceful en `lib/diseases.ts` (zoonoses `any`-tagged) y `lib/drugs.ts` (catálogo completo con comentario no-prescriptivo). **Bloque 2** (service dogs Ley 26.858): tabla `petServiceDog` en schema, migration `0026_pet_service_dog.sql`, 5 actions en `app/actions/service-dog.ts` (`upsertServiceDogAction`, `submitServiceDogVerificationRequestAction`, `setServiceDogVisibilityAction`, `retireServiceDogAction`, `revokeServiceDogCredentialAction`), event_type `service_dog_credential_verification`, banner en `/p/[publicToken]` con sub-warning rabies. **Bloque 3** (compliance checklist) son notas técnicas, no implementación. |
| `2026-05-18-physical-tag-design.md` (v1.0) | 🟡 Spec only — pending decisions in §15 open questions | (plan a escribir post-OK) | Chapa identificadora física que cuelga del collar con QR a `/p/[publicToken]`. Modelo Estonia-style: identidad canónica en software, chapa como UX layer. Multi-tag por pet, redirect `/t/[serial]` → `/p/[publicToken]`, activación self-serve, revocación granular. **Material y fabricante son placeholders explícitos** — research de opciones AR (Luna Accesorios, Laser Eleven, K9, Grabados Piroso) + opciones import. Decisiones pendientes: auto-revoke on death, DIY QR, interop con otros sistemas. ~3 días cuando se planee. |
| `2026-05-18-foster-volunteers-pool-design.md` (v1.4) | ✅ Implementado | `plans/2026-05-18-foster-volunteers-pool.md` | Pool de owners voluntarios proactivos para foster. Tabla `foster_volunteers` (preferences + status + `available_slots`) + `foster_proposals` (org-initiated, pet-específica, timeframe estimado, 7d expiry). Volunteer acepta → materializa `ownership.role='foster'` con `allow_co_foster` flag opcional. **Decisiones cerradas v1.4**: D16 modelo de slots single-use (cada inscripción +1, cada aceptación -1, prompt post-termination "¿volver al pool?"); D17 co-foster opt-in por checkbox del primer foster; D18 cascade auto-cancel cuando slots queda en 0. Todo público entre orgs del pool (sin gating de detalle). Notes visibles en listado + count agregado aceptaciones/rechazos. Match warnings (no bloqueos) por mismatch de preferences — la org elige con criterio, el voluntario decide al recibir. Heredados v1.1-v1.3: D13 DNI verificado + perfil hidratado, D14 + §17 adoption eligibility flag, D15 + §6.10 foster con capacidades plenas como owner. 4 fases A-B-C-D shipped. **Follow-up:** aplicar `db/foster_rls.sql` en Supabase Studio. |
| [`../archive/2026-05-18-maltreatment-reporting-design.md`](../archive/2026-05-18-maltreatment-reporting-design.md) | ⚪ **SUPERSEDED — archived** | — | Quedó superseded por la implementación real en `welfare_reports` table (`db/schema.ts:885-983`, `app/actions/welfare.ts`, `app/denuncias/nueva/`). La arquitectura `ghost_subject` propuesta NO debe seguirse — el código usa una tabla separada con `subjectKind` enum polimórfico. Movido a `docs/archive/` en sprint 1 PR-007. Mantenido solo como referencia histórica del análisis comparativo con el form del MPF CABA (https://denuncias.fiscalias.gob.ar/). Para trabajar el feature ver `plans/2026-05-18-welfare-reports-polish.md` + futuro spec del welfare-officer queue. |
| `2026-05-18-adoption-listing-public-design.md` (v1.3 ✅ + v1.4 🟢) | ✅ v1.3 Implementado / 🟢 v1.4 Ready for CC | (v1.4 addendum requiere sistema de casos) | **v1.3 implementado**: 6 fases shipped + F5.5 cascade + org-side review surface. Páginas: `/adoptar`, `/adoptar/[petToken]`, `/adoptar/[petToken]/postular`, `/refugios/[orgToken]`, `/mis-mascotas/postulaciones`, `/org/[orgToken]/adopciones`, `/org/[orgToken]/adopciones/[appEventId]`. Helper `lib/adoption-listing.ts` con `queryAdoptionListing` (4 cross-spec guards D18-D21). Apply intent JWT en `lib/apply-intent.ts` (HMAC, 15min TTL, cookie httpOnly). Migrations 0023 (eligibility) + 0030 (listing columns). 3 server actions de application lifecycle. Cascade auto-rejection en `finalizeAdoptionAction` (F5.5). Después del catalog phase-2 cleanup, los event_types collapsaron a `adoption_application_resolved` (outcome discriminator). **v1.4 addendum** (§12.5): consent obligatorio + vista "Historial del postulante en MiMAR" scope-bound durante review + RLS extensions a pets/ownerships/pet_events del applicant. Depende del sistema de casos para los `cases.adoption_application_id` chained policies. |
| `2026-05-19-cases-event-attachment-design.md` (v1.1) | 🟢 Ready for CC | `plans/2026-05-19-cases-system.md` | Define el objeto **Caso** (expediente) como capa de coordinación liviana sobre el event log: tabla `cases` minimal, columna `case_id` nullable en `pet_events` y `welfare_reports`, 5 modos de attachment (`opens` / `requires-open` / `attaches-when-open` / `optional` / `never`), 13 case_kinds preliminares (7 en subset v1), cascade-emission rules para multi-case closures, visibility scope-bound vía RLS. Catálogo evento × case-kind completo para los 41 event_types. **v1.1** desdobla `adoption_pipeline` en `adoption_listing` (org-side) + `adoption_application` (applicant-side) por asimetría de flujos. Decisiones D1-D5. |
| `2026-05-19-cases-lifecycles-design.md` (v1.0) | 🟢 Ready for CC | `plans/2026-05-19-cases-system.md` | Sucesor del attachment spec. Define lifecycle interno de los 7 kinds del subset v1: estados (open/escalated/closed/merged), phases observables (derived), eventos que abren/cierran/avanzan, crones de auto-cierre + escalation, normativas aplicables (lookup `lib/case-normatives.ts`), matrix de notifications por kind, visibility tweaks específicos. 1 reapertura permitida (`adoption_reversed`), 3 crones de cierre automático, 2 crones de escalation visible. Decisiones cross-kind L1-L10. |
| `2026-05-19-pet-profile-v2-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Rediseño UX del profile en `/mis-mascotas/[publicToken]`: saca el `<EventTimeline>` (queda en `/libreta` y `/historial`), introduce "Casos abiertos" prominent, "Logros" (achievements chips) y "Estado actual" como info derivada del state. 5 achievements POC: `service_dog` + `i_was_adopted` + `lost_and_found` ya computables; `i_had_litter` + `globetrotter` marcados `not_yet_computable` (requieren event_types nuevos). Sin schema migration; compute on-read. ~1 semana. |
| `2026-05-19-bite-from-unowned-animal-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Víctima autenticada reporta bite donde el animal NO está en MiMAR. Tabla `temporary_pet_descriptions` + `cases.primary_subject_kind='unowned_animal'`. 3 vías de matching automatic (microchip / DIM token / DNI). Reconciliation hook on signup: si DNI matchea temps, propone vincular cases históricos a pets nuevas del user. Reasignación manual govt/admin a pet real. Extiende bite-rabies-observation cumpliendo el mismo período legal (10 días) sobre animales no-registrados. Solo autenticados en v1. Depende del sistema de casos. ~8 días. |
| `2026-05-19-pet-profile-v2-design.md` (v1.1) | 🟢 Ready for CC | (plan a escribir post-OK) | **v1.1** agrega §4.7 PPP card (Ley 4078 / 14.107 — display + atestación, export real es placeholder) y §4.8 Service Dog credential card (Ley 26.858 — credential visual destacada en profile + modo presentación). v1.0: rediseño UX profile (saca timeline) + 5 achievements POC. Sin schema migration. ~1 semana. |
| `2026-05-19-pregnancy-tracking-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Embarazo como `clinical_info_logged(sub_kind='pregnancy')`. Pair `pregnancy_phase='started'/'ended'` con outcome (live_birth / stillbirth / miscarriage / termination / unknown). Denormalized flag `pets.pregnancy_status`. Solo female + dog/cat/other. Sección destacada en pet profile v2 mientras in_progress. Activa achievement A4. Capture rápida entries para "está embarazada" / "parió N" / etc. ~5 días. |
| `2026-05-19-eno-vet-direct-report-and-owner-alerts-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Dos extensiones al surveillance: (a) vet con `vet.clinical_write` emite `clinical_info_logged(sub_kind='disease_diagnosis')` que bypassa matcher + emite `outbreak_signal` directo severity critical, (b) owner-facing alerts para zoonoses peligrosas — override D1 del surveillance spec, lista cerrada curada por SME en `lib/disease-public-alert-catalog.ts`. Incluye catálogo legal por disease (`lib/disease-legal-anchors.ts`) anclado a Ley 15.465 + Res. MS 1715/1144/1811 + DL 8056 + Ord. 41.831 + Res. CVPBA 05/2020. ~5 días. |
| `2026-05-19-cross-org-transfer-ux-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | UI completa del handshake refugio→refugio. Activa `custody_transfer_handshake` case_kind del set deferred. Sender form + receiver inbox + accept/reject/cancel + cron expiry 30d. Single-pet por proposal (bulk defer). Capability `org.transfer.propose` y `org.transfer.accept`. Depende del sistema de casos. ~5 días. |
| `2026-05-19-org-abuse-investigation-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Org members de verified org emiten welfare_report con `reporter_organization_id` populated + severity auto-`critical` + notif urgent inmediata + priority sort en `/gob/maltrato` queue. Capability `welfare.report` automatic a TODA la org (roles admin/coordinator/member/vet_individual). Multi-source escalation cuando 2+ orgs reportan misma pet. NO crea case_kind nuevo — extiende welfare_denuncia. ~4-5 días. |
| `2026-05-19-decomiso-welfare-authority-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Govt/admin ejecuta decomiso (Ley 14.346) desde `/gob/decomisos/nuevo` con motivo enum + 2+ adjuntos obligatorios + selección refugio destinatario. Activa `custody_episode` case_kind del set deferred. Handoff govt→refugio con 7 días de expiry (más corto que cross-org 30d). Notif urgent al owner previo. Subject puede ser registered_pet o unowned_animal. Cron expire daily 12h. Depende del sistema de casos + reusa de cross-org transfer receiver UI. ~7-8 días. |
| `2026-05-19-performed-by-autocomplete-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Combobox dual: linked (FK a orgs/vets verified) + texto libre fallback. Schema delta a 6 events (vaccination, deworming, sterilization, vet_visit, clinical_info, microchip) — payload extensions, NO columnas SQL. Helper `searchVetsAndClinics(query, jurisdiction)` con boost por jurisdiction match. Component `PerformedByCombobox` reusable. NO backfill retroactivo. Display libreta distingue linked vs texto. Independiente del sistema de casos. ~5-6 días. |
| `2026-05-19-govt-business-rules-poc-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Framework configurable de business rules por jurisdicción. POC con 3 rule_types PPP. Tabla `govt_business_rules` + defaults en `lib/business-rules-defaults.ts` + resolver con cascada (locality > province > country > defaults) + validators Zod por rule_type. Form específico por rule_type (NO JSON editor genérico). Dashboards `/admin/jurisdicciones/*` (CRUD) + `/gob/reglas` (read-only). Re-evaluation automática de pets afectados (cron) + audit log obligatorio. Solo admin gestiona; govt informa/consume. Independiente del sistema de casos. ~7-9 días. |
| `2026-05-19-pet-spaces-catalog-design.md` (v1.0) | 🟡 Spec only — pending review + plan | (plan a escribir post-OK) | Catálogo polimórfico de lugares físicos relevantes para mascotas: 7 kinds en v1 (`public_pet_space`, `vaccination_point`, `shelter`, `vet_clinic`, `pet_store`, `crematorium`, `pet_cemetery`, `municipal_pet_handling_facility`). Surface owner-facing en `/mis-mascotas/espacios` + widget `/inicio` (MapLibre + clustering). Tres caminos de ownership coexistentes: admin seed (113 espacios CABA) → govt curation scoped por jurisdicción → org self-request via nuevo `approval_request.type='map_listing_request'`. Owner solo lee + reporta correcciones (`pet_space_correction`). 100% informativo (sin event_type nuevo). Kind sensible `municipal_pet_handling_facility` con disclaimer + anclaje Ley 14.346. 10 fases A-J. ~2-3 semanas. 8 preguntas abiertas §15 antes del plan. |

### Plans (implementation, listos para Claude Code)

| Plan | Status | Spec relacionado | Notas |
|------|--------|------------------|-------|
| `2026-05-15-timeline-type-filter.md` | ✅ Implementado | `specs/2026-05-15-timeline-type-filter-design.md` | — |
| `2026-05-16-event-agent-foundations.md` | ✅ Implementado | — (diseño en chat) | `lib/event-agent-registry.ts` con `EVENT_AGENT_REGISTRY` + `buildAgentDeeplink()`. Form de peso retrofitteado como referencia de URL-prefill (acepta `?kg=&occurredAt=&notes=`). Tests en `lib/event-agent-registry.test.ts`. |
| `2026-05-16-vecino-mascota-en-transito.md` | ✅ Implementado | — (diseño en chat) | `custodyKind` field en `PetForm`, `shelter_custody` ownership branch en `createPetAction` |
| `2026-05-16-libreta-sanitaria-parte-a.md` | ✅ Implementado | — | `lib/libreta-sanitaria.ts` con `LIBRETA_SANITARIA_EVENT_TYPES`, `NON_LIBRETA_EVENT_TYPES`, `LIBRETA_FILTER_CHIPS`, helpers + test de cobertura |
| `2026-05-16-libreta-sanitaria-parte-b.md` | ✅ Implementado | — | Ruta `/mis-mascotas/{token}/libreta` con vista agrupada + cronológica + print stylesheet. Componentes `LibretaSanitariaView`, `LibretaIdentityHeader`. Grouping en `lib/libreta-sanitaria.ts`. |
| `2026-05-16-libreta-sanitaria-parte-c.md` | ✅ Implementado | — | Tabla `libreta_share_tokens`, ruta pública `/libreta/compartir/[shareToken]`, server actions `createLibretaShareAction`/`revokeLibretaShareAction`, `SharesManager` owner UI, view-tracking via `pet_events` type `libreta_shared_viewed`. |
| `2026-05-17-mimar-rebrand-and-portal-restructure.md` | ✅ Implementado | — | Paths alineados (`/pro`, `/gob`) + pasada de copy user-facing completa: títulos, notifications, error messages, banners y subjects de email todos como "MiMAR". `DIM` queda exclusivamente como codename interno (schema, tokens `DIM-XXXX-XXXX`, code identifiers, comments, audit logs). |
| `2026-05-17-code-rename-refugio-to-org.md` | ✅ Implementado | — | `app/refugio/` ya no existe; código vive en `app/org/[orgToken]/`. |
| `2026-05-17-symptom-disease-surveillance.md` | ✅ Implementado | `specs/2026-05-17-symptom-disease-surveillance-design.md` | 5 fases shipped (Fases 3+4 combinadas en un solo commit por estar acopladas). Catálogo de 23 síntomas, matcher fuzzy, `outbreak_signal` event type, server action + form, integration tests. TODO inline en `routeOutbreakSignalNotification` para swap a govt-scope routing cuando lande admin page Fase 0. |
| `2026-05-17-lost-and-found-complete.md` | ✅ Implementado | `specs/2026-05-17-lost-and-found-complete-design.md` | 7 fases shipped en PRs #49–#55. Cross-check + match flow + disclosure prefs + enriched + return-to-owner + broadcast + polish. |
| `2026-05-17-bidirectional-geocoding.md` | ✅ Implementado | `specs/2026-05-17-bidirectional-geocoding-design.md` | Server action `app/actions/geocoding.ts` (auth-gated) delega a `lib/geocoding.ts` (proxy Nominatim + token bucket 5 req/sec). `LocationFields` mode="point" integra text + map con debounce 600ms + skipNextForward flag. `LocationPicker` ahora soporta drag del pin además de click. `MarkLostForm` migrado: el input separado `lastKnownLocation` se removió y vive integrado en `LocationFields` con `inputNames.description="lastKnownLocation"`. 18 tests con fetch mockeado en `__tests__/geocoding.test.ts`. Privacy D10: server NO loguea queries. |
| `2026-05-18-bite-rabies-observation.md` | ✅ Implementado | `specs/2026-05-18-bite-rabies-observation-design.md` (v1.1) | Las 6 fases shipped en 6 commits. F0=schema + Zod + capability. F1=owner self-report + UI. F2=surveillance escalation hook. F3=cron + CLI auto-close. F4=org-side reporting + `OrgBiteForm`. F5=death-during-observation hook + `during_rabies_observation` flag. F6=`professionalCloseRabiesObservationAction` + `/admin/observaciones[/[token]]` surface con outcome configurable. Follow-up menor: vercel.json cron schedule. |
| `2026-05-18-event-catalog-cleanup.md` | ✅ Implementado | — | Cleanup estructural del catálogo: borrados 4 events redundantes (subsumidos por `clinical_info_logged`), bites refactoreados bajo `incident_reported` (umbrella + `incident_type` discriminator), agregados 5 events nuevos (`adoption_withdrawn`, `custody_dispute_raised/resolved`, `microchip_replaced/revoked`), deprecado `adoption_application_reviewed`. Migración 0018 agrega `pets.in_custody_dispute`. Test existente de cobertura (`__tests__/event-schemas.test.ts`) ajustado + nuevo orphan-schema check. AGENTS.md Event catalog actualizado a 39 types con sección "Deprecated event types" + nueva subsección "Cross-cutting event design patterns" (4 patterns). Spec de bite-rabies-observation bumpeada a v1.1. |
| `2026-05-19-event-catalog-phase-2.md` | ✅ Implementado | — | Segunda fase del cleanup: 5 colapsos / movimientos. (1) `foster_proposal_{accepted,rejected,cancelled,expired}` → `foster_proposal_resolved` con `outcome` discriminator. (2) `adoption_{revoked,withdrawn}` → `adoption_reversed` con `actor` discriminator. (3) `adoption_application_{approved,rejected}` → `adoption_application_resolved` con `outcome` discriminator. (4) `libreta_shared_viewed` movido fuera de `pet_events` a tabla nueva `share_telemetry` (migration 0032). (5) `microchip_revoked` absorbido por `microchip_replaced` con `new_chip_number: nullable`. Net del catálogo: 48 → 41 event_types. Tests nuevos en `lib/event-schemas.test.ts` (24 assertions para los 3 resolvers + microchip merged); tests existentes adaptados (foster, libreta-share, adoption-review, adoption-cascade). User opt-out de preservation: DB se va a limpiar pronto, no se agregaron catch-all renderers ni retro-compat tests; seed scripts quedan stale hasta el wipe. |
| `2026-05-18-admin-page-fases-10-14.md` | ✅ Implementado (5 fases) | `specs/2026-05-18-admin-page-next-phases-design.md` (v3.0) | Cinco fases shipped: F11 (`6f6444a`) regional dashboards govt `/gob/vigilancia` + `/gob/perdidas` con scoping por jurisdicción. F12+13+14+10 (`465b1fc`) — `/admin/sistema` con 4 cards de métricas + actividad govt; bulk approve/reject en `/admin/cola` y `/gob/cola` con `bulk_action_id` shared en audit_log; cron `/api/cron/auto-expire-approvals` diario 04:00 UTC + tabla `cron_runs` + RLS; `/gob/disputas` + `/gob/disputas/[token]` para resolver custody disputes con migration 0025 (tablas `custody_disputes` + `custody_dispute_parties`). Decisiones operativas registradas en commit: authorRole 'admin' colapsa a 'govt' en pet_events.author_role (precise role en payload); bulk revoke UI queda como follow-up porque el attachment uploader bulk es trabajo aparte. |
| `2026-05-18-localities-catalog-indec.md` | ✅ Implementado (parcial) | `specs/2026-05-18-localities-catalog-indec-design.md` (v2.0) | Fases A-E aplicadas. A=schema (`0019_ar_localities.sql` + `0020_drop_slug_unique.sql`), B=importer `scripts/import-indec-localities.ts` (4027 rows imported), C=helpers `lib/ar-localidades.ts` + `app/actions/localities.ts` (60 req/min rate limit), D=`LocalityCombobox` + 3 forms refactoreados, E=`lib/jurisdiction-validation.ts` + 2 actions govt + `scripts/normalize-existing-jurisdictions.ts`. **Follow-ups en READMEpriorities #7**: validar 5 actions restantes + importar CABA barrios separately. |
| `2026-05-16-health-campaigns-and-scheduling.md` | ✅ Implementado | `specs/2026-05-16-health-campaigns-and-scheduling-design.md` (v2.1) | 10 fases shipped en 7 commits (`f0430f3`..`8ed5a9a`). Schema (4 tablas polymorphic + reminders FK), aprobación org/vet via `findAuthoritiesForJurisdiction`, schedule rules, materialización cron + script + botón, owner search + book (advisory lock + DB constraint para races), attendance + cancelaciones, integración con flujo existente, review UI en `/gob/servicios` + `/admin/servicios`, polish. 24h reminder cron documentado como TODO en `plans/2026-05-18-scheduling-24h-reminder-cron-todo.md`. |
| `2026-05-18-foster-volunteers-pool.md` | ✅ Implementado | `specs/2026-05-18-foster-volunteers-pool-design.md` (v1.4) | 4 fases A-B-C-D shipped en commits `7069d72` (A) → `2a5e3a3` (B) → `fb3d9bb`+`401ea03` (C) → `3308e28`+`8d522a9` (D) → `a92366e` (D12 E2E). A=schema foster_volunteers + foster_proposals + ownerships.allow_co_foster + 6 nuevos EVENT_TYPES. B=adoption_eligible columns + adoption_eligibility_set event + setAdoptionEligibilityAction + gate en finalizeAdoption. C=9 server actions (3 voluntarios + 5 propuestas + matching helper) + extensiones de endFoster/recordDeath/finalizeAdoption + cron `expire-foster-proposals` (patrón close-rabies-observations) + RLS policies en `db/foster_rls.sql`. D=10 pages/rutas UI (mix `/cuenta/*` + `/org/[orgToken]/*`). D12=test E2E al action layer (vi.mock createClient en lugar de Playwright); cazó bug real de ordering en acceptFosterProposalAction que violaba CHECK `foster_proposals_response_consistent` — fix: crear ownership primero, single UPDATE flip atómico. **Follow-up:** aplicar `db/foster_rls.sql` en Supabase Studio. |
| `2026-05-18-welfare-reports-polish.md` | ✅ Implementado | — (cerró gaps de la implementación welfare_reports existente) | 4 fases shipped. F1=bridge bug fix en `app/actions/welfare.ts` — los 3 pet_events INSERT (abandonment/maltreatment/symptom) ahora copian `locationLat/locationLng` desde el welfare_report. F2=`EventMap` renombrado a `LocationMap` y movido a `components/`; los 2 detail pages de denuncia (`/denuncias/[id]` y `/denuncias/codigo/[code]`) renderean el mapa, no solo coords text. F3=rate-limit anónimo persistente — migration 0027 `rate_limit_buckets` + `enforceRateLimit` en `lib/rate-limit.ts` (UPSERT atómico con `ON CONFLICT DO UPDATE`); welfare action consume 1/min + 3/hour por IP solo para anonymous. F4=docs ya marcaban maltreatment-reporting como SUPERSEDED. **NO incluyó** welfare-officer queue `/gob/maltrato` ni moderation queue (specs/plans separados — siguen pending). |
| `2026-05-19-cases-system.md` | 🟢 Ready for CC | `specs/2026-05-19-cases-event-attachment-design.md` (v1.1) + `specs/2026-05-19-cases-lifecycles-design.md` (v1.0) | Plan ejecutable de **sistema de casos**. 7 fases A-G secuenciales con sub-PRs por fase: A=schema (tabla `cases`, `case_id` columns, RLS shells), B=lib code (`case-kinds`, `case-attachment` rules, `case-normatives` lookup, `case-lifecycles/*.ts` per kind, `case-helpers`, `notification-templates`), C=cron infra (4 nuevos + refactor rabies a 12h), D=refactor 5 server-action workflows (welfare, bite/rabies, lost/found, custody dispute, foster) — D6 adoption queda stub hasta plan adoption-listing-public, E=UI `/casos/[publicCode]` + 4 entry points (con 1-click pet button visible en case detail), F=RLS completo `db/cases_rls.sql` + función `can_read_case` + smoke tests cross-case, G=backfill opcional (3 categorías: welfare_reports open, custody_disputes open, observaciones rábicas open). Verificación final con checklist. ~2-3 semanas. |
| `2026-05-19-caba-barrios-import-execution.md` | 🟢 Ready for execution | `specs/2026-05-18-localities-catalog-indec-design.md` (v2.0) — D6 follow-up | El script `scripts/import-caba-barrios.ts` ya escrito (48 barrios Ley CABA 1.777). Pasos: dry-run → live run → verificación SQL → tweak menor de ranking en `LocalityCombobox` para contextual boost cuando user es CABA → tests smoke. Sin lib/schema cambios. ~½ día. |
| `2026-05-19-fix-vet-portal-routing.md` | 🟢 Ready for CC | — (fix doc puntual) | Vet con `professional.provider` granted ahora default landing es `/pro` (no `/mis-mascotas`). Sin block de acceso por URL directa (un vet puede tener mascotas propias). Audit de chrome non-owner para sacar links auto a /mis-mascotas. Placeholder mínimo de `/pro` para que el redirect tenga destino. F1-F4 (F5 ActiveRoleBadge opcional). ~½-1 día. |
| `2026-05-19-fix-service-dog-404.md` | 🟢 Ready for CC | — (fix doc puntual) | Asimetría: el profile muestra link "Perro de asistencia" para todas las pets dog, pero `/asistencia` page filtra estricto `role='owner'` → 404 cryptico para vecinos-en-tránsito, fosters, co_owners, caretakers. Fix: hide link si `role !== 'owner'` (F1) + reemplazar 404 por mensaje friendly explicando que la credencial Ley 26.858 requiere dueño legal permanente (F2). Audit de `devolucion/page.tsx` con mismo patrón (F3). ~½ día. |
| `2026-05-18-additional-species-and-permanent-conditions.md` | ✅ Implementado | `specs/2026-05-17-additional-species-design.md` (species blocks 1+2) + diseño ad-hoc para permanent conditions | Catálogo `lib/permanent-conditions.ts` con 17 códigos (`ciego`, `vision_reducida`, `sordo`, `audicion_reducida`, `tres_patas`, `miembro_no_funcional`, `paralisis_posterior`, `usa_carrito`, `incontinencia_urinaria/fecal`, `epilepsia`, `diabetes`, `fiv_positivo`, `felv_positivo`, `cardiopatia`, `cognitiva`, `otra`). Migration `0031_pet_permanent_conditions.sql` agrega 3 columnas a `pets` + CHECK + GIN index. `PetForm` con fieldset agrupado (sensorial / motor / médico / otro), conditional `permanent_conditions_other` cuando hay `otra`, toggle `disclose_conditions_publicly`. Render condicional en `/p/[publicToken]` (banner indigo) + sección "Necesidades especiales" en `/adoptar/[petToken]`. |
| _(captura-rápida — sin plan formal)_ | ✅ Implementado (commits `bc5f827`, `52498ec`, `c2ca343`) | — (diseñado en chat) | Captura rápida sin LLM. `lib/event-capture-registry.ts` (rename del viejo `event-agent-registry`) con 14 entradas + `buildCaptureDeeplink`. `lib/event-capture-matcher.ts` con patrones regex es-AR para 14 event_types + extractor de fechas naturales (`hoy`, `ayer`, `anteayer`, `hace N días`, `DD/MM[/AAAA]`, `el DD de {mes}`). Página `/mis-mascotas/[publicToken]/anotar` con textarea + 8 quick-action cards. 41 tests del matcher. 13 forms retrofiteados con URL-prefill (peso ya tenía; vacuna/antiparasitario/esterilización/vet/microchip/nota/fallecimiento/checkin con slots, los 5 complejos solo abren sin slots). |
| _(inicio-dashboard — sin plan formal)_ | ✅ Implementado (commits `8c259b2` → `c389418`) | — (diseñado en chat) | Dashboard `/inicio` con 9 widgets: greeting, captura rápida (pet picker), notificaciones (top 5 unread), mis mascotas (top 6), próximos turnos, tratamientos en curso (medication_started sin _stopped), workflows abiertos (7 dominios fan-out con severity badges), workflows previos (top 10 resueltos), news placeholder, regulations placeholder (lista localities de mascotas vivas). Foundation en `lib/owner-dashboard.ts` (8 funciones Promise.all-able). 3 componentes compartidos extraídos (`PetCard`, `NotificationCard`, `AppointmentCard`). Redirect post-login owner: `/mis-mascotas` → `/inicio`. |

| `2026-06-18-projection-primitives.md` | ✅ Implementado | `specs/2026-06-18-projection-primitives-design.md` + `specs/2026-06-18-metrics-ia-handoff-design.md` | **Item 0 — metrics-IA foundation.** `lib/metrics/` con `ProjectionContext`, `suppressSmallCells` (k=5 enforced via branded type), `resolveAnalyticsPeriod`/`windows`, `activePetsCondition`/`dogsInScopeCondition`, React.cache dedup (`cachedActivePetCount`/`cachedDogCount`). `lib/govt-home-kpis.ts` migrado a `ProjectionContext`; duplicados `DashboardActor`/`DashboardJurisdiction`/`petsScopeClause`/`petEventsScopeClause` eliminados de ambos fetcher files. Pattern B documentado en `docs/architecture/hexagonal-lite.md`. K-anonymity gap cerrado. |
| `2026-06-18-wave2-item8-loading-skeletons.md` | ✅ Implementado | `specs/2026-06-18-wave2-ux-hardening-handoff.md` (Item 8) | **Wave 2 Item 8 — Loading & skeleton states.** 4 new skeleton components (`Skeleton`, `OpKpiSkeleton`, `OpCardSkeleton`, `LnCardSkeleton`), shimmer CSS in globals.css, 16 `loading.tsx` files across gob/admin/org/owner/(public) segments. `prefers-reduced-motion` → static via global CSS. `role="status"` + `aria-busy` + SR "Cargando…" on every loading region. 23 RTL/unit assertions in `__tests__/skeleton.test.tsx`. |

**Leyenda:**

- ✅ Implementado — código en main, verificado en repo
- 🟢 Ready for CC — plan auto-contenido, listo para sesión de Claude Code
- 🟡 Spec only — necesita plan escrito antes de implementar
- 🔵 In progress — Claude Code está trabajando ahora

---

## Cross-cutting dependencies

Conviene saberlas porque cruzan varios specs:

- **Portal rename (este plan)** destraba:
  - Coherencia entre todos los specs/plans (todos referencian `/org/[orgToken]`, `/gob`, `/admin`, `/pro` consistentemente)
  - Reducción de fricción cuando llegue el code rename físico de `app/refugio/` → `app/org/`
  - Habilita la implementación de sub-rutas `/pro/servicios` + `/pro/agenda` para vets independientes
- **Admin page Fase 0** (schema foundation: `account_type`, `admin_grants`, `approval_requests`, `audit_log`) destraba:
  - El routing a govts del symptom surveillance (hoy fallback a admins vía `/admin`; post-Fase 0 irá a `/gob`)
  - La aprobación real de vet upgrades (hoy se setea matriculaVerified=true por Studio)
  - La verificación real de organizaciones (hoy se hace por Studio)
  - El scheduling approval del spec de campaigns (hoy no existe)
  - La separación de surfaces `/gob` (approval queue scoped a localidad) vs `/admin` (meta-admin + fallback)
- **Libreta sanitaria Parte A** ya implementado destraba:
  - Parte B (ruta dedicada)
  - Parte C (Tier-2 shareable)
  - El narrativo "quedó en tu libreta sanitaria" del spec de scheduling cuando se implemente
  - Filtros consistentes en el pet profile timeline (ya integrado)
- **Hardening del event-sourcing** (Zod schemas estrictos, validateEventPayload, triggers append-only) ya implementado destraba:
  - El surveillance feature usa `validateEventPayload` para los payloads de `symptom_observed` y `outbreak_signal`
  - El lost-and-found feature extiende Zod schemas de `status_changed`, `custody_transfer_proposed`, `custody_transferred`
  - Cualquier feature futuro que agregue event_types

---

## Convenciones

**Naming:**
- Archivos con prefijo de fecha `YYYY-MM-DD-descripción-corta.md`
- Specs siempre terminan en `-design.md` (e.g., `health-campaigns-and-scheduling-design.md`)
- Plans no terminan en `-design.md` para distinguirlos visualmente
- Versiones en el header del doc (`v1.0`, `v1.1`, `v2.0`), no en el nombre del archivo

**Brand y rutas:**
- Producto user-facing: **MiMAR** (Mi Mascota Argentina). Aparece en title, copy, notifications.
- Codename interno: **DIM**. Schema, code, tokens, audit. Nunca cambia.
- Paths user-facing: `/mis-mascotas`, `/pro`, `/org/[orgToken]`, `/gob`, `/admin`, `/p/[publicToken]`, `/libreta/compartir/[shareToken]`.
- Spanish naming convention para paths (`/gob` abreviado de "gobierno", no `/government`; `/organizaciones` se acortó a `/org` por brevedad consistente con `/admin`, `/pro`, `/p`).

**Cambios mayores a un spec:**
- Bumpear versión en el header
- Documentar qué cambió respecto a la versión anterior en una línea
- Si la implementación correspondiente ya empezó, evaluar si el plan también necesita rewrite

**Cuando un plan se ejecute:**
- El commit final puede actualizar el README marcando el plan como ✅ Implementado
- Mantener el archivo del plan (es referencia histórica de cómo se construyó la feature)

**Cuándo escribir un spec vs un plan directo:**
- Spec si el feature tiene decisiones de producto, cambios de schema, o múltiples archivos
- Plan directo si es un retrofit/fix puntual cuya intención es obvia

**Lectura obligatoria antes de cualquier plan:**
- `AGENTS.md` end-to-end (principles, data model, libreta sanitaria, user roles, etc.)
- El spec correspondiente (cuando exista)
- Cualquier doc que el plan liste en su sección "0. Antes de tocar nada"

---

## Docs fuera de `docs/superpowers/`

**Activos** (`docs/`) — siguen siendo fuente de verdad en aspectos específicos. Citables como canon en specs/plans nuevos:

- `docs/legal-framework-full.md` — framework legal AR vivo, sigue iterando.
- `docs/org-portal-plan.md` — ✅ Implementado en código (`app/org/[orgToken]/*`). Sigue referenciado como canon de flows en plans recientes (por ej. `foster_ended.payload.reason='adoption'` patrón Flow 7).
- `docs/org-portal-event-flows.md` — Flows 1-9 canónicos del org portal. Referenciado por specs/plans nuevos.
- `docs/org-portal-permissions.md` — capability matrix canon. Referenciada por todos los specs que tocan capabilities.
- `docs/patterns/petition-prerequisites.md` — patrón reusable para pre-condiciones de capabilities.

**Archivados** (`docs/archive/`) — prompts históricos y material 2021. Ver `docs/archive/README.md` para inventario:

- Prompts del rebuild 2026 ya implementados: `event-sourcing-hardening-prompt.md`, `org-portal-prompt.md` (se mantienen como referencia histórica del proceso; el feature está vivo en código).
- Material del proyecto universitario 2021: paper CONAIISI, Business Model Canvas, Event Sourcing notes.

Los archivados NO se mantienen activamente. Si algún detalle se vuelve relevante para una decisión nueva, migrarlo a `AGENTS.md` o un spec nuevo en lugar de editar el archivo en `archive/`.

(Notas anteriores mencionaban `v1-closure-prompt.md` y `location-unification-prompt.md` — esos archivos ya no existen, fueron limpiados antes. Esta entrada queda corregida.)
