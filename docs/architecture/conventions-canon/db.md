# Conventions canon — Database, RLS and the event spine

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-05
> Verified against code on 2026-09-05 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `db` · 97 rules (47 ENFORCED, 22 PARTIAL, 28 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-154 | A pull request that modifies db/schema.ts must include a new file under db/migrations/, unless labeled ci:no-migration or the commit contains [no-migration]. | `.github/workflows/ci.yml:462` | ENFORCED | `.github/workflows/ci.yml:456-514` | .github/workflows/ci.yml:498-513 — the step greps the PR diff for db/schema.ts, then for `^db/migrations/[0-9]+.*\.sql$`, and `exit 1` at :513 when the first matched and the second did not. The two escapes at :483-487 ([no-migration] commit token, ci:no-migration label) are exactly the ones the rule names. *Job gated to pull_request events (:460), which is the rule's own scope.* |
| CANON-155 | A second drizzle-kit push against a freshly-migrated schema must report zero pending changes, modulo a documented list of known drizzle-kit false-positive drift statements. | `.github/workflows/ci.yml:581` | ENFORCED | `.github/workflows/ci.yml:578-585` | .github/workflows/ci.yml:615-635 — the step re-runs `pnpm db:push`, exits 0 on 'No changes', strips the ten documented drizzle-kit false positives (:621-631), and `exit 1` at :634 when any `^(ALTER TABLE\|CREATE\|DROP\|DO )` line survives. Subject, predicate and the documented exclusion list all match the rule. *The v3 basis stopped at the step NAME (:581) and the comment; the failing predicate is the shell body at :615-635, which is what makes this one a real CI enforcer rather than a declaration.* |
| CANON-156 | db-doctor-staging must check out the same branch staging deploys from (main), because comparing db/migrations/ on disk against the staging ledger across two different trees produces phantom drift. | `.github/workflows/db-doctor-staging.yml:20` | ENFORCED | `.github/workflows/db-doctor-staging.yml:82` | scripts/check-scheduled-fence-refs.ts:461-466 — for every scheduled workflow not in REF_EXEMPT (which holds codeql.yml only, :106-117), any actions/checkout whose `ref` !== DEPLOY_REF ('main', :85) is a finding. db-doctor-staging.yml is scheduled (:40-41) and pins `ref: main` at :82 and :132. Wired via lint:sched-refs inside `verify` (package.json:76, :79). *Same status as v3 but on a different ground: the v3 basis rested on the literal `ref: main` (which would be P10); the fence at :461-466 is what actually fails. stripComments() at :198 means a `ref:` in prose cannot fake the pin.* |
| CANON-157 | db-doctor-staging must skip (not run) when STAGING_DATABASE_URL is absent, and its alert must distinguish an audited-nothing run from a real pass so it never falsely closes an open alert. | `.github/workflows/db-doctor-staging.yml:110` | PARTIAL (subset) | `.github/workflows/db-doctor-staging.yml:110` | scripts/check-scheduled-fence-refs.ts:561-573 (auditedFindings) fails a workflow that wires red-streak-alert AND carries a step-output guard but passes no `audited:` — that covers the second conjunct (db-doctor-staging.yml:143 passes needs.doctor.outputs.audited). The first conjunct is not covered: :566 `if (!STEP_OUTPUT_GUARD.test(source)) continue;` means deleting the STAGING_DATABASE_URL guard (db-doctor-staging.yml:57-68, :75) silences the fence instead of failing it. *Uncovered part: 'must skip (not run) when STAGING_DATABASE_URL is absent' — the guard's existence is unfenced. The v3 basis reused CANON-156's ground ('the workflow wiring IS the mechanism'), which is exactly the reasoning R2 rejects.* |
| CANON-158 | `pet_events` rows are append-only — never edit or delete; corrections are new events — enforced at the database level by the `enforce_pet_events_append_only` trigger (not RLS). | `CLAUDE.md:14`<br>`AGENTS.md:500`<br>`docs/architecture/rls-coverage.md#table-by-table-45-tables`<br>`docs/agents/collaborating-writer.md:40-42` | ENFORCED | `db/triggers.sql:120,195,200`<br>`db/migrations/0127_pet_events_append_only.sql:32,99,104`<br>`db/migrations/0104_scan_events_retention.sql:34`<br>`__tests__/rls/function-hardening.test.ts:38` | db/triggers.sql:120 defines enforce_pet_events_append_only(); :187-188 raises restrict_violation for every tg_op not caught earlier, and :192-200 attaches it as pet_events_no_update (BEFORE UPDATE) and pet_events_no_delete (BEFORE DELETE) row triggers. db/migrations/0127_pet_events_append_only.sql:32,99,104 is the migration form. It is a trigger, not RLS, as the rule states. *Declared, not verified against the live catalog. Two audited escape hatches exist by design: the GUC override (triggers.sql:136-157, writes audit_log 'pet_events_mutation_override') and the narrow scan-purge DELETE (:163-182, scanner-role credential_scanned older than 90 days, writes 'scan_event_purged'). Both are documented carve-outs, not gaps in the trigger.* |
| CANON-159 | Operational caches (`pets.*` columns, `ownerships`) and government aggregate columns are dual-written by design for hot reads, but must never outrank the event spine — drift is made observable via `rederivePetCache` and a detect-only cron rather than pretended away, and no dashboard read may treat a cache as the log. | `CLAUDE.md:15`<br>`AGENTS.md:146` | PARTIAL (declared-limit) | `__tests__/pet-cache-rederivation.test.ts:163-177`<br>`lib/infra/rederive-pet-cache.ts`<br>`scripts/detect-pet-cache-drift.ts` | __tests__/pet-cache-rederivation.test.ts:163-195 re-derives every DIM-% pet's cache columns and fails on drift (non-vacuity floor :174); :219-239 adds one whole-padron death_recorded invariant with a deaths>0 guard. The file's header :29-31 declares what is NOT covered — `owner`, `foster` and `shelter_custody` ownership rows have NO drift detection — and the rule's 'no dashboard read may treat a cache as the log' half has no enforcer. *Header :201-207 quantifies the sweep's reach (43 pets of 32.430 before the whole-corpus death invariant). Basis called the shape 'subset'; the fence's own header states the exclusion, which is R5.* |
| CANON-160 | The DNI must never be stored or compared in plaintext: equality matching uses `WHERE dni_hash = hashDni(input)` (`lib/utils/dni-hash.ts`), and human disambiguation in operator UI uses `dniLast4()` only. | `CLAUDE.md:17`<br>`AGENTS.md:1601-1608`<br>`docs/agents/collaborating-writer.md:58-59` | PARTIAL (adoption-unfenced) | `__tests__/dni-hash-pepper.test.ts:128-176`<br>`db/schema.ts:321-322,414-416` | __tests__/dni-hash-pepper.test.ts:128-175 pins the helpers' behaviour (dniLast4 returns exactly 4 chars and never the full DNI; hashDni matches two independently computed HMAC vectors) and db/schema.ts:321-322,414-416 shows only dni_hash/dni_last4 columns with a unique index — but no fence forces a call site to use hashDni()/dniLast4(), and no scripts/check-* bans a plaintext DNI column or comparison. *Same gap the v3 basis names (a new plaintext column cannot fail); shape recorded as adoption-unfenced rather than subset because the rule's predicate is the helpers' behaviour with adoption implied.* |
| CANON-161 | Every event is an immutable, append-only fact about a pet's life, and no view may be treated as source of truth over the event log. | `AGENTS.md:22` | UNENFORCED | — | No fence tests whether a read path derives from events rather than a cache. The append-only half duplicates CANON-158 (trigger); the 'no view is source of truth' slogan is the SUPERSEDED wording — CLAUDE.md invariant #3 replaced it on 2026-07-24 and AGENTS.md:22 still carries it. *Refuter contradiction #3 confirmed: AGENTS.md:22 contradicts CLAUDE.md invariant #3 and CANON-159 at d7dbf25f7.* |
| CANON-162 | The data model must include columns and roles for veterinary and government actors from day one, so those actors can be activated later with no schema rewrite. | `AGENTS.md:142` | UNENFORCED | — | A design-history statement with no checkable predicate: nothing can fail on 'a future actor needed a schema rewrite'. No fence enumerates actor-readiness columns. |
| CANON-163 | Do not change the pet-cache fitness sweep back to selecting all pets in the DB; scope it to seed pets whose publicToken starts with DIM-. | `AGENTS.md:1510` | UNENFORCED | `__tests__/pet-cache-rederivation.test.ts:163-177` | The cited enforcer IS the artifact the rule constrains: __tests__/pet-cache-rederivation.test.ts:167-170 selects `where(like(pets.publicToken, 'DIM-%'))`. Nothing fails if that where-clause is widened back to the whole table — no fence, test or type reads this file's scope. Self-executing declaration, true at this SHA. *The basis cites the where-clause and the seedPets.length>0 floor; both are the declaration itself. Widening would re-introduce the documented flake (:144-150), i.e. nondeterministic reds — not an enforcer.* |
| CANON-164 | Use generatePublicToken() from lib/infra/publicToken.ts for any seed pet whose cache fitness should be enforced by CI. | `AGENTS.md:1516` | UNENFORCED | `__tests__/pet-cache-rederivation.test.ts:153,163-177` | The cited enforcer's predicate is cache drift, not token provenance: __tests__/pet-cache-rederivation.test.ts:167-170 merely SELECTS the DIM-% pets, so a seed pet minted with any other token silently leaves the swept set instead of failing anything. The static seed fence explicitly declines to look: scripts/check-seed-ids.ts:17-19 'Deliberately NOT flagged: publicToken/referenceCode-shaped id columns (PANO- … is an accepted, documented exception)'. *The basis states the same mechanism ('silently outside the fence') and reads it as enforcement; a violation removes coverage rather than producing a red, so nothing can fail.* |
| CANON-165 | All migrations must use IF EXISTS / IF NOT EXISTS guards; do not remove them. | `AGENTS.md:1526` | UNENFORCED | — | No fence scans db/migrations/*.sql for unguarded DDL. The only check-*.ts hit for 'IF NOT EXISTS' is check-storage-write-policies.ts, which uses the clause in its own SQL rather than auditing migrations. *scripts/check-ledger-honesty.ts (db:doctor) audits migration EFFECTS, not guard syntax — a different rule (see missingRows).* |
| CANON-166 | Every `pets` row must have a corresponding `pet_registered` event in the append-only spine, with no grandfather baseline for existing rows — a cache row with no spine event is forbidden. | `AGENTS.md:1548`<br>`scripts/check-spine-integrity.ts:3` | ENFORCED | `scripts/check-spine-integrity.ts:3-27`<br>`package.json:70` lint:spine<br>`__tests__/check-spine-integrity.test.ts` | scripts/check-spine-integrity.ts:227-260 lists every pets row with no pet_registered event and exits 1; there is no grandfather baseline (header :9-14) and partitionOrphans (:96-106) exempts ONLY seed_tag='perf', never a token prefix. lint:spine is in the verify chain (package.json:79); the partition logic is separately pinned by __tests__/check-spine-integrity.test.ts. *Two stated conditions: the one exemption is pets.seed_tag='perf' (:76, printed on every run), and the fence exits 0 as a SKIP when the DB is unreachable or the host is non-local without --allow-remote (:166,:198) — so a DB-less CI box passes it silently. I weighed the perf exemption as an R5 declared limit and did not cap on it: it is a named non-production fixture class, and the rule's own operative clause (no grandfather baseline) holds exactly.* |
| CANON-168 | Institutional accounts must have dni_hash IS NULL, CHECK-enforced by the profiles_institutional_no_pii constraint. | `AGENTS.md:1610` | ENFORCED | `db/schema.ts:441-444`<br>`db/migrations/0106_dni_less_identity.sql:119` | db/schema.ts:441-444 declares check profiles_institutional_no_pii: `account_type = 'personal' or (dni_hash is null and matricula_number is null and matricula_jurisdiccion is null and miarg_sub is null)` — an institutional row with dni_hash set fails. The only DROP is 0106_dni_less_identity.sql:116 (drop-then-recreate inside the migration that authored it). *Declared, not verified against the live catalog. The constraint is strictly wider than the rule (also bars matricula/miarg_sub on institutional rows).* |
| CANON-169 | Set DNI_HASH_PEPPER in the Vercel environment before real DNI data lands; a mismatched pepper makes every stored hash mismatch and reversible via rainbow table. | `AGENTS.md:1612` | ENFORCED | `__tests__/dni-hash-pepper.test.ts:1-16,24` | __tests__/dni-hash-pepper.test.ts:29-49 asserts hashDni THROWS /DNI_HASH_PEPPER/ on a remote-DB production deploy when the pepper is unset, when it is still the committed dev default, and on a self-hosted (non-VERCEL) prod deploy; :51-56 is the non-vacuity arm (a real secret hashes). The operational requirement is made fail-closed at runtime. *The literal 'set it in Vercel env' is not repo-checkable; what fails is hashing without it.* |
| CANON-170 | Every table (especially a new PII/tenant-scoped one) must have Row Level Security ENABLED in its migration, and be classified as either carrying at least one RLS policy or explicitly allowlisted as intentional deny-all (`DENY_ALL_ALLOWLIST` / `RLS_INTENTIONALLY_EXCLUDED`, with a reason); a public table left unclassified, or an `RLS_REQUIRED` table that loses catalog-level RLS, must fail CI; owner-scoped tables are also added to the cross-tenant e2e probes. | `AGENTS.md:1614-1621`<br>`scripts/check-rls-coverage.ts:5`<br>`docs/architecture/rls-coverage.md#authz-contract-two-layers`<br>`__tests__/rls/coverage.test.ts:1-20,226-317` | PARTIAL (subset) | `scripts/check-rls-coverage.ts:422`<br>`package.json:36` lint:rls<br>`__tests__/rls/coverage.test.ts:226-317`<br>`__tests__/check-rls-coverage.test.ts` | scripts/check-rls-coverage.ts:419,473-477 fails any public table with relrowsecurity=false or zero policies unless keyed in DENY_ALL_ALLOWLIST (:75), reading pg_class/pg_policies live (:147-155); __tests__/rls/coverage.test.ts:227-260 fails an RLS_REQUIRED table without RLS and any public table classified in neither list. Uncovered clause: nothing fails when an owner-scoped table is missing from the cross-tenant e2e probes — no script or test reads e2e/cross-tenant-isolation.spec.ts's table coverage. *Previous basis addressed only the classification half, which I agree is doubly enforced; the trailing e2e-probe clause of the rule text has no enforcer. Both live mechanisms skip loudly when DATABASE_URL is remote/unreachable (script :42-51), so the DB half is real only on the local verify run.* |
| CANON-171 | Never return a raw event payload JSONB blob to a caller; project only the fields callers need. | `AGENTS.md:1625-1627` | UNENFORCED | — | The cited lint:events (check-event-payload-parity.ts) has a DIFFERENT predicate: its header :10-11 says every payload key READ must be a key some schema can write. It never inspects what a resolver RETURNS, so a `select payload` handed straight to a caller passes it. *Downgraded from draft PARTIAL: the cited enforcer was a name/topic match on 'payload', not a predicate match.* |
| CANON-172 | Push visibility/privacy decisions into the SQL query, not into render-layer redaction after fetch. | `AGENTS.md:1634-1636` | UNENFORCED | — | No fence distinguishes a SQL-side predicate from a JS-side filter. lint:view-scope and lint:scope govern jurisdiction scope-clauses, not the fetch-then-redact shape this rule bans. |
| CANON-173 | Scan events must never carry the raw IP; scanner-role payloads are limited to is_self_scan, viewer_authenticated, scan_ip_area, scan_coords?, scan_accuracy_m?. | `AGENTS.md:1649` | ENFORCED | `lib/events/event-schemas.ts:765-769` (credentialScanned, .strict())<br>`__tests__/log-scan-location.test.ts:192,220,289` | lib/events/event-schemas.ts:765-789 defines credentialScanned as a z.object(withVersion({ is_self_scan, viewer_authenticated, scan_ip_area?, scan_coords?, scan_accuracy_m? })).strict() — a CLOSED key set matching the rule's list exactly, so a raw-IP key cannot pass validateEventPayload. __tests__/log-scan-location.test.ts:221-227 supplies x-real-ip with no geo headers and asserts scan_ip_area is null and the raw IP never reaches the row; :282 asserts self-scans carry no scan_ip_area at all. *Adoption of validateEventPayload on the write path is via lib/events/validated-event-values.ts:32, which parses the insert values.* |
| CANON-174 | scan_coords/scan_accuracy_m may be captured only when the pet is lost and the scanner explicitly granted browser geolocation, re-checked server-side. | `AGENTS.md:1651` | ENFORCED | `__tests__/log-scan-location.test.ts:233,245,257` | __tests__/log-scan-location.test.ts:233-243 drives logScanAction with petRow.status='lost' + coords and asserts payload.scan_coords/scan_accuracy_m are stored (rounded server-side); :245-255 sets status='active', passes the same coords and asserts the payload has NEITHER property; :257-265 drops out-of-range coords; :202-203 the no-coords case. Real writer, captured insert. *The 'granted' half is structural — coords only exist in the payload when the browser granted them; the fenced half is the server-side status re-check.* |
| CANON-175 | Scanner-role event rows must always have recorded_by_user_id = NULL, with no scanner identity link even when authenticated. | `AGENTS.md:1652` | PARTIAL (subset) | `__tests__/log-scan-location.test.ts:192,206`<br>`src/modules/pets/application/scans/log-scan.ts:186` | __tests__/log-scan-location.test.ts:197,206-218 pins recordedByUserId null + authorRole 'scanner' + no visitor id anywhere, for the scan writer (log-scan.ts:186-187). The basis calls that 'the single writer'; it is not — src/modules/pets/application/sighting/report-pet-sighting.ts:277-278 also emits authorRole 'scanner' with a bare recordedByUserId: null literal that no test asserts (sighting tests hold only a fixture, lost-mode-sightings.test.ts:109) and no DB CHECK covers. *rg '"scanner"' over non-test source finds exactly two producers; both comply today, one is fenced.* |
| CANON-176 | Scan events (`pet_events` rows with `event_type='credential_scanned'`, `author_role='scanner'`) must be purged after a 90-day retention window via the audited cron job; self-scans (`author_role='owner'`) are never purged, and every purged row produces an `audit_log` entry (`action='scan_event_purged'`). | `AGENTS.md:1654-1656`<br>`docs/architecture/rls-coverage.md#sensitive-set--backstop-status` | ENFORCED | `__tests__/scan-retention.test.ts:9-14,143-180`<br>`db/triggers.sql:113-180`<br>`db/migrations/0104_scan_events_retention.sql:34`<br>`__tests__/cron-purge-scan-events-route.test.ts` | db/triggers.sql:163-182 — the purge escape hatch fires only for DELETE + author_role='scanner' + event_type='credential_scanned' + occurred_at older than retention_days (90, :131), and inserts an audit_log row action='scan_event_purged' per deleted row (:169-179); anything else falls through to the append-only rejection. __tests__/scan-retention.test.ts:145-170 / :174-202 / :206+ probe the TTL boundary and the owner self-scan against the live DB. *declared, not verified against the live catalog. No test asserts the audit_log row itself (rg found no 'scan_event_purged' assertion in either test) — the trigger body is what guarantees it; the action name is pinned by __tests__/audit-log-action-check.test.ts:91. Live 90 matches the doc.* |
| CANON-177 | caretaker_email must be shown only to the titular in their own care panel, never on a public surface or to a third party. | `AGENTS.md:1675` | UNENFORCED | — | Refuter verdict reused: __tests__/caretaker-public-contact.test.ts asserts only that the public resolver discloses the caretaker's FIRST name and phone (:93-176); caretakerEmail appears as fixture input at :52 and is never asserted absent. No fence covers the negative. |
| CANON-178 | erase_subject_data must never touch the caretaker_email of the grantor (the third party's own email), only that of the invited caretaker as subject. | `AGENTS.md:1684` | ENFORCED | `__tests__/subject-rights-caretaker-foster-contact.test.ts` | __tests__/subject-rights-caretaker-foster-contact.test.ts:345-355 calls the live public.erase_subject_data(grantor) RPC and asserts row.caretakerEmail === INVITEE_EMAIL (untouched); :495-516 asserts the INVITEE-side erase sentinels the same column. Both halves of the rule pinned against the real function, not a fixture. *DB-backed test, vitest db project, under test:verified. The v3 basis capped this at PARTIAL explicitly because the assertion was not confirmed at line level; :353-355 is that assertion, with the decision stated in the comment above it.* |
| CANON-179 | Ending an accepted caretaker grant must always go through endCaretakerGrantAtomically() as the single writer, never a bare SQL status flip without the paired event. | `AGENTS.md:1685` | UNENFORCED | — | endCaretakerGrantAtomically is defined once (lib/infra/end-pet-ownerships.ts) but no fence enforces exclusivity: nothing scans for a bare UPDATE on pet_caretaker_grants.status the way check-titular-gate.ts propagates a required guard by effect. *check-spine-integrity.ts's second arm covers the analogous rehome-sponsorship orphan (CANON-216) but has no caretaker-grant arm.* |
| CANON-180 | Every table in the public schema must be declared in exactly one of `IN_EXPORT`, `IN_ERASE`, `EXEMPT` (with a reason), or `KNOWN_GAP` (with a note) — verified both ways against the live `export_subject_data`/`erase_subject_data` function bodies; `KNOWN_GAP` must not grow past its declared ceiling (17) without the ceiling being raised in the same commit. | `AGENTS.md:1689`<br>`scripts/check-subject-rights-coverage.ts:14`<br>`__tests__/check-subject-rights-coverage.test.ts:1-9,74-171` | ENFORCED | `scripts/check-subject-rights-coverage.ts:254`<br>`package.json:37` lint:subject-rights<br>`__tests__/check-subject-rights-coverage.test.ts:154,160,170` | scripts/check-subject-rights-coverage.ts:246-345 runs all four checks against the live pg_get_functiondef bodies — unclassified table (:256), a table in two lists (:271), forward direction (an IN_* entry the body does not mention), reverse direction (an EXEMPT/KNOWN_GAP entry the body DOES mention, :339-344) — exiting 1 at :404/:460/:489. The ceiling is pinned in __tests__/check-subject-rights-coverage.test.ts:154-170 in BOTH directions (<=17 at :160 and >=17 at :170), so closing a gap cannot be re-spent. *Header :19-28 states the limit honestly: it proves MENTION, not predicate correctness. Same skip-on-non-local-DB contract as lint:spine (:77-80). The ceiling lives in the test, so that half rides test:verified rather than verify.* |
| CANON-181 | The subject-rights coverage fence proves table mention only, not that the redaction predicate is correct. | `AGENTS.md:1689` | UNENFORCED | — | This is a declared LIMITATION of check-subject-rights-coverage.ts, not a rule with a violation: nothing can fail on it. Confirmed against the fence — its checks 1-4 classify tables against live function bodies and never assert what a redaction does to a column's value. *Downgraded from draft ENFORCED. A self-description of a coverage gap is not an enforced rule; it belongs in the audit as a known gap, and the row is kept so a later pass does not re-derive it.* |
| CANON-182 | The `KNOWN_GAP` table count must live in exactly one place in AGENTS.md (§7), fenced against `Object.keys(KNOWN_GAP).length` so it cannot silently go stale; every number stated in the subject-rights-coverage header must itself be derived/fenced, not hand-typed. | `AGENTS.md:1691`<br>`scripts/check-subject-rights-coverage.ts:38` | PARTIAL (declared-limit) | `__tests__/documented-subject-rights-counts.test.ts:128-172,199-206` | __tests__/documented-subject-rights-counts.test.ts:200-210 pins AGENTS.md §7's cell to GAP = Object.keys(KNOWN_GAP).length (:59) and :128-197 derives every count in scripts/check-subject-rights-coverage.ts's header (:32-36 six/twenty-two/sixteen, plus the three GAP sentences) — but the file's own header at :19-26 declares that a claim MOVED into wording it does not know about is invisible to it. *The anchor-disappearance case IS caught loudly (:118-120); the residual is a restatement in novel wording, which is the 'silently go stale' the rule forbids. The v3 basis reads :199-206 as asserting §7 is the ONLY place; what it asserts is that §7's cell is live and that no OTHER count in PROSE_PATHS contradicts it.* |
| CANON-183 | Reporting content must be a derivation on read (notReportedClause subtracting the flagged event id), never a mutation of the reported event. | `AGENTS.md:1703` | ENFORCED | `__tests__/content-report-read-coverage.test.ts:222,225,287-290`<br>`db/triggers.sql:120,195,200` | db/triggers.sql:192-200 creates pet_events_no_update/pet_events_no_delete BEFORE UPDATE/DELETE running enforce_pet_events_append_only() (:120, raising at :187 'pet_events is append-only'), so mutating the reported event is refused at the DB; the derivation-on-read half is fenced by __tests__/content-report-read-coverage.test.ts:221-223,281-292. *declared, not verified against the live catalog; the trigger object is referenced by db/migrations (0066, 0079, 0104, 0121).* |
| CANON-184 | Every production read of `pet_events` that can surface a lost-feed `note_added` message must apply `notReportedClause()` — or be a declared exemption with a written reason, or be triaged as not-a-lost-note-read — with the full enumeration derived from code and verified in both directions; no such reader may be unaccounted for. | `AGENTS.md:1707`<br>`__tests__/content-report-read-coverage.test.ts:1-25,281-293` | PARTIAL (declared-limit) | `__tests__/content-report-read-coverage.test.ts:191,222,229,243-262,287-290` | __tests__/content-report-read-coverage.test.ts:281-292 sweeps the tree for unaccounted readers and :232-279 checks both directions, but the fence's own header at :209-216 declares the gap: shape 3 (a reader with no event_type filter) 'is not detectable in general', lib/metrics/event-ledger.ts and lib/analytics/dashboards/exports.ts are hand-listed, and 'a sixth such reader added tomorrow WILL sail through this fence'. *MUST_SUBTRACT holds TEN entries (:49-60) while the AGENTS.md quote says twelve; no numberKey on the row, so recorded not scored. The v3 basis cites the same both-direction assertions but not the declared limit above them.* |
| CANON-185 | Subject access (art. 14) and erasure (art. 16) must be served only through the export_subject_data / erase_subject_data RPCs, never ad hoc queries. | `AGENTS.md:1738-1739` | UNENFORCED | — | check-subject-rights-coverage.ts enforces that every table is CLASSIFIED against the two function bodies; it never enforces that the app reaches those functions rather than writing its own export/erase query. No fence scans app/ or src/ for an ad-hoc subject-rights path. *__tests__/subject-rights-rpcs.test.ts exercises the RPCs themselves; it does not enumerate callers.* |
| CANON-186 | submitter_ip must expire after 30 days for everyone, not only for a subject who requested erasure. | `AGENTS.md:1743` | ENFORCED | `lib/infra/data-lifecycle.ts:102` ORG_CONTACT_IP_TTL_DAYS<br>`__tests__/cron-data-lifecycle.test.ts:671-755` | __tests__/cron-data-lifecycle.test.ts:717-744 inserts two ORDINARY org_contact_messages rows (no erasure request anywhere in the fixture), runs purgeOldOrgContactIps() and asserts the past-TTL row's submitter_ip is null, the fresh one's survives, and the message body is untouched — that is the 'for everyone' half. :746-756 pins convergence. lib/infra/data-lifecycle.ts:102 ORG_CONTACT_IP_TTL_DAYS = 30, used as the cutoff at :309. *The value 30 is live in the constant, but the test derives its fixtures from ORG_CONTACT_IP_TTL_DAYS (:719,:748) rather than from a literal, so a change to the constant would not be caught by an assertion — the behaviour is pinned, the policy number is not. Not enough to cap the row: the rule's contested clause is 'for everyone', and that is what the test exercises.* |
| CANON-187 | The account must not store province or locality; jurisdiction is a fact of the pet, not the account. | `AGENTS.md:1744` | UNENFORCED | — | db/schema.ts:391-392 still declares profiles.jurisdictionProvince / jurisdictionLocality; the comment at :378-389 records that migration 0205 removed the WRITER, removed the form field and nulled both columns, keeping them only so export_subject_data's row_to_json shape is stable. Nothing fails if a new writer appears. *Not a doc contradiction: the columns exist but are inert by migration 0205, which is what the doc's 'no guarda' means in practice. It is unfenced, not untrue.* |
| CANON-188 | pet_events is append-only by design and therefore exempt from soft-delete. | `AGENTS.md:1745` | UNENFORCED | — | Refuter verdict reused: no soft-delete classification registry lists pet_events as EXEMPT, and check-rls-coverage.ts has no soft-delete exemption list. __tests__/public-soft-delete-resolution.test.ts:360 documents the survival by design but does not assert the exemption as a rule over all tables. |
| CANON-189 | Every pet-holder access resolver must filter isNull(pets.deletedAt) on both the person and org paths so a soft-deleted pet resolves to 404 everywhere in one place. | `AGENTS.md:1746` | ENFORCED | `__tests__/public-soft-delete-resolution.test.ts` | __tests__/public-soft-delete-resolution.test.ts:523 (org path — a surviving shelter_custody sponsorship on an erased pet) and :527 (person path — a surviving live owner row) both assert resolvePetHolderAccess returns { kind: 'none' }, with :531-537 non-vacuity through both paths on a live pet. The two isNull(pets.deletedAt) terms sit at lib/infra/pet-access.ts:354 and :407, and requirePetAccess/requireAlivePetAccess/requireTitularAccess funnel through the same function (:215). *The basis explicitly says per-branch coverage 'was not confirmed at line level'; it is, at :523 and :527. getFormerOwnerReadAccess carries its own isNull(pets.deletedAt) at pet-access.ts:656 with no runtime assertion.* |
| CANON-190 | Enabling or tightening an RLS policy can never break the app: the action edge (Drizzle via the `BYPASSRLS` postgres role) is the sole authoritative authorization layer for all tables, and RLS is defense-in-depth for the PostgREST surface only — it never governs the action edge, so enabling deny-all for PostgREST writes is always safe. | `AGENTS.md:1002-1007`<br>`docs/architecture/rls-coverage.md#authz-contract-two-layers` | UNENFORCED | — | An architectural premise, not a checkable predicate: nothing can fail on 'tightening RLS would have broken the app'. The nearest fences (lint:rls, lint:scope-authz, __tests__/rls/*) enforce that RLS EXISTS and is scoped, never that the action edge is the only authority. |
| CANON-191 | Owner-facing RLS policies in db/rls.sql must be applied via Supabase Studio, never via pnpm db:push, which would propose dropping unmodeled policies. | `AGENTS.md:1024` | UNENFORCED | — | An operator procedure, not a repo predicate. No fence detects that db:push was used; the drift it would cause is visible to lint:rls / __tests__/rls/coverage.test.ts only after the fact and only as a missing policy, not as a violation of this rule. |
| CANON-192 | When a data-model field list in AGENTS.md disagrees with db/schema.ts, the schema wins and the AGENTS.md sketch is treated as the bug. | `AGENTS.md:357-359` | UNENFORCED | — | A precedence rule for humans reading two documents. No fence diffs AGENTS.md's field sketches against db/schema.ts (contrast __tests__/event-catalog-count.test.ts, which DOES fence one specific documented number). |
| CANON-193 | Exactly one of owner_user_id or owner_organization_id must be set per ownerships row, CHECK-enforced. | `AGENTS.md:424` | ENFORCED | `db/schema.ts:1096-1099` | db/schema.ts:1096-1099 declares check ownerships_polymorphic_holder: `((owner_user_id IS NOT NULL)::int + (owner_organization_id IS NOT NULL)::int) = 1` — exactly-one, so neither both-set nor neither-set can be stored. No DROP CONSTRAINT for it in db/migrations. *Declared, not verified against the live catalog.* |
| CANON-194 | At most one active ownerships row per pet may have role='owner', enforced by a partial unique index. | `AGENTS.md:425` | ENFORCED | `db/schema.ts:1100-1102` | db/schema.ts:1100-1102 declares uniqueIndex ownerships_one_active_owner_per_pet on (pet_id) where `role = 'owner' AND ended_at IS NULL` — a second live owner row on the same pet raises 23505. No DROP INDEX for it in db/migrations. *Declared, not verified against the live catalog.* |
| CANON-195 | At most one active caretaker ownerships row per pet is allowed, enforced by a partial unique index, duplicating the cap already held by pet_caretaker_grants because ownerships is what RLS and reads join against. | `AGENTS.md:434` | ENFORCED | `db/schema.ts:1108-1110` | db/schema.ts:1108-1110 declares uniqueIndex ownerships_one_active_caretaker_per_pet on (pet_id) where `role = 'caretaker' AND ended_at IS NULL`; the comment at :1103-1107 states the reason (the owner index is `where role='owner'` and constrained caretaker rows not at all). No DROP INDEX for it in db/migrations. *Declared, not verified against the live catalog.* |
| CANON-196 | A caretaker must never be able to transfer, list for adoption, change jurisdiction, edit identity, issue a libreta link, or designate another caretaker; the deny-list lives in lib/domain/titular-only.ts and is enforced by requireTitularAccess and the RLS function has_titular_write_access(). | `AGENTS.md:435`<br>`scripts/check-titular-gate.ts:8` | ENFORCED | `scripts/check-titular-gate.ts:51,79-82,91`<br>`package.json:24` lint:titular-gate<br>`db/rls.sql:98-107,210,400`<br>`db/migrations/0190_titular_only_rls.sql:77-93,129-130`<br>`db/migrations/0199_revoke_anon_titular_write_access_oracle.sql:68`<br>`lib/domain/titular-only.ts` | scripts/check-titular-gate.ts indexes every exported async function and object-literal async method, propagates titular-only EFFECTS transitively along call edges (:42-51), and flags any function that authorizes through a PERSON_PATH_GUARD (:98-102) and reaches an effect without requireTitularAccess (:91). TITULAR_GATE_ALLOWLIST is EMPTY at :117, so there is no ratchet slack. lib/domain/titular-only.ts:54-118 carries all six denied capabilities as ids (transfer-initiation, adoption-eligibility-publishing, jurisdiction-change, caretaker-sub-designation, libreta-share-minting, identity-field-edits). lint:titular-gate is in verify. *Effect-shaped, not call-site-shaped, so a new module is caught on first commit. Honest residuals stated at :58-72 (regex over file-local bodies; an event type built from a variable escapes) — which is why the RLS counterpart public.has_titular_write_access() exists as an uncorrelated second layer. I did not open db/rls.sql or migrations 0190/0199; the previous basis cites 0190:77-130 and 0199:68 as live, and the lint arm alone already carries the verdict.* |
| CANON-197 | opened_reason_params must be {} when a reason code has no params; a CHECK constraint makes 'code without params' unrepresentable. | `AGENTS.md:445` | PARTIAL (subset) | `db/schema.ts:4213-4216` | db/schema.ts:4213-4216 check cases_opened_reason_structured_pair (`opened_reason_code is null or opened_reason_params is not null`) makes (code, NULL) unrepresentable — the rule's second clause. The `{}` clause is not enforced at write: src/modules/cases/domain/opened-reason.ts:109-208 is a strict discriminated union, but its only parse site is the READ path (opened-reason-display.ts:48); the write path (cases-repository.ts:88, `const { code, ...params } = openedReason`) never parses, so a non-literal carrying extra keys reaches opened_reason_params unchecked. *CHANGED FROM MY DRAFT (ENFORCED) after opening opened-reason.ts:109-208 and rg-ing OpenedReasonSchema across src/lib: my draft assumed the closed union was applied at the write door. It is not — the only .parse/.safeParse is on display. tsc's excess-property check still catches literal call sites, and check-opened-reason-coverage.ts:25-27,51 confines the write to cases-repository.ts, so practical coverage is high; the named residue (param-less code + junk params) is real and matches the previous basis and the judge.* |
| CANON-198 | Only one pending PetTransfer per pet is allowed, enforced by a partial unique index on (pet_id) WHERE status='pending'. | `AGENTS.md:473` | ENFORCED | `db/schema.ts:4539-4541` | db/schema.ts:4539-4541 declares uniqueIndex pet_transfers_one_pending_per_pet on (pet_id) where `status = 'pending'` — the second concurrent pending transfer on one pet raises 23505; closed transfers are outside the predicate so retries stay possible (rationale at :4536-4538). No DROP INDEX for it in db/migrations. *Declared, not verified against the live catalog.* |
| CANON-199 | A microchip code must be unique among active pet_identifications rows, enforced by a partial unique index on (code) WHERE kind='microchip_iso' AND status='active'; tattoo codes may legitimately collide. | `AGENTS.md:486` | ENFORCED | `db/schema.ts:4627-4629` | db/schema.ts:4627-4629 declares uniqueIndex pet_identifications_chip_unique on (code) where `kind = 'microchip_iso' AND status = 'active'` — active chips cannot collide, and the predicate leaves tattoo rows (and retired chips) unconstrained, exactly as the rule says. No DROP INDEX for it in db/migrations. *Declared, not verified against the live catalog. Sibling CHECK chip_requires_iso_fields at :4630-4633 pins the 15-char length (see CANON-499).* |
| CANON-200 | An event with a real-world location must populate the top-level PetEvent.location_point column, never duplicate it inside payload. | `AGENTS.md:839` | UNENFORCED | — | No fence compares an event's location_point against its payload. lint:events (check-event-payload-parity.ts) checks only that a READ key is writable by some schema, so a schema that legitimately declares a location key inside payload would pass it. *Note the rule is stated as 'should' in the source, which is why no fence was built.* |
| CANON-201 | The pet is the credential: it must be a globally-unique public token resolving to a QR-verifiable public page. | `CLAUDE.md:13` | ENFORCED | `db/schema.ts:463`<br>`__tests__/public-token-in-url-invariant.test.ts:154` | db/schema.ts:463 declares pets.public_token as text().notNull().unique() — global uniqueness of the credential token is a live DB constraint. __tests__/public-token-in-url-invariant.test.ts:183 fails when an internal id is interpolated into a route whose segment declares a public token, over a corpus with a non-vacuity floor of 500 links (:158-163, 932 measured 2026-08-21). *The 'QR-verifiable public page' half is not itself pinned by either enforcer — uniqueness and public-token-in-URL are; QR rendering is not. Same conclusion as the previous basis, reached from the same two enforcers.* |
| CANON-202 | No assertion may compare a clock instant read on the host against a column defaulted with defaultNow(); take the instant from the database or drop the window and match on payload instead, and never widen the comparison with a tolerance. | `CLAUDE.md:43`<br>`docs/agents/handoff-2026-08-30.md:152-160` | UNENFORCED | — | Refuter verdict reused: __tests__/_helpers/db-now.ts:31 exports dbNow() — the REMEDY CLAUDE.md prescribes — but nothing scans assertions for `new Date()` compared against a defaultNow() column. A helper is not an enforcer; no lint:* key and no fence test covers the predicate. *CANON-228 folded in (pattern 5, same subjectKey): it stated the same rule from the handoff and scored ENFORCED because it named the helper. The refuter's opened-enforcer verdict governs, not the average.* |
| CANON-203 | Never widen a time-window assertion with a tolerance to paper over host/DB clock drift. | `CLAUDE.md:43` | UNENFORCED | — | Same absence as CANON-202: no fence inspects assertion shapes. A tolerance added to a timestamp comparison is invisible to biome, tsc and every check-*.ts script. *Kept separate from CANON-202: the subject is the same incident but the predicate (a tolerance constant) is distinct and separately checkable if anyone builds the fence.* |
| CANON-204 | Migrations are forward-only and immutable (`db/migrations/NNNN_*.sql`); recount the next free integer at write time — never hardcode one from an earlier plan. | `CLAUDE.md:45`<br>`docs/agents/collaborating-writer.md:41-43` | UNENFORCED | — | No fence detects a duplicate or out-of-order migration number, and none detects an edit to an already-applied migration file. drizzle's own journal would notice a checksum change at apply time, but that is not a repo gate and nothing in verify reads it. *scripts/check-ledger-honesty.ts (db:doctor, not in verify) audits migration EFFECTS via probes — a different rule, and unmapped by any canon row (see missingRows).* |
| CANON-205 | pnpm db:bootstrap is idempotent and localhost-only, and should be preferred for a fresh setup because it applies triggers, RLS, and storage policies that db:push alone does not. | `CONTRIBUTING.md:27` | UNENFORCED | — | A setup instruction. Nothing fails if a developer uses db:push instead; the consequence (missing triggers/RLS/storage policies) would surface later through lint:rls or __tests__/rls/*, not as a violation of this rule. |
| CANON-206 | The `account_type`/`role` pairing invariant (`account_type='personal'` ↔ `role` in `{owner, vet}`; `account_type='institutional'` ↔ `role` in `{govt, admin}`) is enforced exclusively at the application layer for every write path — the database CHECK that migration 0015 added was dropped by migration 0016, so do not assume the DB refuses a mismatched pair. | `README.md:46`<br>`__tests__/account-type-role-invariant.test.ts:1-16,272-311` | PARTIAL (subset) | `__tests__/account-type-role-invariant.test.ts:1-16,272-311` | __tests__/account-type-role-invariant.test.ts:12-22 lists FIVE write paths by hand and tests those; the fitness sweep at :282-308 is scoped `inArray(profiles.id, createdUserIds)` (:287), so it only sees rows this file created. Nothing enumerates the writers of profiles.role/account_type, so a sixth write path is unfenced; DB CHECK dropped by db/migrations/0016_drop_role_match_check.sql. *The app-layer-only half is true at this SHA (0016 drops the CHECK); the prior basis rested it on db/schema.ts:434-440, which does not change the 'every write path' gap.* |
| CANON-207 | `registryCounts()`'s dormancy cutoff must be bound as an ISO string, never a raw JS Date, when interpolated into a `sql` fragment. | `__tests__/census-registry-counts.test.ts:1-11` | ENFORCED | `__tests__/census-registry-counts.test.ts:114` | __tests__/census-registry-counts.test.ts:114-132 runs registryCounts against real Postgres with a real trailing-12m window and asserts the dormant/active/total/incomplete counts; :134-145 re-runs it with a custom 36-month threshold. Re-interpolating a raw Date makes postgres-js throw ERR_INVALID_ARG_TYPE and both go red. *Behavioural, not textual: the fence is the driver's own throw, exercised against the live DB (db project).* |
| CANON-208 | Every JSON key read from a `pet_events.payload` column in application code must be a key actually written by some schema in the `lib/events/*event-schemas.ts` family, and every schema-written key must have a live reader or be accounted for. | `__tests__/check-event-payload-parity.test.ts:1-9,281-327`<br>`scripts/check-event-payload-parity.ts:10-11` | PARTIAL (subset) | `scripts/check-event-payload-parity.ts:10-11,448`<br>`package.json:22` lint:events<br>`__tests__/check-event-payload-parity.test.ts` | scripts/check-event-payload-parity.ts:435-451 checks one direction only — each extracted read key against the union of schema-written keys — and runScan (:417-475) contains no converse pass: nothing reports a schema-written key with no live reader. The second half of the rule has no enforcer. The read half is itself gated for JS accessors at :358 (/\bpetEvents\b/). *The only other failure modes are an empty read-file set (:426-429) and stale baseline entries (:463-468). Prior basis reached the same shape from the same missing direction.* |
| CANON-209 | The live `pg_proc` body of every repo-owned Postgres function must match the source that claims to define it: `db/triggers.sql` is authoritative for the functions it defines, and for a migration-only function the last (highest-numbered) migration wins. | `scripts/check-function-parity.ts:11`<br>`__tests__/check-function-parity.test.ts:1-3,53-71` | ENFORCED | `scripts/check-function-parity.ts:123`<br>`package.json:88` check:function-parity<br>`scripts/check-ledger-honesty.ts:412`<br>`.github/workflows/ci.yml:853-881` | scripts/check-function-parity.ts:89-105 builds the authoritative body per function — db/triggers.sql overrides, otherwise the last defining migration — and :148-153 compares each against pg_proc.prosrc, setting exit 1 at :227 (exit 2 at :183 for an unreachable database). .github/workflows/ci.yml:853-881 runs `pnpm check:function-parity` as its own step against the job's local 127.0.0.1:54322 database. *Deliberately outside pnpm verify (ci.yml:878-879) because verify must run without Docker; package.json:88 is the manual key, the CI step is the wiring that can fail. Its `if:` guard keys on the seed step, not on the parity result.* |
| CANON-210 | `openedReason` prose must never be written as a literal string/template outside `opened-reason-prose.ts`, and only `cases-repository.ts` may write the `opened_reason` column directly. | `__tests__/check-opened-reason-coverage.test.ts:1-8,22,79` | PARTIAL (declared-limit) | `scripts/check-opened-reason-coverage.ts`<br>`package.json:55` lint:opened-reason<br>`__tests__/check-opened-reason-coverage.test.ts:22,79` | scripts/check-opened-reason-coverage.ts:164 excludes every scripts/seed* file from SCAN_FILES by declaration (:163), and scripts/seed-panorama.ts:2702,2861,2989 plus scripts/seed-demo-scenario.ts:508 assign literal `openedReason: "..."` outside opened-reason-prose.ts - rule-1 violations the fence cannot see. Rules 1/2 themselves are real (:76-116) and lint:opened-reason is in verify (package.json:55,79). *The v2 basis rested on __tests__/check-opened-reason-coverage.test.ts:22,79; I opened it - every case there feeds synthetic content to scanRule1/scanRule2, so it says nothing about the SCAN_FILES corpus that drops the seeds. __tests__/ and *.test.ts are excluded too (:162), justified as fixtures; production app/lib/src coverage is genuine.* |
| CANON-211 | Every RLS policy must name its roles explicitly (`pg_policies.roles` must not be the bare `{public}` default) unless allowlisted with a reason, and no `storage.objects` SELECT/ALL policy may grant read access through a predicate that never references `auth.uid()`. | `scripts/check-rls-coverage.ts:9`<br>`scripts/check-rls-coverage.ts:12`<br>`__tests__/check-rls-coverage.test.ts:29-99` | ENFORCED | `scripts/check-rls-coverage.ts:9,12`<br>`package.json:36` lint:rls<br>`__tests__/check-rls-coverage.test.ts:29-99` | scripts/check-rls-coverage.ts:215-218 flags every pg_policies row whose roles array is exactly ['public'] unless allowlisted in PUBLIC_ROLE_ALLOWLIST, and :327-332 flags a storage.objects SELECT/ALL policy admitting a caller role whose qual never contains 'auth.uid()'; both feed the violation list that exits 1 at :502 (messages :483, :488). lint:rls is in verify (package.json:36,79). *Reads the live catalog, not migrations (:28-31). Skips loudly against a remote or unreachable DB (:42-57), so the run that counts is the local verify. I did not need __tests__/check-rls-coverage.test.ts (the basis's citation) — the live script's two predicates decide it.* |
| CANON-212 | Every table `erase_subject_data` reaches must also be returned by `export_subject_data`, unless declared in the frozen `ERASE_ONLY_KNOWN` exception list — art. 16 (erasure) may not reach further than art. 14 (export) without disclosure. | `__tests__/check-subject-rights-coverage.test.ts:174-215` | ENFORCED | `scripts/check-subject-rights-coverage.ts`<br>`package.json:37` lint:subject-rights<br>`__tests__/check-subject-rights-coverage.test.ts:174-215` | __tests__/check-subject-rights-coverage.test.ts:190-199 computes every IN_ERASE table absent from IN_EXPORT and fails unless it is in ERASE_ONLY_KNOWN (live: case_events, libreta_share_tokens); :202-208 fails a stale exception entry, so closing one frees no slot. The IN_* lists it reads are themselves verified both ways against the live RPC bodies by the sibling fence. Discovered by vitest's mechanical partition (vitest.config.ts:24). *v3 recorded lint-script/verify; the deciding assertion is in the vitest file (the script contains no ERASE_ONLY_KNOWN), so kind=vitest and wired=test:verified. Status unchanged.* |
| CANON-213 | Every event type whose payload schema has `payload_version > 1` must have at least one upcaster registered, and each upcaster's output must match a golden v1→latest fixture exactly. | `__tests__/event-upcaster-coverage.test.ts:1-22,122-167` | ENFORCED | `__tests__/event-upcaster-coverage.test.ts:1-22,122-167`<br>`__tests__/event-schemas.test.ts:80,118` | __tests__/event-upcaster-coverage.test.ts:107-109,122-130 throws listing every type whose schema literal is >1 with no entry in registeredUpcasterTypes(), with a non-vacuity floor at :114-117; the golden side at :167-173 asserts upcastPayload(...) toEqual the exact v2 shape. lib/events/event-upcasters.ts:39-50 registers exactly one upcaster, and it is the one that fixture covers. *v3 basis was file-existence plus header prose; the predicate checks out on the lines. Two soft spots: latestVersionFromSchema falls back to 1 when introspection fails (:40-44), and nothing would force a golden fixture for a FUTURE second upcaster — today's registry has one entry, fully covered.* |
| CANON-214 | `loadGobPetSubView`'s open-cases list must include only cases within the govt operator's own jurisdiction (province+locality), never a sibling barrio or another province, even when the operator legitimately reaches the pet through an in-scope case. | `__tests__/gob-pet-subview-jurisdiction-fence.test.ts:1-17,133-167` | ENFORCED | `__tests__/gob-pet-subview-jurisdiction-fence.test.ts:1-17,133-167` | __tests__/gob-pet-subview-jurisdiction-fence.test.ts:81-120 seeds three real open cases (CABA/Palermo, Salta, CABA/Almagro) against one pet, then :134-141 asserts a Palermo govt operator's openCases has length 1 and is the in-scope id, :143-157 asserts the out-of-province and sibling-barrio ids are absent, and :159-166 loads all three as admin — the control that stops the exclusions passing on an empty list. *Real Postgres through loadGobPetSubView, no mocks; runs in the vitest 'db' project. v3 basis was file-presence; the predicate checks out on the lines.* |
| CANON-215 | Only the professional-close use-case and the death-record cascade may emit a `rabies_observation_ended` event or set `pets.rabies_observation_status` to a `completed_*` value; no production code may name an `ownerClose*Observation` action. | `__tests__/rabies-professional-closure-fence.test.ts:1-20,72-126` | ENFORCED | `__tests__/rabies-professional-closure-fence.test.ts:1-20,72-126`<br>`db/schema.ts:717` petsRabiesObservationStatusValid | __tests__/rabies-professional-closure-fence.test.ts:83-88 flags any file outside ALLOWED_WRITERS emitting `eventType: "rabies_observation_ended"`; :111 flags setObservationStatus(…, "completed_*"); :122 flags any ownerClose*Observation identifier. Corpus app/+src/+lib minus tests (:59-65) with a >200-file floor (:69) and a bites-control (:92-100). *The status half anchors on the helper spelling; the only production writer of pets.rabies_observation_status is the repository method (src/modules/events/infrastructure/events-repository.ts:666-679), so the single write door is what the anchor names. The basis's extra citation (db/schema.ts:717 value-domain CHECK) constrains values, not authorship — it does not change the verdict either way.* |
| CANON-216 | Every hand-off that closes a rehome-sponsorship `shelter_custody` row must write a matching `rehome_sponsorship_ended` spine event (same `ownership_id`) in the same transaction; an unmatched started event with an ended/missing custody row is drift. | `scripts/check-spine-integrity.ts:33`<br>`__tests__/rehome-sponsorship-drift.test.ts:1-25,160-183` | ENFORCED | `scripts/check-spine-integrity.ts:29-42`<br>`package.json:70` lint:spine<br>`__tests__/rehome-sponsorship-drift.test.ts` | scripts/check-spine-integrity.ts:125-160 queries every rehome_sponsorship_started with no rehome_sponsorship_ended carrying the same payload.ownership_id whose custody row is ended or missing, and :262-283 exits 1 on any hit — no baseline, no exemption. __tests__/rehome-sponsorship-drift.test.ts:156-215 plants both orphan shapes against a real DB and proves an ended event for a DIFFERENT ownership_id does not heal one. *The 'same transaction' half is not statically checked; the fence detects the resulting drift after the fact, which is the shape the rule itself names as drift. Same skip-on-unreachable/remote caveat as CANON-166.* |
| CANON-217 | Every project-owned Postgres function flagged by the Supabase advisor must pin `search_path = ''`, and `export_subject_data`/`erase_subject_data`/every SECURITY DEFINER function in `public` must not be EXECUTE-able by `anon`. | `__tests__/rls/function-hardening.test.ts:1-21,79-194` | ENFORCED | `__tests__/rls/function-hardening.test.ts:1-21,38,58,79-194`<br>`db/migrations/0114_advisor_security_warns.sql:42`<br>`db/migrations/0199_revoke_anon_titular_write_access_oracle.sql:68` | __tests__/rls/function-hardening.test.ts:80-112 queries pg_proc.proconfig and fails on any listed function without a pinned search_path; :168-192 is catalog-DERIVED (`p.prosecdef and has_function_privilege('anon', …)`) with an EMPTY ANON_EXECUTE_ALLOWED (:73), and :194-209 floors the SECURITY DEFINER population at 6 so it cannot pass vacuously. *Live-catalog probe, not a declaration read; runs in the vitest `db` project.* |
| CANON-218 | A govt operator's RLS-level read of `pet_service_dog` and `pet_identifications` must be scoped to their own jurisdiction (province+locality via govt_assignments); an out-of-jurisdiction row must not leak through PostgREST, and a setup failure must fail the suite rather than skip. | `__tests__/rls/matrix-govt-jurisdiction.test.ts:1-21,29-30,175-216` | ENFORCED | `__tests__/rls/matrix-govt-jurisdiction.test.ts:1-21,29-30,175-216` | __tests__/rls/matrix-govt-jurisdiction.test.ts:195-220 probes a real out-of-jurisdiction fixture through PostgREST as a signed-in govt operator (0 rows on pet_service_dog and pet_identifications) with admin positive controls; :159-168 throws when PostgREST rejected the CREDENTIAL so a 401 cannot read as 'closed'; :77-86 throws out of beforeAll on any setup failure. *The province+locality policy TEXT is asserted at catalog level in __tests__/rls/coverage.test.ts per the comment at :207-209 (not opened); this file carries the behavioural half plus the fail-loud half.* |
| CANON-219 | Per-role allow/deny access must be behaviorally probed by a matrix test, not just structurally checked; the probe suite must throw and fail on any setup error rather than degrade to a green skip, and every role × table cell must declare all 4 operations. | `docs/architecture/rls-coverage.md#backstop-gap-assessment-ws-a-finding`<br>`__tests__/rls/matrix.test.ts:16-29,773-798` | PARTIAL (declared-limit) | `__tests__/rls/matrix.test.ts:16-29,773-798`<br>`__tests__/rls/matrix.data.ts` | __tests__/rls/matrix.test.ts:64 `OPERATIONS_UNDER_TEST = ["select"]` and the header at :8-9 ("See the OPERATIONS_UNDER_TEST constant below for the gate; extend it when payload helpers are added") — insert/update/delete cells are DECLARED (:781-794 all 4) and thrown-on-setup (:778, :805) but never behaviourally probed (:812-825 switch has only a select probe). *The v3 basis rests on the 4-op completeness arm and the absence of a skip guard, both of which I confirmed; neither speaks to the probe coverage, which is the half the rule's first clause asks for. Write allow/deny is covered structurally elsewhere (__tests__/rls/write-path-matrix.test.ts), not by this matrix's probes.* |
| CANON-220 | The `ownerships` table must expose no INSERT/UPDATE/DELETE policy reachable by `anon` or `authenticated` through PostgREST, and an attacker holding a legitimate row must not be able to repoint it to a victim pet or self-promote its role. | `__tests__/rls/ownerships-write-lockdown.test.ts:1-29,152-263` | ENFORCED | `__tests__/rls/ownerships-write-lockdown.test.ts:1-29,152-263` | __tests__/rls/ownerships-write-lockdown.test.ts:153-174 fails on any pg_policies row for ownerships with cmd in (INSERT,UPDATE,DELETE,ALL) reachable by anon/authenticated/public; :190-212 attacks with four forged roles, :214-234 repoints at a victim pet and :236-259 self-promotes, each verified against Drizzle ground truth; :265-297 are the legitimate-path controls. |
| CANON-221 | `pet_tags` SELECT must be scoped to the activating user or a current pet owner, `TO authenticated` only — a cross-owner stranger and anon must read zero rows. | `__tests__/rls/pet-tags-select.test.ts:1-16,115-140` | ENFORCED | `__tests__/rls/pet-tags-select.test.ts:1-16,115-140` | __tests__/rls/pet-tags-select.test.ts:126-134 an authenticated stranger reads [] for the victim serial, :136-147 anon reads nothing, and :116-124 is the positive control (the owner reads exactly their row) so the zeros are denials rather than a dead client; sign-in throws on error (:102-104). |
| CANON-222 | No RLS-enabled public table may expose an unconditional SELECT to `anon`/`authenticated`/`public` unless explicitly allowlisted with a non-empty justification. | `__tests__/rls/read-path-matrix.test.ts:1-36,121-162` | ENFORCED | `__tests__/rls/read-path-matrix.test.ts:1-36,121-162` | __tests__/rls/read-path-matrix.test.ts:133-145 derives offenders from pg_policies (PERMISSIVE, cmd SELECT/ALL, RLS-enabled public base tables) whose USING is absent or `true` and reachable by anon/authenticated/public, minus a one-entry allowlist; :122-131 floors the introspected surface at >0, :147-160 kills stale allowlist entries, :162-169 requires a non-empty reason. *Sibling of CANON-241 (scripts/check-scope-authz.ts item 4), which the refuter validated over the scope-gated subset; this one covers every RLS-enabled table.* |
| CANON-223 | The `revocations` storage bucket's INSERT policy must restrict uploads to institutional admin/govt accounts — the predicate must not be the bucket name alone. | `__tests__/rls/revocations-bucket.test.ts:1-20,85-138` | ENFORCED | `__tests__/rls/revocations-bucket.test.ts:1-20,85-138`<br>`scripts/check-storage-write-policies.ts` | __tests__/rls/revocations-bucket.test.ts:94-115 fails any storage.objects INSERT/ALL policy whose with_check names the bucket without also constraining on profiles/role; :117-136 probes anon and a signed-in non-authority upload (plus a service-role list confirming nothing landed); :138-147 is the admin positive control and :86-92 pins the bucket private. |
| CANON-224 | No RLS policy over a `deleted_at`-bearing table may reference `deleted_at` in its USING clause today — the zero count is the tracked open finding, not an implemented suppression guarantee. | `__tests__/rls/soft-delete-read-surface.test.ts:1-40,573-600` | ENFORCED | `__tests__/rls/soft-delete-read-surface.test.ts:1-40,573-600` | __tests__/rls/soft-delete-read-surface.test.ts:580-605 joins pg_policies to information_schema.columns on column_name='deleted_at' for cmd in (SELECT,ALL) and fails if ANY qual matches /deleted_at/; :594-597 guards the join against a vacuous zero. The rule states the zero as the tracked finding, which is exactly what the assertion pins. *Live count is 0 policies referencing deleted_at; the test goes red when the fix arrives, by design. ENFORCED describes the fence, not the safety of the system.* |
| CANON-225 | The `welfare-evidence` storage bucket must grant no `anon`/`authenticated` policy of any kind (list/get/upload); only the service-role signing path may read or write it. | `__tests__/rls/welfare-evidence-bucket.test.ts:1-29,109-176` | ENFORCED | `__tests__/rls/welfare-evidence-bucket.test.ts:56-58,65,109-176` | __tests__/rls/welfare-evidence-bucket.test.ts:120-142 fails any storage.objects policy whose qual/with_check names the bucket and is reachable by anon/authenticated/public (list/get/upload alike); :148-174 probes anon LIST at root, LIST inside a known folder, signed-URL minting and upload; :176-184 repeats them for an authenticated stranger; :110-118 pins the bucket private. *Basis adds :56-58 (throws on missing env rather than skipping), consistent with what I read of the file's setup.* |
| CANON-226 | No RLS-enabled public table may expose an unconditional write (INSERT/UPDATE/DELETE) to `anon`/`authenticated` unless explicitly allowlisted with a non-empty justification. | `__tests__/rls/write-path-matrix.test.ts:1-38,157-194` | ENFORCED | `__tests__/rls/write-path-matrix.test.ts:1-38,157-194` | __tests__/rls/write-path-matrix.test.ts:166-181 derives offenders from pg_policies (PERMISSIVE writes on RLS-enabled public base tables) whose gating clause (:122-132, with_check for INSERT, qual otherwise) is absent or `true` and low-trust reachable; INTENTIONAL_UNCONDITIONAL_WRITES is EMPTY (:49-77), the strictest state, and :158-164 floors the introspected surface at >0. *The 'non-empty justification' clause (:194-201) is vacuous against an empty allowlist, but the substantive rule is at its strictest.* |
| CANON-227 | A seed script may only fabricate `event_notification_outbox` rows with `status: "delivered"` if it also sets `attempts >= 1`, since the real drainer never delivers with zero attempts. | `__tests__/seed-outbox-invariants.test.ts:1-18,64-76` | PARTIAL (subset) | `__tests__/seed-outbox-invariants.test.ts:1-18,64-76` | __tests__/seed-outbox-invariants.test.ts:64-76 flags an insert window containing '"delivered"' and NOT matching /\battempts:/ — presence only. A seed writing `attempts: 0` alongside status delivered passes, which is precisely the state the rule says production cannot produce; the >= 1 half of the rule has no assertion. *Scope and non-vacuity are sound: scripts/**/*.ts (:34) with a floor and a named-file pin at :56-62. The v3 basis describes it as 'a state assertion over seeded rows' — it is not: :33-51 is a static source scan of scripts/**/*.ts with no DB client, which is why the value half cannot be checked.* |
| CANON-229 | Domain facts must enter through flows, never raw INSERTs, and if a raw INSERT seems unavoidable a QA/data agent must stop and hand the need to the main agent. | `docs/agents/qa-mutation-contract.md:7-9` | UNENFORCED | — | An agent-behaviour contract, not a repo predicate: the violation happens in a session, not in a committed file. The nearest structural backstop is the append-only trigger plus lint:spine, which catch the CONSEQUENCE (an orphan pets row) rather than the act. *CANON-230 is the enforceable half of this same contract (re-derive the projection afterwards) and IS covered.* |
| CANON-230 | If a QA/data agent inserts events directly, it owns the projection: `pnpm vitest run __tests__/pet-cache-rederivation.test.ts` must be green afterward. | `docs/agents/qa-mutation-contract.md:10-12` | PARTIAL (declared-limit) | `__tests__/pet-cache-rederivation.test.ts:163-177` | __tests__/pet-cache-rederivation.test.ts:163-195 goes red when a directly inserted event leaves a DIM-% pet's cache behind, and :219-239 catches death_recorded drift across the whole padron. But the header :201-207 declares the reach it lacks — the sweep 'watched 43 pets out of 32.430', and the excluded PANO-* corpus is where drift actually lived — so a direct insert on a non-DIM pet touching any non-status column fails nothing. *QA/data agents mutate the PANO-* seed corpus, which is exactly the excluded class. The basis stops at 'a direct insert that skips the dual-write turns it red', true only inside DIM-% or for death.* |
| CANON-231 | app/gob/analytics/export/actions.ts must write one analytics_export_generated audit_log row per export, carrying actor, schema version, slices, format, period, jurisdiction, storage path, and per-slice row/rejected counts. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | PARTIAL (subset) | `scripts/check-audit-log-coverage.ts:14-18`<br>`package.json:20` lint:audit-log<br>`scripts/check-audit-log-actions-declared.ts`<br>`db/schema.ts:2381` | app/gob/analytics/export/actions.ts:279 writes action 'analytics_export_generated'; scripts/check-audit-log-coverage.ts:321-362 only requires SOME reachable audit write for an operator+mutating action, and check-audit-log-actions-declared.ts only requires the literal to be catalogued. No enforcer asserts the payload fields (actor, schema version, slices, format, period, jurisdiction, storage path, per-slice counts) or one-row-per-export. *rg 'analytics_export_generated' finds no test asserting the row's payload (the only test hit, __tests__/check-audit-log-coverage.test.ts:108, is a fixture string)* |
| CANON-232 | The retention_until column on profiles, pets, pet_identifications, and custody_disputes must stay inert (no writer sets it, no cron purges by it) until legal retention periods are decided. | `docs/architecture/retention-policy-pending-decision.md#context` | UNENFORCED | — | lib/infra/data-lifecycle.ts:16-19 restates the rule as a standing instruction ('These columns are intentionally left inert... DO NOT add purge logic here without explicit sign-off') — but a comment cannot fail. No fence enumerates writers of retention_until or purge targets. *Live confirmation of the four tables: db/schema.ts:403, :665, :1213, :3637, :4618, :4708 carry retentionUntil — note that is SIX declarations, more than the four the doc names; the extra ones were not resolved to table names in this pass.* |
| CANON-233 | The data-lifecycle cron (/api/cron/data-lifecycle) must purge only targets with explicit non-PII expiry semantics — notifications, rate-limit buckets, cron_runs. | `docs/architecture/retention-policy-pending-decision.md#context` | PARTIAL (subset) | `__tests__/cron-data-lifecycle.test.ts` | __tests__/cron-data-lifecycle.test.ts:594-600 pins result.backlogged to exactly five target keys (rateLimitBuckets, notifications, pushSubscriptions, orgContactIps, cronRuns), so a sixth purger fails there; but nothing asserts the rule's 'explicit non-PII expiry semantics' predicate — org_contact_ips (raw IPs) and push_subscriptions are purged and sit outside the doc's three-target list. *docSays: 3 (notifications, rate-limit buckets, cron_runs). Live purger set is 5 (:37-41). The v3 basis said nothing constrains the target set; :594-600 does constrain it via the injected-purger result shape, which does not rescue the non-PII predicate.* |
| CANON-234 | erase_subject_data must anonymize PII on request per Ley 25.326 art. 16, and the product must never invoke a nonexistent legal retention obligation to refuse an erasure request. | `docs/architecture/retention-policy-pending-decision.md#errata-2026-08-17--the-sanitary-event-retention-rationale-was-false` | ENFORCED | `__tests__/privacy-retention-claim.guard.test.ts` | __tests__/privacy-retention-claim.guard.test.ts:139-149 bans nine retention-obligation phrasings and the three norm citations (41.831 / 14.072 / SENASA) on both citizen subject-rights files, comments stripped (:56-62); :170-172 requires the page to state 'no invocamos ninguna obligación legal de conservación' and :162-176 the rest of the honest replacement, so deleting the paragraph does not satisfy it. *The art.16 anonymization half of the rule rests on scripts/check-subject-rights-coverage.ts (verify gate lint:subject-rights) plus the erase_subject_data migrations — not opened in this pass, so that half is asserted, not verified here.* |
| CANON-235 | pet_events retention after an erasure request must be defined as part of the same decision — sanitary events on a soft-deleted pet currently have no retention_until column and no defined period. | `docs/architecture/retention-policy-pending-decision.md#errata-2026-08-17--the-sanitary-event-retention-rationale-was-false` | UNENFORCED | — | Refuter verdict reused: the rule is itself a statement of an OPEN decision (source doc is retention-policy-pending-decision.md); pet_events has no retention_until column to check. Nothing to enforce until the PO decides. |
| CANON-236 | pet_identifications and pet_transfers must carry no write RLS policies — inserts/updates rely solely on the action-edge service-role bypass, by design. | `docs/architecture/rls-coverage.md#sensitive-set--backstop-status` | UNENFORCED | — | __tests__/rls/write-path-matrix.test.ts bans UNCONDITIONAL writes on every RLS-enabled table; it does not assert that these two tables carry ZERO write policies. A narrow, conditional write policy added to either would pass every existing fence while violating this rule. |
| CANON-237 | Every audit action literal the application writes must be present in the AUDIT_LOG_ACTIONS catalog, derived from source rather than from a hand-maintained list. | `scripts/check-audit-log-actions-declared.ts:28` | ENFORCED | `scripts/check-audit-log-actions-declared.ts:1034`<br>`package.json:21` lint:audit-actions<br>`db/migrations/0184_audit_log_action_check.sql` | scripts/check-audit-log-actions-declared.ts:28-53 derives every written action literal from source (direct .insert(auditLog), the lib/infra/audit-log.ts helpers, and a cross-file relay index) and requires membership in AUDIT_LOG_ACTIONS; floors :163-167 (catalog 100 / writers 4 / files 60 / sites 80 / actions 60) fail the run at :991-1019,:1077. lint:audit-actions is in verify (package.json:21,79). *derived from source on both sides; needs no DB* |
| CANON-238 | Every value stored in pets.breed must exist in the ALL_BREEDS TypeScript catalog. | `scripts/check-catalog-drift.ts:1` | PARTIAL (subset) | `scripts/check-catalog-drift.ts:1,17-19,24`<br>`package.json:39` lint:catalogs | scripts/check-catalog-drift.ts:60-75 queries pets where breed not in ALL_BREEDS and exits 8 on any hit — subject and predicate match exactly. But :42-51 calls reportSkip and process.exit(0) whenever DATABASE_URL is not a LOCAL host and --allow-remote is absent, so only the local dev database is ever judged; staging/prod values are unfenced (species has CHECK pets_species_valid from 0178, breed has no constraint). *The previous basis quotes :1/:17-19/:24 and never opens the remote-skip branch, which is why it read ENFORCED. lint:catalogs is in verify (package.json:39,79).* |
| CANON-239 | A destination database's columns, CHECK constraints, indexes, and unique constraints must match a reference database built from migrations, by name and by definition. | `scripts/check-env-schema-drift.ts:21` | PARTIAL (manual-wiring) | `scripts/check-env-schema-drift.ts:164`<br>`package.json:92` db:drift | scripts/check-env-schema-drift.ts:45-58 queries pg_constraint (contype c and u) and pg_indexes by name and definition, plus columns (:60+), comparing REFERENCE_DATABASE_URL against DATABASE_URL and exiting 7 on drift (header :36). Wiring is package.json:92 db:drift only; rg finds no verify or workflow caller, and :111 skips outright on a non-local host without --allow-remote. *Needs two live databases, so it cannot join verify; the skip path at :111 also makes an unattended run a no-op against staging.* |
| CANON-240 | Every table gated by the application's jurisdiction scope-clause layer must also have RLS enabled at the database (relrowsecurity=true). | `scripts/check-scope-authz.ts:16` | ENFORCED | `scripts/check-scope-authz.ts:16,399-407`<br>`package.json:40` lint:scope-authz | scripts/check-scope-authz.ts derives the scope-gated table set mechanically from the scope-clause files (:379-397) with a zero-derivation failure at :399-408, reads c.relrowsecurity from pg_class (:433-435), and evaluateCoherence :275-277 emits an 'rls_disabled' violation for any gated table whose flag is not true; violations print at :353 and exit 1 (:483). lint:scope-authz is in verify (package.json:40,79). *A gated identifier absent from public is a separate 'missing_table' violation (:271-273), so the derivation cannot pass by drifting. Skips loudly on a remote/unreachable DB (:420-427).* |
| CANON-241 | A scope-gated table's RLS policy must not be an unconditional (USING true or absent) permissive SELECT/ALL grant to public/anon/authenticated. | `scripts/check-scope-authz.ts:35` | ENFORCED | `scripts/check-scope-authz.ts:35,132,399-407`<br>`package.json:40` lint:scope-authz | isUnconditionalRead (scripts/check-scope-authz.ts:246-254) returns true for a PERMISSIVE SELECT/ALL policy held by an untrusted role whose qual is '', 'true' or '(true)'; evaluateCoherence :279-289 turns each such policy on a scope-gated table into an 'unconditional_read_policy' violation unless its `table:policy` key is in UNCONDITIONAL_POLICY_ALLOWLIST, which is `{}` at :132 — the strictest state. Violations exit 1 (:483/:502) with the remedy at :366. *Live allowlist size is 0, matching the row's number. Reads pg_policies live, so it judges the database rather than a migration text.* |
| CANON-242 | No renderable text column in the live database may contain a seed-marker pattern (e.g. seed plumbing visible to a funcionario or citizen). | `scripts/check-seed-hygiene.ts:4` | PARTIAL (subset) | `__tests__/seed-hygiene.test.ts:6,14,22,34`<br>`scripts/hygiene-rules.ts`<br>`scripts/check-seed-hygiene.ts:62` | scripts/hygiene-rules.ts:26-43 — the scan's corpus is FIVE hand-listed columns (profiles.display_name, welfare_reports.description, pets.name, organizations.display_name, notifications.title) under the header 'Renderable text columns seed scripts are known to touch'. __tests__/seed-hygiene.test.ts:35-49 fails on any hit in those, so every other renderable text column in the live DB is outside the predicate. *Marker patterns are also a closed list of four (hygiene-rules.ts:46-54). Not vacuous — a real hit in a listed column fails the suite. The v3 basis argues wiring only (the vitest twin opens a live postgres connection, which I confirm at :14/:28); wiring was never the gap, the column corpus is.* |
| CANON-243 | The pet-photos and event-attachments storage buckets' INSERT grant predicate must stay frozen to the two named, pinned predicates; no new bucket-name-only write grant is allowed. | `scripts/check-storage-write-policies.ts:25` | ENFORCED | `scripts/check-storage-write-policies.ts:503`<br>`package.json:38` lint:storage-policies | scripts/check-storage-write-policies.ts:189-197 pins the two grants by name with their exact predicate text; :470 defines permissive as 'predicate never mentions auth.uid()'; :501-512 fails an unfrozen permissive write grant, a frozen grant whose predicate changed (widening AND narrowing), and a frozen entry the scan no longer finds; exits 1 at :553/:568/:640. Header rule 7 adds a MIN_WRITE_POLICIES non-vacuity floor. lint:storage-policies is in verify. *Declared blind spot (:37-43): a bucket with NO policy is invisible, so a later auth.uid()-carrying grant on uploads-staging would not be seen — but a bucket-name-only grant on ANY bucket is caught, which is the rule's second clause. Static scan of db/**/*.sql, not the live catalog. The prior judge pass records the floor's value as MIN_WRITE_POLICIES=8 at :155 with the exit at :557-568; I saw the exit lines but not the constant, and it does not change the verdict.* |
| CANON-244 | The "db" vitest project must run with fileParallelism:false to avoid cross-test pollution over the shared local Postgres stack. | `vitest.config.ts:102` | UNENFORCED | `vitest.config.ts:102-104` | vitest.config.ts:102-104 fileParallelism:false on the db project is the scheduling setting itself — flipping it changes how the suite runs and fails no check. An rg for fileParallelism across ts/tsx/mjs/yml/json finds it only in vitest.config.ts and in explanatory comments (db/index.ts, __tests__/approval-routing-fanout.test.ts). *self-executing declaration, true at this SHA. The previous basis says 'The config line IS the mechanism' and still graded ENFORCED — the P10 error in the calibration set.* |
| CANON-245 | The "db" vitest project must run a globalSetup that gracefully closes the postgres.js pool after the suite to prevent worker-exit teardown errors. | `vitest.config.ts:106` | UNENFORCED | `vitest.config.ts:106-111`<br>`__tests__/global-setup.ts` | vitest.config.ts:106-111 declares globalSetup for the db project, and __tests__/global-setup.ts:128-133 teardown() is an explicit NO-OP ('pool drain happens automatically via idle_timeout') — so the rule's stated predicate, a globalSetup that gracefully closes the postgres.js pool after the suite, is not implemented, let alone checked. No test reads globalSetup. *The config entry is a self-executing declaration and the pool-drain claim in its comment (:106-110) is contradicted by the no-op teardown; the previous basis notes the same open defect while grading ENFORCED.* |
| CANON-497 | No renderable column in the seeded database may carry a seed marker; every reserved account (including the zero-pet owner) must keep 0 pets and 0 org memberships; and seeded notifications must satisfy brand casing and the welcome category from migration 0157 — all asserted with a non-vacuity control. | `__tests__/seed-hygiene.test.ts:34-105`<br>`__tests__/seed-hygiene.test.ts:52,62,78` | PARTIAL (subset) | `__tests__/seed-hygiene.test.ts`<br>`scripts/check-seed-hygiene.ts` | __tests__/seed-hygiene.test.ts:34-50, :52-68 and :78-100 do carry all three assertions against the live seeded DB, but the non-vacuity control at :102-125 covers ONLY the reserved-account guard; the seed-marker and notification scans have none (no other test imports findSeedHygieneOffenders / findNotificationHygieneOffenders), and the marker half is limited to the five columns in scripts/hygiene-rules.ts:31-42. *The rule's closing clause ('all asserted with a non-vacuity control') is true of one assertion of three. The v3 basis is a stub carried over from the reclassification pass with no evidence line of its own.* |
| CANON-498 | No phantom locality in ar_localities may double-count a real one, in any of the three shapes the phantom has worn — the first being a 'locality' spanning its entire province, a violation only when its canonical name resolves to its own province AND it has no departamento (so real capitals sharing their province's name are spared). | `scripts/check-locality-integrity.ts:3-6`<br>`scripts/check-locality-integrity.ts:1-14` | ENFORCED | `package.json:15`<br>`scripts/check-locality-integrity.ts` | check-locality-integrity.ts:70-78 (whole-province aggregate via isWholeProvinceAggregate), :94-98 (superseded source) and :119-133 (coverage floor, the anti-vacuity check) all exit 1; package.json:15 puts lint:locality in the verify chain and ci.yml:842-844 runs it in the seeded DB job. __tests__/check-locality-integrity.test.ts:93 pins the capital-sparing clause (name match but has a departamento). *The live gate exits 0 when the DB is unreachable (header :44-47) and ci.yml:70-71 confirms the four DB-backed fences are excluded from the no-Postgres check job, but the predicates are pinned offline at __tests__/check-locality-integrity.test.ts:79-165 and CI runs the live gate after seeding, so the skip is not a vacuity hole. Basis carried no independent evidence ('as reported by the reclassifying group'); I confirmed it against the enforcer.* |
| CANON-499 | A microchip identification's code must be exactly 15 characters and non-null (`chip_requires_iso_fields`), and a profile's dni_hash must be unique when present (`profiles_dni_hash_unique`). | `db/schema.ts:4630-4633, db/schema.ts:414-416` | ENFORCED | `db/schema.ts` | db/schema.ts:4630-4633 declares check chip_requires_iso_fields: `kind <> 'microchip_iso' OR (code IS NOT NULL AND length(code) = 15)` — exactly 15 and non-null, both halves. db/schema.ts:414-416 declares uniqueIndex profiles_dni_hash_unique on (dni_hash) where `dni_hash IS NOT NULL` — unique when present. No DROP for either in db/migrations. *Declared, not verified against the live catalog.* |
| CANON-500 | The ~110 value-domain CHECK constraints declared in db/schema.ts (status enums, jurisdiction-province canonicalisation, non-negative counts, window sanity, XOR/consistency pairs) are the database's own value contract and may not be dropped by a later migration. | `db/schema.ts (110 `check(` declarations, e.g. :717, :2955, :3153, :4191-4221)` | PARTIAL (manual-wiring) | `db/schema.ts`<br>`scripts/check-env-schema-drift.ts` | db/schema.ts holds 110 `check(` declarations (rg count at this SHA), each live in a rebuilt DB. But the rule's predicate is "may not be DROPPED by a later migration", and the only thing that would detect a drop is scripts/check-env-schema-drift.ts, wired to package.json:92 `db:drift` — not in the verify chain (package.json:79) — and it needs two live databases (REFERENCE_DATABASE_URL + DATABASE_URL, header :29-30). *docSays: ~110. The drift comparator's own header (:8-19) records that a repo-only reader cannot detect this class at all. The previous basis left partialShape 'unclear'; the shape is manual-wiring.* |
| CANON-507 | The database must expose no outbound-HTTP surface: the pg_net, http, dblink and pgjwt extensions must be absent, audited rather than asserted from a prior review's claim. | `scripts/check-pg-net-exposure.ts:1-6` | UNENFORCED | `scripts/check-pg-net-exposure.ts` | scripts/check-pg-net-exposure.ts:49-50 - 'Solo lee catalogo. No escribe nada. Sale 0 siempre que pueda conectarse: su trabajo es informar, no fallar un gate.' Report-only, so it cannot FAIL on a violation, and no package.json key invokes it (only the header's manual `pnpm exec tsx` line at :47). *The script's own header (:5-10) records that pg_net IS installed on the measured database (put there by the Supabase platform), so the rule's absence claim is not live either. v2 called this manual-wiring without reaching the exit-0 contract.* |
| CANON-508 | Each recent migration must carry a probe asserting the EFFECT of its SQL as a query returning exactly one boolean column `ok`, so the migration ledger cannot claim an effect the database does not have. | `scripts/check-ledger-honesty.ts:83-87` | PARTIAL (manual-wiring) | `scripts/check-ledger-honesty.ts`<br>`package.json:96` | scripts/check-ledger-honesty.ts:8-10 'NOT part of pnpm verify'; package.json:96 wires it only to db:doctor. Its PROBES manifest (:106-211) carries 9 probes for migrations 0158-0182 while db/migrations holds 210 files, and :90 states 'The point is not coverage - it is a tripwire'. *Two independent caps: manual wiring (db:doctor only) and a 9-probe subset of 210 migrations. v2 basis cited only :83-87 and never counted the probes; same status, firmer evidence.* |
| CANON-509 | `case_events` is append-only: the enforce_case_events_append_only trigger refuses UPDATE and DELETE, mirroring enforce_pet_events_append_only. | `db/migrations/0121_case_events_append_only.sql:9` | ENFORCED | `db/migrations/0121_case_events_append_only.sql` | db/migrations/0121_case_events_append_only.sql:34 creates enforce_case_events_append_only(); :71-73 and :76-78 attach it as BEFORE UPDATE and BEFORE DELETE row triggers on public.case_events. db/triggers.sql:210,247-254 re-declares the same pair for fresh bootstraps. No DROP TRIGGER for it in any later migration. *Declared, not verified against the live catalog. Carries the same audited GUC override (app.allow_event_mutation + _actor, audit_log row) as the pet_events trigger — which is exactly the 'mirroring' the rule asserts. The previous basis rested on the migration header line :9; I read the trigger bodies.* |
