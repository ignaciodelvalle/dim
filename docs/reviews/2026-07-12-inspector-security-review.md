# Fresh-context adversarial SECURITY review — govt inspector (2026-07-12)

> Cursor was usage-limited; a Claude fresh-context agent ran the fence review. Verdict: SHIP
> for the fence itself, BUT found MED-1 = a real cross-jurisdiction case-existence leak in the
> pet sub-view (findOpenCasesForPetWithCodes unfiltered). MED-1 MUST be fixed before push
> (safe default = in-scope filter). LOW-1 timing folded in; LOW-2 (closed-case nexus) = PO note.

Security review could not complete.

The Security Review subagent failed twice with the same blocker:

> Increase limits for faster responses. You're out of usage. Switch to Auto, or ask your admin to increase your limit to continue.

Also, your message ended at **“Review these files:”** with no file list — if that was truncated, paste the paths when you retry.

**Next step:** switch to Auto (or raise the limit), then ask again for `/review-security` (or paste the inspector file list and I’ll do a manual read-only adversarial pass in this session without the subagent).

--- full report below (from the Claude reviewer, pasted) ---
