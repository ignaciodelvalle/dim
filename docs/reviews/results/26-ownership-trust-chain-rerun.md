The re-audit is written to `docs/reviews/results/26-ownership-trust-chain-rerun.md`. Summary:

**Only 2 of the original 9 findings were actually remediated** — I verified each by reading the current code, not trusting commit messages:

**Fixed (verified correct):**
- `owner-accept-return.ts` now takes the advisory lock + re-checks `hasPendingProposal` before executing, matching its siblings.
- `cases-repository.ts::closeCase` folds the terminal-status guard into the UPDATE's WHERE clause and branches on rowcount — genuinely idempotent now.

**Still open (7), with one sharpened during re-verification:**
1. **HIGH** — `expire-pet-transfers.ts` still does a blind status update with no `expectedStatus`, even though the repo now supports that exact guard (used by accept/reject/cancel). Cron can stomp an already-resolved transfer's status back to `"expired"`.
2. **HIGH** — `accept-cross-org-transfer.ts` still checks case status once, pre-tx, no lock. I traced through the actual failure mode: the `closeCase` fix doesn't help because it runs *after* the destructive writes and its return value is discarded — an accept can still fully execute after a concurrent reject/cancel already closed the case (double-accept from the same org is separately caught by a DB unique index, but accept-vs-reject/cancel is not).
3. **HIGH** — `transfer-custody.ts` is still fully unilateral. New detail found on re-audit: it's also the *only* custody-moving writer with no open-dispute check at all (every sibling — propose, initiate, claim — has one).
4. **MED** — `submit-free-claim.ts` unchanged: instant grant on a code match alone, no evidence/waiting period.
5. **MED** — `org-reject-owner-return.ts` still missing the lock its sibling `owner-reject-return.ts` has.
6. **LOW** — `custody_disputes.status` still has no "escalated" value; disagrees with `cases.status`.
7. **LOW** — Still only a 365-day SLA, no shorter initial-triage deadline.

The report also documents what's genuinely solid (the return-to-owner lock discipline, the cross-org/foster expiry crons' idempotent in-tx recheck pattern, and the dispute resolution arbiter/jurisdiction model) so remediation work has a clear template to copy from.That background search finished — it was superseded by the `Grep` tool call I ran right after (which returned the same information faster). No follow-up needed; the re-audit report is already complete and written to `docs/reviews/results/26-ownership-trust-chain-rerun.md`.
ts:90` (+134-186) — `caseRow.status !== "open"` is still checked once before the transaction and never re-verified under a lock inside it, unlike the sibling fix just applied to `owner-accept-return.ts`. The `closeCase` TOCTOU fix (#3 in scorecard) does not close this hole: it runs at the END of this function's tx (line 176), *after* `insertPetEvent`/`endShelterCustody`/`insertShelterCustody` already ran, and its return value is never inspected (`await repo.closeCase(...)` — result discarded). Net effect: a receiver's **accept** racing the sender's **cancel** or the receiver's own **reject** on the same case can still commit the full custody flip (event + ownership rows) even though `closeCase` silently no-ops for this caller because the case was already closed by the other writer — the case ends up `status='cancelled'/'rejected'` while custody has, in fact, moved. (Double-accept from the same org is separately caught by the `ownerships_one_active_shelter_custody_per_pet_org` unique index, which aborts that transaction — so this is specifically an accept-vs-reject/cancel race, not accept-vs-accept.) · **HIGH** · Acquire `pg_advisory_xact_lock(hashtext(caseRow.id))` and re-`SELECT ... FOR UPDATE` + re-check `status === 'open'` inside the tx before any write, exactly like `accept-pet-transfer.ts`'s `findTransferByIdForUpdate` pattern.

3. `src/modules/transfers/application/transfer-custody.ts:71-247` (`transferCustody`) — still fully unilateral: the only destination check is `destination.verified` (line 104); no acceptance step. Re-verified it is **also** the only custody-moving writer with no dispute gate at all — `findOpenDispute`/`pet.inCustodyDispute` is checked by `initiatePetTransfer` (`owner-transfer-rules.ts:45`), `proposeCrossOrgTransfer` (`findOpenDispute`), and `submitFreeClaimForUser`/`submitClaimDisputeForUser`, but `transferCustody` has no such check anywhere in the file. A source org can dump a pet under active custody dispute onto a non-consenting destination org with zero receiver consent and zero dispute check. · **HIGH** · Route through the same propose/accept handshake used for cross-org transfers, and add the `findOpenDispute` guard used by every sibling writer.

4. `src/modules/pets/application/claim/submit-free-claim.ts:37-95` — unchanged: a "free" pet (no active custody row of any role) is granted outright on nothing more than a 15-digit chip or tattoo code match, no photo/evidence, no waiting period, no notice to anyone. The `SELECT ... FOR UPDATE` only prevents two concurrent claimants from both winning — it does not add any evidentiary bar. · **MED** · Require a corroborating artifact (photo of the pet + tag, or a short objection window with a notification path) before finalizing outright ownership.

5. `src/modules/return-to-owner/application/org-reject-owner-return.ts` (whole file, esp. lines 40-68) — still no `pg_advisory_xact_lock` / `hasPendingProposal` re-check, unlike `owner-reject-return.ts:75-82` and `actor-cancel-proposal.ts:65-71` which both got this treatment. An org's reject can still land after a concurrent owner accept already executed the transfer, appending a spurious `custody_transfer_cancelled` event onto a pet whose custody already moved. · **MED** · Add the same `pg_advisory_xact_lock(hashtext(pet.id))` + `hasPendingProposal` re-check.

6. `db/schema.ts:3099-3102` (`custody_disputes_status_valid` CHECK) vs `src/modules/cases/application/escalate-stale-disputes.ts:92` — unchanged: `custody_disputes.status` is still constrained to `open|resolved|withdrawn`; escalation only exists as `cases.status='escalated'` (cron) or a `note_added` pet event (`escalate-dispute.ts`, manual). The dispute row itself never reflects escalation. · **LOW** · Add `"escalated"` to the CHECK + enum, or have `resolveDisputeUseCase`/detail views read the linked case's status instead of the dispute row's.

7. `src/modules/cases/application/escalate-stale-disputes.ts:41` (`staleAfterDays ?? 365`) — unchanged: still the only SLA, still just a warning notification with no consequence, `pets.inCustodyDispute` never auto-resolves. · **LOW** · Add a shorter first-response SLA (e.g. 30/60 days) distinct from the year-long stale flag.

## What's actually solid (re-confirmed)

- `owner-accept-return.ts:213-218` — lock + `hasPendingProposal` re-check under the lock, matches every sibling (`actor-cancel-proposal.ts`, `owner-reject-return.ts`, `org-accept-owner-return.ts`, `propose-return-as-refugio.ts`, `owner-propose-return-to-org.ts`). Test added asserting a committed cancel beats a racing accept.
- `cases-repository.ts::closeCase:148-181` — the guard is now part of the `UPDATE ... WHERE status NOT IN ('closed','merged')` predicate, branches on `rowCount`, and is idempotent. Good in isolation; the gap is that call sites in the transfer/dispute modules don't act on the "I lost the race" signal it now provides (see #2).
- `expire-cross-org-transfers.ts` → `TransfersRepository.expireOneCrossOrgCase` (`transfers-repository.ts:769-867`) — genuinely idempotent: re-`SELECT` status inside its own tx and bails if not `'open'` before writing anything. This is the pattern #1 and #2 above should copy.
- `expire-foster-proposals.ts` / `FosterRepository.expirePendingProposals` — same idempotent per-row-tx-with-recheck shape (per its own doc comment); consistent with the cross-org cron.
- `initiate-pet-transfer.ts` (owner→owner propose) and `proposeCrossOrgTransfer` both correctly block on an open dispute before allowing a proposal, and both routes require the counterparty's explicit accept — the two-phase consent model works everywhere except `transferCustody` (#3).
- `resolveDisputeUseCase` (`resolve-dispute.ts`) has a real arbiter model: role-gated to `admin`/`govt`, govt further scoped to jurisdiction match, ≥100-char resolution summary required, validates the transfer target exists/active before moving custody, atomic tx, audits, fans out to all parties. The adjudication workflow itself is sound — the gaps are the missing "escalated" dispute-row state (#6) and the weak SLA (#7), not the resolution mechanics.
