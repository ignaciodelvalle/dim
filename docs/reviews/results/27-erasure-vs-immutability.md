1. `lib/infra/auth-guards.ts:29-36` · `requireUserOrRedirect` never checks `profiles.deletedAt` — a self-"erased" account (soft-deleted, PII hashed) still authenticates and gets a fully functional session · **HIGH** · redirect to `/login` (or a "cuenta eliminada" screen) when the profile's `deletedAt` is set.

2. `app/actions/subject-rights.ts:16-19` · `eraseMySubjectDataAction` calls only the `erase_subject_data` RPC then `signOut()` — never `supabase.auth.admin.deleteUser(user.id)` — so `auth.users` (email + password hash) survives "erasure" forever; combined with #1 the user can log back in immediately · **HIGH** · call `admin.deleteUser` with the service-role client after the RPC succeeds.

3. `db/migrations/0106_dni_less_identity.sql:150` (current `erase_subject_data`) · never touches `pet_events`/`case_events` payloads; third-party PII embedded in payload — e.g. `incident_reported.victim_contact_name`/`victim_contact_phone` (`lib/events/event-schemas.ts:860-861`) — has zero redaction path (the append-only trigger in `db/migrations/0127_pet_events_append_only.sql:91` blocks any write without the audited `app.allow_event_mutation` override, which the RPC never invokes) · **HIGH** · add a narrow, audited redaction step for non-sanitary PII fields, distinct from the legitimate sanitary-record retention that already covers vaccination/medical payloads.

4. `db/migrations/0106_dni_less_identity.sql:150` vs `db/schema.ts:3089-3093` · `custody_disputes` already carries the full `pii.apply_baseline` columns (`deletedAt`/`purpose`) built for exactly this, but `erase_subject_data` never scrubs rows where the subject is `raisedByUserId`/`resolvedByUserId`; `export_subject_data` (`db/migrations/0087_subject_rights_consent_completeness.sql:94-106`) *does* return those same rows — access and erasure are asymmetric · **MED** · soft-delete/redact the subject's `custody_disputes` rows in the same RPC.

5. `db/schema.ts:1282-1301` (`attachments`) · rows and their Supabase Storage objects (photos, `approval_evidence`/`revocation_evidence` — can include ID/matrícula document scans) are never purged on erasure; the `onDelete:'cascade'` FK only fires on a hard `pets`/`petEvents` delete, which never happens (append-only + soft-delete only) · **MED** · enumerate and delete the subject's Storage objects (or flag for purge) inside `erase_subject_data`.

6. `lib/events/event-schemas.ts:170,210,339` (`vet_name`, `administered_by`) · free-text snapshots of a professional's real name are never anonymized when that vet erases their own account — unlike `recordedByUserId` (FK, resolves through the hashed `profiles.displayName`), this text is baked into every payload verbatim · **LOW** · document as accepted professional-attribution retention, or route through the same redaction path as #3.

7. `app/(public)/p/[publicToken]/page.tsx:181-199,367-372` · the Tier-0/1 public QR credential (vaccination boolean, service-dog rabies-risk banner) reads raw `pet_events` rows directly with no `overlayAmendments` and no `upcastPayload` — a correction to a wrong `vaccination_administered` entry never reaches the credential a stranger scans · **HIGH** · fetch `event_amended` rows alongside and run through `overlayAmendments` before deriving each badge.

8. `lib/metrics/program-health.ts:325` and, generally, every raw-SQL aggregate over `pet_events` in `lib/metrics/**` / `lib/analytics/govt-dashboards.ts` / `lib/analytics/compliance-metrics.ts` · `payload->>'x'` / `EXISTS` predicates have no join to `event_amended` — a corrected vaccination, weight, or sterilization event never changes rabies coverage, sterilization rate, AMR density, or any other govt/analyst KPI; the old (wrong) value counts forever · **HIGH** · add a shared "latest-corrected-payload" CTE/view (LATERAL join to the newest `event_amended` per target id) reused by every metrics fetcher.

9. `lib/projections/pet-compliance.ts`, `pet-weight.ts`, `pet-status.ts`, `pet-microchip.ts`, `pet-tattoo.ts`, `pet-adoption-eligibility.ts`, `pet-rabies-observation.ts`, `travel-compliance.ts` · none call `overlayAmendments` internally — correctness depends entirely on the caller pre-overlaying; `lib/analytics/owner-dashboard.ts` does it, but govt/public callers of the same projection functions don't · **MED** · apply the overlay inside the projection layer itself so a new caller can't forget it.

10. `lib/infra/rederive-pet-cache.ts:125` (`rederivePetCache`) · used by both the drift-detection harness and `scripts/rebuild-projections.ts --apply`; replays raw payloads with no `overlayAmendments` — "repairing" cache drift can re-derive `pets.*` columns from the PRE-correction value · **MED** · call `overlayAmendments(events)` before every `replay*` invocation.

11. `lib/events/event-upcasters.ts` registry · `upcastPayload` is called from exactly 3 production sites (`lib/events/events.ts:71,179`, `components/pet-profile/asiento-fields.ts:208`, one adopciones page) — `lib/projections/**`, `lib/metrics/**`, and `src/modules/**` never call it, and `__tests__/event-upcaster-coverage.test.ts` only enforces that a versioned schema HAS an upcaster, not that readers actually apply it · **HIGH** · add a fitness test (or a single payload-access helper that always upcasts) so a new `payload_version` bump can't silently break a projection/metrics reader that reads raw fields.

12. `scripts/detect-pet-cache-drift.ts:13-19` · cache-drift reconciliation for `pets.status`/weight/microchip/jurisdiction is READ-ONLY BY DESIGN and manual ("an operator must inspect each case and choose the fix"); no cron runs it and no DB trigger/generated column derives these columns transactionally from `pet_events` · **MED** · schedule a detect-only alerting cron at minimum; consider a DB trigger deriving `pets.status`/`deceasedAt` directly from `pet_events` inserts so the highest-stakes column can't diverge at all.

13. **Hard-delete of `pets`/`pet_events`/`profiles` in production code paths:** clean — grep across `app/`, `lib/`, `src/modules/` finds `db.delete(pets|petEvents|profiles)` only in `__tests__/**`; every production erasure path is soft-delete via the RPCs above, consistent with the append-only invariant.

14. **Schema-versioning mechanics (`payload_version` + `lib/events/event-upcasters.ts`):** clean design — `withVersion()` bakes a literal into every schema, `upcastPayload` is pure/chainable, `__tests__/event-upcaster-coverage.test.ts` gates CI on write-side coverage, and `docs/superpowers/event-versioning.md` documents the contract precisely. The gap is adoption on the read side (#11), not the mechanism itself.
