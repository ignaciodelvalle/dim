**Dimension verdicts:** single write-path **not clean** · dedupe **not clean** · dead-letter drain **not clean** · ARCH-P **not clean** · delivery-channel **clean** (documented in-app-only in `docs/design/handoffs/2026-07-04-notifications-consistency-review.md:21-23`)

1. `scripts/notifications-service-baseline.json:1` · 69 baselined files still `db.insert(notifications)` with no `dedupe_key`/dead-letter · **HIGH** · migrate each to `createNotification()`/`createNotificationsBulk()` and delete its baseline key per PR
2. `db/triggers.sql:54` · `handle_new_user` direct INSERT bypasses service; no `dedupe_key` · **MED** · move welcome notify to app post-signup via `createNotification({ dedupeKey: 'welcome:'+userId })` or add explicit DB exception doc + dedupe column
3. `scripts/check-notifications-service.ts:40` · scanner omits `db/triggers.sql` (and `scripts/` seeds) so trigger/seed bypasses never fail CI · **MED** · add `db/triggers.sql` (+ seed globs if desired) to scan or a sibling SQL guard
4. `db/migrations/0124_notifications_dedupe_key_and_dead_letter.sql:91` · dead-letter table shipped with “follow-on retry cron” comment but no drainer route/registry entry exists · **HIGH** · add `/api/cron/drain-notification-dead-letter` that replays `WHERE resolved_at IS NULL` through `createNotification()` and sets `resolved_at`
5. `lib/infra/notification-service.ts:223` · double-failure path (insert + dead-letter both throw) still only `console.error` — notification permanently lost · **MED** · emit `audit_log`/metric/alert on this branch; optional external pager
6. `lib/infra/notification-service.ts:218` · dead-letter writes have no dedupe/upsert — repeated failures stack duplicate rows · **LOW** · `ON CONFLICT (dedupe_key) WHERE resolved_at IS NULL DO UPDATE` or skip if unresolved row exists
7. `src/modules/events/actions.ts:1017` · ARCH-P `flushNotifications` catches insert failure with `console.error` only — no dead-letter · **HIGH** · map `pending[]` to `createNotification()` with per-row `dedupeKey`
8. `src/modules/adoption/actions.ts:52` · same ARCH-P silent swallow on post-tx flush · **HIGH** · same `createNotification()` migration
9. `src/modules/events/application/writers.ts:56` · writer test-path flush identical silent swallow · **MED** · delegate flush to `createNotification()`
10. `src/modules/pets/application/sighting/report-pet-sighting.ts:239` · direct insert, no `dedupe_key`, no `related_event_id` — retry/double-submit duplicates urgent avistaje rows · **MED** · `createNotification({ dedupeKey: 'sighting:'+eventId+':'+ownerId, relatedEventId: eventId })`
11. `app/(public)/p/[publicToken]/encontre/action.ts:326` · same pattern for found-pet notify (baselined direct insert, no dedupe) · **MED** · route through `createNotification()` with stable event-scoped key
12. `lib/infra/business-rules-reeval.ts:137` · pet flag UPDATE commits before notify; bare `insert` at `:157` — notify throw skips forever on next run (`nowPpp === flag`) · **HIGH** · notify via `createNotification()` inside try/catch per owner **before** or with compensating retry keyed `ppp-flip:${petId}:${userId}`
13. `lib/infra/business-rules-reeval.ts:157` · bulk direct insert, no `dedupe_key`, no dead-letter · **HIGH** · `createNotificationsBulk()` with `dedupeKey: 'ppp:'+petId+':'+userId`
14. `lib/infra/owner-disease-alerts.ts:102` · direct insert relies on 0088 index only; insert failure in default `db` path has no dead-letter · **MED** · `createNotification({ dedupeKey: 'disease-alert:'+triggerEventId+':'+userId, relatedEventId })`
15. `app/api/cron/auto-expire-approvals/route.ts:104` · in-tx direct insert, no `dedupe_key`; not on service path · **MED** · post-tx `createNotification({ dedupeKey: 'approval-expired:'+r.id })`
16. `lib/infra/lost-pet-broadcast.ts:198` · outer catch returns empty on member-query/other pre-insert failures — zero dead-letter, caller thinks broadcast ran · **HIGH** · on catch, dead-letter built payloads or surface `{ deadLetteredCount, error }` to caller/metrics
17. `lib/infra/lost-pet-broadcast.ts:192` · `createNotificationsBulk` result (`deadLetteredCount`) discarded — partial fanout invisible · **MED** · propagate counts; fail/metric when `deadLetteredCount > 0`
18. `lib/infra/lost-pet-broadcast.ts:178` · `episodeScope` defaults to `pet.id` — new lost episode without `episodeKey` collides with prior broadcast dedupe · **MED** · make `episodeKey` required in production call sites (already passed from `set-pet-lost-use-case.ts:388`)
19. `lib/infra/notifications.ts:199` · `runVaccineDueScan` ignores `dead_lettered` results; cron JSON reports only `insertedCount` · **MED** · track `deadLetteredCount`; warn/fail cron when nonzero
20. `lib/infra/notifications.ts:386` · `runPostAdoptionCheckinScan` same blind spot on proactive/missed inserts · **MED** · same dead-letter accounting
21. `db/migrations/0124_notifications_dedupe_key_and_dead_letter.sql:34` · `dedupe_key` nullable — all legacy direct inserts remain outside partial unique index · **HIGH** · finish baseline migration then `ALTER TABLE notifications ALTER COLUMN dedupe_key SET NOT NULL` (with backfill)
22. `src/modules/cases/application/escalate-stale-decomiso-handoffs.ts:119` · 7-day idempotency is read-then-act without DB key — concurrent cron runs can double-notify · **LOW** · `createNotification({ dedupeKey: 'decomiso-stale:'+caseId+':'+userId+':'+weekBucket })`
23. **Delivery channel:** **clean** — in-app-only explicitly documented (`docs/design/handoffs/2026-07-04-notifications-consistency-review.md:21-23`, `AGENTS.md` open-questions push deferred); not a silent code gap within scoped runtime paths
