# Deck facts — designer handoff for the officials presentation

> Snapshot: `b975f3e9d` (`main`; `11c0ffc57` pushed 2026-09-02 plus `lenses/A01.md`) · Audited SHA: `d7dbf25f7` (lenses ran before WU-0 merged) · Facts: `docs/architecture/facts.json`
> Status: draft — finalized 2026-09-02 by the synthesis writer; fresh review fixes applied 2026-09-02

Everything in section 1 survived at least one adversarial refuter; everything in batch A survived three. Everything in sections 3 and 4 is a claim the audit found we **cannot** make. The rule for this deck: a slide may say what section 1 says, may quote a number from section 2 verbatim, and may not say anything section 3 or 4 forbids.

**Scope caveat that belongs on the deck's own methodology slide, if it has one:** 15 of 36 audit lenses ran. This is a partial audit. Nothing here licenses a claim about an area no lens covered.

---

## 1. What survived refutation — grouped by the question an official actually asks

Each item names the property, the file it lives in, the fence that would fail if someone broke it (`guardedBy`), and the lens that verified it. **"no fence — candidate"** means the property is true today and nothing in CI would notice if it stopped being true. Those are still true, and they are the ones to fix rather than to boast about.

**One row is not like the others.** The `profiles` write-lockdown row below is the *fix* for the audit's CRITICAL, landed on 2026-09-02 — it did not survive a refuter, because until that day it was not true. It is listed here because the property is now real and fenced, and it is cross-referenced to §3, where the finding is told in full. Every other row in section 1 is a property that existed before the audit and withstood attack.

### "¿Quién puede ver qué?"

| property | path | guardedBy | lens |
|---|---|---|---|
| All five `/api/panorama/*` route handlers resolve the caller through one shared gate that runs maintenance, session, erasure, deactivation, shift-expiry, role AND `accountType` — the exact set the page guard enforces, answered with a status code instead of a redirect. | `app/api/panorama/_guard.ts:71` | `scripts/check-authz-guards.ts` (`INSTITUTIONAL_GUARDS`, route-guard rule) | A01 |
| Every non-public `/api/v1` handler derives identity from the verified bearer token, never from the request body — the token is passed explicitly to `requireLiveUser({supabase, accessToken})` so it is validated by `getUser(jwt)`. Zero hits for a body-supplied `userId`/`petId` owner across all 33 route files. | `app/api/v1/me/caretaker-grants/route.ts:37` | `scripts/check-authz-guards.ts:701` (partial — the bearer-only half itself is unfenced) | A01, A11 |
| Bearer-only identity on the whole `/api/v1` surface: no cookie fallback exists anywhere, and the only five routes that skip the guard carry a written `@no-auth-required` reason. | `app/api/v1/me/route.ts:119` | `scripts/check-authz-guards.ts` — no fence on the bearer-only half | A11 |
| No RLS-enabled public table exposes an unconditional read to `anon`/`authenticated`/`public`. The single allowlisted exception (`time_slots`) was verified column by column to carry nothing that identifies a person. | `__tests__/rls/read-path-matrix.test.ts:47` | itself — a live catalog sweep with a stale-allowlist test | A02 |
| The intentional-open-write allowlist is **empty**, and a third test fails if an allowlist entry stops matching a real policy. The three original `with check (true)` write policies are all closed. | `__tests__/rls/write-path-matrix.test.ts:49` | itself — catalog sweep + stale-exception test | A02 |
| `profiles` has **no PostgREST write surface at all** — a SELECT policy and nothing else, so no caller holding the public anon key can write any column of any profile row, their own included. Every legitimate writer goes through the server's own privileged connection. | `db/migrations/0211_profiles_lock_postgrest_writes.sql` | `__tests__/rls/profiles-write-lockdown.test.ts` (five PATCH probes; the positive control is a Drizzle write, so an accidental total lockout also fails) | A01 — **and this is the audit's CRITICAL, see §3** |
| A govt operator who legitimately reaches a pet still receives only the open cases inside their own jurisdiction: sibling-barrio and out-of-province cases are excluded from the payload; admin stays universal. | `lib/infra/gob-pet-subview.ts:171` | `__tests__/gob-pet-subview-jurisdiction-fence.test.ts` — real Postgres, real SQL, no mocks | A10 |
| Every `/org/{orgToken}` server action pins its capability check to the URL-resolved org, and a lint inside `pnpm verify` fails the build if one reverts to a bare `requireCapability`. The allowlist is empty. | `src/modules/foster/actions.ts:87` | `scripts/check-confused-deputy.ts` (`lint:authz-orgtoken`, in the verify chain) | A10 |
| A barrio-grain operator may request a province-level panorama unit, but the repository AND-s the operator's own scope clause into every per-metric query — 36 of 36 metric branches — so the rows come back narrowed to their assignments. | `src/modules/panorama/infrastructure/repository-history.ts:168` | second SQL fence in the same file — no targeted test | A10 |
| The notifications inbox read scopes on the caller's `user_id` inside the WHERE clause, unconditionally, and the read and all three mutations use the identical predicate. | `src/modules/notifications/application/read/list-notifications-for-user.ts:75` | no fence — candidate | A06 |
| The raw per-locality `observed_value` behind an alert never reaches a govt reader: `alert_firings` has RLS enabled with **zero policies** and an explicit "every row is invisible" comment; only a live admin sees it, through the app's own connection. | `db/migrations/0111_alert_firings.sql:118` | `__tests__/rls/coverage.test.ts:274` (asserts the deny-all table carries no policies) | A10 |
| All six alert-triage writes go through the full liveness chain (`requireLiveUser` + role + `accountType`), not a role-only lookup. | `app/actions/alert-firings.ts:84` | `__tests__/alert-firings-admin-guard.test.ts` — expired-shift, maintenance, erased and deactivated cases all assert zero writes | A10 |

### "¿Los datos pueden alterarse?"

| property | path | guardedBy | lens |
|---|---|---|---|
| The `pet_events` append-only triggers ship in the **migration chain**, not only in bootstrap — a migrate-only deploy still gets `pet_events_no_update` and `pet_events_no_delete`. This closed three HIGH findings from the 2026-07 audit. | `db/migrations/0127_pet_events_append_only.sql:96` | the migration itself + `__tests__/pet-events-append-only.test.ts` (guard covers the trigger's behaviour, not the binding on a migrate-only DB) | A08 |
| The audited override that permits a spine edit is **transaction-scoped everywhere** — every setter in the repo uses `set local` or `set_config(..., is_local => true)`. No session-scoped setter exists, so it cannot leak across a pooled connection. | `__tests__/_helpers/db-overrides.ts:85` | itself + the append-only test's "blocks future mutations again once the tx ends" case | A08 |
| Every pet must have its `pet_registered` event. The spine-integrity gate is **blocking and baseline-free**: the only exemption is an explicit seed tag, it is counted and printed on every run including when it is zero, and an unreachable database is reported as "this run proved nothing about the spine" rather than as a pass. | `scripts/check-spine-integrity.ts:218` | itself, wired as `lint:spine` inside `pnpm verify` | A08 |
| The public QR credential folds corrections before rendering: a stranger scanning the code sees the corrected clinical value, never the superseded one. | `packages/contract/src/events/event-types.ts:20` · `lib/domain/credential-badges.ts` | `lib/domain/credential-badges.test.ts:106` ("surfaces the CORRECTED drug name, never the mistyped original") | A08 |
| An amended rabies dose reads **identically on the web QR page and on the phone** — both surfaces are fed by the same array and derive through the same overlay. | `src/modules/pets/application/read/load-public-credential.ts:177` | `lib/domain/credential-badges.test.ts:236` (an amended name/date flips the semaphore) | A08 |
| The drift detector and every `pets.*` cache column it derives are computed from the **corrected** stream: corrections fold once at the source and all eight replay projections inherit the fold, so an amendment can never read as permanent drift. | `lib/infra/rederive-pet-cache.ts:326` | `__tests__/pet-cache-rederivation.test.ts:383` + its non-vacuity twin at `:649` | A08 |
| Erasure never deletes a spine row. Art. 16 redactions are UPDATEs inside one explicitly opened and explicitly closed override window attributed to the acting user; **no migration anywhere in the repo issues a DELETE against `pet_events` or `case_events`.** | `db/migrations/0208_subject_rights_watermarks_tag_interest_org_invitations.sql:537` | the 0127 append-only trigger + `__tests__/subject-rights-rpcs.test.ts` | A05 |
| A cross-organisation custody accept takes the pet's advisory lock and re-checks case status, dispute state, source custody and sponsorship **inside** the transaction, before any write. | `src/modules/transfers/application/accept-cross-org-transfer.ts:244` | `src/modules/transfers/application/__tests__/use-cases.test.ts:1789` (lock-order < status-order < insert-order) | A09 |
| A cross-org transfer proposal can only be accepted by the org it names: a mismatch between the case column and the payload **refuses** rather than picking one, and neither id falls back to the caller's own org. | `src/modules/transfers/domain/cross-org-rules.ts:169` | `src/modules/transfers/domain/__tests__/cross-org-rules.test.ts` — the file's own header marks this the security boundary | A09 |
| Two simultaneous seizures of one animal cannot both commit: a partial unique index on `(primary_pet_id, case_kind)` for open/escalated cases is the real backstop, and `custody_episode` is not exempt from it. | `db/schema.ts:4142` | `__tests__/seed-case-guards.test.ts:195` (parses the migration DDL and asserts the exempt list) | A09 |
| A change of titularidad closes the outgoing owner's caretaker arrangement completely — the ownership row, the grant, the `caretaker_ended` event and any pending invitation. | `src/modules/transfers/infrastructure/transfers-repository.ts:472` | `__tests__/caretaker-grants-vs-owner-change.test.ts:220`, with an explicit non-vacuity control | A09 |
| Idempotency keys are a client's private retry token, not a global name: `POST /api/v1/pets` refuses anything but a canonical UUID, and the duplicate lookup is keyed on `(key, owner)` under an advisory lock on the pair — two users presenting the same string cannot reach each other's pet. | `app/api/v1/pets/route.ts:207` | `__tests__/api-v1-pets-route.test.ts` (27 idempotency assertions) + `register-pet.test.ts` for the owner-scoping half | A11 |

### "¿Qué pasa con mi DNI?"

| property | path | guardedBy | lens |
|---|---|---|---|
| Public open-data downloads apply k=5 **plus** complementary suppression against subtraction attacks **plus** cross-dataset joint suppression — no province cell and no arithmetic difference against the published national total can recover a count below the threshold. | `lib/open-data/province-suppression.ts:88` | `lib/open-data/__tests__/province-suppression.test.ts`, `datasets.test.ts`, and a disclosure-parity test | A06 |
| DNI has no plaintext resting place: the schema carries only a hash and the last four digits, and the one writer passes through `hashDni`. | `lib/utils/dni-hash.ts` · `db/schema.ts:321` | `__tests__/adoption-registered-adopter-finalize.test.ts:336` | A06 |
| Owner phone and last-seen location are **never fetched** on a non-disclosing lost credential — the loader substitutes a SQL `null` literal into the SELECT list, so the value never leaves Postgres; the ownership join is pinned to `role='owner'` so a temporary caretaker's phone cannot ride the titular's consent onto the public page. | `src/modules/pets/application/read/load-public-credential.ts:237` | `__tests__/public-credential-owner-row-role.test.ts` for the role predicate; **no test pins the SQL projection** — candidate | A03 |
| Coordinates never leave Postgres as values anywhere on the public lost feed — the listing asks for an `IS NOT NULL` boolean to render "punto marcado en el mapa" and never for the latitude or longitude; the item type has no field that could carry one. | `src/modules/lost/infrastructure/lost-listing-read.ts:188` | no fence — candidate | A03 |
| The public lost feed touches **no owner-identity table at all**: no profiles join, no ownerships join, no phone, no e-mail. Owner contact is reachable only through the credential, where the disclosure flag gates the SQL projection itself. | `src/modules/lost/infrastructure/lost-listing-read.ts:115` | no fence — candidate | A03 |
| The public denuncia receipt discloses nothing but existence: an anonymous holder of a constancia code learns the report exists and when it was filed. Free text, accused description, coordinates and evidence all require a MAC-verified session bound to that report id, and minting one requires an exact match against the reporter e-mail already on file. | `app/(public)/denuncias/codigo/[code]/page.tsx:134` | `__tests__/denuncia-code-public-pii-contract.test.tsx:227` — including "never mints a signed evidence URL" | A06 |
| The public finder page's service-role read of the owner's e-mail is double-gated: it is reached only when the pet is `lost`, the finder form is disabled, AND the owner opted in per-field — and the ownership row is pinned to `role='owner'`. | `app/(public)/p/[publicToken]/encontre/page.tsx:163` | `scripts/check-titular-row-resolution.ts` (`lint:titular-row`, empty allowlist) covers the role predicate; the consent-order placement is unfenced — candidate | A01 |
| The third-party geocoding proxy never logs the address it geocoded — every one of its seven log sites carries only an error class name, an HTTP status or a constant string. | `lib/infra/geocoding.ts:277` | no fence — candidate (convention only) | A06 |
| Government row-level exports are an **allowlist, not a filter**: each slice is a non-strict schema, so an undeclared field is stripped by construction. No name, owner, microchip, DNI, performer identity or coordinate is declarable, and every date is bucketed to the month. | `lib/analytics/govt-exports.ts:87` | `__tests__/govt-exports.test.ts:181` (per-slice field-stripping behaviour) | A06 |
| Art. 16 outranks a deactivation: an institutionally deactivated account is refused the art. 14 export and **granted** the art. 16 erasure, on the same credential — an organisation cannot stand between a person and Ley 25.326 by flipping one column. | `app/api/v1/me/privacy/route.ts:230` | `__tests__/api-v1-me-privacy-route.test.ts` — three named assertions go red on the symmetry mutation | A05 |
| The one irreversible step of the supresión — deleting stored photographs — is bounded to pets the subject actually owns (`role='owner'`), so a foster's erasure cannot destroy the real owner's images; the sweep for abandoned uploads pages properly instead of silently stopping at an SDK default. | `src/modules/auth/application/subject-rights/erase-subject-data.ts:97` | `erase-subject-data.test.ts` in the same module | A05 |
| Erasure redacts the notification inbox itself, idempotently, and hard-deletes push registrations. | `db/migrations/0208_subject_rights_watermarks_tag_interest_org_invitations.sql:704` | `__tests__/subject-rights-rpcs.test.ts:1042` (title/body/ctaUrl/ctaLabel all asserted) | A06 |
| Push notifications go out on the RFC 8291 **encrypted** path: the third-party push service sees an endpoint and ciphertext, never the pet name or the finder's phone. | `lib/infra/web-push.ts:127` | `__tests__/web-push-send.test.ts:136` (dropping the key pair fails the assertion) | A06 |
| The denuncia identity/content split survived a `DROP VIEW`: both views are `security_invoker`, the reporter-identity view is revoked from `PUBLIC`, `anon` and `authenticated` outright, and the migration that had to recreate the content view restated the grants rather than trusting Postgres to carry them. | `db/migrations/0210_welfare_content_view_observed_symptoms.sql:21` | none — no CI fence enumerates views (this is finding A02-3) | A02 |
| On the phone, the libreta share token has exactly **one** exit — the OS share sheet. It is never rendered as text, logged, copied to the clipboard, fetched, or handed to a URL opener, and a cancelled share leaves nothing behind. | `apps/mobile/src/shares/SharesScreen.tsx:205` | no fence — candidate (upheld by absence) | A11 |
| Mobile refresh tokens live in the device Keystore only, never in ordinary app storage, marked device-only so they do not travel to a new phone in a backup — and no inbound link can inject a session. | `apps/mobile/src/auth/secure-store-auth-storage.ts:261` | the named test file does not cover this property — candidate | A04 |

### "¿Cómo sabemos que funciona?"

| property | path | guardedBy | lens |
|---|---|---|---|
| CI runs **every** gate `pnpm verify` runs — 68 of 68 (67 `lint:*` fences plus `verify:mobile`) — and adding a gate to `verify` without adding it to the workflow fails the build naming it. | `scripts/check-ci-lint-parity.ts:79` | itself, wired as `lint:ci-parity` and run in CI | C06 |
| CI's test step is `test:verified`, which distrusts vitest's exit code **in both directions**: it grades "did every discovered test file report?" from a filesystem census independent of vitest's own report, then re-folds vitest's status so a crashing run cannot be normalised. | `scripts/run-verified-suite.ts:98` | itself + `scripts/check-suite-coverage.ts` | C06 |
| A repo-walking fence proves every public-token resolver carries a throttle **before** the token is resolved: it discovers resolvers by walking `app/` and `src/` rather than reading a hand list, checks guard-before-resolve ordering, requires a distinct bucket per route, and requires every exemption to still point at a real file with a written reason over 40 characters. It ships with ~15 RED-control tests fed synthetic broken sources. | `__tests__/public-token-throttle-coverage.test.ts:592` | self-guarded | A03 |
| One response envelope for the entire `/api/v1` surface, with a non-vacuous fence: a route missing the envelope import fails, a hand-built response fails, and discovery dropping below the real route count fails. | `lib/infra/api-v1.ts:58` | `scripts/check-api-v1-envelope.ts` (`lint:api-v1`, in the verify chain) | A11 |
| The per-IP rate-limit map is complete in both directions and the surface's aggregate ceiling is pinned by **computation, not prose** — an exact equality against a reduce over the live map, independently re-derived by hand during the audit. | `lib/infra/api-v1-limits.ts:919` | `__tests__/api-v1-rate-limit-families.test.ts` | A11 |
| All 25 cron route directories are accounted for — none dead, none manual-only. A 26th route added without registering it fails a parity test that reads the real directory at runtime, and dropping a handler throws at module load. | `lib/infra/cron-dispatcher.ts:473` | `__tests__/cron-registry-parity.test.ts` | C04, C06 |
| One failing scheduled job cannot abort the fleet, and partial outcomes survive a hard kill: each job runs in its own try/catch and the accumulated outcomes are persisted after **every** job, with the persistence callback itself wrapped so a broken writer cannot take the fleet down. | `lib/infra/cron-dispatcher.ts:409` | `__tests__/cron-daily-dispatcher.test.ts` (a throwing job, a 4xx job, and a throwing callback all tested) | C04 |
| Existence oracles are closed on the mobile API: a resource the caller may not reach, a token that resolves to nothing, and an erased pet produce a **byte-identical** 404; a throttle answers one 429 for both budgets; a read that blows its database budget answers 503, never 404. | `app/api/v1/pets/[publicToken]/credential/route.ts:212` | `__tests__/api-v1-credential-route.test.ts:744` + the soft-delete resolution sweep | A11 |

### "¿Dónde corre?"

| property | path | guardedBy | lens |
|---|---|---|---|
| Every cron route is secret-gated with a constant-time comparison and **fails closed in production** when the secret is unset. All 25 call the same guard. | `lib/domain/cron-auth.ts:30` | `__tests__/cron-registry-parity.test.ts:127` (narrower than the property — a substring check that excludes the dispatcher itself, which `scripts/check-authz-guards.ts` covers instead) | C04 |
| No CI path can apply a migration to a remote database: nothing in the seven workflows invokes the migrator or a deploy, every `DATABASE_URL` in CI is the local stack, and the one privileged remote credential is used by a read-only audit that refuses to report a skip as a pass. | `.github/workflows/db-doctor-staging.yml:65` | none structural — a discipline, not a mechanism | C06 |
| The QA bootstrap script cannot touch a remote: every network call is `localhost`, every database call targets the named local container, it holds no secret and invokes no deploy tool. It also verifies a real build asset rather than just a 200 on the port. | `scripts/qa-up.ps1:204` | no fence — candidate | C06 |
| The UTC schedule does not slice an Argentine day in half: the daily bag fires at 04:00 UTC = 01:00 ART, the same calendar date in both zones, and slot materialisation converts local clock times through the rule's own stored timezone rather than the server's. | `lib/infra/slot-materialization.ts:148` | the named guard does **not** cover this — candidate | C04 |
| `x-real-ip` is not a client-choosable rate-limit key through the edge these limiters run behind: every per-IP ceiling takes the last forwarded hop, never the first client-supplied one, and a rotated-header run against staging produced the same ceiling as the fixed-header control. | `lib/infra/rate-limit.ts:188` | `__tests__/caller-ip.test.ts` (a first-hop regression turns it red) + a recorded staging measurement | A03 |
| Both login budgets are spent **before** the identity provider is even contacted, and the fence asserts the relationship between the two limits rather than their digits. | `src/modules/auth/application/login.ts:160` | `__tests__/api-v1-auth-routes.test.ts:322` | A04 |
| A password-reset request cannot enumerate accounts, because there is nothing to branch on: an address with an account and one without take a byte-identical path, the provider's result is never bound to a name, and the success arm carries no fields at all. | `src/modules/auth/application/password-reset/request-password-reset.ts:160` | `__tests__/password-reset.test.ts:118` | A04 |
| The mobile 401 policy is one refresh and one retry — never a loop — and it never retries a shift expiry; a deactivated or erased account ends the session immediately. | `apps/mobile/src/api/client.ts:254` | `apps/mobile/src/api/client.test.ts:195` ("signs out after ONE failed retry — not a loop") | A04 |
| The open-data endpoint spends its per-IP budget **before** it validates the dataset name, serves only k=5-suppressed province aggregates behind a 24-hour cache, and funnels every unexpected failure into a bare error with no internal detail. | `app/(public)/transparencia/datos/[dataset]/route.ts:84` | its own route test asserts 429-before-404 and that no internal detail reaches the 500 body | A03 |
| The signed-upload staging bucket is deny-all to every caller role and enforces its size and MIME ceilings **at the object store**, so an oversized or wrongly-typed upload is refused even when none of our code runs. | `db/migrations/0206_uploads_staging_bucket.sql:114` | the migration's own replay-time assertions + `scripts/check-storage-write-policies.ts` | A07 |
| The batch attachment signer carries a pet fence against caller-supplied event ids: a caller with access to one animal cannot pass another animal's event ids and receive signed URLs for its clinical attachments. | `src/modules/pets/application/timeline-attachments/sign-timeline-attachments.ts:87` | `app/actions/sign-timeline-attachments.test.ts:159` (compiles the WHERE clause and asserts both predicates) | A07 |
| The revocations upload policy names the caller **and** locks out deactivated and erased actors at the database layer — the one storage policy in the repo that gets the full liveness predicate right. | `db/migrations/0188_revocations_upload_admin_govt_only.sql:44` | `scripts/check-storage-write-policies.ts` classifies it as scoped but checks nothing further — candidate | A07 |

---

## 2. Numbers a slide may quote

All 35 keys of `docs/architecture/facts.json`. The file at this snapshot declares `"sha": "36c8204c9"` — it was regenerated **after** the lenses ran, so two of its values have moved since the audited SHA (see the caveats). **Quote these verbatim with the marker syntax** so the parity fence can find and update them; do not retype a number into prose.

**Where the marker syntax is actually enforced, and where it is not.** `__tests__/architecture-facts.test.ts` cross-checks every `<!-- fact:key -->` marker against `facts.json` — but only inside `docs/architecture/**`, `docs/presentation/**` (when it exists), `AGENTS.md` and `CLAUDE.md`. **`docs/reviews/**` is not in that scan**, so the markers in this table are a convention, not a fence: if a value below drifts from `facts.json`, nothing turns red. The table was hand-verified against `facts.json` at this snapshot. A deck built from it should be re-checked against `facts.json` on the day it is presented, or moved into `docs/presentation/` where the fence would own it.

| key | value with marker |
|---|---|
| `event_types` | `<!-- fact:event_types -->55<!-- /fact -->` |
| `verify_fences` | `<!-- fact:verify_fences -->67<!-- /fact -->` |
| `lint_scripts` | `<!-- fact:lint_scripts -->67<!-- /fact -->` |
| `check_scripts` | `<!-- fact:check_scripts -->72<!-- /fact -->` |
| `modules` | `<!-- fact:modules -->22<!-- /fact -->` |
| `pages` | `<!-- fact:pages -->262<!-- /fact -->` |
| `route_handlers` | `<!-- fact:route_handlers -->82<!-- /fact -->` |
| `layouts` | `<!-- fact:layouts -->10<!-- /fact -->` |
| `migrations` | `<!-- fact:migrations -->211<!-- /fact -->` |
| `tables` | `<!-- fact:tables -->53<!-- /fact -->` |
| `enums` | `<!-- fact:enums -->22<!-- /fact -->` |
| `rls_enabled_tables` | `<!-- fact:rls_enabled_tables -->55<!-- /fact -->` |
| `security_definer_functions` | `<!-- fact:security_definer_functions -->8<!-- /fact -->` |
| `cron_route_dirs` | `<!-- fact:cron_route_dirs -->25<!-- /fact -->` |
| `vercel_crons_declared` | `<!-- fact:vercel_crons_declared -->2<!-- /fact -->` |
| `cron_jobs` | `<!-- fact:cron_jobs -->23<!-- /fact -->` |
| `ci_workflows` | `<!-- fact:ci_workflows -->7<!-- /fact -->` |
| `vitest_files` | `<!-- fact:vitest_files -->1489<!-- /fact -->` |
| `e2e_specs` | `<!-- fact:e2e_specs -->45<!-- /fact -->` |
| `mobile_jest_files` | `<!-- fact:mobile_jest_files -->78<!-- /fact -->` |
| `org_capabilities` | `<!-- fact:org_capabilities -->16<!-- /fact -->` |
| `kpi_descriptors` | `<!-- fact:kpi_descriptors -->86<!-- /fact -->` |
| `k_anonymity_k` | `<!-- fact:k_anonymity_k -->5<!-- /fact -->` |
| `token_prefixes` | `<!-- fact:token_prefixes -->12<!-- /fact -->` |
| `denuncia_kinds` | `<!-- fact:denuncia_kinds -->9<!-- /fact -->` |
| `operator_shift_hours` | `<!-- fact:operator_shift_hours -->8<!-- /fact -->` |
| `throttle_per_min` | `<!-- fact:throttle_per_min -->600<!-- /fact -->` |
| `throttle_per_hour` | `<!-- fact:throttle_per_hour -->6000<!-- /fact -->` |
| `signed_url_ttl_seconds` | `<!-- fact:signed_url_ttl_seconds -->3600<!-- /fact -->` |
| `projections` | `<!-- fact:projections -->13<!-- /fact -->` |
| `service_role_call_sites` | `<!-- fact:service_role_call_sites -->34<!-- /fact -->` |
| `canon_rows` | `<!-- fact:canon_rows -->513<!-- /fact -->` |
| `canon_enforced` | `<!-- fact:canon_enforced -->175<!-- /fact -->` |
| `canon_partial` | `<!-- fact:canon_partial -->93<!-- /fact -->` |
| `canon_unenforced` | `<!-- fact:canon_unenforced -->245<!-- /fact -->` |

Four numbers that need a caveat if a slide uses them:

- **`service_role_call_sites = 34`.** Every one of these bypasses RLS by design. That is a fine thing to say out loud; it is not a fine thing to leave unexplained next to a slide that says "Row Level Security".
- **`canon_enforced = 175` of `canon_rows = 513`.** The canon's own rulebook is explicit that `ENFORCED` means *something in the tree fails when the rule is broken* — not that the rule is good, current, or covered beyond its literal predicate. 245 rules are UNENFORCED, which does not mean broken; it means nothing would notice. (The 513th row is the app→db boundary rule that landed with its fence on 2026-09-02; it is PARTIAL — the fence pins a baseline rather than forbidding the import outright — so neither the ENFORCED nor the UNENFORCED count moved.)
- **`migrations = 211`** counts `.sql` files, and it is now exact: `db/migrations/` holds 211 `.sql` files at this snapshot. The directory listing returns **212 entries** because the extra one is `db/migrations/meta/`, Drizzle's journal directory — not a migration. The 209-vs-210 discrepancy the drafts flagged is resolved: migration `0211` landed and `facts.json` was regenerated with it; `0212` (the `pet_events` PostgREST write lockdown) followed on 2026-09-02.
- **`vitest_files = 1489`** moved four times after the lenses ran (1485 → 1486 with migration 0211's fence, → 1487 with the e2e-cleanup test, → 1489 with the app→db boundary fence and migration 0212's write-lockdown fence). A slide quoting a test count should quote it with a date.

---

## 3. Cannot be claimed

Direct input for the es-AR `limites-honestos.md`. Every line below is a claim currently made in the government pitch deck (`docs/design/handoffs/2026-07-07-govt-personas-pitch.md`) or in `README.md` that lens **D05** verified and found **FALSE** or only **PARTIAL**. Written so a non-technical reader can act on it.

### First: what the audit found, and what it did about it

A deck that shows an audit and shows no findings is not credible. This is the one to say out loud, because it is the strongest thing in the room: **the audit found a CRITICAL security hole — found by the lens run that started 2026-09-01, confirmed 5/5 by refuters, and closed the next day (2026-09-02) by migration 0211, commit `ae97186b9`.**

| | |
|---|---|
| **What it was** | Any signed-in user could send one direct request to the database's public API and rewrite their own profile row — including the column that says whether they are an administrator. The authorization layer reads that column and trusts it. So any owner could have made themselves a national administrator. |
| **Why it existed** | The database rule that governed the profile table pinned *which row* a person may edit (their own) and never *which columns*. That reads as safe to every automated check the project has. The application-layer version of this same hole was found and fixed in the 2026-07 audit; this was the same class on a path nobody had considered — eight earlier review passes had shared the same blind spot. |
| **How it was found** | Not by the step that hunts for bugs. By the step whose job is to attack the audit's own conclusions: a reviewer assigned to disprove a property the audit had already written down as **healthy**. That step is the reason to keep paying for it. |
| **How sure we are** | The project's rule is that a CRITICAL goes to five independent reviewers instead of three. It went to five and **all five confirmed it** (workflow `wf_8bd36c20-bc1`; the vote is recorded in the project's decision log under `sdd/audit-2026-09/decisions`). |
| **What was done** | Migration `0211` removes the database's write permission on that table entirely — the server writes profiles through its own privileged connection, so nothing legitimate was using the public path. An automated test now fails if the permission ever comes back. Commits `ae97186b9` and `36c8204c9`. |
| **When** | Found 2026-09-01 (lens run), closed 2026-09-02 by migration 0211. Applied to the live database (Supabase `DIM-staging`) the same day it closed. There is no separate production database at this stage — staging is the only live environment, so nothing is waiting on a second rollout. |
| **What is still open** | The **same mistake on a second table** (`pet_events`, the clinical ledger), which would let an owner post an event that falsely claims a government or veterinary signature. It is a HIGH, not a CRITICAL, and it is queued as migration `0212` for the next batch. A deck should not claim the class is closed — it should say one of two doors is shut and the second is scheduled. |

A slide may say: *an independent adversarial audit found one critical issue, it was confirmed by five reviewers, and it was closed and deployed the same day.* A slide may **not** say the audit found nothing, that the system passed cleanly, or that the class is fully closed.

### FALSE — the statement is wrong as written (3)

| id | what the document says | what is actually true | evidence |
|---|---|---|---|
| **D05-15** | The pitch lists "government officials moderating anonymous reports from their own jurisdiction" as a **future gap** — "today only admin, the design is written, phased". | **It already shipped, end to end.** Government accounts moderate the anonymous report queue for their assigned localities today: approve, reject as abuse, or escalate to the national administration, with the jurisdiction re-checked at every one of the three actions. Presenting a delivered capability as a future gap under-sells the product in the room, and an official reading it would wait for a phase that already happened. | `lib/infra/auth-guards.ts:291` grants jurisdiction-scoped moderation to the `govt` role; `src/modules/welfare/actions.ts:309,343,374` — all three write actions re-run the guard and re-check scope. Three independent reviewers walked it end to end. |
| **D05-50** | The README says government analytics and the shelter-side cruelty-report intake are "deferred by design — both built, neither wired into navigation yet". | **Both are one click away today.** The shelter-side intake has its own navigation entry, and the analytics dashboard was folded into a navigable hub as its "Analítica" view on 2026-08-01. A reader would go hunting for two screens that are already in the menu. | `components/layout/nav-presets.ts:266` (the org intake nav item); `app/gob/analytics/page.tsx:1` records the absorption into the Programa hub. |
| **D05-51** | The README states the minimum required Node.js version as 22.13. | **The real minimum is 22.23.0**, and the repo pins 22.23.2. Versions below that break real toolchain features — three separate ones were measured. The open range as written would also admit Node 25, which turns 5 test failures into 139. The correct range is already stated correctly in `CONTRIBUTING.md` in the same repository. | `package.json:195` declares `">=22.23.0 <23"`; `.nvmrc` pins 22.23.2; `scripts/check-node-version.ts:9` names `>=22.13.0` by name as the stale range the fence exists to kill. |

### PARTIAL — true in part, misleading as stated (17)

| id | the limit to state honestly | evidence |
|---|---|---|
| **D05-1** | The pitch spells the brand with a capital M seven times. The product's actual brand is **miMAR**, lowercase m — the lint rule bans the other spelling. The fence only scans code files, never `.md`, which is why the deck escaped it. The substance of the pitch (a State-facing offer, 54 government pages) is real. | `scripts/check-brand-casing.ts:131` globs only `.ts`/`.tsx`; the pitch has 7 hits of the wrong casing and 0 of `miMAR`. |
| **D05-5** | The pitch's "outreach" step now redirects into the Operativos hub, and — the part that matters — the overdue-vaccination list it lands on is aggregated **by locality, not by neighbourhood (barrio)**. Say the aggregation level out loud. | `app/gob/outreach/page.tsx:1` records the absorption; the destination screen has no barrio axis. |
| **D05-14** | The unified `CAS-` case code covers the case system, **not the whole escalation chain**: welfare reports keep their own `DEN-` reference and custody disputes keep a `DIS-` token in the same hub. A 2026-07-28 finding recorded one dispute appearing under two codes with two different state chips. | `src/modules/welfare/domain/reference-code.ts:2,56`; `app/gob/casos/page.tsx:1-15`; `src/modules/cases/domain/case-kinds.ts:44-50`. |
| **D05-20** | "Better than any private alternative" rests on **no benchmark** measuring the four axes the sentence names (legal framing, privacy, surveillance, compliance dashboards). The one benchmark on file compares landing pages and UX — and records that a competitor already pilots with a municipality and holds a data-protection registry badge. Drop the comparative, or scope and date it. | `docs/design/handoffs/2026-07-04-benchmark-landing.md:110-149`. |
| **D05-22** | "Street vaccination, last night's report and this morning's bite in one closable queue" is **two of three legs, not zero and not three**. A deadline-ranked queue merges rabies observations, welfare reports and regulatory cases; street vaccination is not one of its domains. | `app/gob/acciones/page.tsx:1-11`; `app/gob/acciones/_lib/worklist-core.ts:44` lists three domains and no vaccination domain. |
| **D05-23** | "Legally anchored" holds where it is declared, but **only 2 of the KPI catalogue's entries cite an actual statute, and both are scoped to CABA / Buenos Aires**. An official outside those two jurisdictions sees a generic placeholder instead of their own law. Say what the code does say: the two norm-backed KPIs cite the norm resolved to the operator's jurisdiction, the rest show their data source and target, and one explicitly declares that no legal mandate exists. | `lib/metrics/metric-legal-basis.ts:69-76` has exactly 2 entries, both province-scoped. |
| **D05-26** | Two of the five items on the government roadmap have **already shipped**: jurisdictional moderation (see D05-15), and the create-campaign → assign-appointments → measure-attendance cycle, which runs end to end today through organisation accounts. What is genuinely missing is the government console's own create screen — not the capability. | `app/gob/moderacion/page.tsx` redirects into the shipped queue; the campaign form, agenda rule form and campaign metrics all exist and are wired. |
| **D05-27** | The README's opening sentence spells the brand with the deprecated casing **and** implies national-registry status the product does not have. Say: a working pilot, not an officially adopted national registry. | README has 3 hits of the wrong casing, 0 of `miMAR`; the public "Acerca de" page already uses the correct casing. |
| **D05-28** | The sentence naming DIM as the internal codename spells the public brand exactly as the project's own lint rule bans it. Also: audit-log rows are keyed to users, organisations and approvals — **not** to DIM tokens, as the sentence implies. | `scripts/check-brand-casing.ts` (rule 1 bans the wrong casing, rule 2 permits DIM only as a hyphenated token prefix); `lib/ui/branding.ts:13`. |
| **D05-36** | Two of the three public welfare-report URLs in the README's table **do not exist as written** (they need the `/denuncias` prefix), and the table omits two more live pages. Separately, "reference-code tracking" overstates it: the code alone confirms the report exists — seeing its **status** needs the reporter's own session or an e-mailed access link, so a reporter who left no e-mail can never see status. | All five pages live under `app/(public)/denuncias/...`; there is no top-level `/buscar` or `/codigo` directory. |
| **D05-41** | "Personal accounts have Mi Argentina identity" names a capability **nothing can use yet**: the column exists, nothing writes it, and the identity callback is unimplemented pending credentials. The half that IS enforced is the other one: institutional accounts are blocked from owning pets by a database constraint and a trigger. Both guards key on `account_type`; the role mapping itself is application-layer only. | `lib/infra/miarg-oidc.ts:145-147` throws "not implemented"; `db/schema.ts` carries the CHECK and the `ownerships_institutional_no_pets` trigger. |
| **D05-44** | "`infrastructure/` is the only place that issues database queries" **does not hold** — over 150 application-layer files import the database driver directly, with no fence covering that layer. The half that IS machine-enforced is the domain layer's purity: zero violations. A boundary decision has since been taken (writes fenced to the module layer, page reads baselined shrink-only), but the fence is not written yet — do not describe it as in place. | `biome.json` scopes the restriction to `src/modules/*/domain/**`; 153 application-layer files import `drizzle-orm`. Decision recorded 2026-09-02; `scripts/check-app-db-boundary.ts` does not exist at this snapshot. |
| **D05-45** | "Authorization is enforced at the action edge, **not** in the database" is too absolute. For at least one browser-direct file-upload path (revocation evidence), no server action mediates the write and the database's Row Level Security is the **only** thing enforcing access. Migration 0188 says so in writing. | 12 RLS test files, 6 authorization fences in `verify`; `db/revocations_storage.sql:44-55` is a real policy gating a browser-direct upload. |
| **D05-52** | "Refuses to run against a non-local database" is not absolute — **a single command-line flag disables the guard**, warn-only, with no confirmation prompt. And the seed step it then runs is documented as re-runnable but **NOT idempotent**; it has duplicated rows on staging once already. | `scripts/db-bootstrap.ts:129` (the guard) and `:83` (the override). |
| **D05-54** | "Integration tests run against local Postgres" overstates it: the suite is **deliberately split** into a parallel, database-free project and a serial database project, and only a subset genuinely needs Postgres. The same README already states the split correctly one table earlier — the two sentences contradict each other. | `vitest.config.ts:9` states the rationale directly. |
| **D05-55** | The project-layout diagram **omits the mobile app workspace entirely** — a jest-tested Expo client that is itself gated inside the commit checklist — and the shared code package both the website and the phone import, which is the whole reason this is a workspace. | `apps/mobile/package.json` exists with its own test script; the README has zero mentions of `apps/`. |
| **D05-56** | "Legacy server actions, now thin re-export shims" is true of most, not all: **55 of 70 carry the shim header; a handful still hold real orchestration and query the database directly**, including one 505-line file. | 70 non-test files in `app/actions/`; 55 carry the shim header; `app/actions/decomiso.ts` is 505 lines with four transactions. |

**Two bookkeeping notes for whoever writes `limites-honestos.md`.** The source list in `docs/reviews/2026-09-fresh/lenses/D05.md` (§"For limites-honestos.md") has 20 items, and the mapping is not one-to-one with the 20 findings above: its item 12 concerns the stack table's Row-Level-Security row (claim **D05-42**, which was voted **TRUE** — 2 of 3 reviewers held it accurate under a live user path, because RLS is the sole enforcement for browser-direct storage uploads), and **D05-50** is missing from that list even though it is one of the three FALSE claims. Use the table above, which is keyed by claim id.

**One thing the audit could NOT check and a lawyer must:** the legal citations themselves (Ley 22.953, Ord. CABA 41.831, Ley CABA 5470, Ley 14.346, Ley CABA 4078 / Prov. 14.107, Ley 25.326, Ley 27.275) were confirmed as anchors *in the code*, never verified as legally correct or current. A deck for officials should not present a statute number this audit merely found in a string literal.

---

## 4. Do not draw

Stubs, redirects and absences. Each has a lens citation. A diagram that draws any of these as a live box is a diagram that will be contradicted in the room.

| do not draw | what is actually there | lens |
|---|---|---|
| **A Mi Argentina federation arrow, box, or handshake.** | Env-gated stub. The callback returns 404 when the integration is disabled and a labelled 501 otherwise; the profile writer **throws unconditionally** rather than half-write a profile. No account holds a Mi Argentina identity today. Draw it as a hatched future state or leave it off. | A04 (prior 10, closed as "no half-wired trust"), D05-25, D05-32, D05-41 |
| **A SENASA notification or live batch-export arrow.** | The export engine exists; there is no screen and no batch notification. The pitch itself lists this as a gap, correctly. | D05-11 (TRUE gap), D05-26 |
| **A "Mascotas CABA" street-care integration.** | Nothing exists. The pitch lists it as a gap, correctly. | D05-10 (TRUE gap) |
| **A crash-reporting box on the web app.** | The crash reporter is **mobile-only**. A06's scope names the "web-has-no-Sentry decision" explicitly, and the web-side redaction seam is wired only server-side through the error sink. If the deck shows observability, show it on the phone. | A06 |
| **Any storage garbage-collection or retention job for photos and attachments.** | There is none. 25 cron routes, **zero** of which touch storage; only event-triggered deletion exists, so every replaced photo, failed-transaction orphan and unconfirmed staged upload accumulates without bound. No cache-control header is set anywhere either. | A07-6 (RN-4 B30, B31) |
| **A GPS/EXIF-stripping gate on the public evidence path.** | The function whose docblock calls itself "the gate" that "fails closed" has **zero callers**, and if wired in today it would fail OPEN — it answers from the client-declared file type, while the actual stripping runs under a non-fatal catch. | A07-5 |
| **A "claim your pet by DNI" flow.** | Disabled by a hard constant. Every call returns "El reclamo por DNI está temporalmente pausado" before any database call, held pending Mi Argentina identity verification. | A08 (the A08-5 refutation established this) |
| **`/gob/analytics` or `/gob/outreach` as standalone destinations.** | Both are absorbed redirects — analytics into the Programa hub as its "Analítica" view, outreach into the Operativos hub. Draw the hubs. | D05-50, D05-5 |
| **A PDF export from the government analytics screen.** | It emits CSV or JSON only. | D05 (areas-not-reached note on the persona wishlist) |
| **An e-mail or SMS delivery leg on the notification service.** | The notification service has exactly **one** external delivery leg: web push. Everything else is in-app. | A06 (gap round) |
| **Cell suppression on the government row-level export screen.** | That surface applies **no** cell suppression, by a recorded PO decision — row-level re-identification by grouping remains open. Field-level allowlisting is what it does have, and that is real. | A06 (scope note on the govt-exports healthy item; `docs/architecture/privacy-known-limitations.md` PD1) |
| **"Suppressed map cells have no companion total to subtract against."** | False, and recorded as accepted debt: a companion scope total IS published today — a mortality KPI equals the sum of the same province's department cells, and the coverage denominator is in the footer. The code's own comment calls this "KNOWN, NOT FIXED". | A10 (gap round, refuted healthy claim; limitations KA1/KA2) |
| **`db/rls.sql` (or any `db/*_rls.sql`) as "the RLS configuration".** | It is a reference snapshot that has drifted from the live policy set, and its own header still invites an operator to paste it into Studio — which would re-open two policies migrations closed. Migrations are the source of truth. This is the same file whose profiles block was the CRITICAL, so the invitation is not a cosmetic issue. | A02-2, A01-R1 |
| **A production environment.** | There is one live database: Supabase `DIM-staging`. The old `DIM` project is INACTIVE. Migrations 0209, 0210 and 0211 are applied there. A deck may say "pilot running on a live environment"; it may not draw a staging→production promotion arrow as if both ends existed. | C06, and the 0211 rollout record |
