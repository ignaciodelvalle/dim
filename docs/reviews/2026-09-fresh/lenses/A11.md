# A11 — API v1 surface (mobile contract)

> Snapshot: `d7dbf25f7` (`main`) · Audit: 2026-09-fresh · Lens A11
> Verified against code on 2026-09-02 by workflow agents (finder + refuters) · Status: draft

## Ground truth

- Branch: `main`
- SHA: `d7dbf25f7`
- Date: 2026-09-02
- Files read: 36
- Scope: Everything the mobile app calls: the full route inventory under `app/api/v1/**` (every `route.ts` with its methods), the bearer auth helper (who validates the JWT, is the user checked as live/not-erased/not-deactivated, is the token audience checked), the response envelope and error shape across routes, the rate-limit family assigned to each route, the anti-oracle rules of `docs/architecture/api-invariants.md` (existence oracles via 404 vs 403, timing, enumeration), input validation (zod schemas in `packages/contract/src/api` or inline), idempotency keys on mutating routes, versioning and unknown-field handling, CORS and cache headers, and parity between what `packages/contract/src/api` declares and what the routes actually return and what `apps/mobile/src/api` consumes.
- What's new: this lens is new in 2026-09. The whole `/api/v1` surface was built for the Android pilot (2026-08); `apps/mobile/src/api` client; `packages/contract/src/api` types; rate-limit families; `Idempotency-Key` handling; mobile claims, transfers, custody, denuncias, shares, turnos endpoints.
- Priors triaged: none (this lens has no prior audit to carry forward — `priors: []`).

## Verdict

**SOLID WITH FINDINGS** — no CONFIRMED CRITICAL or HIGH; three CONFIRMED MED findings put this above plain SOLID (updated by the gap round below).

- Confirmed by severity: CRITICAL 0, HIGH 0, MED 3, LOW 4 (total 7)
- Refuted: 0
- Healthy verified / claimed: 9 / 11
- Prior closed / still-open / not-reproducible: 0 / 0 / 0 (no priors for this lens)
- Unverified (no refuter reached): 0

## Healthy — verified solid

### Bearer-only identity on /api/v1 — no cookie fallback exists anywhere on the surface
- Property: every authenticated `/api/v1` route resolves WHO from the `Authorization` header alone (`createClientFromBearer` + `requireLiveUser` with the raw token), and the only five routes that skip the guard carry a written `@no-auth-required` reason.
- Path: `app/api/v1/me/route.ts:120`
- Guard: `scripts/check-authz-guards.ts` (`pnpm lint:authz`, chained in `verify`) — refuter notes `guardVerified=false`: the script enforces the guard-or-marker half but nothing fences the bearer-only half itself (a route added with a cookie guard would pass the fence).
- What would break it: a handler falling back to `createClient()` (cookies) when the bearer header is missing/garbage, or a `requireLiveUser` call passing only `{ supabase }` instead of `accessToken` (which `live-user.ts:277-288` warns relies on a supabase-js internal rather than validating the token explicitly).
- Evidence: `rg -n "@/lib/supabase/server|cookies\(\)" app/api/v1 --glob '!*.test.*'` → no output (also widened by refuter to `createServerClient` / `createRouteHandlerClient` / `request.cookies` / `req.cookies`, still zero). `rg -L "requireLiveUser" app/api/v1 -g "route.ts" --files-without-match` → exactly 5 (`auth/login`, `auth/password-reset`, `auth/signup`, `localities`, `pets/[publicToken]/credential`), each with a `@no-auth-required` reason. All 28 (finder)/39 (refuter, wider sweep) real `requireLiveUser` calls pass `accessToken`.

### One response envelope for the whole surface, with a non-vacuous fence
- Property: every `/api/v1` response goes through `apiV1Json`/`apiV1Error`, so `cache-control: no-store` and charset are unoverridable and error bodies are single-key codes from the contract vocabulary; the fence's discovery floor equals the real route count.
- Path: `lib/infra/api-v1.ts:58`
- Guard: `scripts/check-api-v1-envelope.ts` (`pnpm lint:api-v1`, chained in `verify`) — refuter `guardVerified=true`: the fence fails a route missing the envelope import (:305), a hand-built response line (:312), and discovery dropping below the floor (:340).
- What would break it: a sibling module (`payload.ts`/`commands.ts`) building a `Response` directly, outside the fence's `app/api/v1/**/route.ts` glob.
- Evidence: `scripts/check-api-v1-envelope.ts:246` `MIN_V1_ROUTE_FILES = 33`; `rg --files app/api/v1 | rg -c "route.ts$"` → 33 (floor == real count). Sweep of all 30 non-route files under `app/api/v1` for `NextResponse`/`new Response`/`Response.json` → only a comment in `credential/payload.ts:9`. Refuter additionally swept the `route.ts` files themselves for the same patterns → zero hits.

### Existence oracles are closed: not-permitted, not-existing and erased all answer the same 404, and a failed read never does
- Property: a pet the caller may not reach, a token that resolves to nothing, and a soft-deleted pet produce a byte-identical 404; a throttle answers one 429 for both budgets; a budget-exceeded read answers 503, never 404.
- Path: `app/api/v1/pets/[publicToken]/credential/route.ts:212`
- Guard: `__tests__/api-v1-credential-route.test.ts` and `__tests__/public-soft-delete-resolution.test.ts` (the pet-detail door itself is the gap tracked as A11-3) — refuter `guardVerified=true` for the credential door.
- What would break it: a 403 where a sibling gives 404 (checked: the refuter found nine distinct `*_forbidden` codes, not the finder's stated four, but every one fires strictly after access resolution — `move/commands.ts:108`, `profile/commands.ts:119`, `amend/route.ts:246` all do `access.kind === "none" -> 404` before their 403, and `lost`/`shares` commands type-exclude the `"none"` kind entirely); a distinct code per throttle budget (checked: all 82 `429`s on the surface are the single code `rate_limited`, none with extra headers); a read failure answering 404 instead of 503 (checked: every `DbBudgetExceededError` arm answers `temporarily_unavailable` 503).
- Evidence: `credential/route.ts:212` `case "throttled": apiV1Error("rate_limited",429)` — "identical for a token that exists and one that does not"; `:219` `case "not_found"` — a soft-deleted pet reaches the same branch byte for byte; `:227` `case "degraded"` — 503 + retry-after, "NOT 404". `pets/[publicToken]/route.ts:198`, `photo/route.ts:223`, `return/route.ts:253` — same `access.kind === "none"`/`!== "owner"` → `not_found` 404 pattern. `__tests__/api-v1-credential-route.test.ts:744` "makes not_found and throttled differ ONLY in status and error code"; `:1105-1116` soft-delete → 404.

### The per-IP bucket map is complete in both directions and the aggregate ceiling is pinned by computation, not prose
- Property: every `api_v1_*` bucket a route spends keyed on `callerIp` has a declared family, no family entry survives its route's deletion, and the surface's total per-IP per-minute ceiling is an exact `toBe` on a reduce over the map.
- Path: `lib/infra/api-v1-limits.ts:919`
- Guard: `__tests__/api-v1-rate-limit-families.test.ts` — refuter `guardVerified=true`.
- What would break it: the aggregate pin comparing a computed value against itself (checked: refuter independently re-derived the 38-entry family census by hand — 18×600 + 10×120 + 2×60 + 600 + 240 + 2×180 + 240 + 120 + 144 + 60 = 13,884 — matching `toBe(13_884)` against the real `reduce` at `api-v1-limits.ts:1212-1236`, confirmed the adoption family's per-IP constant is 60, not the unrelated 5 in `adoption-application-limits.ts:132`, which would have moved the sum if conflated). Explicitly out of scope: the per-user half of the family rule — see A11-2.
- Evidence: `rg -c "^  api_v1_[a-z0-9_]+: \"" lib/infra/api-v1-limits.ts` → 38. `__tests__/api-v1-rate-limit-families.test.ts:183` `MIN_IP_BUCKETS = 38`; `:754` `expect(API_V1_CGNAT_FAMILY_IP_CEILING_PER_MINUTE).toBe(13_884)`; `:339` "declares a family for every per-IP bucket the routes actually spend"; `:348` "has no family entry for a bucket that no longer exists".

### Idempotency keys are a client's private retry token, not a global name
- Property: `POST /api/v1/pets` refuses anything but a canonical UUID in the `Idempotency-Key` header, and the duplicate lookup inside the transaction is keyed on `(key, owner)` so two users presenting the same string cannot resolve to each other's pet.
- Path: `app/api/v1/pets/route.ts:207`
- Guard: `__tests__/api-v1-pets-route.test.ts` (27 idempotency assertions) — refuter `guardVerified=true`, though notes the owner-scoping assertion actually lives in `register-pet.test.ts`, not the named file.
- What would break it: a replay where an attacker POSTs with a victim's key and gets the victim's pet (checked: `pets-repository.ts:139` takes `pg_advisory_xact_lock(hashtext(ownerUserId || ':' || key))` — the lock is on the pair, and the select at `:145-154` ANDs `eq(clientIdempotencyKey)` with `eq(recordedByUserId, ownerUserId)`, so a replay matches nothing and creates the attacker's own pet); a non-UUID key reaching the transaction and raising a permanently-retryable failure (checked: `isValidIdempotencyKey` trims then validates against `IDEMPOTENCY_KEY_PATTERN`, a strict anchored UUID regex, before the limiter is even spent — a missing header trims to `""` and fails the same predicate).
- Evidence: `packages/contract/src/api/pets.ts:36-37` `IDEMPOTENCY_KEY_PATTERN`; `app/api/v1/pets/route.ts:207-210` trim + `apiV1Error("idempotency_key_required", 400)`. `src/modules/pets/infrastructure/pets-repository.ts:139` advisory lock on `ownerUserId || ':' || clientIdempotencyKey`; `:148`/`:152` `eq(clientIdempotencyKey)` AND `eq(recordedByUserId, ownerUserId)`.

### The mobile client gates every read on payloadVersion and its error switch is exhaustive over the whole vocabulary
- Property: all 20 GET wrappers pass `expectedPayloadVersion` (localities re-implements the same check inline because it is public and bypasses `apiRequest`), and `error-copy.ts` has one case per `API_V1_ERROR_CODES` entry with no `default`.
- Path: `apps/mobile/src/api/endpoints.ts:268`
- Guard: `apps/mobile/src/api/client.test.ts` (jest under `apps/mobile`, run by `pnpm verify:mobile` inside `verify`) — refuter `guardVerified=false`: `client.test.ts:300` tests `apiRequest`'s gate mechanism, not per-wrapper coverage — a new GET wrapper omitting `expectedPayloadVersion` fails nothing there. Exhaustiveness on the error side is enforced by TypeScript (`apiErrorMessage` returns `string`, no `default`) via `verify:mobile` → typecheck, not by this named test.
- What would break it: an ungated read rendering a screen from a payload shape it is guessing at (checked: refuter corrected the finder's own reproduction — `rg -c 'method: "POST"' apps/mobile/src/api/endpoints.ts` returns 23, not 20, because 3 extras are `performRequest` auth calls at login/signup/password-reset, not `apiRequest` sites; hand-enumerating the 40 real `apiRequest` sites gives 20 POST / 20 non-POST, and the 20 `expectedPayloadVersion` occurrences sit 1-3 lines inside exactly those 20 non-POST calls, a line-for-line match); an added contract error code with no copy (checked: 80 `API_V1_ERROR_CODES` entries == 80 `case` statements, no `default`).
- Evidence: `endpoints.ts:441` `if (payload?.payloadVersion !== LOCALITIES_PAYLOAD_VERSION)`. Non-POST `apiRequest` sites: 268,276,294,318,340,503,547,602,665,721,778,829,891,981,1012,1093,1264,1290,1351,1483; matching `expectedPayloadVersion` sites one line later at each.

### Claimed healthy, not verified

None — all six healthy claims from the finder were reached by a refuter and marked `VERIFIED`.

## Findings (CONFIRMED)

### MED

#### A11-1 — GET /api/v1/adoptions/{petToken} is in the authenticated-read family but spends no per-user bucket
- Kind: bug
- Path: `app/api/v1/adoptions/[petToken]/route.ts:87`
- Canon rule: `lib/infra/api-v1-limits.ts:100-101` — "a route in the `authenticated-read` family spends both budgets or it is not in the family."
- Exploit: the handler spends only `api_v1_adoptions_read_ip` (600/min per IP, line 87-90); its import list carries `API_V1_AUTHENTICATED_READ_IP_LIMIT` but no `*_USER_LIMIT`, and no second `spendBudget` follows `requireLiveUser` at line 110. The sibling catalogue GET (`adoptions/route.ts:123-130`) spends `api_v1_adoptions_read_user` (120/min) in the equivalent spot. `src/modules/adoption` has rate-limit imports only in write paths (`actions.ts`, `submit-adoption-application.ts`). Any signed-in account can loop `GET /api/v1/adoptions/{petToken}` at 600/min from one IP — 3 DB reads per call (`readAdoptionDetail` + `findApplicantProfile` + `findExistingApplication`) — with **no per-account ceiling at all** across rotating addresses, while every other read on this surface caps the account at 120/min.
- Evidence:
  ```
  rg -n "spendBudget\(\s*$|api_v1_adoptions_read" "app/api/v1/adoptions/[petToken]/route.ts" "app/api/v1/adoptions/route.ts"
  ```
  ```
  adoptions/route.ts:95:    !(await spendBudget(
  adoptions/route.ts:96:      "api_v1_adoptions_read_ip",
  adoptions/route.ts:123:    !(await spendBudget(
  adoptions/route.ts:124:      "api_v1_adoptions_read_user",
  adoptions/[petToken]/route.ts:87:    !(await spendBudget(
  adoptions/[petToken]/route.ts:88:      "api_v1_adoptions_read_ip",
  ```
- Refuter notes:
  - reproduce (high): confirmed; only overstatement in the finder's own claim was "every sibling" — `me/privacy/route.ts` also spends only IP, but its per-user bucket is documented as delegated into `exportSubjectDataFor` (`:135-136`), so the adoption read stands alone as truly unbounded. MED stands.
  - correctness (high): reproduced independently; also swept `API_V1_AUTHENTICATED_READ_IP_LIMIT` vs `*_USER_LIMIT` imports across all of `app/api/v1` — 20 files import IP, 18 import USER, and the two missing are exactly `adoptions/[petToken]/route.ts` and `me/privacy/route.ts`.
  - impact (high): mechanism confirmed real, no compensating layer found; capped at MED because the payload is a public adoption listing (throttle gap, not a disclosure).
- Fix: spend `API_V1_AUTHENTICATED_READ_USER_LIMIT` on a per-user bucket immediately after `requireLiveUser` at line 110, in the shape `adoptions/route.ts:122-130` already uses (reuse `api_v1_adoptions_read_user`, per the route's own "shares with the catalogue" docblock).
- fixClass: code-sdd
- priorId/priorStatus: none (new)

#### A11-3 — GET /api/v1/pets/{publicToken} is the only one of 33 v1 routes whose handler no test imports
- Kind: fence-gap
- Path: `app/api/v1/pets/[publicToken]/route.ts:96`
- Exploit/mutation: `rg --files -g "route.ts" app/api/v1` lists 33 route modules; the set of `@/app/api/v1/**/route` imports across `__tests__`/`e2e` is 32 distinct modules, and the one missing is `@/app/api/v1/pets/[publicToken]/route`. The only repo hit for that literal path outside a test is `scripts/load-probe-api-v1.ts:350` (a probe string, not a test). The handler carries the surface's core anti-oracle rule at line 198 — `if (access.kind === "none") return apiV1Error("not_found", 404)` under "a pet this caller may not read and a pet that does not exist answer IDENTICALLY" — plus five liveness refusals and IP-then-guard-then-user limiter ordering, none of it exercised directly. Mutating line 198 to a 403 would keep `pnpm verify` green (`check-api-v1-envelope.ts` and `check-authz-guards.ts` match on neither `not_found`/`403`/`forbidden`) and keep the vitest suite green, turning the mobile app's most-called authenticated endpoint into an existence oracle for DIM tokens.
- Evidence:
  ```
  rg -o "@/app/api/v1/[a-zA-Z0-9/_\[\]-]*route" __tests__ --no-filename | sort -u
  rg -n -F "pets/[publicToken]/route" __tests__ e2e apps
  ```
  ```
  32 distinct route modules listed; @/app/api/v1/pets/[publicToken]/route absent
  rg -n -F "pets/[publicToken]/route" __tests__ e2e apps -> (no output)
  __tests__/api-v1-pet-detail-payload.test.ts:32: import { buildOwnerPetDetailV1 } from "@/app/api/v1/pets/[publicToken]/payload";
  ```
- Refuter notes:
  - reproduce (high): confirmed via `comm` diff between route-file list and test-import list; e2e only touches `/credential`, not the detail GET; the two fences (`check-api-v1-envelope.ts`, `check-authz-guards.ts`) don't reference the mutated codes.
  - correctness (high): reproduced independently, same 33/32 split and same absent path; MED not HIGH because this is coverage/convention, not a fence that falsely reports a checked property.
  - impact (high): confirmed; explicitly tie-broken to MED because line 198 is correct today — nothing is exposed now, this is a demonstrated undetectable-regression path, not a live false-green.
- Fix: add `__tests__/api-v1-pet-detail-route.test.ts` importing `{ GET }` from the handler, modelled on `__tests__/api-v1-pet-libreta-route.test.ts`; minimum assertions: identical 404 for not-held vs non-existent token, the four `requireLiveUser` refusals map to documented status+code, and a `DbBudgetExceededError` answers 503 with retry-after, never 404.
- fixClass: code-sdd
- priorId/priorStatus: none (new)

### LOW

#### A11-2 — The rate-limit families fence collects per-IP buckets only, so the "spends both budgets" family rule is unenforced
- Kind: fence-gap
- Path: `__tests__/api-v1-rate-limit-families.test.ts:194`
- Exploit/mutation: `ipBucketsIn` (lines 194-202) keeps a bucket only when its call text contains `callerIp(`; the stricter parser at line 304 explicitly skips per-user buckets (`if (!keyedOn.startsWith("callerIp(")) continue;`). No assertion in the file maps a route file to a per-user call site — the family rule (`api-v1-limits.ts:100-101`) is asserted only in prose. This is the mechanism behind A11-1.
- Evidence:
  ```
  rg -n -B2 -A10 "callerIp" __tests__/api-v1-rate-limit-families.test.ts
  ```
  ```
  194:function ipBucketsIn(source: string): string[] {
  199:    if (rest.includes("callerIp(")) found.push(bucket);
  ```
- Refuter notes:
  - correctness (medium): headline confirmed (no route→per-user assertion in this file), BUT the finder's stated mutation is wrong — deleting `adoptions/route.ts:122-130` turns `__tests__/api-v1-adoptions-route.test.ts:305` red (`expect(control.limits.some((l) => l.identifier === SUBJECT)).toBe(true)`), so the suite would not stay green for that specific route. The gap is real but partly covered ad hoc by per-route tests; LOW stands.
- Fix: add a third assertion mapping per-user call sites to families with `*_USER_LIMIT` constants, with an explicit allowlist (with reasons) for families whose per-user half deliberately lives in the use-case layer.
- fixClass: fence-candidate
- priorId/priorStatus: none (new)

#### A11-4 — welfare-reports quotes a stale aggregate per-IP ceiling (13 044) in the paragraph written to stop that exact drift
- Kind: doc-drift
- Path: `app/api/v1/welfare-reports/route.ts:99`
- Exploit/mutation: hand-summing all 38 `API_V1_IP_BUCKET_FAMILIES` entries at this SHA gives 13,884, matching the `toBe(13_884)` pin in `__tests__/api-v1-rate-limit-families.test.ts:754`. The route's own docblock still says 13,044 — two doors behind (`move` +120, `return` +720). No code mutation; the code and the test pin are both correct. The paragraph exists specifically to correct an earlier stale version of itself ("prose is not compiled") and is stale again.
- Evidence:
  ```
  rg -n "13 044|13_884|13\.884" app/api/v1/welfare-reports/route.ts __tests__/api-v1-rate-limit-families.test.ts
  ```
  ```
  welfare-reports/route.ts:99: // quietly: it reads 13 044 on the merged tree - 11 064 was this file's own
  api-v1-rate-limit-families.test.ts:754:    expect(API_V1_CGNAT_FAMILY_IP_CEILING_PER_MINUTE).toBe(13_884);
  ```
- Refuter notes:
  - correctness (high): confirmed exactly; the constant is computed via `Object.values(...).reduce(...)` at `api-v1-limits.ts:1212-1214`, not transcribed, so on a green tree the true aggregate is 13,884. LOW is right — the same docblock family (`api-v1-limits.ts:1209-1210`) already tells readers to read the constant, not a sentence.
- Fix: replace the transcribed number with a pointer to `API_V1_CGNAT_FAMILY_IP_CEILING_PER_MINUTE` and its `toBe` assertion, matching `lib/infra/api-v1-limits.ts:440-449`'s own treatment of itself.
- fixClass: doc-fix
- priorId/priorStatus: none (new)

#### A11-5 — The mobile 429 countdown copy is unreachable — no /api/v1 429 carries retry-after
- Kind: doc-drift
- Path: `apps/mobile/src/api/client.ts:286`
- Exploit/mutation: no `apiV1Error("rate_limited", 429, ...)` call on the surface passes a third argument — every `rate_limited` 429 is the two-argument form, and `lib/infra/api-v1.ts:67-70` states `extraHeaders` is for the 503's retry-after, "NOT for a 429." The client's `apiFailureMessage` branch at lines 292-296 requires `code === "rate_limited" && retryAfterSeconds !== null`; `retryAfterSeconds` is read from a response header that never arrives, and a foreign platform 429 body falls through `apiV1ErrorCode` to `temporarily_unavailable` instead. The branch is unreachable against this server, while the comment above it (lines 285-289) asserts it is a live mitigation ("what stops a person tapping the button eight more times").
- Evidence:
  ```
  rg -n "rate_limited\", 429, |\"retry-after\"" app/api/v1 -g "route.ts"
  rg -n -i "retry-after" docs/architecture/api-invariants.md
  ```
  ```
  (no apiV1Error("rate_limited", 429, ...) 3-arg call found)
  docs/architecture/api-invariants.md:860: for `Retry-After` on 429. It is NOT set, on either 429 branch.
  lib/infra/api-v1.ts:68-70: "`extraHeaders` exists for the 503's `retry-after`. It is NOT for a 429"
  ```
- Refuter notes:
  - correctness (high): confirmed on both halves — 40 `rate_limited` calls in route files plus 2 in `commands.ts`, all two-argument; the mobile gate at `client.ts:292-296` is unreachable against this server given the error-copy allowlist (`error-copy.ts:38-41`) that degrades unknown-shaped bodies to `temporarily_unavailable`.
- Fix: either make the claim true (rejected by `api-invariants.md:860` — only one of the paired 429 branches could give an honest value, breaking the byte-identical anti-oracle pair), or rewrite the client comment to say the branch is defensive against a future server, citing `api-invariants.md:860` for why it isn't reachable today. Keep the branch and its unit test either way.
- fixClass: doc-fix
- priorId/priorStatus: none (new)

## Refuted

None. All five findings and all six healthy claims were reached by at least one refuter, and no refuter voted `refuted: true`.

### Unverified — no refuter reached

None. Every finding and every healthy claim in this lens received at least one refuter vote.

## Prior triage

| priorId | status | evidence |
|---|---|---|
| — | — | no priors for this lens (`priors: []`) |

## Nits

- `apps/mobile/src/api/client.ts:163` — `performRequest` spreads `...spec.headers` AFTER the `authorization` entry, so a `RequestSpec` carrying an `authorization` header would silently replace the bearer token the session port just resolved. No caller does it today (the field's docblock says it's for `idempotency-key` "and nothing else so far"), but the ordering makes the guarantee a convention, not a structure. One-line fix: spread `spec.headers` first, or delete an `authorization` key from it.
- `app/api/v1/me/privacy/route.ts:109` — this route spends `api_v1_me_privacy_read_ip`/`_write_ip` with no per-user sibling, which reads like A11-1 at a glance but is not the same case: the per-user budgets live in `src/modules/auth/application/subject-rights/{export,erase}-subject-data.ts` (the web door and bearer door share one counter), and the route says so at lines 21 and 316-320. Worth a one-line pointer at the limiter call site itself so a reader auditing this URL doesn't have to travel two files to learn nothing is missing.

## Coverage

Areas read:
- **API v1 routes (pets, adoptions, me, welfare-reports):** `app/api/v1/me/route.ts`, `app/api/v1/pets/[publicToken]/route.ts`, `app/api/v1/pets/[publicToken]/credential/route.ts`, `app/api/v1/pets/[publicToken]/photo/route.ts`, `app/api/v1/pets/[publicToken]/return/route.ts`, `app/api/v1/pets/[publicToken]/shares/route.ts`, `app/api/v1/pets/route.ts`, `app/api/v1/adoptions/[petToken]/route.ts`, `app/api/v1/adoptions/route.ts`, `app/api/v1/me/pet-claims/route.ts`, `app/api/v1/me/privacy/route.ts`, `app/api/v1/welfare-reports/route.ts`, `app/api/v1/me/notifications/commands.ts`
- **Infra / cross-cutting:** `lib/supabase/bearer.ts`, `lib/infra/api-v1.ts`, `lib/infra/api-v1-limits.ts`, `lib/infra/live-user.ts`, `lib/infra/rate-limit.ts`, `middleware.ts`
- **Fences:** `scripts/check-api-v1-envelope.ts`, `scripts/check-authz-guards.ts`, `scripts/check-api-guard-headers.ts`, `package.json`
- **Docs:** `docs/architecture/api-invariants.md`
- **Tests:** `__tests__/api-v1-rate-limit-families.test.ts`, `__tests__/api-v1-adoptions-route.test.ts`, `__tests__/api-v1-pet-detail-payload.test.ts`
- **Mobile client:** `apps/mobile/src/api/client.ts`, `apps/mobile/src/api/endpoints.ts`, `apps/mobile/src/api/error-copy.ts`, `apps/mobile/src/api/client.test.ts`, `apps/mobile/src/config/api.ts`, `apps/mobile/src/auth/supabase-auth.ts`, `apps/mobile/src/observability/sentry.ts`
- **Contract:** `packages/contract/src/api/errors.ts`
- **Application:** `src/modules/pets/application/register-pet.ts`

Areas not reached:
- The command modules behind mutating routes (`app/api/v1/me/{transfers,caretaker-grants,appointments,profile,privacy,adoption-applications,pet-claims}/commands.ts`, `app/api/v1/pets/[publicToken]/{lost,shares,move,profile,return,photo}/commands.ts`, `events/writers.ts`) were swept for user-id scoping by count (`resolvePetHolderAccess` appears 2-5 times each), not read line by line.
- Field-level parity between `packages/contract/src/api/*.ts` wire types and the `app/api/v1/**/payload.ts` builders — envelope, error vocabulary (80/80), and `payloadVersion` gating were verified, but no per-field diff of any individual payload was performed.
- The three `app/api/v1/auth/**` handler bodies — throttle families, `@no-auth-required` reasons and absent CORS were confirmed, but the login/signup/password-reset flows themselves were not read; user enumeration via response shape on those three is unexamined.
- `app/api/v1/localities/route.ts` and `app/api/v1/appointments/**/route.ts` handler bodies (only limiter call sites, families and 404 branches inspected).
- `apps/mobile/src/api/endpoints.ts` was read structurally (paths, methods, version gating, idempotency-key plumbing), not per-endpoint; request-body parity between each wrapper's input type and its route's zod schema is unverified.
- e2e specs touching `/api/v1`, and `scripts/load-probe-api-v1.ts`.
- Timing-based oracles — status/code/body equality between not-permitted and not-existing arms was checked, but no timing analysis was performed (read-only, no runtime).

<details>
<summary>Full filesRead list (36)</summary>

```
app/api/v1/me/route.ts
app/api/v1/pets/[publicToken]/route.ts
app/api/v1/pets/[publicToken]/credential/route.ts
app/api/v1/pets/[publicToken]/photo/route.ts
app/api/v1/pets/[publicToken]/return/route.ts
app/api/v1/pets/[publicToken]/shares/route.ts
app/api/v1/pets/route.ts
app/api/v1/adoptions/[petToken]/route.ts
app/api/v1/adoptions/route.ts
app/api/v1/me/pet-claims/route.ts
app/api/v1/me/privacy/route.ts
app/api/v1/welfare-reports/route.ts
app/api/v1/me/notifications/commands.ts
lib/supabase/bearer.ts
lib/infra/api-v1.ts
lib/infra/api-v1-limits.ts
lib/infra/live-user.ts
lib/infra/rate-limit.ts
middleware.ts
scripts/check-api-v1-envelope.ts
scripts/check-authz-guards.ts
scripts/check-api-guard-headers.ts
package.json
docs/architecture/api-invariants.md
__tests__/api-v1-rate-limit-families.test.ts
__tests__/api-v1-adoptions-route.test.ts
__tests__/api-v1-pet-detail-payload.test.ts
apps/mobile/src/api/client.ts
apps/mobile/src/api/endpoints.ts
apps/mobile/src/api/error-copy.ts
apps/mobile/src/api/client.test.ts
apps/mobile/src/config/api.ts
apps/mobile/src/auth/supabase-auth.ts
apps/mobile/src/observability/sentry.ts
packages/contract/src/api/errors.ts
src/modules/pets/application/register-pet.ts
```

</details>

## Gap round (completeness critic)

The first pass of this lens read `apps/mobile/src/api` only. Five mobile flow directories — `apps/mobile/src/claims`, `custody`, `denuncias`, `shares`, `transfers` — were entirely unread, totalling 7,605 lines of screens and view models driving exactly the mutating routes the base pass left unopened (`/me/pet-claims`, `/pets/{token}/move` and `/return`, `/welfare-reports`, `/pets/{token}/shares`, `/me/transfers`). This round diffed each flow's client body against its route's zod schema and wire type, read the libreta-share exit path (SharesScreen.tsx:37, :200-208) for TTL/revocation/error-object behavior, and traced client failure branches for third-party PII given A06-2 (mobile Sentry ships with no `beforeSend`/`beforeBreadcrumb`). A quick disciplined-negative sweep (`rg -n "AsyncStorage|SecureStore|MMKV|console\.(log|warn|error|info)|captureException|addBreadcrumb|Clipboard|Share\.share"` over the five dirs, excluding tests) returned only three hits, and `rg -n -i "dni" apps/mobile/src` showed no DNI in these dirs, so the round spent its budget on contract parity and error paths rather than re-litigating local persistence.

### Healthy — verified solid (gap round)

#### The only client-supplied row id on the shares surface is narrowed twice before it can revoke anything
- Property: `revoke_libreta_share` carries a `shareId` the client chose, with the pet only in the URL path — the handler will not act on it until the row is proven to belong to that pet, then the writer independently re-checks creator-or-admin. A missing row and a row on somebody else's animal answer identically (404).
- Path: `app/api/v1/pets/[publicToken]/shares/commands.ts:287`
- Guard: `src/modules/pets/application/libreta-share/find-share-for-pet.ts` (pet narrowing) + `revokeLibretaShareForUser`'s own creator-or-admin check; `__tests__/api-v1-shares-route.test.ts:424-429` — refuter `guardVerified=true`.
- What would break it: dropping the `eq(libretaShareTokens.petId, petId)` conjunct from `find-share-for-pet.ts:37`, or calling `revokeLibretaShareForUser` directly on `input.shareId` without the pre-read (both checked, neither reproduces). The mobile client also never sends `shareToken` in place of `shareId` (`shares-view-model.ts:219/220`).
- Evidence: `find-share-for-pet.ts:37` `.where(and(eq(libretaShareTokens.id, shareId), eq(libretaShareTokens.petId, petId)))`; `shares/commands.ts:286-291` 404 on `row === null`; `revoke-libreta-share.ts:35-43` creator-or-admin → `share_forbidden` 403; web caller (`app/actions/libreta-share.ts`) has no pet predicate but is still creator-or-admin gated, so no cross-user break.

#### No transfer command lets the body name the actor — custody decisions come only from the verified session
- Property: `validateRecipientMatch` falls back to an email comparison when a proposal was addressed to an accountless recipient, so `callerEmail` decides custody — but it is resolved server-side from `requireLiveUser`'s verified user, never from the body or a header. `initiate` re-reads the active owner row from the DB; `cancel` compares `fromOwnerId` to the session user.
- Path: `app/api/v1/me/transfers/route.ts:217`
- Guard: `lib/infra/live-user.ts` `requireLiveUser` + `src/modules/transfers/domain/owner-transfer-rules.ts:124-134`; `__tests__/api-v1-me-transfers-route.test.ts:338-351` — refuter `guardVerified=true`.
- What would break it: a `callerEmail`/`asUserId` field on the wire. Checked and absent from all four command members (`packages/contract/src/input/transfer.ts:162-203`) — the only email hit in that file is a comment.
- Evidence: `route.ts:211` `transferCommandInputSchema.safeParse(body)`; `:214-218` `runTransferCommand({ userId: live.user.id, callerEmail: live.user.email ?? "", input: parsed.data })`; `owner-transfer-rules.ts:130-133` id match when `toOwnerId` resolved, else lowercased email match; `initiate-pet-transfer.ts:102-104`; `cancel-pet-transfer.ts:48-50`.

#### The libreta share token has exactly one exit from the phone, and a failed share leaves nothing behind
- Property: `shareToken` is a bearer secret over the animal's medical record. On mobile it is never rendered as text, logged, clipboarded, fetched, or handed to `Linking.openURL` — the sole exit is `Share.share`, whose rejection is swallowed with no data captured. It is revocable from the phone (per-row `canRevoke`) and its TTL is 7/30/90 days or an explicit "sin vencimiento".
- Path: `apps/mobile/src/shares/SharesScreen.tsx:205`
- Guard: none (upheld by absence, not a fence) — refuter `guardVerified=false`: `SharesScreen.test.tsx` only asserts `mockShare` was called; nothing there would fail if a `console.log(url)` or a `Clipboard` copy were added, and `sentry.ts:41-45` still has no `beforeSend`/`beforeBreadcrumb`.
- What would break it: any `console.*`/`captureException` in this file, a clipboard button, or a `fetch` of the share URL (that last one concretely: it would put a live medical credential into an unscrubbed Sentry HTTP breadcrumb, per A06-2). Swept for all four in the five flow directories — none found.
- Evidence: `rg -n "libretaShare|Linking\.|openURL" apps/mobile/src --glob '!*.test.*'` → shares/ hits only `SharesScreen.tsx:81,206,304,307` and `shares-view-model.ts:54-55`, no `Linking.openURL` under `shares/`; `SharesScreen.tsx:208` `await Share.share({...})` / `:209-211` empty `catch {}`; revoke is by row id (`shares-view-model.ts:219`) never by token; `shares-view-model.ts:66-76` TTL choices `[7,30,90]` + `{days:null,label:"Sin vencimiento"}`.

### Claimed healthy, not verified (gap round)

#### "Request-body parity across all five mobile flows is by construction, not by convention"
- Reason refuted (confidence high): parity is NOT one schema on both sides everywhere. The denuncias half parses `welfareReportFileInputSchema`/`welfareReportResolveLocationInputSchema` (the union MEMBERS) client-side while the route parses the union itself (`welfareReportCommandInputSchema`) — functionally equivalent today but not the claimed single-schema mechanism, and the code's own comment at `denuncia-view-model.ts:273-284` flags this deliberate divergence. Separately, `TransferDetailScreen.tsx` bypasses the contract parse entirely for accept/reject/cancel: `run()` (:132-136) takes a typed `TransferCommandInput` and posts straight to `sendTransferCommand` with no `safeParse`, hand-building the three command literals (:236, :271-275, :295) and hand-copying `reason: rejectReason.trim() || null` instead of calling the unused `buildRejectTransfer`. This is the mechanism behind the confirmed finding A11-G2 below.

#### "Every rendered failure in the five flows is a fixed sentence keyed on an enum, with no third party crossing the wire"
- Reason refuted (confidence medium): the bounded return-to-owner half (`autoCancelBody`, `proposal-queries.ts:207-214`) holds exactly as claimed. But `claim-view-model.ts:136-138` interpolates SERVER-SUPPLIED text into a rendered `warn`-tone Callout, including a stranger's initials — `` `${name} ya tiene dueño/a registrado/a (${ack.ownerInitials})` `` — plus another person's pet name at :140/:142, contradicting both "fixed sentence" and "no third party." The evidence claim that "all 18 setNotice/setError calls pass `failureMessage(result)`/`built.message`/a literal" also misses `DevolucionScreen.tsx:130`, which passes `acceptedMessage(result.payload, petName)` whose `err` arm renders a server string verbatim.

### Findings (CONFIRMED) — gap round

#### MED

##### A11-G1 — A client-asserted province/locality on a denuncia clears the "Jurisdicción sin verificar" badge with no cross-check against the coordinates in the same body
- Kind: security
- Path: `app/api/v1/welfare-reports/commands.ts:277`
- Exploit: an authenticated caller POSTs `/api/v1/welfare-reports` with `locationLat/Lng` a real point in Salta but `locationProvince:"Tierra del Fuego"`, `locationLocality:"Ushuaia"`. `commands.ts:277-286` feeds the body's pair to `normalizeLocationForWrite`, which canonicalises the province STRING and range-checks lat/lng independently (`lib/domain/location-normalize.ts:103-119`, never comparing the two); `resolveRoutableJurisdiction` (`lib/infra/jurisdiction-from-text.ts:196-203`) returns `unverified:false` on the mere presence of a province string. The row lands in Ushuaia's govt queue with the badge absent — `WelfareDenunciaRow.tsx:182-191` shows the caveat sentence only when `jurisdictionUnverified` is true, so the badge's absence is the misleading signal, not a printed positive claim. The web takes the identical client-derived pair (`src/modules/welfare/actions.ts:905-924`), so this is a system-wide trust property, not mobile-only.
- Evidence: `rg -n "export async function resolveRoutableJurisdiction" -A 14 lib/infra/jurisdiction-from-text.ts` → `196: if (input.province) { return {..., unverified: false }; }`.
- Refuter notes (3 votes, all confirmed the mechanism, one voted `refuted:true` on impact only):
  - impact (medium confidence, `refuted:true`, no severity vote): mechanism real, but the SAME caller can reach an equally badge-free, internally-consistent row today via the unauthenticated-cost `resolve_location` command (no `welfare_auth`), so the mutation buys nothing over an honest path — argued for LOW/hygiene, not MED.
  - correctness (high, `refuted:false`, MED): reproduced end to end through the contract's free-text fields (`packages/contract/src/input/welfare-report.ts:249-250`) and the route's lack of any pair validation.
  - reproduce (high, `refuted:false`, MED): reproduced at every layer including the DB routing key (`db/migrations/0034_cases_rls_expanded.sql:60` scopes govt visibility by `jurisdiction_province`, so the asserted province literally is the routing key).
  - Net: 2-of-3 confirm at MED; kept at MED per majority, noting the dissent's honest-path argument in the fix.
- Fix: don't let a client-echoed pair set `unverified:false` — cross-check the province/locality against `locationLat`/`locationLng` (containment or reverse-geocode agreement) and fall back to `unverified:true` on disagreement, or make `resolve_location` return an opaque signed candidate handle the client can only echo, not restate as free text.
- fixClass: code-sdd
- priorId/priorStatus: none (new)

#### LOW

##### A11-G2 — TransferDetailScreen builds accept/reject/cancel bodies by hand, bypassing the three contract-validating builders only their own unit test calls
- Kind: convention
- Path: `apps/mobile/src/transfers/TransferDetailScreen.tsx:271`
- Exploit/mutation: `buildAcceptTransfer`/`buildRejectTransfer`/`buildCancelTransfer` (`transfers-view-model.ts:213/218/223`) are imported only by `transfers-view-model.test.ts` — no screen calls them. `TransferDetailScreen.tsx:236/271-275/295` posts hand-built literals whose only free field is `reason`, presently satisfying the schema by coincidence (`.trim() || null` hand-copies the builder's own trim). A future edit to `optionalNote` in `packages/contract/src/input/transfer.ts:128-133` (runtime refinement, not the TS type) would silently diverge here while every other screen picks it up for free via its builder's `safeParse`. No exploit today — `run()` is typed to `TransferCommandInput`, so a structural field addition is still caught by `tsc`.
- Evidence: `rg -n "buildAcceptTransfer|buildRejectTransfer|buildCancelTransfer" apps/mobile/src` → defs at `transfers-view-model.ts:213,218,223`, all other hits in `transfers-view-model.test.ts` only.
- Refuter notes: correctness (high, confirmed) — every claim checked out; the one correction is that the divergence risk is narrower than stated (structural fields are caught by TS; only runtime-only refinements of `optionalNote` would diverge silently), which the finder's own severity vote (LOW) already reflects.
- Fix: have `TransferDetailScreen` call the three builders and branch on `built.ok`, matching `TransferInitiateScreen.tsx:85-92`.
- fixClass: code-sdd
- priorId/priorStatus: none (new)

### Refuted (gap round)

None. Both gap-round findings were confirmed by majority refuter vote. Two healthy claims failed verification and are listed under "Claimed healthy, not verified" above rather than here, since they were finder healthy-claims, not findings.

### Nits (gap round)

- `app/api/v1/me/transfers/route.ts:145` — `callerEmail: live.user.email ?? ""` reaches `validateRecipientMatch`'s email arm; unexploitable today only because `db/schema.ts:4512` declares `to_owner_email` `notNull` and the sole production writer always supplies a contract-validated address. A one-line guard refusing `accept` on an empty `callerEmail` would remove the reliance on that side fact.
- `src/modules/return-to-owner/application/owner-accept-return.ts:127` — `pet_deceased` is unreachable: line 124 pushes `pet_not_lost` for any non-`"lost"` status, which a deceased pet always is, and line 131 takes `failures[0]`. An owner accepting a return on a deceased animal gets "ya no figura como perdida" instead of the deceased-specific sentence `autoCancelBody` has ready.
- `apps/mobile/src/shares/shares-view-model.ts:75` — the never-expiring share LINK is a flat fourth TTL option with no risk copy, while the never-expiring Tier-2 window twenty lines later (`:112-116`) gets an "Avanzado" eyebrow and explanation. Mirrors the web (`ShareLibretaSheet.tsx:59`), so shared gap not a mobile regression — but of the two permanent options, the unmarked one is the secret URL onto the medical record that never dies.

### Coverage (gap round)

Areas read this round: the five previously-unread mobile flow directories (`apps/mobile/src/claims`, `custody`, `denuncias`, `shares`, `transfers` — view models and screens), the matching contract input schemas (`transfer.ts`, `share.ts`, `welfare-report.ts`) and API wire type (`pet-shares.ts`), the matching mutating-route command modules (`me/transfers`, `welfare-reports`, `pets/[publicToken]/shares`, `/return` — commands only, not `move` or `pet-claims`), the jurisdiction/location normalization chain (`lib/infra/jurisdiction-from-text.ts`, `lib/domain/location-normalize.ts`), transfer domain rules and use cases, return-to-owner application logic, and the two share-rendering surfaces (`WelfareDenunciaRow.tsx`, `ShareLibretaSheet.tsx`).

Areas not reached this round: the five flows' own test files (~2,300 lines, unopened — whether the mobile jest suite asserts these request bodies is unverified); `app/api/v1/me/pet-claims/commands.ts` and `app/api/v1/pets/[publicToken]/move/commands.ts` (writer authorization not opened, taken from route parsing and docblocks only); response-side payload PII (only what the client SENDS was diffed, not what comes back, except `PetSharesV1`); `@sentry/react-native`'s default breadcrumb integrations (reasoned from absence of touch points, not read directly — A06-2's `beforeSend`/`beforeBreadcrumb` absence itself is verified directly); `ClaimScreen.tsx`'s `sightingToken`/`claimDisputeUrl` and `DenunciaScreen.tsx`'s `followUpUrl` (not traced for bearer-secret status); non-dispatch, non-notice rendering paths of `MudanzaScreen.tsx`, `DevolucionScreen.tsx`, `TransfersScreen.tsx`, `TransferInitiateScreen.tsx`.
