# D05 — Pitch-claims verification

> Snapshot: `d7dbf25f7` (`main`) · Audit: 2026-09-fresh · Lens D05
> Verified against code on 2026-09-02 by workflow agents (finder + refuters) · Status: draft

## Ground truth

- Branch: `main`, SHA `d7dbf25f7`, audited 2026-09-02.
- Documents read: `docs/design/handoffs/2026-07-07-govt-personas-pitch.md` (58 lines) and `README.md` (175 lines).
- 63 claims extracted, one row/bullet at a time.
- Refuter policy: 1 correctness refuter on claims the finder called TRUE, escalated to 3 lenses (correctness, user-path, official-reading) whenever that single refuter contested the class; the 3-lens panel ran directly on every claim the finder called PARTIAL or FALSE. Final class = majority of refuter votes; ties keep the finder's class and are marked CONTESTED. 45 files read across code, config, migrations, and docs (full list in Coverage).

## Verdict

**TRUE 43 / PARTIAL 17 / FALSE 3 / UNVERIFIED 0** (63 claims)

- 16 claims changed class through refutation (10 moved PARTIAL/FALSE → TRUE, 6 moved TRUE → PARTIAL; none moved to FALSE).
- 0 claims contested (every refutation reached a clean majority; no ties).

## Claims — TRUE

| id | source | quote | evidence (cmd) | safe wording |
|---|---|---|---|---|
| D05-2 | handoff:8 | `govt` \| Funcionario de salud animal / bienestar / zoonosis de un municipio, provincia o CABA \| **Solo su jurisdicción** | `rg -n 'govtAssignments\|govt_assignments' db/schema.ts \| head; rg -n 'jurisdiction' lib/infra/auth-guards.ts \| head` | A govt account sees only the localities assigned to it in `govt_assignments`; scope enforced per row at the action edge, covered by an RLS jurisdiction matrix test. |
| D05-3 | handoff:9 | `admin` \| Operador técnico de la **plataforma** \| Universal — todo el país | `rg -n 'universal' lib/infra/auth-guards.ts \| head -5` | admin is the platform operator role: universal scope, no jurisdiction filter applied. |
| D05-4 | handoff:11 | El `admin` le crea la cuenta, le asigna barrios y actúa de respaldo. | `git ls-files 'app/admin/**/page.tsx' \| rg 'govts'` | An admin creates the govt account at `/admin/govts/new` and assigns it localities (`govt_assignments` rows). |
| D05-6 | handoff:28 | **Qué le damos:** ✅ panel con severidad | `rg -n 'severity\|Alertas priorizadas' app/gob/page.tsx \| head` | The `/gob` panel opens with up to five prioritized alerts, each labelled Prioridad alta or media. |
| D05-7 | handoff:28 | vigilancia epidemiológica | `git ls-files 'app/gob/vigilancia/**' \| rg 'page.tsx'` | Epidemiological surveillance: signals, outbreaks, zoonosis, and an investigations workflow, all under `/gob/vigilancia`. |
| D05-8 | handoff:28 | observaciones A8/A9 | `rg -n -A4 'rabies_observation_compliance_10d:' lib/metrics/kpi-catalog.ts; rg -rn 'A8\|A9' --glob 'app/gob/observaciones/**'` | Repeat as written. |
| D05-9 | handoff:28 | cumplimiento chip/PPP | `rg -n 'ppp' lib/analytics/compliance-metrics.ts -i \| head -6; rg -n -A3 'microchip_penetration:' lib/metrics/kpi-catalog.ts` | PPP attestation compliance (Ley CABA 4078 / Prov 14.107) and microchip penetration, both catalogued KPIs with declared numerators. |
| D05-10 | handoff:28 | **Gap:** integración de la atención en calle (Mascotas CABA) | `rg -rni 'mascotas caba\|atencion en calle\|street intake' --glob 'app/**' --glob 'src/**' --glob 'lib/**' -l` | Gap: no integration with street-level care operations (Mascotas CABA). |
| D05-11 | handoff:28 | **Gap:** ... + export por lote a SENASA. | `head -16 lib/analytics/senasa-export.ts; rg -rn 'senasa-export' --glob 'app/**' --glob 'src/**' -l` | Repeat as written. |
| D05-12 | handoff:33 | /gob/maltrato (severidad + **export MPF** ✅) | `head -12 app/gob/maltrato/page.tsx; rg -n 'export async function generateMpfExport' src/modules/welfare/application/generate-mpf-export.ts; git ls-files 'app/gob/maltrato/**' \| rg -i mpf` | Repeat as written. |
| D05-13 | handoff:33 | → /gob/decomisos | `git ls-files 'app/gob/decomisos/**' \| rg page.tsx` | /gob/decomisos — list, intake form, and per-case detail (detail hands off to the shared `/gob/casos/[publicCode]` route). |
| D05-16 | handoff:38 | **En MiMAR:** /gob/analytics, /gob/programa, Panorama (mapa comparativo), CSV. | `head -14 app/gob/analytics/page.tsx; git ls-files 'app/gob/panorama/**' 'app/gob/programa/**'; rg -n 'json. : .csv' app/gob/analytics/export/actions.ts` | Repeat as written. |
| D05-17 | handoff:38 | **Gap:** que el número canónico sea inatacable (ya cerramos la colisión antirrábica 42/54 y 42/11). | `head -46 lib/metrics/kpi-catalog.ts` | Repeat as written. |
| D05-18 | handoff:43 | **En MiMAR:** /gob/cola (matrículas + orgs), decisión auditada. ✅ Fuerte. | `rg -n 'organization_verification\|service_dog_credential' app/gob/cola/page.tsx; git ls-files 'app/gob/historial/**' 'app/gob/rupga/**' \| rg page.tsx` | Repeat as written. |
| D05-19 | handoff:46 | la privacidad defendible (k-anon, audit de PII, sin DNI en claro) | `rg -n 'k-anonymity\|suppressSmallCells\|complementarySuppress' lib/analytics/subregion-redaction.ts \| head; rg -n 'export function hashDni\|export function dniLast4' lib/utils/dni-hash.ts; rg -rn 'logPiiQuery' --glob 'src/modules/**' -l` | k-anonymity at k=5 with complementary suppression against subtraction attacks, an audit row for every PII query and export, and DNI stored only as a hash (last four digits for display). |
| D05-21 | handoff:46 | El North Star —**población, no mascota individual**— | `rg -n 'North Star' README.md` | The North Star is population-scale animal health, not the individual pet record. |
| D05-24 | handoff:52 | **Diferenciador real:** privacidad con trazabilidad (les da miedo la Ley 25.326). | `head -12 app/gob/outreach/export/route.ts` | Privacy with traceability: no PII query or export happens without an audit row, and export payloads are field-allowlisted (the outreach export's audit write is fire-and-forget, so best-effort on that path). |
| D05-25 | handoff:54 | **El habilitador institucional que falta:** login Mi Argentina | `head -23 lib/infra/miarg-oidc.ts` | Mi Argentina login is the missing institutional enabler: the OIDC integration shape is scaffolded and env-gated, but the real flow is unimplemented pending credentials. |
| D05-29 | README:9 | A reborn 2021 university project (UTN), reimagined for 2026 as a modern PWA with public-health-grade architecture. | `git ls-files \| rg 'app/manifest.ts'` | A reborn 2021 UTN project, shipped as an installable PWA (manifest + hand-written push-only service worker; no offline caching). |
| D05-30 | README:13 | Every pet gets a verifiable digital identity — a credential with a QR code ... contact its owner if lost, and (with owner consent) share its vaccination and medical history. | `rg -n 'tier2Active\|ScanLogger\|tier2PublicPermanent' 'app/(public)/p/[publicToken]/page.tsx' \| head -6` | Every pet gets a QR-verifiable public credential; owner contact appears only when the pet is marked lost, and medical history only when the owner turns on Tier 2. |
| D05-31 | README:13 | The same event log that powers each pet's record feeds high-level dashboards for sanitary authorities, public-health analysts, and animal-welfare officers. | `rg -n 'Facts are event-sourced\|rederivePetCache\|dual-written' CLAUDE.md` | Repeat as written. |
| D05-32 | README:15 | The project's North Star is **animal health and welfare at population scale** and, ultimately, **integration with Mi Argentina** | `head -23 lib/infra/miarg-oidc.ts` | Population-scale animal health, and ultimately Mi Argentina federation — scaffolded and env-gated today, not yet connected. |
| D05-33 | README:26 | Mortality & disposal dashboard \| `/gob/mortalidad` \| Govt (jurisdiction-scoped) + admin ... \| Live | `git ls-files 'app/gob/mortalidad/**'; rg -n 'mortalidad' components/layout/nav-presets.ts` | /gob/mortalidad — live, jurisdiction-scoped, with its own govt nav entry. |
| D05-34 | README:28 | Alert inbox & triage \| `/admin/alertas` \| Admin ... \| Live | `rg -n 'reconocida\|en_investigacion\|autoridad_contactada\|resuelta' app/admin/alertas/page.tsx` | /admin/alertas — admin-only; firings move through acknowledge → investigate → contact authority → resolve or dismiss. |
| D05-35 | README:30 | Public shelter profile \| `/refugios/[orgToken]` \| Anyone (no auth) ... \| Live | `rg -n 'verified AND orgType\|notFound' 'app/(public)/refugios/[orgToken]/page.tsx' \| head -4` | /refugios/[orgToken] is public and 404s unless the org is verified AND of type shelter or rescue_network. |
| D05-37 | README:33 | Unified case detail \| `/casos/[publicCode]` \| Anyone (no auth) — role-aware, PII-redacted for anon \| Live | `rg -n 'role-aware\|canReadCase' 'app/(public)/casos/[publicCode]/page.tsx' \| head -3` | /casos/[publicCode] is public and role-aware; anonymous viewers get a redacted view, limited to bite_incident, lost_pet_episode and adoption_listing kinds (rate-limited per IP). |
| D05-38 | README:31 | Public adoption listing \| `/adoptar` \| Anyone (no auth); applying requires login \| Live | `rg -n 'iniciar-sesion' 'app/(public)/adoptar/[petToken]/postular/page.tsx'` | /adoptar is public to browse (no session read at all); applying redirects anonymous visitors to sign-in and returns them to the form. |
| D05-39 | README:42 | Every vet works through a clinic org... There is no separate `/pro` portal. | `git ls-files \| rg '^app/pro'; git ls-files 'app/(app)/cuenta/crear-consultorio/*'; rg -n 'resolveVetLanding' 'app/(app)/inicio/page.tsx'` | Repeat as written. |
| D05-40 | README:46 | The `account_type ↔ role` invariant is enforced at the app layer only. Migration 0015 added a DB CHECK and 0016 dropped it. | `git ls-files 'db/migrations/001[56]_*'; rg -n 'CHECK' db/migrations/0016_drop_role_match_check.sql \| head -4` | Repeat as written. |
| D05-42 | README:56 | Database \| Postgres (via Supabase) + Row Level Security | `git ls-files '__tests__/rls/*'; rg -n 'bypasses Postgres RLS' README.md` | Repeat as written — 2 of 3 refuter votes held this is accurate under a live user path (RLS is the sole enforcement for browser-direct Storage uploads, not decoration). |
| D05-43 | README:60 | Tests \| Vitest (strict TDD) — split into a parallel `unit` project (no DB) and a serial `db` project (against local Postgres); `pnpm test` runs both | `rg -n 'projects:\|name: "unit"\|name: "db"' vitest.config.ts; rg -n '"test":\|"test:verified"' package.json` | Repeat as written. |
| D05-46 | README:89 | Four portals are live end-to-end: | `git ls-files 'app/**/page.tsx' \| wc -l; git ls-files 'app/gob/**/page.tsx' \| wc -l` | Repeat as written. |
| D05-47 | README:91 | `/inicio` (a server-side router, not a screen: it redirects into the most-urgent pet's profile) | `rg -n 'redirect' 'app/(app)/inicio/page.tsx' \| head -6` | Repeat as written; `/inicio` has no JSX at all, every path exits through `redirect()`. |
| D05-48 | README:93 | **Govt** (`/gob`) — KPI dashboard, approval queue (matrículas / org verification / RUPGA), welfare reports + triage, ... | `rg -n 'organization_verification\|service_dog_credential' app/gob/cola/page.tsx; git ls-files 'app/gob/**' \| rg 'perdidas\|disputas\|reglas/page\|servicios/page\|historial\|usuarios'` | Repeat as written. |
| D05-49 | README:94 | **Admin** (`/admin`) — universal queue/cases, admin & govt account management, jurisdictions + business-rule CRUD, ... | `rg -n 'SLA' app/admin/outbox/page.tsx \| head -4; git ls-files 'app/admin/**' \| rg 'govts/new\|reglas/nueva\|moderacion/page\|observaciones/page\|sistema/page'` | Repeat as written; "jurisdictions" is the picker folded into the shared `/reglas` console, not a standalone route. |
| D05-53 | README:153 | bucketed by role — NOTHING new goes in lib/ root (enforced by `pnpm lint:lib-root`) | `rg -n 'lint:lib-root' package.json` | Repeat as written; the fence runs inside `pnpm verify`, and the bucketize is now complete (0 root files). |
| D05-57 | README:156 | lib/infra/publicToken.ts DIM-XXXX-XXXX token generator (DIM is the codename, stays) | `rg -n 'publicToken.ts' scripts/check-brand-casing.ts` | Repeat as written; the file is one of exactly three documented exemptions in the brand-codename fence. |
| D05-58 | README:175 | Proprietary — all rights reserved. See [LICENSE](./LICENSE). | `head -4 LICENSE` | Repeat as written. |
| D05-59 | README:175 | This system's aggregate open data is published separately at `/transparencia` under CC-BY 4.0. | `rg -n -A6 'export const OPEN_DATA_LICENSE' lib/open-data/datasets.ts; git ls-files 'app/(public)/transparencia/**' \| head -3` | Repeat as written; every dataset ships its CC BY 4.0 license, attribution, methodology and k=5 suppression rule as metadata. |
| D05-60 | README:24 | Org portal \| `/org/[orgToken]` \| Org members (shelter, clinic, rescue network, sanitary authority) — capability-scoped \| Live | `git ls-files 'app/org/[orgToken]/**' \| rg page.tsx \| wc -l; rg -n 'lint:authz-orgtoken\|lint:scope-authz' package.json \| head -2` | Repeat as written (43-44 routes; membership gated by URL token, each route re-checks its capability). |
| D05-61 | README:141 | api/cron/ scheduled jobs (outbox drain, auto-expire, escalations) | `git ls-files 'app/api/cron/*' \| head -20` | Repeat as written; 25 cron routes total, also covering ENO queue processing, alert evaluation and data-lifecycle jobs. |
| D05-62 | README:81 | Multi-step writes thread a single transaction through the repository; notifications are flushed post-transaction (best-effort). | `rg -n 'tx support' src/modules/cases/__tests__/cases-repository.test.ts \| head -3` | Repeat as written. |
| D05-63 | README:59 | Lint / format \| Biome | `rg -n '"@biomejs/biome"' package.json; rg -n '"verify":' package.json` | Repeat as written — Biome is the repo's only lint/format tool; the ~66 `scripts/check-*.ts` fences are a separate, additional enforcement layer worth a line of its own in a pitch, not a correction to this row. |

## Claims — PARTIAL

**D05-1** — handoff:1 — `# MiMAR para el Estado — personas, journeys y propuesta de valor`
Why partial: the enforced public brand is "miMAR" (lowercase m), fenced by `scripts/check-brand-casing.ts` Rule 1; the fence's glob covers only `.ts`/`.tsx` under app/components/lib/packages/src, so no `.md` is scanned — the pitch spells the brand wrong 7 times (README 3 more), but the substantive offer (a State-facing pitch, 54 `/gob` pages) is real.
Evidence: `rg -c 'MiMAR' docs/design/handoffs/2026-07-07-govt-personas-pitch.md README.md; head -3 scripts/check-brand-casing.ts; rg -n 'globSync' scripts/check-brand-casing.ts` → pitch 7 hits of "MiMAR" / 0 of "miMAR"; README 3/0; `check-brand-casing.ts:131` globs only `{app,components,lib,packages,src}/**/*.{ts,tsx}`.
Votes: correctness — PARTIAL, casing wrong but content real, safe wording keeps the sentence and only recases. · user-path — PARTIAL, all named `/gob` routes are reachable, only the brand string is wrong. · official-reading — PARTIAL, defect predates the 2026-07-18 recase decision by 11 days; still a stale title today.
Safe wording: miMAR para el Estado — personas, journeys y propuesta de valor.

**D05-5** — handoff:27 — `Panel (alarmas) → /gob/vigilancia (brotes) → /gob/observaciones (cierre 10 días en plazo) → outreach (lista de vencidos por barrio).`
Why partial: `/gob/outreach` is now a permanent, param-preserving redirect into `/gob/operativos?vista=alcance`; more importantly, the "por barrio" list is aggregated by locality — `AlcanceScreen.tsx` has no barrio identifier, only a locality axis.
Evidence: `git ls-files 'app/gob/**' | rg 'vigilancia|observaciones|outreach'; head -6 app/gob/outreach/page.tsx` → `outreach/page.tsx:1` "ABSORBED into the Operativos hub ... redirects into /gob/operativos?vista=alcance".
Votes: correctness — TRUE, redirect is transparent and preserves params, no real defect. · user-path — PARTIAL, real defect is granularity (locality, not barrio), not the redirect. · official-reading — PARTIAL, same granularity gap; safe wording should name the aggregation level explicitly.
Safe wording: Panel (alarmas) → /gob/vigilancia (brotes) → /gob/observaciones (cierre 10 días en plazo) → /gob/operativos?vista=alcance (ranking de antirrábica vencida agregado por localidad, no por barrio; CSV disponible).

**D05-14** — handoff:33 — `→ /gob/casos (código CAS- unificado)`
Why partial: "unificado" is true for cases only — welfare denuncias keep a separate DEN-XXXX-XXXX reference code and custody disputes keep a separate DIS- token in the same hub's Disputas tab; a 2026-07-28 finding recorded one dispute under two codes with two different state chips, fixed by routing rather than by unifying the code.
Evidence: `src/modules/welfare/domain/reference-code.ts:2,56` DEN- format; `app/gob/casos/page.tsx:1-15` F6 fusion header records Disputas kept `[disputeToken]`, "UNCHANGED"; `src/modules/cases/domain/case-kinds.ts:44-50` records the dual-code finding.
Votes: correctness — PARTIAL, CAS- is unified within cases only, not across the escalation chain. · user-path — TRUE, every row a user actually opens on the Casos tab resolves by CAS- code. · official-reading — PARTIAL, "unificado" read literally denies what the denuncia and disputa legs still carry.
Safe wording: /gob/casos unifica 12 de los 13 tipos de caso bajo un único código CAS-XXXX-XXXX (incluidas las escalaciones de maltrato y decomiso). Aclarar: la denuncia sigue teniendo su propio expediente DEN- en /gob/maltrato y las disputas de custodia su código DIS- en la pestaña Disputas del mismo hub.

**D05-20** — handoff:46 — `MiMAR ya le da el marco legal, la privacidad defendible ..., la vigilancia epidemiológica y los tableros de cumplimiento mejor que cualquier alternativa privada del espacio.`
Why partial: the four capabilities are real (verified independently in D05-7/9/19), but the comparative superlative rests on no benchmark measuring those four axes — the one competitor benchmark on file (2026-07-04) compares landing pages, UX and civic layer, not legal/privacy/surveillance/compliance, and records that VetCard already pilots with a municipality and holds an AAIP registry badge.
Evidence: `rg -rni 'competitor|benchmark|alternativa privada|comparison with' --glob 'docs/**' --glob 'lib/**' -l` → finder's evidenceOutput ("no benchmark exists") did not reproduce; a benchmark doc exists at `docs/design/handoffs/2026-07-04-benchmark-landing.md:110-149` naming VetCard, Petco Love Lost, PetHub, 11pets, LATAM registries.
Votes: correctness — PARTIAL, real doc exists but doesn't compare on these 4 axes. · user-path — PARTIAL, all 4 capabilities reachable; superlative unsupported. · official-reading — PARTIAL, VetCard already piloting with a municipality undercuts the superlative further.
Safe wording: miMAR ya le da el marco legal, la privacidad defendible (k-anon, audit de PII, sin DNI en claro), la vigilancia epidemiológica y los tableros de cumplimiento. Sacar el comparativo, o acotarlo y fecharlo citando el benchmark del 2026-07-04 — nunca "mejor que cualquier alternativa privada".

**D05-22** — handoff:48 — `La brecha no es de features sueltos sino de costuras cerradas — el día en que la vacuna en la calle, la denuncia de anoche y la mordedura de esta mañana aparezcan como trabajo cerrable en una sola cola`
Why partial: `/gob/acciones` (built 2026-08, after this deck) already merges observaciones + denuncias + casos into one deadline-ranked queue with a closing affordance each — covering the denuncia and mordedura legs — but street vaccination is not one of its domains, so the seam is two of three legs closed, not zero.
Evidence: `app/gob/acciones/page.tsx:1-11` "merges the three deadline-bearing domains into ONE flat list ranked by deadline"; `worklist-core.ts:44` `WorklistDomain = "observacion" | "denuncia" | "caso"` — no vaccination domain.
Votes: correctness — PARTIAL, two of three legs already one queue via `/gob/acciones`; vaccination absent. · user-path — PARTIAL, same finding, only denuncia rows resolve inline. · official-reading — PARTIAL, but locates the gap differently: denuncia is closable work, bite is a dashboard/KPI surface, vaccination is a results readout — closing the seam means building queues, not merging routes.
Safe wording: Two of the three seams are already one queue: `/gob/acciones` merges rabies observations, welfare denuncias and regulatory cases into a single deadline-ranked list. Street vaccination is not one of its domains — that seam is still open.

**D05-23** — handoff:51 — `Fortaleza: public-health-grade, legally anchored`
Why partial: legal anchoring is real where declared (11 of 70 KPIs carry a `sourceKind`, and the code refuses to invent citations), but the dashboard as a whole is mostly unanchored — only 2 KPIs cite an actual statute, both scoped to CABA/Buenos Aires, so an official outside those two jurisdictions sees a generic provincial-gap placeholder instead of their own law.
Evidence: `rg -n 'Ley CABA 4078|No legal basis' lib/metrics/kpi-catalog.ts lib/analytics/compliance-metrics.ts | head -5`; `metric-legal-basis.ts:69-76` has exactly 2 entries, both province-scoped.
Votes: correctness — PARTIAL, 11/70 KPIs declare a source, only 2 statutory. · user-path — PARTIAL, the popover-level legal-basis registry has exactly 2 entries, both provincial, no national anchor. · official-reading — PARTIAL, national norms exist elsewhere (case normatives, disease anchors) but the dashboard numbers themselves are mostly unanchored.
Safe wording: The two KPIs that rest on a norm — PPP registry compliance and mortality-disposal traceability — cite it, resolved to the operator's own jurisdiction. The rest show their data source and target, and state plainly when no norm backs them (microchip penetration explicitly declares no legal mandate exists).

**D05-26** — handoff:57 — `Moderación govt jurisdiccional (SDD) · integración Mascotas CABA calle · export SENASA/LSUCyF por lote · UX de campañas (crear → asignar turnos → medir asistencia) · Mi Argentina login.`
Why partial: two of five roadmap items have already moved — govt jurisdictional moderation shipped (see D05-15), and the campaign create → assign turnos → measure attendance cycle already runs end to end via organization accounts, with govt limited to review; the list has not been updated.
Evidence: `head -6 app/gob/moderacion/page.tsx` → redirect into the shipped Denuncias hub queue; `git ls-files | rg -i 'campan' | rg -v test` → `ServiceOfferingForm.tsx`, `AgendaRuleForm.tsx`, `campaign-metrics.ts` all exist and are wired.
Votes: correctness — PARTIAL, moderation shipped; campaign creation and turno assignment also already exist (govt-side create surface is what's missing, not the mechanism). · user-path — PARTIAL, same finding, walked end to end through org accounts. · official-reading — PARTIAL, adds that only the govt-console create UX is missing, not the underlying capability.
Safe wording: Ya shipeó la moderación govt jurisdiccional. El circuito crear → asignar turnos → medir asistencia ya existe completo, operado por cuentas de organización; falta que el propio panel gob pueda crear la campaña. Pendiente real: integración Mascotas CABA, export SENASA/LSUCyF (motor construido, sin pantalla), y login Mi Argentina.

**D05-27** — README:3 — `MiMAR is Argentina's digital pet credential and health record system.`
Why partial: substance is true (credential + health record, four live portals verified elsewhere in this lens), but the sentence spells the brand "MiMAR" — the codebase's own canonical casing, fenced by lint, is "miMAR" — and the README also implies national-registry status the product does not yet have (Mi Argentina integration is a placeholder).
Evidence: `rg -c 'MiMAR' README.md; rg -n 'brand is spelled' scripts/check-brand-casing.ts; rg -n 'Acerca de' 'app/(public)/acerca/page.tsx'` → README 3 hits of "MiMAR", 0 of "miMAR"; `acerca/page.tsx:5` uses "miMAR".
Votes: correctness — PARTIAL, casing wrong, substance right. · user-path — TRUE, no route a user visits ever renders "MiMAR" — the defect is confined to this one unfenced doc. · official-reading — PARTIAL, the real risk for an official reader is the implicit "official/national" claim, not the casing.
Safe wording: **miMAR** is a digital pet credential and health record system built for Argentina — today a working pilot, not an officially adopted national registry.

**D05-28** — README:5 — `DIM is the internal codename used in schema, tokens (DIM-XXXX-XXXX), code identifiers, and audit logs. MiMAR is the user-facing brand.`
Why partial: the DIM-codename half is real and fenced (Rule 2 of the brand fence permits it only as a hyphenated token prefix); the brand half spells the brand exactly as the fence's own Rule 1 bans it — canonical casing is "miMAR". "Audit logs" is also unsubstantiated: the `auditLog` schema has no DIM-named column, only payload strings.
Evidence: `head -35 scripts/check-brand-casing.ts` → Rule 2 header, `WRONG_CASE_BRAND` regex banning "MiMAR"/"Mimar"/"MIMAR"; `lib/ui/branding.ts:13` `appName: "miMAR"`.
Votes: correctness — PARTIAL, brand casing wrong per the fence's own rule. · user-path — PARTIAL, no user-facing surface actually renders "MiMAR" (all say "miMAR"); audit-log claim is unsubstantiated. · official-reading — PARTIAL, DIM-as-codename mechanism checks out; brand spelling is the one defect.
Safe wording: DIM is the internal codename — permitted in user-facing surfaces only as the hyphenated credential-token prefix (DIM-XXXX-XXXX), enforced by a lint fence. The brand is miMAR (lowercase m). Audit-log rows are keyed to users, organizations and approvals, not to DIM tokens.

**D05-36** — README:32 — `Public welfare report | /denuncias/nueva · /buscar · /codigo/[code] | Anyone (anonymous, reference-code tracking) | Live`
Why partial: two of the three URLs as written do not exist — the real paths need the `/denuncias` prefix (`/denuncias/buscar`, `/denuncias/codigo/[code]`); the table also omits `/denuncias` and `/denuncias/seguimiento`. Separately, "reference-code tracking" overstates what an anonymous reporter can do — the code alone confirms a report exists (id + date only); seeing its status needs the reporter's own session or an emailed access link.
Evidence: `git ls-files 'app/(public)/denuncias/**' | rg page.tsx` → all five pages live under `.../denuncias/...`, no top-level `/buscar` or `/codigo` directory exists.
Votes: correctness — PARTIAL, URLs need the `/denuncias` prefix. · user-path — PARTIAL, same URL defect plus the tracking-scope overstatement. · official-reading — PARTIAL, same, emphasizing that a reporter who left no email can never see status.
Safe wording: Public welfare report | `/denuncias` · `/denuncias/nueva` · `/denuncias/buscar` · `/denuncias/codigo/[code]` · `/denuncias/seguimiento` | Anyone (anonymous filing; the reference code alone only confirms the report exists — seeing status needs the reporter's own session or an emailed access link) | Live

**D05-41** — README:46 — `Personal accounts (owner, vet) can own pets and have Mi Argentina identity. Institutional accounts (govt, admin) have neither — they are service accounts for governance work.`
Why partial: the institutional-accounts-own-nothing half is DB-enforced (a CHECK plus a trigger); the "have Mi Argentina identity" half is not a present capability for anyone — the `miarg_sub` column exists but has no writer (`upsertProfileFromMiArgClaims` throws unconditionally; the OIDC callback 404s/501s in every configuration).
Evidence: `rg -n 'accountType. = .personal. or' db/schema.ts; rg -n 'TODO\(25b\)' db/schema.ts lib/infra/miarg-oidc.ts | head` → CHECK confirmed; `miarg-oidc.ts:145-147` throws "not implemented — pending Mi Argentina credentials (Item 25b)".
Votes: correctness — PARTIAL, both DB guards key on `account_type` not `role` (that mapping is app-layer only since migration 0016). · user-path — PARTIAL, same finding plus confirmation the layout redirects institutional roles away from the owner area. · official-reading — PARTIAL, adds the trigger (`ownerships_institutional_no_pets`) as a second, independent guard.
Safe wording: Personal accounts own pets and are the only accounts that may carry a Mi Argentina identity — the column exists but nothing writes it yet (OIDC callback still a stub, Item 25b pending). Institutional accounts are DB-blocked from PII and from owning pets. Both guards key on `account_type`; the `govt`/`admin` role mapping itself is app-layer only.

**D05-44** — README:79 — `domain/ is pure and unit-tested without a database; infrastructure/ is the only place that issues Drizzle queries.`
Why partial: the `domain/` purity half is real and machine-enforced (Biome `noRestrictedImports`, 0 violations); the "only place" half is false — 153 non-test files under `application/` (and elsewhere) import `drizzle-orm` directly, with no fence covering that layer.
Evidence: `rg -n -A12 'src/modules/\*/domain' biome.json` → bans `@/db`, `drizzle-orm`, `next` at error level, scoped only to `src/modules/*/domain/**`; `rg -l 'from "drizzle-orm"' --glob 'src/modules/*/application/**' src/modules | wc -l` → 153.
Votes: correctness — PARTIAL, domain purity enforced; infrastructure exclusivity is false (153 application-layer files). · user-path — PARTIAL, extends the count to 90 files repo-wide outside `infrastructure/`, including page-level DB calls in `app/gob/`. · official-reading — PARTIAL, same finding; only the domain half is what's actually enforced.
Safe wording: The domain layer's purity is machine-enforced by a Biome override (0 violations). Drizzle queries, however, are NOT confined to infrastructure/ — 153 application-layer files import drizzle-orm directly, and no fence covers that layer.

**D05-45** — README:80 — `actions.ts is the security boundary — Drizzle bypasses Postgres RLS by design, so authorization is enforced at the action edge, not in the DB.`
Why partial: the mechanism half is real (the app connects service-role/BYPASSRLS, so `actions.ts` is the boundary for that path, backed by 6 authz lint fences); "not in the DB" is too absolute — for at least one browser-direct Storage-upload path (revocation evidence), no server action mediates the write and RLS is the *only* enforcement.
Evidence: `git ls-files '__tests__/rls/*' | wc -l; rg -n 'lint:rls|lint:authz' package.json | head -2` → 12 RLS test files, 6 authz fences chained into `verify`; `db/revocations_storage.sql:44-55` real RLS policy gating a browser-direct upload.
Votes: correctness — PARTIAL, migration 0188 documents the storage-write hole closed "at the RLS layer" precisely because the action edge couldn't reach it. · user-path — PARTIAL, walks the live official flow (revoke org verification → browser upload) where RLS alone gates the write. · official-reading — PARTIAL, same finding, with the broader statistic that RLS covers the schema as genuine defense-in-depth.
Safe wording: `actions.ts` is the primary security boundary for the app's own Drizzle path (service-role, RLS bypassed by design). RLS is not decorative: on paths that never touch `actions.ts` — browser-direct Storage uploads in the revocation/deactivation flows — the database policies are the only thing enforcing access.

**D05-52** — README:112 — `db:bootstrap is idempotent and refuses to run against a non-local database.`
Why partial: the guard is real (exits 4 on a non-local host by default) and the schema steps are genuinely idempotent, but `--allow-remote` disables the guard with only a printed warning and no confirmation prompt — and the seed step it then runs is documented as "re-runnable, NOT idempotent," having cost real rows on staging once already.
Evidence: `rg -n 'LOCAL_HOSTS|allow-remote|refuses' scripts/db-bootstrap.ts | head -6` → `LOCAL_HOSTS` guard at line 129, `--allow-remote` override at line 83.
Votes: correctness — PARTIAL, override is a bare flag, warn-only. · user-path — PARTIAL, extends the finding: the seed step (`seed-test-users.ts`) is explicitly "re-runnable, NOT idempotent" and once duplicated rows on staging. · official-reading — PARTIAL, confirms the guard is opt-out by design ("exists for staging bootstraps"), not hypothetical.
Safe wording: `db:bootstrap` aborts by default when `DATABASE_URL` points at a non-local host (overridable with `--allow-remote`, warn-only, no prompt). Its schema steps are safely re-runnable; the seed step is re-runnable but NOT idempotent.

**D05-54** — README:158 — `__tests__/ integration tests (Vitest, run against local Postgres)`
Why partial: the config itself says the opposite of a blanket claim — the suite is deliberately split into a parallel, DB-free `unit` project and a serial `db` project, because "only the DB-integration subset needs" Postgres; roughly 260 of 666 files under `__tests__/` import `@/db` at all, and DB-project membership is deliberately conservative (counts some files that never actually query).
Evidence: `rg -n 'only the DB-integration subset needs it|name: "unit"|name: "db"' vitest.config.ts` → line 9 states the rationale directly.
Votes: correctness — PARTIAL, README:60 in the same file already states the split correctly — line 158 is a stale, contradicting tree label. · user-path — PARTIAL, same finding, notes `__tests__/project-partition.guard.test.ts` is itself a DB-free file. · official-reading — PARTIAL, same, flags the self-contradiction within the same document as the sharper defect.
Safe wording: `__tests__/` — Vitest tests, split (per the Tests row in the Stack table) into a parallel `unit` project that never reaches the DB client and a serial `db` project that runs against local Postgres.

**D05-55** — README:120 — `## Project layout ... src/ ... app/ ... components/ ... db/ ... lib/ ... __tests__/ ... middleware.ts ... docs/`
Why partial: the layout omits the mobile workspace entirely — `apps/mobile` is a React Native/Expo client with its own jest suite, wired into `pnpm verify` via `verify:mobile` — and also omits `packages/contract`, the shared wire-types package both web and mobile import, which is the whole reason this is a pnpm workspace.
Evidence: `git ls-files 'apps/mobile/package.json'; rg -n '"test"|jest' apps/mobile/package.json | head -3; rg -n 'apps/' README.md` → `apps/mobile/package.json` exists with `"test": "jest"`; zero "apps/" mentions anywhere in README.md.
Votes: correctness — PARTIAL, confirms `verify:mobile` is chained into `pnpm verify`; also flags `packages/` and other top-level dirs (e2e/, scripts/, types/) as omitted. · user-path — PARTIAL, same finding, broadens the fix to include `packages/contract`. · official-reading — PARTIAL, same, notes everything the tree DOES list is accurate — the defect is omission only.
Safe wording: Add the two missing workspace roots: `apps/mobile/` — the React Native (Expo) client, jest-tested and gated by `pnpm verify:mobile` inside `pnpm verify` — and `packages/contract/` — the wire types shared by the web app and the phone.

**D05-56** — README:142 — `actions/ legacy server actions — now thin re-export shims into src/modules`
Why partial: most of `app/actions/` (55-56 of 70 files) does carry a documented "thin shim (strangler migration N/61)" header, but a minority still hold real orchestration and query the database directly — `app/actions/decomiso.ts` is 505 lines with four `db.transaction(...)` blocks and its own Drizzle imports, not a re-export.
Evidence: `git ls-files 'app/actions/*.ts' | rg -v '\.test\.ts$' | wc -l` → 70 files; `rg -l "thin shim" app/actions/*.ts | wc -l` → 55; `wc -l app/actions/decomiso.ts` → 505.
Votes: correctness — PARTIAL, majority-shim but a real remainder still holds logic. · user-path — PARTIAL, the finder's own cited evidence (README:82, welfare shim tests) is about a different directory (`lib/`) entirely and doesn't support the claim either way; opening `app/actions/` directly confirms the same partial picture. · official-reading — PARTIAL, same, adds that a `"use server"` file can only re-export async functions, so these are delegating wrappers rather than literal re-exports even where thin.
Safe wording: `app/actions/` — legacy server actions, now mostly thin delegating shims into `src/modules` (55 of 70 carry a strangler-shim header); a few still hold orchestration and query the DB directly (`decomiso.ts`, 505 lines).

## Claims — FALSE

**D05-15** — handoff:36 — `Gap: que el govt modere las denuncias anónimas de su propia jurisdicción (hoy solo admin — SDD escrito, phased).`
Why false: the SDD this line calls "escrito, phased" has already shipped end to end — `requireDenunciaModerationPrincipal` grants jurisdiction-scoped moderation to `role='govt'`, the queue and detail page filter by the viewer's jurisdictions, and all three mutating actions (approve/reject/escalate) re-check scope. Presenting a delivered capability as a future gap under-sells the product to the room.
Evidence: `head -9 app/gob/moderacion/page.tsx; rg -n 'jurisdiction-moderation|SCOPED to the account' lib/infra/auth-guards.ts` → `auth-guards.ts:291` "role='govt' (SCOPED to the account's active jurisdiction assignments)"; `src/modules/welfare/actions.ts:309,343,374` — approve/reject/escalate all guard-and-scope-check.
Votes: correctness — FALSE, all three write actions re-run the guard and re-check scope; no gate found. · user-path — FALSE, walked end to end (queue → detail → all three actions) with jurisdiction scope enforced at each step. · official-reading — FALSE, same finding; an official reading "hoy solo admin" would wrongly wait for a future phase that already happened.
Safe wording: govt accounts moderate the anonymous denuncia queue of their own assigned localities at `/gob/denuncias?etapa=moderacion`: aprobar, rechazar como abuso, o escalar a la administración nacional, con el alcance verificado en cada acción. Admin conserva alcance universal y se queda con las denuncias sin jurisdicción y las ya escaladas.

**D05-50** — README:98 — `Deferred by design: govt /gob/analytics and org-side maltrato intake — both built, neither wired into navigation yet.`
Why false: both halves are stale. Org-side maltrato has a live nav entry (`/org/[orgToken]/maltrato/recibidos`, role-gated), and `/gob/analytics` was folded into the navigable `/gob/programa` (as its "Analítica" vista) on 2026-08-01 — `/gob/programa` itself has its own nav entry. Neither surface is deferred any more.
Evidence: `rg -n 'analitica|maltrato' components/layout/nav-presets.ts | head -8; head -8 app/gob/analytics/page.tsx` → `nav-presets.ts:266-271` real org maltrato nav item; `analytics/page.tsx:1` "ABSORBED into the Programa hub as the 'Analítica' vista (F9 fusion, 2026-08-01)".
Votes: correctness — FALSE, both surfaces reproduce as navigable at this SHA. · user-path — FALSE, same finding, walked both routes end to end. · official-reading — FALSE, notes the sentence is worse than merely stale — a reader would hunt for two screens that are both one click away.
Safe wording: Omit. Both surfaces are reachable from navigation today: org maltrato intake at `/org/[orgToken]/maltrato/recibidos`, and the govt analytics dashboard as the "Analítica" vista of the navigable `/gob/programa` hub.

**D05-51** — README:102 — `Requires Node ≥ 22.13, pnpm, and Docker Desktop running`
Why false: `package.json` declares `"node": ">=22.23.0 <23"`, and `.nvmrc`/`.node-version` both pin `22.23.2`. `scripts/check-node-version.ts` documents `22.13` by name as the precise stale open range the fence was written to kill, with measured breakage below `22.23.0` (registerHooks needs 22.15, type stripping needs 22.18, a windows-1252 dash difference needs 22.23) — and `CONTRIBUTING.md` in the same repo already states the correct range.
Evidence: `rg -n -A4 '"engines"' package.json; rg -n '22.13|22.23' scripts/check-node-version.ts | head -8` → `package.json:192` `">=22.23.0 <23"`; `check-node-version.ts:9` names `">=22.13.0"` as the stale range the fence kills.
Votes: correctness — FALSE, `.nvmrc` pins 22.23.2; the open `≥22.13` range would also admit Node 25, measured to turn 5 test failures into 139. · user-path — FALSE, same, adds that `pnpm lint:node-version` is the first link of `pnpm verify` and would reject Node 22.13 immediately. · official-reading — FALSE, notes `CONTRIBUTING.md:7` already states the correct closed range in the same tree.
Safe wording: Requires **Node 22.23.0 – 22.x** (`.nvmrc` pins 22.23.2), **pnpm**, and **Docker Desktop** running (the local Supabase stack runs in Docker).

## Contested / unverified

None. All 63 claims reached a clean majority verdict; 0 ties, 0 unvoted claims.

## For limites-honestos.md

1. The pitch and README spell the brand "MiMAR" instead of the product's actual lowercase-m brand "miMAR"; the fence that would catch this only scans code files, not docs.
2. The pitch's literal "outreach" step now redirects to `/gob/operativos?vista=alcance`, and the overdue-vaccine list it lands on is aggregated by locality, not by neighborhood ("barrio").
3. The unified CAS- case code does not cover every step of the escalation chain — welfare denuncias keep a separate DEN- reference code and custody disputes keep a separate DIS- token in the same hub.
4. No benchmark or comparison exists on the four axes the pitch names (legal framing, privacy, surveillance, compliance dashboards) against private-sector competitors; the only benchmark on file compares landing pages and UX.
5. Only the report-triage leg of "street vaccine / last night's report / this morning's bite" is a true one-queue, closable-work item today; bite handling is a KPI/dashboard surface and street vaccination is a results readout, not a shared work queue.
6. Only two KPIs in the whole catalog cite an actual law, and both are scoped to CABA/Buenos Aires — an official outside those two jurisdictions sees a generic placeholder instead of their own law.
7. Two of the five items on the government roadmap list have already shipped (jurisdictional moderation, and the campaign create/assign-turnos cycle via organization accounts) — the roadmap has not been updated to say so.
8. The README's opening line spells the brand with the deprecated casing and implies national-registry status the product does not have yet — the Mi Argentina integration that would make the credential official nationally is still a placeholder.
9. The sentence naming DIM as the internal codename spells the public brand exactly as the project's own lint rule bans; the canonical brand is "miMAR", not "MiMAR".
10. Two of the three URLs in the public welfare-report row do not exist as written, and the reference code by itself only confirms a report exists — seeing its status needs the reporter's own session or an emailed access link.
11. The claim that personal accounts "have Mi Argentina identity" names a capability nothing can use yet — the OIDC callback is unimplemented, so no account holds a Mi Argentina identity today.
12. The stack table lists Row Level Security as part of how the database is secured, but the application's own database connection bypasses RLS by design; RLS is real second-layer enforcement, not the primary mechanism for most app traffic.
13. The claim that `infrastructure/` is "the only place that issues Drizzle queries" does not hold — over 150 application-layer files import the database driver directly, with no fence preventing it.
14. The claim that authorization is enforced "not in the DB" is too absolute — for at least one file-upload path (revocation evidence), the database's Row Level Security is the only thing enforcing access, since no server action mediates that write.
15. "Refuses to run against a non-local database" is not an absolute — a single command-line flag disables the guard and lets the same schema-and-seed script target a remote database, with only a warning printed and no confirmation required.
16. The claim that the integration test directory "runs against local Postgres" overstates it — the suite is deliberately split, and most of those test files run in a parallel, database-free project; only a subset genuinely needs the local database.
17. The README's project-layout diagram omits the mobile app workspace entirely (a jest-tested Expo client that is itself gated inside the commit checklist) and the shared code package used by both the website and the phone app.
18. Not all of the legacy server-actions directory has been reduced to thin pass-through wrappers — most files have, but a handful, including one 505-line file, still hold real business logic and query the database directly.
19. The pitch lists government-level moderation of anonymous reports, scoped to each official's own jurisdiction, as an unbuilt future item; it has already shipped and is live today.
20. The README states an outdated minimum software version requirement; the actual required minimum is higher, and versions below the real minimum break real features of the toolchain.

## Coverage

<details>
<summary>Files read (45)</summary>

- docs/design/handoffs/2026-07-07-govt-personas-pitch.md
- README.md
- package.json
- biome.json
- vitest.config.ts
- LICENSE
- middleware.ts
- db/schema.ts (windows: 300-450, 2060-2130, 2600-2620)
- packages/contract/src/events/event-types.ts (lines 20-200)
- lib/infra/miarg-oidc.ts
- lib/infra/auth-guards.ts (grep windows)
- lib/utils/dni-hash.ts (grep)
- lib/analytics/subregion-redaction.ts (lines 1-70)
- lib/analytics/senasa-export.ts (lines 1-30)
- lib/analytics/compliance-metrics.ts (grep)
- lib/metrics/kpi-catalog.ts (lines 1-50; grep windows 60-120, 370-380, 660-690, 1400-1410)
- lib/open-data/datasets.ts (grep windows)
- components/layout/nav-presets.ts (grep windows)
- components/layout/AppFooter.tsx (grep)
- scripts/check-brand-casing.ts (lines 1-60, 115-135)
- scripts/check-node-version.ts (lines 1-40)
- scripts/db-bootstrap.ts (grep windows)
- app/gob/page.tsx (grep windows)
- app/gob/moderacion/page.tsx
- app/gob/maltrato/page.tsx (head)
- app/gob/outreach/page.tsx
- app/gob/outreach/export/route.ts (lines 1-45)
- app/gob/analytics/page.tsx (head)
- app/gob/analytics/export/actions.ts (grep)
- app/gob/campanas/page.tsx (head)
- app/gob/cola/page.tsx (grep)
- app/gob/observaciones/page.tsx (grep)
- app/admin/alertas/page.tsx (grep)
- app/admin/outbox/page.tsx (grep)
- app/(app)/inicio/page.tsx (grep)
- app/(public)/p/[publicToken]/page.tsx (grep)
- app/(public)/refugios/[orgToken]/page.tsx (grep)
- app/(public)/casos/[publicCode]/page.tsx (grep)
- app/(public)/adoptar/[petToken]/postular/page.tsx (grep)
- app/(public)/transparencia/page.tsx (grep)
- app/(public)/acerca/page.tsx (grep)
- src/modules/welfare/application/generate-mpf-export.ts (grep)
- src/modules/cases/__tests__/cases-repository.test.ts (grep)
- db/migrations/0016_drop_role_match_check.sql (grep)
- apps/mobile/package.json (grep)

Refuters additionally opened dozens more files per-claim (nav-presets.ts full windows, individual page.tsx/actions.ts files, migration SQL, kpi-catalog.ts full sections, biome.json overrides, vitest.config.ts full, etc.) — see each claim's vote evidence blocks in the source JSON for the complete per-vote file list.

</details>

Areas not reached:
- README.md:114 "Studio is at http://localhost:54323" — not verified (would require opening `.env.local.example`, out of scope for a read-only doc audit).
- README.md:7 CI badge repo slug — cannot be verified read-only at this SHA.
- Pitch:28 "A8/A9" — the underlying 10-day rabies-observation machinery is verified TRUE, but the literal codes A8/A9 have no hits under `app/gob/observaciones/**` (they live in `lib/analytics/surveillance-metrics.ts` and `AGENTS.md` instead); deck owner should confirm intent.
- Pitch:38 "42/11" — the 42%/54% collision is documented and closed in `kpi-catalog.ts`; no record of a "42/11" collision was found anywhere in the repo besides the pitch itself.
- Pitch:14-20 (persona wishlist items) — deliberately not classified as assertions per the brief; item 4's "export PDF/CSV" is worth a note since `/gob/analytics/export` emits CSV or JSON only, not PDF.
- Legal citation correctness (Ley 22.953, Ord. CABA 41.831, Ley CABA 5470, Ley 14.346, Ley CABA 4078 / Prov 14.107, Ley 25.326, Ley 27.275) — confirmed as anchors in code, not independently verified as legally correct or current; needs a legal reviewer.
- Production-vs-staging drift — this lens audited the `main` tree at `d7dbf25f7`; per the brief, production trails staging by two migrations, so a live demo may not yet show everything verified here.
- Runtime rendering of every "Live" route — confirmed file existence and reachability (nav entries, guards), not that each page renders correctly at runtime.
- Cross-check note, not a claim in either doc: EVENT_TYPES in packages/contract/src/events/event-types.ts counts 55 entries at this SHA (sed -n '20,200p' | rg -c of the quoted-string lines → 55), matching the project standard.

