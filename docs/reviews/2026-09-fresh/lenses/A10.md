# A10 — Scoping: jurisdiction, org tenant, dashboards

> Snapshot: `d7dbf25f7` (`main`) · Audit: 2026-09-fresh · Lens A10
> Verified against code on 2026-09-02 by workflow agents (finder + refuters) · Status: draft

## Ground truth

- Branch: `main`, SHA `d7dbf25f7`, date 2026-09-02.
- Files read: 86 (full list in Coverage).
- Scope: This lens merges priors 14 (jurisdiction), 21 (authz scoping audit) and 24 (tenant isolation dashboards). Also read the SCOPE/LENS paragraphs of `docs/reviews/briefs/21-authz-scoping-audit.md` and `docs/reviews/briefs/24-tenant-isolation-dashboards.md`. Covers: the jurisdiction fence (`lib/infra/gov-scope.ts`, `scripts/check-scope-*.ts`, `__tests__/gob-*fence*`) — can a govt user of locality X read aggregates or rows of locality Y by changing a query param, path segment, or body field; org tenant isolation — is `org_id` bound from the session, never from input, in every `src/modules/organizations` and org-portal query; the `/gob` sub-views and their narrow-only scope; the Panorama API guard (`src/modules/panorama`, `lib/panorama`, and the `app/api` routes it uses); dashboards and KPIs that mix scopes; admin views that bypass scope by design and whether that is declared.
- What's new since the source briefs: Panorama API guard; new `/gob` sub-views (denuncias, turnos, decomiso); `scripts/check-scope-*.ts` fences; `__tests__/gob-pet-subview-jurisdiction-fence.test.ts`; org portal capabilities (12).
- Priors triaged: 54 items across the three source result files (`docs/reviews/results/14-jurisdiction.md`, `docs/reviews/results/21-authz-scoping-audit.md`, `docs/reviews/results/24-tenant-isolation-dashboards.md`) — 37 closed, 14 still-open, 3 not-reproducible.

## Verdict

**SOLID WITH FINDINGS** — no CONFIRMED CRITICAL or HIGH; 7 CONFIRMED MED and 2 CONFIRMED LOW (main pass + gap round combined).

- Confirmed by severity: CRITICAL 0 · HIGH 0 · MED 7 (4 main + 3 gap round) · LOW 2 (main pass only; the gap round's one LOW finding was refuted).
- Refuted: 2 (A10-5 main pass, A10-G4 gap round).
- Unverified (no refuter vote obtained): 0 findings.
- Healthy verified / claimed: 8 / 11 (4/6 main pass + 4/5 gap round).
- Prior triage: 37 closed / 14 still-open / 3 not-reproducible (of 54 total) — the gap round covers a distinct area (`src/modules/alerts`, `/gob/suscripciones`) and adds no new priors.

See "## Gap round (completeness critic)" below for the src/modules/alerts sweep and its own findings/healthy items.

## Healthy — verified solid

### unit-history's province-level allowance is not a widening hole — a second SQL fence backs it
- Property: The route lets a barrio-grain operator pass a PROVINCE-level unit request (`jurisdictions.some(j => j.province === province)`), but the repository AND-s the operator's own scope clause into every per-metric query, so the returned rows are still narrowed to their assignments.
- Path: `src/modules/panorama/infrastructure/repository-history.ts:168`
- Guard: `src/modules/panorama/infrastructure/repository-history.ts` (second fence at L168) + `app/api/panorama/__tests__/scope-drilldown.test.ts`. Refuter verified the guard mechanism directly in code (36 `conditions.push(sql`(${scope})`)` sites, symmetric across every metric branch) but flagged that `guardVerified: false` — no existing test exercises a barrio operator making a province-level request and asserting barrio-only rows come back; the property holds by code inspection, not by a targeted test.
- What would break it: Deleting any single `if (scope) conditions.push(...)` line, or the L168-171 second fence, would turn the admitted province-level request into a province-wide read.
- Evidence:
  ```
  app/api/panorama/unit-history/route.ts:93-95
    const inScope = locality
      ? jurisdictionScopeContains(jurisdictions, province, locality)
      : jurisdictions.some((j) => j.province === province);

  src/modules/panorama/infrastructure/repository-history.ts
    :273 const scope = petsScope(actor, jurisdictions);      :281 if (scope) conditions.push(sql`(${scope})`);
    :291 const scope = petsScope(actor, jurisdictions);      :299 if (scope) conditions.push(sql`(${scope})`);
    :322 if (scope) conditions.push(sql`(${scope})`);
    :342 if (scope) conditions.push(sql`(${scope})`);
  rg -c over repository-history.ts: "const conditions"=36, "const scope = "=36, "conditions.push(sql`(${scope})`)"=36
  lib/metrics/scope.ts:109 `if (jurisdictions.length === 0) return sql`false``
  ```

### The gob pet sub-view fences the open-cases LIST, not just the gate that reaches the pet
- Property: A govt operator who legitimately reaches a pet through an in-scope welfare nexus still receives only the open cases inside their own jurisdiction; sibling-barrio and out-of-province cases are excluded from the payload, while admin stays universal.
- Path: `lib/infra/gob-pet-subview.ts:171`
- Guard: `__tests__/gob-pet-subview-jurisdiction-fence.test.ts` — real Postgres, real SQL, no mocks. Refuter verified the guard: ran the reproduction against three real cases and confirmed the four named assertions (in-scope only, Salta excluded, sibling CABA/Almagro excluded, admin gets all three).
- What would break it: Passing `undefined` unconditionally as `loadPetSubViewTail`'s `openCasesScope` (dropping the `isGovt ?` ternary on L171, or its mirror on L310 for the operator route) restores the leak of case existence, `publicCode`, kind and open-date across the fence.
- Evidence:
  ```
  lib/infra/gob-pet-subview.ts:166-172
    166:  const hasLink = reportRows.some(reportGrants) || caseRows.some(caseGrants);
    167:  if (!pet || !hasLink) return { ok: false };
    171:  return { ok: true, pet: await loadPetSubViewTail(pet, isGovt ? jurisdictions : undefined) };
    :310 same ternary for the operator route
    :248 findOpenCasesForPetWithCodes(pet.id, openCasesScope)
  lib/infra/case-queries.ts:444+ scopeClause = jurisdictionPairClause([...govtScope], cases.jurisdictionProvince, cases.jurisdictionLocality) ?? sql`false` — fail-closed
  __tests__/gob-pet-subview-jurisdiction-fence.test.ts
    :10 "THE PROOF (real Postgres, real SQL — no mocks)"
    :134 govt (Palermo): openCases carries ONLY the in-scope CABA/Palermo case
    :143 govt (Palermo): the out-of-PROVINCE (Salta) case is EXCLUDED
    :151 govt (Palermo): the SIBLING-barrio (CABA/Almagro) case is EXCLUDED
    :159 admin (universal): openCases carries ALL THREE cases
  ```

### The org-token confused deputy is closed in code AND fenced in the Definition of Done
- Property: Every `/org/{orgToken}` server action the prior audits flagged now pins its capability check to the URL-resolved org via `requireCapabilityForOrgToken`, and a lint wired into `pnpm verify` fails the build if an action with an org-token param reverts to bare `requireCapability`.
- Path: `src/modules/foster/actions.ts:87`
- Guard: `scripts/check-confused-deputy.ts` (`lint:authz-orgtoken`, in the verify chain). Refuter verified the guard: it hard-fails, is in the verify chain (`package.json:30`, `:79`), scans every `"use server"` module under `app/**` and `src/**` (not just legacy filename globs), and its allowlist is empty.
- What would break it: Swapping any flagged call back to single-argument `requireCapability("cap")` would authorize a multi-org member against their most-recently-joined membership instead of the org in the URL. Noted residual soft spot: an action is exempted wholesale if it calls `requireCapabilityForOrgToken` anywhere in its body, even if it also calls bare `requireCapability` on another path — not enough to refute the property.
- Evidence:
  ```
  intake.ts:37: requireCapabilityForOrgToken("intake.create", orgToken)
  chip-match.ts:67-68: const orgAccess = await requireOrgAccessByToken(orgToken); requireCapability("intake.create", orgAccess.organization.id)  (two-arg pinned form, safe)
  adoption/actions.ts:97,150,200,360,393,435,551,626,727: requireCapabilityForOrgToken(...)
  foster/actions.ts:87,132,173,480: requireCapabilityForOrgToken(...)  |  foster/actions.ts:219-223: proposal loaded by token, then requireCapability("foster.assign", proposal.organizationId) (pinned form)
  transfers/actions.ts:491,547,596,649,716: requireCapabilityForOrgToken(...)
  package.json:30 "lint:authz-orgtoken": "tsx scripts/check-confused-deputy.ts"; :79 in the verify chain
  scripts/check-confused-deputy.ts:172-180 isConfusedDeputyOffender(...); :243 process.exit(1); :130 CONFUSED_DEPUTY_ALLOWLIST = {} (empty since 2026-08-22)
  ```

### Province canonicality is enforced by the database, not by discipline, on 11 jurisdiction tables
- Property: `pets`, `organizations`, `organization_coverage`, `welfare_reports`, `govt_assignments`, `approval_requests`, `govt_business_rules`, `service_offerings`, `foster_volunteers`, `custody_disputes` and `cases` each carry a CHECK restricting `jurisdiction_province` to NULL or one of the 24 canonical display names, so no application-layer canonicalization miss can persist a province the scope clauses would fail to match.
- Path: `db/migrations/0055_jurisdiction_province_canonical.sql:191`
- Guard: `db/migrations/0055_jurisdiction_province_canonical.sql` (DB CHECK) + `__tests__/govt-assignments-locality-integrity.test.ts` for the locality half of `govt_assignments`. Refuter flagged `guardVerified: false` — the enforcement is the DB CHECK itself, and no test asserting a non-canonical province is rejected was found; nothing fences a future 15th jurisdiction-bearing table into the guarantee.
- What would break it: This is what holds A10-5 and A10-6 at LOW instead of MED — both write a raw province and both are stopped here. It does NOT cover `jurisdiction_locality` (a CHECK cannot reference `ar_localities`), which is the hole A10-3 and A10-4 sit in. Adding a 12th jurisdiction-bearing table without appending it to this array silently leaves that table outside the guarantee, and migrations are immutable.
- Evidence:
  ```
  0055_jurisdiction_province_canonical.sql:191-216 — tables array of 11, enum_list of 24 canonical names,
  FOREACH DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT %I_jurisdiction_province_canonical
  db/schema.ts: 14 `jurisdictionProvince: text("jurisdiction_province")` declarations total; the 3 beyond
  0055's 11 are profiles (own CHECK in 0097_profiles_location.sql:25-31), alert_subscriptions (own CHECK in
  0108_alert_subscriptions.sql:80-87, same 24 names) and alert_firings (own CHECK in 0111_alert_firings.sql:94)
  rg -l over db/migrations for the constraint name: 0055, 0097, 0117 only
  ```

### Claimed healthy, not verified

- **A crafted `?province`/`?locality` on the Panorama API can never widen a govt's scope** (`src/modules/panorama/application/resolve-request-scope.ts:65`) — refuter reason: the security consequence holds, but the structural claim "every panorama data route resolves its effective scope through ONE shared resolver" is false at this SHA. Only 2 of 5 panorama data routes (`kpis`, `[layer]`) call `resolvePanoramaRequestScope`; `unit-history`, `rule-changes` and `scope` re-parse `?province`/`?locality` and re-implement scope resolution inline, and `unit-history`'s gate uses a materially different rule (`jurisdictions.some(j => j.province === province)`, which `narrowGovtScope` would have narrowed). The resolver's own header admits it covers only "the two data API routes."
- **Whole-province subsumption is one predicate, expressed three ways that agree by construction** (`lib/domain/jurisdiction-canonical.ts:148`) — refuter reason: the three named functions (`jurisdictionScopeContains`, `jurisdictionPairClause`, `narrowGovtScope`) do share one definition and could not be widened through any of them, but a FOURTH whole-province predicate in the same file, `isWholeProvinceAssignment` (`:172`), short-circuits on `j.locality === ""` BEFORE the canonicality gate — for a non-canonical province it answers "whole-province" while the other three answer "exact-match." It is a live consumer (`app/api/panorama/rule-changes/route.ts:94`), and what keeps the four predicates agreeing today is the `govt_assignments` DB CHECK — a constraint on another table, not construction. No live widening is reachable today, but this is the drift the claim itself names as "the real risk," already present rather than merely feared.

## Findings (CONFIRMED)

### MED

**A10-1 — Org tránsitos historial lists foster rows the org was never party to**
- Kind: security · `app/org/[orgToken]/transitos/page.tsx:83`
- Exploit: Org A holds pet P and transfers custody to Org B via the org.transfer.propose/accept handshake. Org B assigns volunteer V as foster; the foster ends. A member of Org A opens `/org/A/transitos?tab=historial`. Line 60 deliberately drops the `endedAt` filter for this tab, so `petIds` still contains P; lines 83-93 then fetch every ended `role='foster'` ownership for those `petIds` with no organization binding, and the row renders `{foster.displayName}` (L238). Org A reads the name of a private volunteer in an arrangement it never made, on a custody episode it did not hold. The prior audit's 24-15 fixed the `activos` half only; this is the other query.
- Evidence:
  ```
  sed -n '55,62p;83,95p' "app/org/[orgToken]/transitos/page.tsx"
  60:        ...(activeTab === "activos" ? [isNull(ownerships.endedAt)] : []),
  83:        : await db
  84:            .select({ ownership: ownerships, foster: profiles })
  85:            .from(ownerships)
  86:            .innerJoin(profiles, eq(profiles.id, ownerships.ownerUserId))
  87:            .where(
  88:              and(
  89:                inArray(ownerships.petId, petIds),
  90:                eq(ownerships.role, "foster"),
  91:                isNotNull(ownerships.endedAt),
  92:              ),
  93:            )
  — no organization predicate anywhere in the historial branch.
  Corroborating: src/modules/transfers/infrastructure/transfers-repository.ts:710-717 (update ownerships set endedAt=now
  where ownerOrganizationId=orgId), :748-751 (insert new ownership for receiver org) — confirms the transfer path leaves
  the ended row (and the pet) permanently in org A's historial petIds.
  ```
- Refuter notes: 3/3 refuted=false, high confidence. reproduce/correctness/impact lenses all traced the same mechanism (missing `endedAt` filter on historial + no org predicate on the foster query + confirmed via `accept-cross-org-transfer.ts`), all voted MED (leaked field is a display name to an org that once held the pet, not a deeper compromise).
- Fix: Bind the historial foster rows to the viewing org the way the `activos` tab is bound: innerJoin `fosterProposals` on `resolvedOwnershipId` with `eq(fosterProposals.organizationId, organization.id)`, or restrict to foster ownerships whose `startedAt` falls inside one of THIS org's ownership windows for that pet.
- fixClass: code-sdd

**A10-2 — Govt proposal writes a client-supplied jurisdiction with no assignment check**
- Kind: security · `src/modules/organizations/application/admin-proposals/propose-vet-upgrade.ts:100` · priorId `21-3` (still-open)
- Exploit: A govt operator whose only `govt_assignments` row is Salta/Salta invokes `proposeVetUpgradeForUser` with `operationalProvince="CABA"`, `operationalLocality="Palermo"` and any `targetUserId`. `loadActorAuthority` (`helpers.ts:50`) hardcodes `jurisdictions: []` and no caller compares the input pair against the actor's assignments, so the `approval_requests` row is written with the attacker-chosen jurisdiction and lands in CABA's queue (`visibleRequestsClause` matches on the row's own columns). Rated MED not HIGH because escalation stops at `approve-request`'s `assertTwoPersonRule` plus `canDecideRequest` — the impact is cross-jurisdiction queue injection with an arbitrary target, not role minting.
- Evidence:
  ```
  admin-proposals/helpers.ts:48-54
    return { profile: { id: profile.id, role: profile.role }, jurisdictions: [],
    // Govt's assignments aren't strictly needed for the propose path
  propose-vet-upgrade.ts:100-101
    jurisdictionProvince: input.operationalProvince.trim(),
    jurisdictionLocality: input.operationalLocality.trim(),
  app/actions/admin-proposals.ts:41-45
    export async function proposeVetUpgradeAction(input) {
      const { user } = await requireAdminOrGovtOrRedirect();
      const result = await _proposeVetUpgradeForUser(user.id, input);
  lib/infra/approval-scope.ts:74-93 visibleRequestsClause — role==="admin" → true; else matches
    eq(approvalRequests.jurisdictionProvince, j.province), eq(approvalRequests.jurisdictionLocality, j.locality)
  admin-decisions/helpers.ts:31-40 — the DECISION side DOES load active govt_assignments (the propose side does not)
  ```
- Refuter notes: 3/3 refuted=false, high confidence, all voted MED. All three traced the same chain: `helpers.ts:50` → no comparison in `propose-vet-upgrade.ts` → boundary is role-only (`app/actions/admin-proposals.ts:44` `requireAdminOrGovtOrRedirect`) → queue routing keyed on the row's own columns (`approval-scope.ts`). Noted the asymmetry is deliberate elsewhere: `propose-org-verification.ts` derives the pair server-side from the `organizations` row (contrast with A10-7).
- Fix: Load the actor's active `govt_assignments` in `admin-proposals/helpers.ts` (mirror `admin-decisions/helpers.ts:33-39`) and reject a govt proposal unless `jurisdictionScopeContains(assignments, operationalProvince, operationalLocality)`; keep admin universal. Route the pair through `resolveCanonicalJurisdiction` before the insert.
- fixClass: code-sdd

**A10-3 — Business-rule locality is trim-only, so a non-catalog spelling makes the rule inert**
- Kind: bug · `app/actions/business-rules.ts:56` · priorId `14-10` (still-open)
- Exploit: An admin creates a locality rule from `/gob/reglas` with `jurisdictionLocality="palermo"` (or a mistyped spelling). The province half is protected — migration 0055 adds `govt_business_rules_jurisdiction_province_canonical` — but the locality column has no CHECK and no catalog lookup, so the row persists. `resolveBusinessRule` matches locality by string equality against `pets.jurisdiction_locality` (canonical, e.g. "Palermo"), so the rule governs zero pets while `/gob/reglas` renders it as configured for that locality: a compliance obligation displayed as active and enforced against nobody. Both `createBusinessRuleAction` (L114) and the update action (L169) go through this one helper.
- Evidence:
  ```
  business-rules.ts:61-67
    61:  const country = (formData.get("jurisdictionCountry") as string|null)?.trim() || "AR";
    62:  const provinceRaw = (formData.get("jurisdictionProvince") as string|null)?.trim();
    63:  const localityRaw = (formData.get("jurisdictionLocality") as string|null)?.trim();
    66:    province: provinceRaw && provinceRaw !== "" ? provinceRaw : null,
    67:    locality: localityRaw && localityRaw !== "" ? localityRaw : null,
  create-business-rule.ts:91-96 — .insert(govtBusinessRules).values({ jurisdictionLocality: params.jurisdictionLocality, ... }) — no catalog resolution
  db/migrations/0055_jurisdiction_province_canonical.sql:191-213 — tables[] includes govt_business_rules; enum_list constrains jurisdiction_province ONLY
  lib/infra/business-rules-resolver.ts:117-118 — eq(govtBusinessRules.jurisdictionLocality, c.locality) — exact string equality
  app/gob/reglas/[country]/[province]/[locality]/nueva/page.tsx:31-35 — legacy route: only decodeURIComponent's the locality, no notFound()/catalog check
  ```
- Refuter notes: 3/3 refuted=false, all voted MED (one at medium confidence, flagging the sanctioned wizard entry as picker-bound so a bad row needs the legacy URL route or a stale/accented spelling — impact real but reach narrower than the raw finding text implies).
- Fix: Run (province, locality) through `resolveCanonicalJurisdiction` inside `normalizeJurisdiction` (or require the `ar_localities` INDEC id from the picker) and reject an unresolvable pair before create/update, exactly as `createPetAction` does.
- fixClass: code-sdd

**A10-4 — Locality-integrity fitness sweep covers govt_assignments but never pets**
- Kind: fence-gap · `__tests__/govt-assignments-locality-integrity.test.ts:56` · priorId `14-18` (still-open)
- Exploit: Production mutation — `UPDATE` one live `pets` row's `jurisdiction_locality` to a stale or off-catalog spelling (a pre-INDEC-rename name, or a trailing space). Every fence in `pnpm verify` still passes: `lint:locality` audits the `ar_localities` catalog, not `pets`; `lint:scope`/`lint:scope-authz` audit predicate discipline and RLS. The pet then matches no locality-scoped operator's fence (`jurisdictionPairClause` compares locality with `=`), so it silently vanishes from the census, the compliance rollups and the padrón of the municipality that governs it. The file's own header names this failure mode ("silently matches ZERO pets") but only reads `govt_assignments`, and 0117's repair pass was likewise assignments-only.
- Evidence:
  ```
  rg -n "describe\(|it\(" __tests__/govt-assignments-locality-integrity.test.ts
  55:describe("govt_assignments locality integrity (issue #758)", () => {
  56:  it("every ACTIVE assignment's jurisdiction_locality resolves against ar_localities (or is the whole-province sentinel)", async () => {
  (no from(pets) match anywhere in the file; grep -rln jurisdiction_locality __tests__ scripts surfaces no pets fitness sweep)
  scripts/check-locality-integrity.ts — all queries FROM ar_localities; no pets reference
  db/migrations/0147_locality_id_fk.sql — only ALTER TABLE pets ADD column + index; no trigger/CHECK tying free-text to catalog
  ```
- Refuter notes: 2/3 refuted=false (high, medium confidence) — confirmed the gap, kept MED because it's a missing defensive sweep rather than a fence reporting green over a claimed property. 1/3 refuted=true, medium confidence — the reproduce/correctness votes hold, but the impact vote points out no vitest fitness test runs against production anyway (the assignments test only covers the local DB project too), and every application write path for `pets.jurisdiction_locality` already canonicalizes (`src/modules/pets/actions.ts:138,448,576`; `intake/create-intake.ts:247`; `movement/record-jurisdiction-move.ts:127`; `movement/record-movement.ts:53` — all via `normalizeLocationForWrite`), arguing this is LOW test-coverage hygiene, not MED. Kept at MED per majority (2 of 3), consistent with the finder's rating.
- Fix: Add a sibling db-project test asserting every active `pets` row's non-null (`jurisdiction_province`, `jurisdiction_locality`) resolves against `ar_localities`, reusing 0117's `canonicalize_locality` shape; pair it with a forward repair migration mirroring 0117 for `pets`.
- fixClass: code-sdd

### LOW

**A10-6 — foster_volunteers UPDATE writes the province uncanonicalized into a CHECK-constrained column**
- Kind: convention · `src/modules/foster/infrastructure/foster-repository.ts:443` · priorId `14-12` (still-open)
- Exploit: A foster volunteer who registered successfully edits their profile with province "Cordoba" (unaccented). The INSERT branch would have stored `canonicalProvince`; the UPDATE branch stores `input.jurisdictionProvince` verbatim and 0055's `foster_volunteers_jurisdiction_province_canonical` CHECK rejects it — the same value that succeeded at creation now 500s on save. `jurisdictionLocality` is uncanonicalized on BOTH branches, so a volunteer can persist an off-catalog locality that no locality-grain lookup will match. The comment preserves the divergence as a deliberate "parity quirk" rather than reconciling it.
- Evidence:
  ```
  foster-repository.ts:426-449
  426:      // INSERT branch — apply canonical province normalization.
  431:          jurisdictionProvince: canonicalProvince,
  432:          jurisdictionLocality: input.jurisdictionLocality ?? null,
  442:    // UPDATE branch — PARITY QUIRK: province is stored WITHOUT canonical
  443:    // normalization in the update path. Preserve as-is.
  447:        jurisdictionProvince: input.jurisdictionProvince ?? null,
  448:        jurisdictionLocality: input.jurisdictionLocality ?? null,
  upsert-foster-volunteer.ts:79-90,110 — canonicalProvince computed via normalizeLocationForWrite({locality:"none"}), passed as a separate arg; raw input.jurisdictionProvince left intact for the update path
  foster/actions.ts:372-380 — upsertFosterVolunteerAction wraps with requireLiveUser only, no zod schema on the jurisdiction strings
  ```
- Refuter notes: 1 vote (correctness lens), refuted=false, high confidence, MED... voted LOW (matching finder). Traced INSERT vs UPDATE divergence verbatim and confirmed no schema validation catches it upstream; noted it's latent (normal wizard flow supplies province from a picker) rather than live.
- Fix: Use `canonicalProvince` in the UPDATE branch too, and run the (province, locality) pair through `normalizeLocationForWrite({ locality: "strict" })` on both branches so INSERT and UPDATE agree.
- fixClass: code-sdd

**A10-7 — Out-of-jurisdiction govt can open a verification proposal in another jurisdiction's queue**
- Kind: convention · `src/modules/organizations/application/admin-proposals/propose-org-verification.ts:26` · priorId `21-4` (still-open)
- Exploit: A Salta-assigned govt operator calls `proposeOrgVerificationForOrg` with any `organizationId`, including a CABA shelter. Nothing compares the org's jurisdiction against the actor's assignments; the pending row is created and appears in CABA's queue. Downgraded from the prior audit's HIGH to LOW because — unlike A10-2 — the jurisdiction here is derived SERVER-SIDE from the `organizations` row (L86-87), so the record is truthful and only a genuinely in-scope authority (`canDecideRequest`) can decide it. Residual impact is queue noise created by a stranger, plus an L28 comment claiming a scope the code does not enforce.
- Evidence:
  ```
  propose-org-verification.ts:26-29
  26:  const auth = await loadActorAuthority(actorUserId);
  28:  // Both admin and govt (in scope of org's locality) can propose. The
  29:  // canDecide guard kicks in at approval time; here we just allow either.
  31-46 select org by id; not found / already verified / no jurisdiction checks — no assignment comparison
  86-87: const province = org.jurisdictionProvince; const locality = org.jurisdictionLocality;
  admin-proposals/helpers.ts:50 — jurisdictions: []
  app/actions/admin-proposals.ts:62 — const { user } = await requireAdminOrGovtOrRedirect(); (role-only)
  ```
- Refuter notes: 1 vote (correctness lens), refuted=false, high confidence, agreed with the LOW downgrade and its reasoning (truthful jurisdiction, decision-side gate still holds, per-org pending idempotency caps noise).
- Fix: Reject a govt proposer unless `jurisdictionScopeContains(assignments, org.jurisdictionProvince, org.jurisdictionLocality)` — which requires fixing `admin-proposals/helpers.ts:50` first (see A10-2) — and correct the L28 comment either way.
- fixClass: code-sdd

## Refuted

**A10-5 — strict locality mode passes raw text through when the province fails to resolve** (`lib/domain/location-normalize.ts:142`, priorId `14-13`)
- Finder's claim: `normalizeLocationForWrite`'s strict branch silently passes through raw locality text with `province: null` when the province string fails to canonicalize (e.g. `"caba"` lowercase), instead of throwing per its documented `@throws` contract; `assign-govt-locality.ts` then falls back to raw strings, relying only on the 0055 DB CHECK to stop the write, which surfaces as a raw Postgres error instead of a clean validation message.
- Refuter reason: The code lines reproduce (`location-normalize.ts:124-151` does return a passthrough when province or locality is absent, and `assign-govt-locality.ts:84-85` falls back to the raw strings), but the trigger is false. `canonicalProvinceNameForStorage` does NOT return null for `province="caba"` — `lib/reference/ar-provincias.ts:98-112` resolves via `normalize()` (trim, casefold, NFD strip diacritics, drop dots) against `ALIAS_TO_CODE` (all-lowercase keys), with the docblock explicitly listing `provinceByName("cordoba")` → `AR-X` and `provinceByName("C.A.B.A.")` → `AR-C`. So `"caba"` resolves and the strict branch runs normally — the finder's scenario never reaches L142. The residual class (a genuinely unresolvable province) is stopped by the same 0055 CHECK the finding itself cites, and the locality-empty half is already unreachable because `assign-govt-locality.ts:62-64` rejects a blank locality before calling the gate.

## Gap round (completeness critic)

Scope: `src/modules/alerts` (7 non-test files, 691 lines) had zero reads in the main pass's coverage matrix, and the main pass's `areasNotReached` separately listed `/gob/suscripciones` among 13 unopened gob sub-views and `lib/metrics/context.ts` / `lib/analytics/dashboards/_scope.ts` among unaudited scope layers. This round audits the alert subscriptions/firings pipeline end to end: who can create/read/triage a subscription or firing, whether a govt actor's scope is derived from the caller or from the subscription row, whether an erased/deactivated admin's rows and standing rules keep being processed, and whether the raw per-locality `observedValue` these rows persist ever reaches a govt reader (the k-anon-bypass shape A06 and the main A10 pass both swept past).

### Findings (gap round)

**A10-G1 — MED — `alert_subscriptions` RLS admin branch fails open for an art.16-erased admin**
- Kind: security · `db/migrations/0137_rls_initplan_auth_subselect.sql:45`
- The admin read-policy EXISTS branch tests `p.role = 'admin' AND p.deactivated_at IS NULL` — no `deleted_at` check — while the erase RPC (`db/migrations/0208_...sql:459-479`) sets `profiles.deleted_at` and leaves `role`/`deactivated_at` untouched, and `erase-subject-data.ts:677` deletes the `auth.users` row without invalidating already-issued access tokens. For the remaining token lifetime (`jwt_expiry=3600`, `supabase/config.toml:160`), a direct PostgREST `GET /rest/v1/alert_subscriptions` as that erased admin still satisfies the admin branch and returns every admin's subscription rows. The app boundary already refuses this actor (`app/actions/alert-subscriptions.ts:38-43` documents the exact scenario), so this is the RLS backstop failing open — the inversion of "RLS is the backstop, not the gate." `db/migrations/0188_revocations_upload_admin_govt_only.sql:60` is the one policy in the repo that gets this right (`AND p.deleted_at IS NULL`), proving the intended form.
  ```
  rg -n -C 3 "alert_subscriptions read by owner or admin" db/migrations/0137_rls_initplan_auth_subselect.sql
  0137:42-45: ALTER POLICY ... USING (((actor_user_id = (select auth.uid())) OR (EXISTS (SELECT 1 FROM profiles p
    WHERE ((p.id = (select auth.uid())) AND (p.role = 'admin'::user_role) AND (p.deactivated_at IS NULL))))));
  rg -n "deleted_at               = now" -B 2 db/migrations/0208_subject_rights_watermarks_tag_interest_org_invitations.sql
  0208:476-479: jurisdiction_locality = NULL, deleted_at = now(), updated_at = now() WHERE id = p_user_id;  (role/deactivated_at not in the SET list)
  ```
- 3/3 refuters confirmed, all voted MED (bounded by one token TTL; erasure is self-only per `erase-subject-data.ts:585-596`; no new subject PII, exposed columns are labels/thresholds keyed to an opaque actor FK).
- Fix: new forward migration adding `AND p.deleted_at IS NULL` to the admin EXISTS branch, matching `requireAdminOrRedirect`; add an erased-admin principal to `__tests__/rls`.
- fixClass: code-sdd

**A10-G2 — MED — `evaluateAlertSubscriptions` derives a govt actor's scope from the subscription row, not the caller — and the in-file comment claims the opposite**
- Kind: security · `lib/metrics/alert-evaluation.ts:124`
- The evaluator builds its jurisdictions array from `sub.jurisdictionProvince`/`sub.jurisdictionLocality` and hands it to `buildProjectionContext`, which (`lib/metrics/context.ts:83`) returns `{ kind: "jurisdictions", jurisdictions }` verbatim for any non-admin actor — no cross-check against `govt_assignments`. The trailing clause of the in-file comment at `:137-139` ("so a govt baseActor keeps its own scoping") is false about this call site: it keeps the SUBSCRIPTION's scoping. **Not reachable today** — govt cannot own a subscription; all three writers are `requireAdminOrRedirect`-gated, and no admin→govt demotion path exists. It becomes live the moment the pending widening `/gob/suscripciones` itself flags (`page.tsx:109-126`, "an authz boundary is a PO decision") ships — `CreateAlertSubscriptionSchema` validates province against the 24-name catalog and nothing against assignments. No test covers a govt baseActor (`rg 'role: "govt"' lib/metrics/alert-evaluation.test.ts` → 0 hits).
  ```
  rg -n -A 3 "const jurisdictions: DashboardJurisdiction\[\] = sub.jurisdictionProvince" lib/metrics/alert-evaluation.ts
  124-126: const jurisdictions = sub.jurisdictionProvince ? [{province: sub.jurisdictionProvince, locality: sub.jurisdictionLocality ?? ""}] : [];
  137-139: "govt actors ignore these fields once scope.kind === 'jurisdictions' ..., so a govt baseActor keeps its own scoping."
  rg -n "actor.role === .admin. \?" lib/metrics/context.ts → 83: return actor.role === "admin" ? {kind:"global"} : {kind:"jurisdictions", jurisdictions};
  app/gob/suscripciones/page.tsx:134 const actor = { role: profile.role } as const;
  ```
- 3/3 refuters confirmed the mechanism, all voted MED (unreachable today; misleading comment sits exactly where a future widening would be coded).
- Fix: for a non-admin baseActor, intersect the subscription's pair with the caller's own jurisdictions before evaluation; correct the `:137-139` comment; add a govt-actor test case.
- fixClass: code-sdd

**A10-G3 — MED — The fleet alert sweep selects subscription owners with no liveness join — an erased/deactivated admin's rules keep firing**
- Kind: bug · `src/modules/alerts/application/firings/record-firings.ts:98`
- The owner sweep (`selectDistinct actorUserId ... where isActive = true`) has no join to `profiles` and no liveness predicate, and `recordFiringsForUser` evaluates each under a hardcoded `{ role: "admin" }` universal scope (`:24`). `alert_subscriptions.actor_user_id` is `ON DELETE CASCADE` (`0108:54`), but erase only soft-deletes the profile and `auth.users` carries no FK to `profiles` (`0166:18-19`) so `deleteUser` cannot cascade the row away either — subscriptions survive both erasure and deactivation. The repo's own fence already registers this as debt: `scripts/check-subject-rights-coverage.ts:197-199` lists `alert_subscriptions` under `KNOWN_GAP`. `deactivate-admin.ts:92-93` sets only `deactivatedAt` on `profiles`, nothing on the subscription rows.
  ```
  rg -n -A 12 "const owners = await db" src/modules/alerts/application/firings/record-firings.ts
  98-108: .selectDistinct({actorUserId}).from(alertSubscriptions).where(afterUserId ? and(eq(isActive,true), gt(actorUserId,afterUserId)) : eq(isActive,true))  -- no profiles join
  rg -n "alert_subscriptions:" scripts/check-subject-rights-coverage.ts → 199: KNOWN_GAP entry, "The subject's own alert label and thresholds (actor_user_id)."
  ```
- 3/3 refuters confirmed, all voted MED (continued automated processing plus operator triage noise on a deleted subject's standing rule; no new subject PII disclosed).
- Fix: join `profiles` in the owner sweep and require `role='admin' AND deactivated_at IS NULL AND deleted_at IS NULL` (mirrors `requireAdminOrRedirect`), or have erase/deactivate set `is_active=false` on the subject's subscriptions; update the `KNOWN_GAP` entry to say which.
- fixClass: code-sdd

**A10-G4 — REFUTED — `jurisdictionLocality` accepted with no province/locality integrity check**
- Finder's claim (LOW): `src/modules/alerts/application/subscriptions/types.ts:39` accepts `jurisdictionLocality` as a bare `z.string().min(1)` with no cross-check against the province, and the DDL has a province CHECK (`0108:80`) but no locality CHECK — so a typo'd pair (e.g. province "Córdoba", locality "Rosario") would create a rule that can never fire while rendering as configured.
- Refuter reason (refuted): the schema gap is real, but `components/admin/AlertSubscriptionForm.tsx` — the ONLY component calling `createAlertSubscriptionAction` — renders no locality input at all (only `metricKey`, `direction`, `threshold`, a `jurisdictionProvince` text field, and `label`). `app/actions/alert-subscriptions.ts:59` therefore always resolves `formData.get("jurisdictionLocality")` to `null`. There is nowhere in the app to paste a mismatched locality; producing the described pair requires a hand-forged POST, not the "plausible paste/typo" the exploit narrative assumes. At most a latent defensive gap should a locality field ever be added to the form.

### Healthy — verified (gap round)

- **The raw per-locality `observedValue` never reaches a govt reader.** `alert_firings.observed_value` is readable only by a live admin, through the app's BYPASSRLS connection — no PostgREST path, no `/gob` reader. `db/migrations/0111_alert_firings.sql:118-122` enables RLS with zero policies and an explicit "every row is invisible" comment; `__tests__/rls/coverage.test.ts:274-286` asserts every `RLS_DENY_ALL` table (including `alert_firings`, `check-rls-coverage.ts:79-80`) carries zero policies. The lens's own attack hypothesis — that `KPI_CATALOG`'s `queue_alerts_open` tile surfaces firings to a `/gob` operator — was tested and fails: that tile's fetcher (`countOpenAlertFirings`, `kpi-catalog-queues.ts:269`) chains only to the admin rail badge and `fetchQueueCockpit`, whose sole caller is `app/admin/page.tsx`. **VERIFIED**, guard confirmed via `coverage.test.ts:274`.
- **The alert escalation notification to govt operators carries the metric and jurisdiction, never the observed value.** `triage.ts:201-212` builds the notification body from `metricLabel` and a jurisdiction label only; `rg -c observedValue` over `triage.ts` returns zero matches. `guardedBy: none` — no test asserts the body omits `observedValue`; the property holds only because the code never reads the column, and `__tests__/alert-firings-triage.test.ts:417-425` only asserts `notif.length >= 1`, never inspecting the body. **VERIFIED** (property holds), but the guard itself is absent — appending the count to the body today would keep every existing test green.
- **All six firing-triage writes go through the full liveness chain (`requireAdminUser` = `requireLiveUser` + role + accountType), not a role-only lookup.** `app/actions/alert-firings.ts:84-93` defines the guard; all six triage wrappers (`:101,113,125,137,146,155`) call it first. **VERIFIED**, guard confirmed via `__tests__/alert-firings-admin-guard.test.ts:101-150` (expired-shift, maintenance, erased, deactivated cases all assert zero writes). Caveat: `evaluateAndRecordFiringsForAllAdmins` (`:47`) is also exported from the same `"use server"` file with an explicit `@no-auth-required` annotation and only the cron route's `CRON_SECRET` gate behind it — worth a separate look, not a refutation of this property.
- **The alert cron is secret-gated, and `recordFiringsForUser` (arbitrary-`userId` writer) is deliberately not re-exported as an addressable server action.** `app/api/cron/evaluate-alerts/route.ts:24-27` calls `authorizeCronRequest(req)` before any DB work; `app/actions/alert-firings.ts:39-41` documents the deliberate omission. **VERIFIED**, guard confirmed via `__tests__/authz-bare-writer-exports.test.ts:93,134-144`, which fails on the one-line mutation of re-exporting it.

### Healthy — claimed, not verified (gap round)

- **Panorama suppressed cells are nulled rather than zeroed, with no companion total to difference against** — refuted. The masking half holds (`load-layer-features-cube.ts:370` nulls a suppressed cell), but a companion scope total IS published today: the "Mortalidad registrada" KPI (`get-panorama-kpis.ts:560,645-647`) equals the sum of the same province's department cells, `noLocalityCount`/`suppressedCount` are both exposed at department grain (`load-layer-features-cube.ts:233-252`), and the footer publishes `coverageDenominator.totalPets` (`:1095`, rendered `PanoramaKpiFooter.tsx:56-60`). The code's own comment at `:693-697` calls this "KNOWN, NOT FIXED", and `docs/architecture/privacy-known-limitations.md:9-27` registers it as PO-accepted limitation KA1/KA2. Not a gap-round finding in its own right (it is prior, documented, accepted debt), but the "no companion total" premise is false.

### Nits (gap round)

- `scripts/check-subject-rights-coverage.ts:198` — the `KNOWN_GAP` entry for `alert_firings.notes` describes it as "written by a govt operator," but every writer of that column is admin-gated (`requireAdminUser`) — a govt operator can never write it. Wrong actor in a debt register meant to keep the art.14/16 gap honest.
- `src/modules/alerts/application/firings/record-firings.ts:61` — the `ALL_ADMINS_MAX_DURATION_MS` comment cites "Vercel Hobby cron functions time out at 60s," while the sibling `budgetHeaders` path (`:80-86`) describes a shared 60s ceiling handed down by the daily dispatcher — two different rationales for one number in one file.

### Coverage (gap round)

Read: `src/modules/alerts/application/firings/{record-firings,types,triage}.ts`, `src/modules/alerts/application/subscriptions/{create-alert-subscription,types,toggle-alert-subscription,delete-alert-subscription}.ts`, `app/actions/{alert-subscriptions,alert-firings}.ts`, `app/gob/suscripciones/page.tsx`, `app/admin/alertas/page.tsx`, `app/api/cron/evaluate-alerts/route.ts`, `lib/metrics/{alert-evaluation,alert-firing-inbox,kpi-catalog-queues,context}.ts`, `lib/analytics/{admin-metrics,govt-home-kpis}.ts`, `lib/panorama/build-panorama-board.ts`, `src/modules/panorama/application/{load-layer-features-cube,get-panorama-kpis}.ts`, `src/modules/auth/application/subject-rights/erase-subject-data.ts`, `src/modules/organizations/infrastructure/org-repository.ts`, `scripts/check-subject-rights-coverage.ts`, `scripts/check-authz-{scoping,guards}.ts`, `scripts/seed-demo-scenario.ts`, `db/migrations/{0108_alert_subscriptions,0111_alert_firings,0137_rls_initplan_auth_subselect,0165_enable_rls_where_the_ledger_lied,0208_subject_rights_watermarks_tag_interest_org_invitations}.sql`, `__tests__/rls/coverage.test.ts`, `__tests__/authz-bare-writer-exports.test.ts`, `lib/metrics/alert-evaluation.test.ts`, `components/panorama/{LayerPanel,FiltroPanel}.tsx` (37 files).

Not reached: `components/admin/AlertInboxTable.tsx` (whether the inbox renders `observed_value`, and how); `app/admin/suscripciones/page.tsx` (assumed identical to `/gob/suscripciones` from its header comment, not opened); `lib/metrics/alert-firing.ts` (`shouldOpenFiring`/`nextStatus` state machine, taken on trust); `__tests__/alert-firings-triage.test.ts` (no test-theater pass); `lib/analytics/dashboards/_scope.ts` (named in the original gap scope, not reached); the remaining 12 unopened `/gob` sub-views; whether the `deactivated_at`-only RLS pattern in A10-G1 repeats across the other migrations using that predicate (0051, 0059, 0105, 0140, 0188 — counted, not read).

## Prior triage

| priorId | status | evidence |
|---|---|---|
| 14-1 | closed | `src/modules/pets/domain/pet-form.ts:124-136` rejects before the action runs (`LOCALITY_REQUIRED`, `LOCALITY_UNRESOLVED`); the `actions.ts:136` guard is now dead-defensive. |
| 14-2 | closed | `pet-form.ts:133-136` derives `provinceForStorage` from `canonicalProvinceNameForStorage` and rejects when it does not resolve. |
| 14-3 | closed | `pet-form.ts:134` returns `LOCALITY_UNRESOLVED` server-side for any submit with empty `provinceCode`; client half (`LocalityPickerAcross.tsx`) not re-verified. |
| 14-4 | still-open | `rg -n "localityIndecId" lib/domain/location-normalize.ts` — zero hits; field never read, callers pass null. Recorded as a nit. |
| 14-5 | closed | `src/modules/pets/actions.ts:548-553` — MOVE (FULL-LOCK jurisdiction path, PO decision #40); profile-edit no longer mutates jurisdiction. |
| 14-6 | closed | `src/modules/pets/domain/pet-diff.ts:109-112` — `jurisdiction_province`/`_locality` are NOT diffable profile fields; jurisdiction moves via `recordMovementWriter`. |
| 14-7 | closed | `src/modules/auth/application/complete-identity.ts:53-54` — the profiles-jurisdiction writer is gone. |
| 14-8 | still-open | `src/modules/welfare/actions.ts:887,1218` still pass `{ locality: "soft" }`; PO-accepted for anonymous denuncia UX, no post-submit normalization job added. |
| 14-9 | still-open | `src/modules/service-offerings/application/create-service-offering.ts:73` still passes `{ locality: "soft" }`. |
| 14-10 | still-open | `app/actions/business-rules.ts:56-68` `normalizeJurisdiction` still trim-only. Promoted to finding A10-3. |
| 14-11 | closed | `src/modules/pets/application/movement/record-movement.ts:41-73` `canonicalizeMovement()` runs `normalizeLocationForWrite` and writes normalized fields + resolved `localityId`. |
| 14-12 | still-open | `src/modules/foster/infrastructure/foster-repository.ts:442-448` — "UPDATE branch — PARITY QUIRK" comment; promoted to finding A10-6. |
| 14-13 | still-open | `lib/domain/location-normalize.ts:142-151` still passes through instead of throwing. Promoted to finding A10-5 (REFUTED). |
| 14-14 | not-reproducible | `components/LocationFields.tsx` not opened within the read budget. Listed in areasNotReached. |
| 14-15 | closed | Full-lock policy implemented — see 14-5, 14-6. |
| 14-16 | not-reproducible | `scripts/import-indec-localities.ts` not opened within the read budget. Listed in areasNotReached. |
| 14-17 | still-open | `rg -n "pets" db/migrations/0117_govt_assignments_locality_canonical.sql` — no output; repair pass still `govt_assignments`-only. Folded into A10-4. |
| 14-18 | still-open | `__tests__/govt-assignments-locality-integrity.test.ts` has exactly one `it` (L56), never touches `pets`. Promoted to finding A10-4. |
| 14-19 | closed | Was already clean — `lib/domain/location-normalize.ts:128` delegates to `resolveCanonicalJurisdiction`, scoping locality to the resolved province. |
| 14-20 | still-open | `lib/domain/location-normalize.ts:154-187` — soft/none branches still return `localityCanonical:false` with no column restriction. Same class as 14-8/14-9. |
| 14-21 | still-open | `lib/infra/ar-localidades.ts:123` — `lower(locality_name) = lower(name)` with no accent fold. Recorded as a nit. |
| 14-22 | still-open | `lib/infra/ar-localidades.ts:88-92` — 68 ambiguous (province, name) pairs resolve to the first row by department. Recorded as a nit. |
| 14-23 | closed | Was already clean — `lib/infra/ar-localidades.ts:57-59` `normalize()` (NFD fold) backs the slug path and search scoring. |
| 21-1 | not-reproducible | `docs/reviews/results/21-authz-scoping-audit.md` is truncated at line 6 (finding #1 unrecoverable). |
| 21-2 | still-open | `src/modules/organizations/application/admin-proposals/helpers.ts:50` still returns `jurisdictions: []`. Root cause of finding A10-2. |
| 21-3 | still-open | `propose-vet-upgrade.ts:100-101` still inserts trimmed client-supplied province/locality with no comparison. Promoted to finding A10-2. |
| 21-4 | still-open | `propose-org-verification.ts:26-29` comment claims scope not enforced in code. Severity downgraded to LOW; promoted to finding A10-7. |
| 21-5 | closed | `lookup-pet-for-decomiso.ts:58-70` now calls `jurisdictionScopeContains` on the pet's full pair. |
| 21-6 | closed | `src/modules/decomiso/application/execute-decomiso.ts:168-171` calls `jurisdictionScopeContains` on the pet's pair. |
| 21-7 | closed | `app/actions/chip-match.ts:68` — `requireCapability("intake.create", orgAccess.organization.id)`, org resolved from URL token first. |
| 21-8 | closed | `app/actions/intake.ts:37` — `requireCapabilityForOrgToken("intake.create", orgToken)`. |
| 21-9 | closed | `src/modules/adoption/actions.ts:97` — `requireCapabilityForOrgToken("intake.create", input.orgToken)`. |
| 21-10 | closed | `src/modules/adoption/actions.ts:150` — `requireCapabilityForOrgToken("adoption.listing.manage", input.orgToken)`. |
| 21-11 | closed | `src/modules/adoption/actions.ts:200` — `requireCapabilityForOrgToken("adoption.listing.manage", input.orgToken)`. |
| 21-12 | closed | `src/modules/foster/actions.ts:87` — `requireCapabilityForOrgToken("foster.assign", orgToken)`. |
| 21-13 | closed | `src/modules/foster/actions.ts:132` — `requireCapabilityForOrgToken("foster.end", orgToken)`. |
| 21-14 | closed | `src/modules/foster/actions.ts:173,480` — `requireCapabilityForOrgToken("foster.assign", input.orgToken)`; L223 is the pinned two-arg form. |
| 21-15 | closed | `src/modules/transfers/actions.ts:716` and siblings (491/547/596/649) — `requireCapabilityForOrgToken("custody.transfer", orgToken)`. |
| 24-1 | closed | `app/gob/vigilancia/investigaciones/[caseCode]/page.tsx:80-85` now passes `jurisdictions` and calls `notFound()` when scope fails. |
| 24-2 | closed | `lib/infra/case-queries.ts:1197-1218` now takes `jurisdictions`/`isAdmin`, returns null for unassigned govt, builds `jurisdictionPairClause`. |
| 24-3 | closed | Same fix as 21-5 — `lookup-pet-for-decomiso.ts:67-70`. |
| 24-4 | closed | Same fix as 21-6 — `execute-decomiso.ts:168-171`. |
| 24-5 | closed | `execute-decomiso.ts:192-195` — unowned path now calls `jurisdictionScopeContains` on the full pair. |
| 24-6 | closed | `app/org/[orgToken]/intake/match/[matchedPetToken]/page.tsx:42-43` — `validateIntakeMatchClaim` before pet load. |
| 24-7 | closed | `src/modules/pets/application/chip-match/confirm-chip-match-refugio.ts:51-54` — write path validates the org-bound claim. |
| 24-8 | closed | `app/gob/decomisos/nuevo/page.tsx:125-144` — receiver combobox builds `jurisdictionPredicate` from `jurisdictionPairClause`. |
| 24-9 | closed | `app/gob/maltrato/[id]/page.tsx:206-227` — query now filters on the report's own province; residual is province-grain not pair-grain (acceptable, public roster). |
| 24-10 | closed | `src/modules/custody-disputes/application/lookup-transfer-target.ts:7-11,46-60` — same `jurisdictionScopeContains` gate as resolve/escalate/withdraw. |
| 24-11 | closed | Same as 21-7 — `app/actions/chip-match.ts:68`. |
| 24-12 | closed | Same as 21-8 — `app/actions/intake.ts:37`. |
| 24-13 | closed | Same as 21-12/13/14 — every foster action resolves the org from the URL token. |
| 24-14 | closed | `src/modules/adoption/actions.ts:360,393` — approve/reject call `requireCapabilityForOrgToken("adoption.review", orgToken)` before review. |
| 24-15 | closed | `app/org/[orgToken]/transitos/page.tsx:60` — `activos` half fixed; `historial` query is a different, still-unbound one → new finding A10-1. |
| 24-headline | closed | `e2e/cross-tenant-isolation.spec.ts` now carries govt/org operator probes (L578, L603), no longer owner A-B only. |

## Nits

- `lib/domain/location-normalize.ts:89` — `localityIndecId` is accepted in the input shape and never read (zero hits in this file); every caller passes null. Either wire it into a `resolveCanonicalJurisdictionById` fast path or drop the field. (prior 14-4)
- `lib/infra/ar-localidades.ts:123` — the case-insensitive fallback compares `lower(locality_name) = lower(name)` without the accent fold `normalize()` (L57-59) applies to the slug path above it. Harmless today because the slug path fires first for accented input. (prior 14-21)
- `lib/infra/ar-localidades.ts:89` — 68 ambiguous (province, name) pairs still resolve to the first row `ORDER BY department_name`. Cannot widen a govt scope, but `resolvePanoramaRequestScope` feeds the picked row's name into `adminLocality`, so an admin drill can silently land on the wrong department's homonym. (prior 14-22)
- `scripts/check-scope-discipline.ts:32` — the header's "Still NOT scanned" list (gob-pet-subview.ts, case-queries.ts, omnibox-search.ts, outbox-query.ts, audit-history-query.ts) is prose; nothing asserts it stays accurate, unlike `SCANNED_GLOBS` which is exported and test-asserted (L28-30).
- `scripts/scope-discipline-baseline.json:1` — 5 of 10 baselined files are labelled REAL DEBT (duplicate copies of a predicate with a reviewed single home). `govt-home-kpis.ts`'s own entry records that this duplication already drifted once; the migration to `dashboards/_scope.ts` exports is still pending.

## Coverage

Areas read:
- **Jurisdiction / scope core**: `lib/infra/gov-scope.ts`, `lib/domain/jurisdiction-canonical.ts`, `lib/metrics/scope.ts`, `lib/domain/location-normalize.ts`, `lib/infra/ar-localidades.ts`, `lib/analytics/jurisdiction-scope.ts`, `lib/analytics/govt-queue-aging.ts`
- **Fences / lint scripts**: `scripts/check-scope-authz.ts`, `scripts/check-scope-discipline.ts`, `scripts/check-confused-deputy.ts`, `scripts/check-view-scope.ts`, `scripts/check-locality-integrity.ts`, `scripts/scope-discipline-baseline.json`, `scripts/authz-scoping-baseline.json`, `package.json`
- **Tests**: `__tests__/gob-pet-subview-jurisdiction-fence.test.ts`, `__tests__/govt-assignments-locality-integrity.test.ts`, `__tests__/macro-invariants/macro-invariants.test.ts`, `app/api/panorama/__tests__/scope-drilldown.test.ts`, `app/api/panorama/__tests__/institutional-gate.test.ts`
- **Panorama**: `app/api/panorama/_guard.ts`, `app/api/panorama/kpis/route.ts`, `app/api/panorama/unit-history/route.ts`, `app/api/panorama/scope/route.ts`, `app/api/panorama/rule-changes/route.ts`, `src/modules/panorama/application/resolve-request-scope.ts`, `src/modules/panorama/infrastructure/repository.ts`, `src/modules/panorama/infrastructure/repository-history.ts`
- **Decomiso**: `src/modules/decomiso/application/decomiso-pet-lookup/lookup-pet-for-decomiso.ts`, `src/modules/decomiso/application/execute-decomiso.ts`, `app/actions/decomiso.ts`
- **Org tenant / capability actions**: `app/actions/business-rules.ts`, `app/actions/chip-match.ts`, `app/actions/intake.ts`, `src/modules/adoption/actions.ts`, `src/modules/foster/actions.ts`, `src/modules/transfers/actions.ts`, `src/modules/foster/infrastructure/foster-repository.ts`, `src/modules/custody-disputes/application/lookup-transfer-target.ts`, `src/modules/pets/application/chip-match/confirm-chip-match-refugio.ts`
- **Pets jurisdiction lifecycle**: `src/modules/pets/application/movement/record-movement.ts`, `src/modules/pets/actions.ts`, `src/modules/pets/domain/pet-form.ts`, `src/modules/pets/domain/pet-diff.ts`, `src/modules/auth/application/complete-identity.ts`, `src/modules/welfare/actions.ts`, `src/modules/service-offerings/application/create-service-offering.ts`
- **Admin proposals / decisions**: `src/modules/organizations/application/admin-proposals/helpers.ts`, `propose-vet-upgrade.ts`, `propose-org-verification.ts`, `src/modules/organizations/application/admin-decisions/helpers.ts`, `approve-request.ts`, `src/modules/organizations/application/admin-institutional/assign-govt-locality.ts`, `src/modules/organizations/infrastructure/authz-resolver.ts`, `src/modules/organizations/domain/capabilities.ts`
- **Org / gob pages**: `app/org/[orgToken]/transitos/page.tsx`, `checkins/page.tsx`, `intake/match/[matchedPetToken]/page.tsx`, `agenda/turnos/[appointmentToken]/page.tsx`, `mascotas/page.tsx`, `pets/no-aptas/page.tsx`, `app/gob/decomisos/nuevo/page.tsx`, `decomisos/page.tsx`, `maltrato/[id]/page.tsx`, `denuncias/page.tsx`, `casos/page.tsx`, `casos/CasosScreen.tsx`, `servicios/[offeringToken]/page.tsx`, `vigilancia/investigaciones/[caseCode]/page.tsx`, `reglas/[country]/[province]/[locality]/page.tsx`
- **Schema / migrations / e2e**: `db/schema.ts`, `db/migrations/0055_jurisdiction_province_canonical.sql`, `db/migrations/0117_govt_assignments_locality_canonical.sql`, `docs/org-portal-permissions.md`, `.github/workflows/ci.yml`, `e2e/cross-tenant-isolation.spec.ts`
- **Priors read**: `docs/reviews/briefs/14-jurisdiction.md`, `docs/reviews/briefs/21-authz-scoping-audit.md`, `docs/reviews/briefs/24-tenant-isolation-dashboards.md`, `docs/reviews/results/14-jurisdiction.md`, `docs/reviews/results/21-authz-scoping-audit.md`, `docs/reviews/results/24-tenant-isolation-dashboards.md`

Areas not reached:
- `lib/analytics/dashboards/_scope.ts` — the C3 consolidation home and one of the two `SCOPE_LAYER_FILES` the scope-authz fence derives from. Its delegates (`lib/metrics/scope.ts`) and the fence watching its siblings were verified, but the file itself was never opened; its exported wrappers (`casesScopeClause`, `custodyDisputesScopeClause`, `petsCurrentJurisdictionClause`) are unaudited at this SHA.
- The ~30 individual fetchers in `lib/analytics/dashboards/*.ts` — swept only with a presence heuristic (does the file reference any scope helper) plus the baselined offenders; no fetcher's WHERE clause was read. The "ghost-payload" class `petEventsScopeClause` warns about would not have been caught by that sweep.
- `lib/metrics/context.ts` (`buildProjectionContext`, `isSubProvincialScope`) and `lib/metrics/province-disclosure.ts` — the planner deciding what an operator may see about a province that is not theirs. `isOwnJurisdictionProvince` feeds it; what the planner does with that was never verified.
- `app/api/gob/_guard.ts` and the `/api/gob/*` route family — the panorama guard's header points at it as the file "which carries the full argument" for the operator shift on reads; only panorama's copy was audited.
- `app/admin/**` — the admin-by-design scope bypasses. The declaration pattern was confirmed only where an admin branch sits inside a shared helper (`petsScopeClause`, `canDecideRequest`, `visibleRequestsClause`), not on the admin portal's own pages.
- The RLS policies themselves (`db/migrations/0140_rls_scope_govt_pii_reads.sql`, `0165`, `0168`) — `check-scope-authz.ts` asserts they exist and are not unconditional; nobody in this pass read what they actually narrow to. It is the backstop for everything above and it is unaudited.
- `apps/mobile` — jurisdiction and org scoping on the mobile client and the `/api/v1` surface it consumes were not examined at all.
- `components/LocalityPickerAcross.tsx` and `components/LocationFields.tsx` — the capture half of prior findings 14-3 and 14-14.
- `scripts/import-indec-localities.ts` — prior finding 14-16 (no fan-out to stored locality strings when INDEC renames a catalog row) is untriaged.
- `e2e/authz-ab-isolation.spec.ts` — a second isolation spec found but not read.
- `scripts/check-authz-scoping.ts` — only its baseline was read (19 files, down from the prior audit's 48), not the fence body; same open question applies to `scripts/scope-discipline-baseline.json` (also count-keyed by design, `check-scope-discipline.ts:192-196`).
- `app/gob/rupga, /historial, /moderacion, /observaciones, /outreach, /suscripciones, /usuarios, /directorio, /operativos, /perdidas, /programa, /sistema, /acciones` — 13 gob sub-views enumerated but never opened; a province-param sweep showed they either narrow or take no province axis, which is a screen, not an audit.

<details>
<summary>Full filesRead list (86)</summary>

```
docs/reviews/briefs/14-jurisdiction.md
docs/reviews/briefs/21-authz-scoping-audit.md
docs/reviews/briefs/24-tenant-isolation-dashboards.md
docs/reviews/results/14-jurisdiction.md
docs/reviews/results/21-authz-scoping-audit.md
docs/reviews/results/24-tenant-isolation-dashboards.md
lib/infra/gov-scope.ts
lib/domain/jurisdiction-canonical.ts
lib/metrics/scope.ts
lib/domain/location-normalize.ts
lib/infra/gob-pet-subview.ts
lib/infra/case-queries.ts
lib/infra/auth-guards.ts
lib/infra/approval-scope.ts
lib/infra/ar-localidades.ts
lib/analytics/jurisdiction-scope.ts
lib/analytics/govt-queue-aging.ts
scripts/check-scope-authz.ts
scripts/check-scope-discipline.ts
scripts/check-confused-deputy.ts
scripts/check-view-scope.ts
scripts/check-locality-integrity.ts
scripts/scope-discipline-baseline.json
scripts/authz-scoping-baseline.json
package.json
__tests__/gob-pet-subview-jurisdiction-fence.test.ts
__tests__/govt-assignments-locality-integrity.test.ts
__tests__/macro-invariants/macro-invariants.test.ts
app/api/panorama/_guard.ts
app/api/panorama/kpis/route.ts
app/api/panorama/unit-history/route.ts
app/api/panorama/scope/route.ts
app/api/panorama/rule-changes/route.ts
app/api/panorama/__tests__/scope-drilldown.test.ts
app/api/panorama/__tests__/institutional-gate.test.ts
src/modules/panorama/application/resolve-request-scope.ts
src/modules/panorama/infrastructure/repository.ts
src/modules/panorama/infrastructure/repository-history.ts
src/modules/decomiso/application/decomiso-pet-lookup/lookup-pet-for-decomiso.ts
src/modules/decomiso/application/execute-decomiso.ts
app/actions/decomiso.ts
app/actions/business-rules.ts
app/actions/chip-match.ts
app/actions/intake.ts
src/modules/adoption/actions.ts
src/modules/foster/actions.ts
src/modules/transfers/actions.ts
src/modules/foster/infrastructure/foster-repository.ts
src/modules/custody-disputes/application/lookup-transfer-target.ts
src/modules/pets/application/chip-match/confirm-chip-match-refugio.ts
src/modules/pets/application/movement/record-movement.ts
src/modules/pets/actions.ts
src/modules/pets/domain/pet-form.ts
src/modules/pets/domain/pet-diff.ts
src/modules/auth/application/complete-identity.ts
src/modules/welfare/actions.ts
src/modules/service-offerings/application/create-service-offering.ts
src/modules/organizations/application/admin-proposals/helpers.ts
src/modules/organizations/application/admin-proposals/propose-vet-upgrade.ts
src/modules/organizations/application/admin-proposals/propose-org-verification.ts
src/modules/organizations/application/admin-decisions/helpers.ts
src/modules/organizations/application/admin-decisions/approve-request.ts
src/modules/organizations/application/admin-institutional/assign-govt-locality.ts
src/modules/organizations/infrastructure/authz-resolver.ts
src/modules/organizations/domain/capabilities.ts
app/org/[orgToken]/transitos/page.tsx
app/org/[orgToken]/checkins/page.tsx
app/org/[orgToken]/intake/match/[matchedPetToken]/page.tsx
app/org/[orgToken]/agenda/turnos/[appointmentToken]/page.tsx
app/org/[orgToken]/mascotas/page.tsx
app/org/[orgToken]/pets/no-aptas/page.tsx
app/gob/decomisos/nuevo/page.tsx
app/gob/decomisos/page.tsx
app/gob/maltrato/[id]/page.tsx
app/gob/denuncias/page.tsx
app/gob/casos/page.tsx
app/gob/casos/CasosScreen.tsx
app/gob/servicios/[offeringToken]/page.tsx
app/gob/vigilancia/investigaciones/[caseCode]/page.tsx
app/gob/reglas/[country]/[province]/[locality]/page.tsx
db/schema.ts
db/migrations/0055_jurisdiction_province_canonical.sql
db/migrations/0117_govt_assignments_locality_canonical.sql
docs/org-portal-permissions.md
.github/workflows/ci.yml
e2e/cross-tenant-isolation.spec.ts
```

</details>
