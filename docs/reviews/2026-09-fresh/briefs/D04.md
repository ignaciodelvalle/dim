# D04 — Process & governance (brief)

Status: DEFERRED — auditoría 2026-09, lote 2. Not yet run. Written 2026-09-02 at HEAD d7dbf25f7.

> Reproduces the audit lens for D04 — runnable by hand, by a human or an external read-only agent, without prior context from the workflow that produced `docs/reviews/2026-09-fresh/lenses/D04.md`.

## Contract

- Read-only. Do not run `pnpm`/tests/builds/migrations/dev servers. Never open, read, or mention `.env*` file contents. Every claim needs an `evidenceCmd` and its literal output pasted in, with a repo-relative path (forward slashes) and line number.
- **SHA check first**: `git branch --show-current && git rev-parse --short HEAD` should read `main` / `d7dbf25f7`. If it differs, note the drift and re-locate every finding by content, not by remembered line number.
- Exclusions: `.claude/worktrees/**`, `gate-*/**`, `docs/archive/**`, `.env*`, `node_modules`, `.next`. GitHub branch-protection settings are NOT readable from the repo checkout — read `.github/workflows/*.yml` and `CODEOWNERS` for what CAN be inferred from committed config, and explicitly mark anything requiring GitHub's own settings UI as `NOT VERIFIABLE FROM THE REPO — ask the PO or check via `gh api repos/:owner/:repo/branches/main/protection``.

## Scope and lens

NEW lens (no prior run). CI advisory-vs-required status, branch protection (to the extent inferable), `docs/agents/open-work.md` staleness, DoD honesty, and which of `recommendations-2026-08-30.md`'s recommendations landed.

**Verified inventory**:
- `CODEOWNERS` exists at repo root — read it in full for what it actually gates.
- 7 GitHub workflow files: `ci.yml`, `codeql.yml`, `db-doctor-staging.yml`, `e2e-nightly.yml`, `mobile-export-nightly.yml`, `panorama-qa-nightly.yml`, `staging-health.yml`.
- `.github/workflows/ci.yml:798-799` carries a comment explicitly rejecting `continue-on-error: true` for fence steps: "A red fence is red, full stop. This is deliberately NOT `continue-on-error: true` + an aggregation step" — read the surrounding ~20 lines for the full reasoning and confirm the actual step definitions honor this (no stray `continue-on-error` elsewhere in the file: `grep -n "continue-on-error" .github/workflows/ci.yml`).
- `docs/agents/open-work.md` (2,883 lines) has real, verified section structure: `## Where the project is` (17), `## Milestones` (30), `## Open work an agent can pick up` (38), `## Landed since this snapshot — do not pick it up again` (147), `## Attempted and turned back` (689), `## Declared debts, with an owner` (816), `## PO-gated — not agent work, do not attempt` (844), `## Two live hazards, today` (860), **`## Docs whose headers lie`** (865 — read this section FIRST, it is the file's own self-audit of doc-header honesty and directly overlaps this lens's DoD-honesty angle), `## Branch topology — changed 2026-08-28` (912).
- `docs/agents/recommendations-2026-08-30.md` has 11 numbered `## ` sections (1 through 11) — the plan describing this lens says "10 recommendations"; recount and reconcile (see D03's note — flag this discrepancy in ONE of D03/D04, not both, to avoid a duplicate finding; this brief nominates D04 as the owner since it's the one directly asked to count them).

## Priors

None — this class of governance audit did not exist before 2026-09.

## Method

1. **`docs/agents/open-work.md` §"Docs whose headers lie"** (line 865) — read this section in full first; it is a pre-existing, self-reported list of doc-honesty gaps. Recount every item in it against the current SHA: closed (the header no longer lies), still-open (reproduce), or the section itself now stale (the doc it names has since changed further).
2. **CI required-vs-advisory.** Read `.github/workflows/ci.yml` in full. For every job/step, determine: does a failure here block a PR merge (as far as inferable from the workflow file — a step with no `continue-on-error` and no `if: always()` masking is required by default in GitHub Actions), or is it structured to report without blocking? Cross-check the no-`continue-on-error` comment at lines 798-799 against every OTHER step in the file for consistency.
3. **Branch protection.** State explicitly that this cannot be verified from the checkout alone. If `gh` is available and read-only API access is permitted by your environment, `gh api repos/:owner/:repo/branches/main/protection` may confirm required-status-checks; otherwise mark this `NOT VERIFIABLE FROM THE REPO` rather than guessing.
4. **`recommendations-2026-08-30.md` landed-vs-open.** Read all 11 numbered sections in full. For each, determine whether its concrete recommendation (not just its observation) has landed in code — cite the fix if closed, or the still-present symptom if open. Section 5 ("`pnpm test:verified` does not cover `apps/mobile`") should be cross-checked against C08's independent finding on the same gap (`apps/mobile/jest.config.js` has no `testTimeout`) — if both lenses ran, they should agree; if only this one ran, state the gap fresh.
5. **`open-work.md` staleness beyond §"Docs whose headers lie".** Sample 5-8 items from `## Open work an agent can pick up` (line 38+) and `## Declared debts, with an owner` (line 816+) — cross-check each against `git log` for the relevant files/areas to see if the item has actually landed since the doc's last update (making the doc stale) or is genuinely still open.
6. **`## PO-gated` accuracy.** Read line 844+ — for each item marked PO-gated, confirm it's still accurate (not something an agent already has clearance for per a more recent CLAUDE.md/docs update) — cross-check against CLAUDE.md's "Delivery strategy (decided — do not re-ask)" section for any overlap where a PO decision has since been made but `open-work.md` wasn't updated.
7. **DoD honesty overall.** Cross-reference CLAUDE.md's own DoD section (long, detailed, with explicit red-signature rules) against whether `open-work.md`, `recommendations-2026-08-30.md`, and any recent PR/commit messages (`git log --oneline -30`) actually follow it — or whether "green" gets asserted with a weaker bar in practice (this is the DIRECT governance question: does the written process match the practiced one).
8. **`CODEOWNERS`.** Read in full — which paths require which reviewers, and does the pattern set still match the current directory structure (a stale glob pointing at a removed/renamed path is itself a finding).
9. **Exploit or mutation per finding.** For a CI-advisory-when-should-be-required finding, state the concrete broken change that could merge today. For a stale `open-work.md` item, state what a new contributor would wrongly attempt.
10. **At least 3 healthy items** — a governance mechanism confirmed to work as documented.

## Output

Produce a JSON object (or equivalent structured notes) with these top-level keys:

- **findings**: array of `{id: "D04-n", title, severity: CRITICAL|HIGH|MED|LOW, kind: doc-drift|process|fence-gap, canonRule?, path, line, evidenceCmd, evidenceOutput(≤400ch), exploitOrMutation, fix(one line), fixClass: doc-fix|code-sdd|po-decision, priorId?, priorStatus?}`
- **healthy**: array of `{title, property, path, line, evidenceCmd, evidenceOutput, whatWouldBreakIt, guardedBy, guardVerified: bool}` — minimum 3
- **priorTriage**: `[]` (no priors this run)
- **nits**: array of `{path, line, note}`
- **coverage**: `{filesRead: [...], areasNotReached: [...]}`

**Severity rubric**:
- CRITICAL: not typical for this lens — reserve for a CI check that is silently advisory on a security-relevant fence (RLS, authz) with no one aware.
- HIGH: a required-sounding fence proven advisory (can be bypassed on merge), a DoD claim in `open-work.md`/`recommendations-2026-08-30.md` that misrepresents what actually gates a release.
- MED: a stale `open-work.md` item that could send a new contributor down a dead path, a `recommendations-2026-08-30.md` item still open with no tracking, a `CODEOWNERS` glob pointing at a moved path.
- LOW: hygiene, a resolved item not yet moved to the "landed" section.

This brief is meant to be runnable by hand by someone who has not seen this workflow — if a step above is ambiguous without prior context, treat that as a bug in the brief and resolve it the most literal way rather than guessing at intent.
