# D03 — `docs/agents/*`, `docs/superpowers/README.md`, `docs/architecture/*`, `docs/adr`, `README.md`, `CONTRIBUTING.md`, mobile run-book; the dangling paths (brief)

Status: DEFERRED — auditoría 2026-09, lote 2. Not yet run. Written 2026-09-02 at HEAD d7dbf25f7.

> Reproduces the audit lens for D03 — runnable by hand, by a human or an external read-only agent, without prior context from the workflow that produced `docs/reviews/2026-09-fresh/lenses/D03.md`.

## Contract

- Read-only. Do not run `pnpm`/tests/builds/migrations/dev servers. Never open, read, or mention `.env*` file contents. Every claim needs an `evidenceCmd` and its literal output pasted in, with a repo-relative path (forward slashes) and line number.
- **SHA check first**: `git branch --show-current && git rev-parse --short HEAD` should read `main` / `d7dbf25f7`. If it differs, note the drift and re-locate every finding by content, not by remembered line number.
- Exclusions: `.claude/worktrees/**`, `gate-*/**`, `docs/archive/**`, `.env*`, `node_modules`, `.next`.

## Scope and lens

NEW lens (no prior doc-vs-code run at this granularity). The repo's meta-documentation layer, plus the "dangling paths" claim.

**Verified inventory**:
- `docs/agents/*.md` — 23 files, including `docs/agents/README.md` (the per-agent contract hub CLAUDE.md references), `docs/agents/open-work.md` (2,883 lines), `docs/agents/recommendations-2026-08-30.md`.
- `docs/adr/*` — 1 file: `docs/adr/2026-07-18-native-readiness.md`.
- `docs/architecture/*.md` — 6 files: `hexagonal-lite.md`, `rls-coverage.md`, `retention-policy-pending-decision.md`, `privacy-known-limitations.md`, `api-invariants.md`, `client-error-sink-pending-decision.md`. Note: `docs/architecture/conventions-canon.md` and `docs/architecture/facts.json` (planned by Deliverable A of the 2026-09 audit plan) do NOT exist yet at this SHA — do not cite them as existing.
- `README.md`, `CONTRIBUTING.md` at repo root — both exist.
- Mobile run-book: no file literally named `run-book` under `apps/mobile/`; the closest are `docs/mobile/emulator-runbook.md` and the general `docs/ops/*-runbook.md` set (`db-bootstrap-runbook.md`, `going-public-runbook.md`, `incident-runbook.md`, `local-dev-runbook.md`, `remote-supabase-bootstrap-runbook.md`) — confirm which of these (if any) is what the audit plan means by "mobile run-book," or flag the absence of a dedicated one as a finding.

**`docs/superpowers/README.md` — verified, matches the plan's claim exactly.** Lines 3-8 carry a stale banner: `> **▶ Rama activa (Cowork ↔ CC):** integration/session-review` with `pnpm test verde (6511)` and a `C:\dim` working-copy claim. This is real, current, and unfixed as of this SHA — read the full banner (lines 1-9) before writing the finding.

**Numbers to recount, not copy** — "the 60 dangling paths": no source for this exact figure was found during brief-writing (`rg -n "dangling" docs` turned up unrelated matches — git internals, an a11y note, an archived doc — none about backticked-path link rot). Treat "60" as **reported by the 2026-09-01 planning session's explorer pass; recount before use** — do not restate it as fact without independently deriving it. `__tests__/link-integrity.test.ts` exists but (per the audit plan's own C.4 note) checks app hrefs, NOT backticked repo-relative paths inside markdown prose — so it does not fence this claim; a genuine backticked-path sweep across `docs/**` (excluding `docs/archive/**`) would need to be run fresh to produce a real count.

## Priors

None — this class of doc-vs-code audit did not exist before 2026-09.

## Method

1. **Confirm the inventory** with `Glob docs/agents/*.md`, `Glob docs/adr/*`, `Glob docs/architecture/*.md`, `Glob docs/reviews/*.md` (for `docs/reviews/README.md` — check if it exists yet; the audit plan describes it as NEW/not-yet-written) — your own counts may differ from this brief's if files were added/removed since 2026-09-02.
2. **`docs/superpowers/README.md` banner.** Read lines 1-25 in full; confirm the stale branch/working-copy claim is still present verbatim, and check whether ANY of the "✅ Implementado" status rows further down the file name a file that no longer exists (`rg -n "✅|Implementado" docs/superpowers/README.md` then verify each named path with `Glob`).
3. **`docs/agents/open-work.md` staleness.** This is a 2,883-line file — do not read it whole; instead sample its most recent-looking entries (search for the highest date near the top or bottom, whichever this file's convention uses) and cross-check 3-5 "open" items against whether they're actually still open in code/git history.
4. **`docs/agents/recommendations-2026-08-30.md`.** Read the section headers (`rg -n "^## " docs/agents/recommendations-2026-08-30.md` — 11 numbered sections, 1 through 11, verified) — note for D04's benefit (don't duplicate its work here) that this document has 11 numbered `##` sections while the audit plan's D04 row describes "10 recommendations"; flag the discrepancy once, in whichever of D03/D04 you're producing, not both.
5. **Backticked-path sweep (produces the real number for "dangling paths").** For each file in `docs/agents/*.md`, `docs/architecture/*.md`, `docs/adr/*`, `README.md`, `CONTRIBUTING.md`, extract every backticked string that looks like a repo-relative path (`^(app|lib|src|db|scripts|packages|apps|components|__tests__|\.github|docs)/` pattern, `:line` suffix stripped) and verify each with `Glob`/`ls`. Report your own total; state explicitly whether it matches "60" or not.
6. **README.md and CONTRIBUTING.md.** Read both in full. Cross-check `CONTRIBUTING.md`'s documented commands (`pnpm test`, `pnpm test:unit`, `pnpm test:e2e:smoke` at lines 78-83, verified) against `package.json`'s actual scripts for accuracy, and against `CLAUDE.md`'s DoD section for consistency (same drift class as D02's finding — coordinate to avoid double-counting; this lens's angle is the FULL sweep of all doc files, D02's is specifically `AGENTS.md`+`CLAUDE.md`).
7. **`docs/architecture/*-pending-decision.md`.** Read `retention-policy-pending-decision.md` and `client-error-sink-pending-decision.md` — per the audit plan's C.5 retire/refresh list, these should carry a "PENDING PO DECISION as of 2026-09" status line; confirm whether they already do.
8. **Mobile run-book.** Read whichever file(s) you determine are the closest match (see Scope note above) and assess whether it's current against `apps/mobile`'s actual setup steps (`apps/mobile/eas.json`, `apps/mobile/app.config.ts`).
9. **Exploit or mutation per finding.** For a dangling-path finding, state what a reader following that backticked path would find instead (nothing, or a different file entirely). For the `docs/superpowers/README.md` banner, state the concrete wrong action a new contributor would take (checking out a dead branch, looking for a nonexistent `C:\dim` directory).
10. **At least 3 healthy items** — a doc file found accurate against a direct code cross-check.

## Output

Produce a JSON object (or equivalent structured notes) with these top-level keys:

- **findings**: array of `{id: "D03-n", title, severity: CRITICAL|HIGH|MED|LOW, kind: doc-drift, canonRule?, path, line, evidenceCmd, evidenceOutput(≤400ch), exploitOrMutation, fix(one line), fixClass: doc-fix|po-decision, priorId?, priorStatus?}`
- **healthy**: array of `{title, property, path, line, evidenceCmd, evidenceOutput, whatWouldBreakIt, guardedBy, guardVerified: bool}` — minimum 3
- **priorTriage**: `[]` (no priors this run)
- **nits**: array of `{path, line, note}`
- **coverage**: `{filesRead: [...], areasNotReached: [...]}`

**Severity rubric**:
- CRITICAL: not applicable to this lens — doc drift alone does not reach CRITICAL.
- HIGH: `docs/superpowers/README.md`'s stale banner (a working, current pointer sending a new contributor to a dead branch/directory), an "✅ Implementado" claim naming a file that does not exist (misleads about what's shipped).
- MED: a real count of dangling backticked paths above a handful, `CONTRIBUTING.md`/`README.md` commands that no longer match `package.json`, a pending-decision doc missing its status line.
- LOW: a single stale path, hygiene, an `open-work.md` entry that's slightly behind but not misleading.

This brief is meant to be runnable by hand by someone who has not seen this workflow — if a step above is ambiguous without prior context, treat that as a bug in the brief and resolve it the most literal way rather than guessing at intent.
