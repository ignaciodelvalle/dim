# D01 — `AGENTS.md` §Data model, §Event catalog, §User roles, §Authorization, §Privacidad, §Legal framework vs code (brief)

Status: DEFERRED — auditoría 2026-09, lote 2. Not yet run. Written 2026-09-02 at HEAD d7dbf25f7.

> Reproduces the audit lens for D01 — runnable by hand, by a human or an external read-only agent, without prior context from the workflow that produced `docs/reviews/2026-09-fresh/lenses/D01.md`.

## Contract

- Read-only. Do not run `pnpm`/tests/builds/migrations/dev servers. Never open, read, or mention `.env*` file contents. Every claim needs an `evidenceCmd` and its literal output pasted in, with a repo-relative path (forward slashes) and line number.
- **SHA check first**: `git branch --show-current && git rev-parse --short HEAD` should read `main` / `d7dbf25f7`. If it differs, note the drift and re-locate every finding by content, not by remembered line number.
- Exclusions: `.claude/worktrees/**`, `gate-*/**`, `docs/archive/**`, `.env*`, `node_modules`, `.next`.

## Scope and lens

NEW lens (no prior doc-vs-code run at this granularity). Six sections of `AGENTS.md` (1,763 lines total), checked against the code they describe — not against each other, and not against their own prose alone.

**Verified section anchors** (`rg -n "^## " AGENTS.md`):
- `§Data model` — line 347
- `§Event catalog — 55 types` — line 680 (the heading itself embeds the count — cross-check `EVENT_TYPES.length` per B07's method; this is the SAME fact two lenses independently verify, which is intentional per the plan's calibration discipline)
- `§User roles & account types` — line 175
- `§Authorization architecture (Wave 5 Item 26)` — line 984 (an H3 subsection nested under `§Aggregation & privacy policy`, line 976 — NOT a standalone H2; verify this nesting yourself, since a naive `rg "^## "` search alone would miss it)
- `§Privacidad y manejo de datos` — line 1597
- `§Legal framework` — line 326

## Priors

None in the old `docs/reviews/briefs/*` numbering — this class of doc-vs-code audit did not exist before 2026-09. Treat `docs/reviews/results/total-audit-synthesis.md`'s self-minted-admin CRITICAL closure as background context for §Authorization only (read it, but this lens's job is fresh verification, not re-litigating that closure — A01 already owns that).

## Method

1. **Read each of the six sections in full** at the line ranges above (each section runs to the next `## ` heading — compute the range yourself with the anchor list, don't guess).
2. **For every concrete, falsifiable claim** in each section (a table name, a column, an enum's value list, a role name, a capability count, a citation to a specific law article), find the corresponding code with `rg`/`Read` and confirm it matches EXACTLY — not "close enough." A claim with no falsifiable content (scene-setting prose) is out of scope; note it as such rather than manufacturing a finding.
3. **§Data model** (line 347+): cross-check every table/column named against `db/schema.ts` directly (53 tables per `rg -c "^export const \w+ = pgTable" db/schema.ts` — recount, don't copy). Flag any table/column the doc describes that no longer exists, or a real table with no doc coverage at all if the section claims completeness.
4. **§Event catalog** (line 680+): confirm the "55" in the heading matches `EVENT_TYPES.length` in `packages/contract/src/events/event-types.ts` (count the array, not a regex over the file — same discipline B07 applies). Check `__tests__/event-catalog-count.test.ts` actually fences this heading's number (read the test, don't assume from its name).
5. **§User roles & account types** (line 175+): cross-check every named role/account_type against `db/schema.ts`'s actual enum/CHECK definitions and against `src/modules/organizations/domain/capabilities.ts` + `src/modules/panorama/domain/capabilities.ts` (both confirmed present) for the capability list.
6. **§Authorization architecture** (line 984, nested at H3): cross-check the "two-layer authorization model" claim against the actual code — does a Drizzle server-action query really bypass RLS via a `BYPASSRLS` connection (`db/index.ts`), and does RLS really fire only for supabase-js/PostgREST as the doc claims (line 1623 makes this exact claim — verify it against `db/index.ts`'s connection config)?
7. **§Privacidad y manejo de datos** (line 1597+): cross-check DNI-hashing claims against `lib/utils/dni-hash.ts`, K-anonymity claims against `lib/metrics/anonymity.ts` (also C02's territory — coordinate, don't duplicate; this lens's angle is "does the DOC match the code," C02's is "is the CODE correct").
8. **§Legal framework** (line 326+): for each Ley 25.326 article citation or SENASA reference, confirm the code path it describes actually exists and behaves as claimed (this overlaps A05's erasure-vs-immutability territory for article 14/16 — again, doc-vs-code here, code-correctness there).
9. **Exploit or mutation per finding**: for a doc-drift finding, state precisely what an engineer or official reading only the doc would wrongly believe, and what the code actually does instead.
10. **At least 3 healthy items** — sections where the doc's concrete claims were spot-checked and found to match code exactly, with the specific claim and the code cited.

## Output

Produce a JSON object (or equivalent structured notes) with these top-level keys:

- **findings**: array of `{id: "D01-n", title, severity: CRITICAL|HIGH|MED|LOW, kind: doc-drift, canonRule?, path, line, evidenceCmd, evidenceOutput(≤400ch), exploitOrMutation, fix(one line), fixClass: doc-fix|code-sdd|po-decision, priorId?, priorStatus?}`
- **healthy**: array of `{title, property, path, line, evidenceCmd, evidenceOutput, whatWouldBreakIt, guardedBy, guardVerified: bool}` — minimum 3
- **priorTriage**: `[]` (no priors this run)
- **nits**: array of `{path, line, note}`
- **coverage**: `{filesRead: [...], areasNotReached: [...]}`

**Severity rubric**:
- CRITICAL: a §Authorization or §Privacidad claim that, if believed, would lead an engineer to build an insecure feature believing it's guarded (e.g. "RLS enforces X" when it doesn't for the actual connection used).
- HIGH: a §Legal framework citation that misdescribes an actual compliance mechanism (art. 14/16 erasure claims), a §Data model claim describing a table/column that no longer exists and would break a new engineer's mental model.
- MED: a stale count/number (like a fence-count mismatch) with no security consequence, a §User roles claim slightly out of sync with the actual capability list.
- LOW: prose drift, an outdated example, hygiene.

This brief is meant to be runnable by hand by someone who has not seen this workflow — if a step above is ambiguous without prior context, treat that as a bug in the brief and resolve it the most literal way rather than guessing at intent.
