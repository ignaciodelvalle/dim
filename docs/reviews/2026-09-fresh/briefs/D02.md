# D02 — `AGENTS.md` §Feature inventory, §Design rules, §Test-runner conventions, §Naming, §How Claude should work + `CLAUDE.md` vs code (brief)

Status: DEFERRED — auditoría 2026-09, lote 2. Not yet run. Written 2026-09-02 at HEAD d7dbf25f7.

> Reproduces the audit lens for D02 — runnable by hand, by a human or an external read-only agent, without prior context from the workflow that produced `docs/reviews/2026-09-fresh/lenses/D02.md`.

## Contract

- Read-only. Do not run `pnpm`/tests/builds/migrations/dev servers. Never open, read, or mention `.env*` file contents. Every claim needs an `evidenceCmd` and its literal output pasted in, with a repo-relative path (forward slashes) and line number.
- **SHA check first**: `git branch --show-current && git rev-parse --short HEAD` should read `main` / `d7dbf25f7`. If it differs, note the drift and re-locate every finding by content, not by remembered line number.
- Exclusions: `.claude/worktrees/**`, `gate-*/**`, `docs/archive/**`, `.env*`, `node_modules`, `.next`.

## Scope and lens

NEW lens (no prior doc-vs-code run at this granularity). `AGENTS.md`'s process/convention sections, plus `CLAUDE.md` in full (110 lines — small enough to read whole), against actual code and tooling.

**Verified section anchors in `AGENTS.md`** (`rg -n "^## " AGENTS.md`):
- `§Feature inventory` — line 1135
- `§Naming` — line 1268
- `§Design rules (UI conventions)` — line 1286
- `§Test-runner conventions (Item 29 — Wave 5)` — line 1428
- `§How Claude should work in this repo` — line 1756 (last section, runs to EOF at line 1763)

**The known fence-count discrepancy — verify both instances, don't just trust this brief's claim:**
```
rg -n "\bfences\b" AGENTS.md
```
This pass found TWO different numbers for the same underlying fact: line 1530 says "46 at last read" (in the context of `pnpm verify`'s `lint:*` chain — read lines 1528-1533 for the exact phrasing, "46 fences" is a paraphrase, not the literal string), and line 667 says "the 45 fences" (inside `§Libreta sanitaria`, an unrelated section — a stray comparison, not the fence-count's canonical home). Recount the actual live number:
```
grep -c '"lint:' package.json
→ 66
```
66 wired `lint:*` scripts today — both 45 and 46 are stale, and they disagree with EACH OTHER inside the same document, which is itself worth a finding independent of either number's staleness relative to code.

**`CLAUDE.md` cross-check**: `CLAUDE.md`'s own "Definition of Done" section states the actual gate is `pnpm verify` + `pnpm test:verified` (not `pnpm test`) — verify every OTHER doc this lens touches (starting with `CONTRIBUTING.md:78`, already confirmed to still say plain `pnpm test`) is consistent with `CLAUDE.md`'s own stated gate. Also verify `CLAUDE.md`'s brand-casing claim ("brand casing on user-facing labels is `miMAR`") against `scripts/check-brand-casing.ts`'s actual enforcement — read the script, don't assume from the doc's description of it.

## Priors

None — this class of doc-vs-code audit did not exist before 2026-09.

## Method

1. **Read each `AGENTS.md` section in full** at the anchors above, plus `CLAUDE.md` in full.
2. **Fence-count reconciliation.** Run both greps above; state explicitly that AGENTS.md disagrees with itself (667 vs 1530) before even reaching the code-vs-doc comparison; then state the gap between either doc number and the live 66.
3. **§Feature inventory** (line 1135+): for each named feature, confirm it exists in the code with a working entry point (`app/` route or action) — flag any feature claimed as shipped that has no corresponding route/action, or a shipped feature entirely missing from the inventory.
4. **§Naming** (line 1268+): spot-check 5-8 naming conventions claimed (file naming, function naming, table naming) against actual files — `rg`/`Glob` for counter-examples.
5. **§Design rules (UI conventions)** (line 1286+): cross-check against the actual fences enforcing them (`scripts/check-ui-invariants.ts`, `scripts/check-design-tokens.ts`) — does the doc's description of each rule match what the fence actually checks (read the fence, not just its `lint:*` name)?
6. **§Test-runner conventions** (line 1428+): cross-check every command/behavior claim against `package.json`'s actual scripts and `scripts/run-verified-suite.ts`/`scripts/check-suite-coverage.ts` (also C08's territory for correctness — this lens's angle is doc-vs-code, C08's is code-vs-reality).
7. **§How Claude should work in this repo** (line 1756, runs to EOF): cross-check any process claim (e.g. an agent-briefing convention, a commit-message rule) against `docs/agents/README.md` and actual recent commit messages (`git log --oneline -20`) for consistency.
8. **`CLAUDE.md` vs code**: brand casing (`scripts/check-brand-casing.ts`), the DoD gate description, the working norms list (e.g. "a fresh-context reviewer is a standard pre-push step" — is there any code/CI mechanism enforcing this, or is it purely a process convention with no fence, which is fine but should be stated honestly rather than implied as enforced).
9. **Exploit or mutation per finding.** For a stale-number finding, state exactly what a new contributor following the doc literally would get wrong. For a feature-inventory gap, name the missing route/action or the undocumented-but-shipped feature.
10. **At least 3 healthy items** — claims spot-checked and found to match code exactly.

## Output

Produce a JSON object (or equivalent structured notes) with these top-level keys:

- **findings**: array of `{id: "D02-n", title, severity: CRITICAL|HIGH|MED|LOW, kind: doc-drift, canonRule?, path, line, evidenceCmd, evidenceOutput(≤400ch), exploitOrMutation, fix(one line), fixClass: doc-fix|code-sdd|po-decision, priorId?, priorStatus?}`
- **healthy**: array of `{title, property, path, line, evidenceCmd, evidenceOutput, whatWouldBreakIt, guardedBy, guardVerified: bool}` — minimum 3
- **priorTriage**: `[]` (no priors this run)
- **nits**: array of `{path, line, note}`
- **coverage**: `{filesRead: [...], areasNotReached: [...]}`

**Severity rubric**:
- CRITICAL: not typical for this lens — reserve for a §How Claude should work claim that, if followed literally, would cause an agent to violate a hard invariant (e.g. an instruction that contradicts the append-only rule).
- HIGH: a §Test-runner conventions claim that misdescribes the actual DoD gate in a way that could ship a broken build believing it passed (cross-file with CLAUDE.md's own DoD section, which is normative — a AGENTS.md claim contradicting it is the defect).
- MED: the fence-count self-contradiction (667 vs 1530) and its drift from the live 66, a §Feature inventory claim describing a feature that no longer exists as described.
- LOW: naming-convention drift with no functional consequence, prose staleness.

This brief is meant to be runnable by hand by someone who has not seen this workflow — if a step above is ambiguous without prior context, treat that as a bug in the brief and resolve it the most literal way rather than guessing at intent.
