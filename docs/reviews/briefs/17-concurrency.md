Adversarial review — CONCURRENCY & IDEMPOTENCY. SCOPE: lib/** write paths (event inserts, ownership transfer, lost-mode toggle, scan/movement capture), any plain db.insert on a hot path. LENS: Race conditions — two concurrent requests double-inserting an event / double-transferring ownership / both toggling lost-mode? Idempotency guards on retryable writes (a dedupe key / unique constraint / upsert), or can a retry create a duplicate? Check-then-write TOCTOU gaps (read state, decide, write — without a lock/constraint)? The movement/scan-location capture idempotent? Any counter/aggregate updated non-atomically?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code — no generic advice/praise/prose. 'clean' if fine. Terse.
