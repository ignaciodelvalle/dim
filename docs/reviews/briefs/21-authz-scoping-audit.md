Adversarial SECURITY audit — AUTHZ SCOPING / TENANT ISOLATION (the #1 prod risk: one unscoped server query = cross-tenant PII leak; DB is BYPASSRLS so app-layer is the ONLY defense). SCOPE: the 48 offenders in scripts/authz-scoping-baseline.json, all lib/** + src/modules/** + app/actions/** server actions/queries that return PII or tenant/jurisdiction data. LENS: For each, does the query carry a jurisdiction/tenant/owner WHERE predicate derived from the SESSION (not client input)? Find any that calls a role guard (admin/govt/org) then runs a PII/aggregate query with NO scoping predicate — a govt user in CABA could read Mendoza pets, or org A read org B. List each real leak: the file:line, the missing predicate, the exact fix (add the jurisdiction/tenant clause). Distinguish genuinely-scoped (safe) from unscoped.

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (this repo has app/ lib/ db/ components/ AND src/modules/ — a hexagonal layer; use real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
