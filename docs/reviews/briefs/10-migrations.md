Adversarial review — MIGRATION DISCIPLINE. SCOPE: db/migrations/** (126 files), scripts/migrate.ts. LENS: Any migration that is NOT forward-only/idempotent (would break on re-run or on a partially-applied DB)? CREATE INDEX CONCURRENTLY inside a transaction (fails) without the -- dim:no-transaction marker? A destructive/irreversible step (DROP column/table, data delete) without a guard? Checksum-tracking gaps (a migration editable after apply)? Enum value added in a way that can't be rolled forward? Schema.ts drift vs the migration history (drizzle-kit push would drop something)?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
