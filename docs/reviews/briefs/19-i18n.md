Adversarial review — i18n & CONTENT (es-AR). SCOPE: components/**, app/** UI, lib/ui labels, any place a DB enum/UUID/status reaches the screen. LENS: Any raw UUID / DB enum value / snake_case key / null/blank rendered to the user instead of an es-AR label (the case-kind class)? English leaking into user-facing UI (the English-code/Spanish-UI invariant broken)? Date/number formatting non-es-AR (comma decimals, dot thousands, DD/MM)? Hardcoded strings that should be centralized? Pluralization bugs?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code — no generic advice/praise/prose. 'clean' if fine. Terse.
