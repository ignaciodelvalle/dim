Adversarial review — DRIZZLE / POSTGRES-JS QUERY PATTERNS. SCOPE: lib/** queries, db/index.ts (the BYPASSRLS postgres-js client). LENS: N+1 query loops (a query inside a .map/for)? fetch-then-filter-in-JS on PII/tenant data (should be a SQL WHERE predicate — the jurisdiction-drift class)? Missing transaction boundaries on multi-write operations? select() over-fetch (select * when few cols needed)? Keyset/cursor pagination correctness (stable sort, no dup/skip at page edges)? Any raw sql with interpolated user input (injection)?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
