Adversarial audit — RIGHT-TO-ERASURE vs APPEND-ONLY LEDGER + corrections superseding + schema versioning (pre-launch guide §E; the immutable libreta's hardest tension). SCOPE: the event ledger (pet_events/case_events, db/triggers.sql, db/migrations), the erasure/data-rights path (any "eliminar mis datos" / Ley 25.326 flow — search app/actions, src/modules, app/(app)/cuenta), the amendment path (src/modules/events/application/amendment), and all derived read-models (lib/projections/**, pet-compliance, the aggregates in lib/metrics/**). LENS: (1) ERASURE vs IMMUTABILITY — how is "delete my data" reconciled with a cross-party append-only ledger? Is there an anonymization/tombstone strategy (redact PII, keep the event skeleton) vs an impossible/absent hard-delete? What happens to events SIGNED BY THIRD PARTIES (vet/govt) when the owner erases — is their authorship/PII handled? Find any erasure path that hard-deletes events OR any that leaves PII unredacted. (2) CORRECTIONS SUPERSEDE EVERYWHERE — "a correction is a new event": does a corrected value supersede the erroneous one in ALL consumers (credential, compliance/al-día, surveillance aggregates, KPI rollups)? Find any read-model where the OLD value still counts after an amendment. (3) SCHEMA VERSIONING — can an event type's payload shape evolve without breaking historical replay/projection? Is there a version field or a migration story for old event payloads? (4) DERIVED STATE CONSISTENCY — the existence of cache-drift detection + manual reconciliation signals fragile projections: can pets.status (and other cached columns) be derived TRANSACTIONALLY (trigger/generated column/consistency-guaranteed projection) instead of a cache that diverges and needs a reconcile cron?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (app/ lib/ db/ components/ src/modules/ — hexagonal; real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
