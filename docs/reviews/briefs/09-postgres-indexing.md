Adversarial review — POSTGRES SCHEMA & INDEXING. SCOPE: db/schema.ts (46 tables), db/migrations/** indexes. LENS: Hot filter+sort queries (jurisdiction-scoped govt reads, case/event lists, panorama aggregates, notification queries) WITHOUT a covering composite index? Composite index column-order wrong for the query's WHERE+ORDER BY? FK on-delete/cascade semantics dangerous (a delete that cascades to append-only event tables, or SET NULL that breaks referential meaning)? text where an enum/check belongs? Missing UNIQUE/NOT NULL where the domain requires it? Over-indexing (unused indexes)?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
