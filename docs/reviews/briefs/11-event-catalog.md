Adversarial review — EVENT CATALOG & PAYLOAD SCHEMAS. SCOPE: lib/events/event-schemas.ts, the 48 pet_event types + case_events, components/pet-profile/asiento-fields.ts. LENS: Every event type's payload validated by a zod schema before insert (no unchecked jsonb)? Payload upcast/versioning handled (old events with a different shape)? The asiento/DOM field whitelist airtight — can any hash / internal id / raw payload key / PII leak to the rendered UI? Any event type with a payload field that's read but never validated? Enum drift between the DB enum and the TS union?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
