Adversarial security review — RLS DEFENSE-IN-DEPTH (anon/authenticated PostgREST surface via supabase-js publishable key). SCOPE: db/rls.sql, db/migrations/*rls* + *0086*/*0113*/*0123*/*0125*, __tests__/rls/**, db/schema.ts. LENS: Does every PII/tenant table have RLS ENABLED with either a correct policy or documented deny-all? Is the WRITE surface closed (INSERT/UPDATE/DELETE), not just reads? Any table with RLS enabled but a policy that's effectively 'true'? Any new table added without RLS? Do the two public-intake writes (welfare_reports, pets) have correct WITH CHECK constraints?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
