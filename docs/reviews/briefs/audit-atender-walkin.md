Adversarial security audit — the NEW "Atender mascota" walk-in clinical-signing flow (added this session, commits a2ae314e/0aa646c5/3b3cdad5). SCOPE: app/org/[orgToken]/atender/** (atender-access.ts resolveAtenderContext/resolveAtenderPet, actions.ts, page.tsx, CodeEntryForm.tsx, [publicToken]/page.tsx, AtenderCaptureMounter.tsx) + how it calls the clinical writers (createVaccination/createDeworming/createClinicalInfo/createMedicationStart/createNote in src/modules/events) + the #43 provenance mapping it mirrors from lib/infra/pet-access.ts. INTENT of the feature: a matriculated vet at a clinic signs a clinical event on a pet the org does NOT hold custody of; consent proxy = the member holds `event.write` AND knows the pet's high-entropy DIM code (DIM-XXXX-XXXX, ~31^8). LENS — try to BREAK it: (1) PII leakage — does resolveAtenderPet or the signing surface expose ANY owner PII (name/phone/DNI/address/last-seen)? It should return only pet identity (id/token/name/species/status). (2) Authz bypass — can a member WITHOUT event.write reach the signing surface or the actions? Can a NON-member? Is the deletedAt (erased-account) lockout applied here like the pet-access boundary? Is admin-implicit-grant correctly scoped? (3) Provenance integrity — is verified_professional stamped ONLY when the signer has a validated matrícula (not merely org.verified)? Does the mirrored 3-line mapping drift from lib/infra/pet-access.ts? (4) Scope creep — can this surface perform NON-clinical mutations (custody/transfer/adoption/ownership), or only the 5 clinical events? (5) Enumeration / abuse — rate-limiting on the code-entry lookup? Can it be used to confirm which DIM codes exist? (6) Cross-org — does orgToken in the URL properly bind the acting org (no confused-deputy)?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code. No praise/prose. 'clean' if fine. Terse.
