Adversarial audit — DENORMALIZED CACHE vs EVENT LOG pairing (Invariant #3: no cache column written without a matching event → drift). SCOPE: every write to a cached/denormalized column on pets and related tables — pets.status, estimated_weight_kg, pregnancy_*, rabies_observation_*, jurisdiction_province/locality, potentially_dangerous_breed, sterilized, microchip fields — across lib/**, src/modules/**, app/actions/**. LENS: For EACH write to such a column, is it paired with a matching pet_events insert in the SAME transaction (so the value is re-derivable)? List every write that updates a cache column WITHOUT emitting the corresponding event (the drift class — e.g. a profile edit that sets a status/weight without an event). Give file:line + which event should be emitted.

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (this repo has app/ lib/ db/ components/ AND src/modules/ — a hexagonal layer; use real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
