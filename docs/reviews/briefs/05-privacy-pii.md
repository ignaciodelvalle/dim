Adversarial security review — PRIVACY TIERS & PII. SCOPE: app/(public)/p/[publicToken]/**, lib/dni-hash.ts, lib/metrics/anonymity.ts, lib/projections (public projections), any query returning owner PII. LENS: Tier-0 (/p, no owner PII) / Tier-1 (lost, disclosure-gated) / Tier-2 (público/share) boundaries — any leak of owner name/phone/email/DNI/exact-location into a tier that shouldn't have it? DNI ever stored/logged/returned in plaintext (must be hashDni/dniLast4 only)? k-anonymity k=5 enforced on EVERY aggregate/choropleth (no cell < 5)? Free-text fields (notes, descriptions) sanitized for PII before public display? Jurisdiction-drift: govt PII/aggregate queries scoped on pets.jurisdiction (canonical) not on event payload?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
