Adversarial review — TESTING STRATEGY & COVERAGE. SCOPE: __tests__/**, e2e/**, scripts/check-*.ts, package.json (verify vs test). LENS: The pnpm verify vs pnpm test gap (verify skips unit tests — what class of bug ships?)? Source-DOM guards that break on refactor (brittle)? Fitness-sweep test hermeticity (tests leaving DB residue under concurrency — the TEST-SS class)? e2e coverage of the crisis paths adequate? RLS read AND write matrix present? Any critical path (auth, PII boundary, event integrity) with NO test? UI changes verifiable without screenshots (the paint-bug gap)?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code — no generic advice/praise/prose. 'clean' if fine. Terse.
