Adversarial architecture review — PROJECTION CORRECTNESS (event-sourced views). SCOPE: lib/projections/** (esp. pet-compliance.ts, pet cache re-derivation), and callers that read a cached projection. LENS: Is every view a pure (events, filters)->view? Any place a cached/materialized view is treated as the source of truth instead of re-derivable from events? Cache-vs-derive DRIFT risks (a cache written without re-deriving, like a manual field edit)? Is re-derivation deterministic and total? Any projection that silently drops or misorders events?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
