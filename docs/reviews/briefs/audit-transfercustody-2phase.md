Adversarial audit — the NEW transferCustody two-phase receiver-consent flow (added this session, commits 861bfcf8/cba7b4f9). SCOPE: src/modules/transfers/application/transfer-custody.ts (now routes through consent instead of a unilateral flip) + propose-cross-org-transfer.ts + accept-cross-org-transfer.ts (extended to honor to_role/from_role) + the custody_transfer_proposed event schema change in lib/events/event-schemas.ts + transferCustodyAction. INTENT: a cross-org custody handoff must now require the RECEIVER org to accept (was unilateral); the permanent-owner path (to_role=owner, for santuario/decomiso) must survive through accept, not just shelter_custody. LENS — try to BREAK it: (1) Consent bypass — can the source org still complete a custody flip WITHOUT the receiver accepting (any remaining unilateral path)? (2) to_role integrity — does accept honor to_role so a to_role=owner proposal creates an OWNER ownership (not silently downgraded to shelter_custody)? Can a proposer set to_role=owner to escalate a receiver into permanent ownership unexpectedly? (3) Race/idempotency — double-accept, accept-after-cancel/reject/expire, accept-after-source-lost-custody; is the case/proposal status guarded IN the mutating UPDATE (not just pre-read)? (4) Authz — who can propose (source must hold custody.transfer + actually hold the pet)? who can accept (receiver-side capability)? (5) Ledger/foster — is the ownership flip + foster cascade at ACCEPT correct + append-only-consistent? any orphaned foster or double-close? (6) Same-org / internal moves — any legitimate path broken by the reroute?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code. No praise/prose. 'clean' if fine. Terse.
