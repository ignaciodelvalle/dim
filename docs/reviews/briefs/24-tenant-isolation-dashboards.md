Adversarial audit — MULTI-TENANT ISOLATION in operator dashboards (isolation is per-query filtering, not RLS structure; no org-vs-org / jurisdiction-vs-jurisdiction e2e). SCOPE: every fetcher behind app/gob/** (jurisdiction-scoped) and app/org/** (tenant-scoped) — the dashboard/list/detail queries in lib/analytics/**, lib/metrics/**, src/modules/**, and the page.tsx loaders. LENS: Does EACH query filter by the viewer's resolved jurisdiction (gob) or organization (org), derived server-side from the session? Find any dashboard/list/detail query where the tenant/jurisdiction predicate is missing, weak (trusts a client param), or applied in JS after fetching (fetch-then-filter — leaks in the payload). Focus on queries NOT covered by e2e/cross-tenant-isolation.spec.ts. List file:line + the leak + the scoping fix.

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (this repo has app/ lib/ db/ components/ AND src/modules/ — a hexagonal layer; use real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
