Adversarial audit — AUTH RECOVERY & SESSION HARDENING (pre-launch guide §H). SCOPE: the password-reset / account-recovery flow, session lifecycle, and login hardening — app/(auth)/**, app/actions/auth*, the Supabase Auth integration (middleware, session refresh), and the logout path (components/layout masthead menu + the operator portal shells). LENS: (1) PASSWORD RESET / RECOVERY — is there a reset flow at all? If so: reset-token entropy + short expiry + single-use; does the request form leak account existence (enumeration — same response whether or not the email exists)? Rate-limited? (2) SESSION — expiry + rotation on privilege change; secure/httpOnly/sameSite cookies; and is logout reliable and INDEPENDENT of a single page rendering (this was a real bug — /cuenta hang blocked the only logout; #50/#48 relocated it to the masthead/operator menus — verify no portal still depends on a fragile page for sign-out)? (3) LOGIN HARDENING — rate-limit / lockout / backoff on repeated failed logins (brute-force); is the leaked-password check enabled? (4) SSO — the "Mi Argentina" federation path: is the stub safe (no half-wired trust), and what will the callback need to harden when it lands? Find: missing/weak reset-token handling, account-enumeration on reset/login, unthrottled login, a portal whose only logout depends on a fragile page.

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (app/ lib/ db/ components/ src/modules/ — hexagonal; real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
