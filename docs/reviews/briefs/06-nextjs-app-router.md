Adversarial review — NEXT.JS 15 APP ROUTER correctness. SCOPE: app/** (server vs client components, page.tsx/layout.tsx), components/** ('use client'). LENS: 'use client' boundaries minimal + correct? Any non-serializable prop (function/class/Date) passed from a Server to a Client Component? Hydration-determinism bugs (server render != first client render — window/Date/random/matchMedia read during render, different DOM subtree under a condition)? RSC data-fetching waterfalls (sequential awaits that should be parallel)? Suspense boundaries missing on slow data? Any Server Component importing client-only code or vice-versa?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
