Adversarial audit — OWNERSHIP / TRANSFER / DISPUTE trust chain (pre-launch guide §C; the fraud surface of a portable-identity padrón). SCOPE: claim-existing-pet, custody/adoption transfer, cross-org transfer, and custody-dispute flows — src/modules/transfers/**, src/modules/pets/** (claim/return-to-owner/chip-match), src/modules/cases/** (disputes), the expiry crons (expire-*-transfers, expire-*-proposals). LENS: (1) CLAIM EXISTING PET — what evidence does claiming an already-registered pet require (microchip, tag, vet attestation, prior-owner consent)? Can someone claim a pet they don't own with no proof? Two competing claims on one animal → does it open a dispute or silently overwrite ownership? (2) TRANSFER — does custody/adoption transfer require verifiable consent of BOTH parties (not unilateral)? Is every transfer written to the append-only ledger? Do pending proposals EXPIRE, and are the expiry crons correct + idempotent (no double-apply, no resurrecting an expired proposal)? (3) DISPUTES — is there an adjudication workflow with a defined arbiter, explicit states (open/escalated/closed), an SLA, and a correct effect on pet.status? Find: evidence-free claim, unilateral transfer, ownership overwrite without dispute, arbiter-less/SLA-less dispute, non-idempotent expiry.

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (app/ lib/ db/ components/ src/modules/ — hexagonal; real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
