Adversarial architecture review — EVENT-SOURCING INTEGRITY in a Next.js/Drizzle/Postgres app. SCOPE: db/triggers.sql, lib/events/**, db/schema.ts (pet_events, case_events, audit_log), and any code that sets app.allow_event_mutation / app.allow_audit_mutation GUCs. LENS: Is append-only TRULY enforced by the triggers with no bypass? Any UPDATE/DELETE path on event tables outside the accountable-override GUC? Is the override GUC properly scoped (per-tx) and always audited? Are corrections-as-new-events honored (no in-place edits)? Any way an event's occurred_at / payload can be mutated post-insert?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
