Adversarial audit — CRON FLEET scale + failure semantics (crons are load-bearing for legal/compliance windows; Vercel maxDuration 60s, treats HTTP 200 as success/no-retry). SCOPE: vercel.json (the ~21 crons), app/api/cron/**, lib/infra/*cron*.ts, lib/infra/cron-registry.ts. LENS: For each cron: (a) is it BOUNDED/batched/cursor-paginated or does it scan nation-wide unbounded (would exceed 60s as the registry grows — rabies-close, ENO outbox, materialize-slots, expire-transfers, welfare-escalation, business-rules-reeval)? (b) does it correctly return non-200 / mark cron_runs.status='failed' on failure (some were just fixed — verify none still lie)? (c) is there ANY alerting/paging on cron_runs.status='failed' or cron-health unhealthy (capstone: zero active alerting)? List the at-risk crons + the concrete fix (batch/cursor / status / add-alert-hook).

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (this repo has app/ lib/ db/ components/ AND src/modules/ — a hexagonal layer; use real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
