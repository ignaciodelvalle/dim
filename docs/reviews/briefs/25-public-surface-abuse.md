Adversarial audit — PUBLIC / UNAUTHENTICATED SURFACE abuse-resistance (pre-launch guide §B). SCOPE: every route/handler reachable with NO session — the public credential page (app/(public)/p/[token]/** or /p/[publicToken]), the code-lookup resolvers (DIM-/DEN-/P-DIM- → internal id), QR-scan capture, and denuncia/avistaje submit actions (app/actions/**, src/modules/**). LENS, four axes: (1) NON-ENUMERABILITY — is the public token high-entropy and SEPARATE from the sequential internal id? Can an attacker walk DIM-XXXX-XXXX / DEN-XXXX-XXXX to harvest the padrón? Find any public route that accepts the internal id, a short/sequential token, or leaks the id in the payload. (2) RATE-LIMITING / ANTI-ABUSE — is there throttling (per-IP AND global) on code lookup, QR scan, and denuncia/avistaje submit? What stops mass scraping or a flood of fake sightings/denuncias? Find public endpoints with zero throttling. (3) DATA MINIMIZATION — does the public credential enforce the owner's privacy toggles (show name/phone/location = off) SERVER-SIDE (the field is never fetched/serialized), not merely hidden in the DOM/CSS? Compare the anonymous payload vs the logged-in payload; find any PII (name/phone/exact location/DNI) that reaches an anonymous response or a query param. (4) SCAN/SIGHTING EVENT INTEGRITY — can scan/sighting counters be inflated or forged (replayed, unauthenticated writes, no dedupe)?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code (this repo has app/ lib/ db/ components/ AND src/modules/ — hexagonal; use real paths). No generic advice/praise/prose. 'clean' if fine. Terse.
