Adversarial review — ERROR HANDLING & RESILIENCE. SCOPE: lib/infra/env.ts, app/error.tsx + global-error.tsx, lib/infra/notification-service.ts, cron handlers, service-layer try/catch. LENS: The ARCH-P silent-swallow anti-pattern — any catch that swallows an error without logging/dead-lettering/re-throwing (a failure that vanishes)? Boot env validation fail-closed correct (prod requires the secrets, local doesn't)? Error boundaries cover the async/Suspense trees? Cron failures retried or dead-lettered (not lost)? The notification_dead_letter actually drained anywhere? Unhandled promise rejections?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code — no generic advice/praise/prose. 'clean' if fine. Terse.
