Adversarial review — NOTIFICATION SYSTEM. SCOPE: lib/infra/notification-service.ts, cron/broadcast senders, db notifications + notification_dead_letter tables, scripts/check-notifications-service.ts. LENS: Is the single createNotification write-path honored everywhere (no direct db.insert into notifications bypassing dedupe/dead-letter)? Dedupe key collisions or gaps (same event double-notifies, or distinct events dedupe together)? Dead-letter drain path exists + idempotent? The ARCH-P silent-swallow fully closed (a failed notify never silently lost)? Delivery-channel: is in-app-only a documented limitation or a silent gap (reminders that never reach the user)?

OUTPUT: numbered list. Each = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues in the actual code — no generic advice/praise/prose. 'clean' if a dimension is fine. Terse.
