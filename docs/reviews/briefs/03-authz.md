Adversarial security review — APP-LAYER AUTHORIZATION (RLS is NOT a backstop; the DB connection is BYPASSRLS). SCOPE: lib/**/*.ts server actions/queries, scripts/check-authz-guards.ts, scripts/check-authz-scoping.ts, lib/infra (auth/guards). LENS: Is EVERY mutation + PII read app-guarded (role + ownership/tenant)? Any exported *ForUser/*ForAuthority/*ForOrg that lets a caller pass an arbitrary subject id (impersonation)? Guard-called-but-not-jurisdiction-scoped (a govt/admin guard passes, then a PII query runs with NO jurisdiction/tenant predicate)? Any server action reachable without a guard?

OUTPUT: a numbered list. Each finding = `path:line` · one-line issue · severity(HIGH/MED/LOW) · concrete one-line fix. ONLY real issues you can point to in the actual code — no generic advice, no praise, no summary prose. If a dimension is clean, write 'clean'. Be terse and specific.
