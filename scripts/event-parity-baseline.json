{
  "src/modules/lost/infrastructure/lost-listing-read.ts#last_known_location": "Legacy fallback key. Pre-Lost&Found-Fase-1 status_changed rows (before location_description existed) stored the lost location under last_known_location; some historical rows on production predate the rename. No current writer schema (statusChanged in lib/events/event-schemas.ts) emits this key — new writes only ever set location_description. This read is a read-only fallback for old rows, not a live write path. See also app/(public)/p/[publicToken]/page.tsx#last_known_location for the same historical fallback on the public credential.",
  "app/(public)/p/[publicToken]/page.tsx#last_known_location": "Same legacy fallback as src/modules/lost/infrastructure/lost-listing-read.ts#last_known_location (see that entry) — the public credential's lost-pet location line falls back to the pre-rename key for historical status_changed rows.",

  "lib/analytics/owner-dashboard.ts#summary": "PRE-EXISTING BUG, NOT a justified exception — found by lint:events landing (task #76), not fixed here (out of this change's lane: scripts/ only, no app/src/lib product-code fixes). PetEventMetadata.summary reads payload.summary, but no writer schema in lib/events/event-schemas.ts has ever emitted a `summary` key — the collapsed PetHealthTimeline header preview's summary line is always null in production. Needs a follow-up: either derive summary from real per-event-type fields at the read boundary, or drop the field. Tracked here only so lint:events doesn't block pnpm verify while the fix is scheduled separately; do NOT treat this as a precedent for adding more entries — new ghost-payload reads must be fixed, not baselined.",

  "src/modules/cases/application/close-followup-expired-adoptions.ts#followup_until": "PRE-EXISTING BUG, NOT a justified exception — found by lint:events landing (task #76), out of this change's lane (scripts/ only). adoptionFinalized never emits a `followup_until` key; the actual field is `post_adoption_followup_months` (a duration in months from the adoption date, not a timestamp). findFollowupExpiredAdoptions()'s EXISTS subquery on payload->>'followup_until' is therefore always NULL/false — the post-adoption follow-up-window cron never finds a case to close. Needs a follow-up: compute the deadline from adoption_finalized.occurred_at + post_adoption_followup_months instead of reading a nonexistent payload key. Tracked here only so lint:events doesn't block pnpm verify while the fix is scheduled separately."
}
